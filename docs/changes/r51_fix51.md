# fix51 · 五项修正（修-D／修-E／修-F／修-G／修-H）

> **本件之源**：`team/round51_prompts.md` §二之三【任务 For Sophia】（2026-09-20 领队**裁八**：五项俱系整行替换，与批丙之 append 增量混列会使合入者分辨不出，照 r50 裁十一〔`fixes_places.csv` 另立〕与 fix44c／r49 先例，**另立一件**）。
> **拟文之出处**：`docs/changes/r50_diwang.md` §10 修正建议表 修-D／修-E／修-F／修-G／修-H，**照其拟文办，不自行改写**（口径一）。
> **本件之界**：**只动三行**——`places.L_HUAN` 一行、`sources.Z018`／`Z125` 两行，**整行替换；零新增、零删除，九表行数全不变**；`sources` 两行之 `notes` **纯追加，原文零删除**（有逐字机器反证）。
> **`coord_certainty` 一字未动**（口径二：本件不代升，列上报，俟领队裁）。
> **不自行合入**；与 `round51_peijue/`（批丙）、`r51b_chuwu/` 三件互不相混。
> **仓库实况（2026-09-20 落笔当日实读）**：`L_HUAN.certainty`＝`low`、`lat`／`lng`＝35.85／116.70、`modern_location`＝「约在今山东宁阳/肥城一带，异说」、`coord_certainty`＝`low`；`Z018.notes` 只著录郑伯克段一事；`Z125.notes` 现书「三邑……本批一律不立 `places` 点」。**与任务书二之三表中所列「现值」逐项相符，无一处须勘。**

---

## 1. 本件之数

| 表 | 合入前 | 整行替换 | 新增／删除 | 合入后 |
|---|---:|---:|---:|---:|
| `places` | 104 | **1**（`L_HUAN`） | 0／0 | **104** |
| `sources` | 195 | **2**（`Z018`／`Z125`） | 0／0 | **195** |
| 其余七表 | — | 0 | 0／0 | 不变（有机器反证） |

**九表行数全不变**——本件是纯替换件，不触任何行数不变量（`tools/qa/r43_prod_check.js` :68–71 所锁之 195／104／506／265 四数、`tools/qa/vision_r46.js` :351 所锁之 506，**本件一个也不动**）。

---

## 2. `places.L_HUAN` —— 修-D／修-E／修-F 三项（整行替换，所动五栏）

| 栏 | 原值 | 新值 | 项 |
|---|---|---|---|
| `certainty` | `low` | **`medium`** | 修-D |
| `lat` | 35.85 | **35.88** | 修-E |
| `lng` | 116.70 | **116.78** | 修-E |
| `modern_location` | 约在今山东宁阳/肥城一带，异说 | **约在今山东省宁阳县北而稍西三十余里一带** | 修-F |
| `coord_basis` | （旧文） | **旧文一字照留 ＋ 纯追加一段留痕** | 留痕纪律 |
| `coord_certainty` | `low` | **`low`（一字未动）** | 口径二 |
| 其余六栏（`id`／`ancient_name`／`state`／`place_type`／`description`／`source_ids`） | — | **一字未动**（机器反证） | — |

### 2.1 修-D · `certainty` 由 `low` 升 `medium`

**据**（照拟文）：杨注三处互见（页 97／1570／1576，定八明言「詳桓公三年經注」）＋《中国历史地图集》第一册·春秋·齐鲁幅（页 26–27）同标——本库地望优先序前二位一致。
**未升 `high` 之由**（本件所补之自限，非改拟文）：三处杨注之文略异（二作「北而稍西」、一作「西北」），且所定是「县北三十余里」一隅之方位而非故城之实址。此二句已随 `coord_basis` 之追加段入库。

### 2.2 修-E · 坐标收紧至 35.88／116.78

**回校**（按 conventions §4 公式**实算**，非引拟文之数）：`x=(116.78-105.0)/17.0*1200=832`、`y=700-(35.88-29.5)/9.0*700=204`——**与拟文所报 x=832、y=204 逐一相符**（模拟设断言实算复核，见 §5）。落点在覆盖范围内（东经 105.0–122.0、北纬 29.5–38.5）。

**方位实算**（模拟内实算并打印）：
- **新点**较宁阳县治（约 35.76／116.81）**北约 13.3 公里、西约 2.7 公里**——北向位移为西向之近五倍，正合二书主流之指「**北而稍西**」；
- **旧点**较县治**北约 10.0 公里、西约 9.9 公里**——两向几等，其形为**正西北**，即 r50 判「**证增而点未校**」之由（`L_HUAN.coord_basis` 旧文自著）。

**自限**：**只取本库两位小数之概位，不据「三十余里」四字自造精度**（手法照 `L_YU` r31、`L_JIELIANGCHENG` r44d 先例）；**不据谭图图面目测量点冒充实测**（2026-09-19 领队口径二之明令）。

### 2.3 修-F · `modern_location` 之「肥城」收窄

**据**（照拟文）：肥城约北纬 36.18，去宁阳四十余公里，与二书之指不合。
**新值**：「约在今山东省宁阳县北而稍西三十余里一带」。

**★ 一处须上报之细节 ——「异说」二字随之而去**：原值末有「异说」二字，其所指之异说即**宁阳／肥城两说**；肥城既收窄，宁阳一地之内二书并无异说（杨注三处之文略异系同一地之措辞小异，已全记于 `coord_basis`），故「异说」二字之所指已消，本件随之去。**此二字之去不在拟文明文之内**，属本件依拟文之义所作之连带处置，**随件上报**（§4 上报二）——若领队以为宜留，改作「约在今山东省宁阳县北而稍西三十余里一带（杨注三处之文略异）」即可，**只动一栏**。

### 2.4 `coord_basis` 之纯追加（留痕纪律之所必需）

**何以须动这一栏**：`L_HUAN.coord_basis` 旧文末段自陈「三项本轮续裁未及，**仍开放**……**本件仍不代改**。本行本轮实际所动者，**惟 `coord_basis` 一栏，且系纯追加**」——**本件落地之后，此三句即不实**（三项已改、所动者已不止 `coord_basis` 一栏）。依 §7 v1.29／§10.1「判定史留原貌，事实错就地勘正加注」，**旧文一字不删，于其后纯追加一段**，记明：① 三项逐项之落实与其据（含未升 `high` 之自限）；② 坐标之回校 x=832、y=204 与新旧两点之方位实算；③ 「异说」二字之去与其上报；④ **`coord_certainty` 本件一字不动及其理由**（见 §3）。

**此栏之动不在拟文之内，随件上报**（§4 上报三）：若领队以为 `coord_basis` 宜一字不动，删去该追加段即可，**其余四栏之改不受影响**。

---

## 3. `coord_certainty` —— 本件不代升（口径二），上报待裁

r50 **裁十**判本栏维持 `low`，其理由是「**证增而点未校则不升**」（`L_HUAN.coord_basis` 旧文详载其判）。**本件既已校点（修-E 落实），该理由之前提即消。**

**而「前提消」不等于「可升」**——升档须有**新判据**，不得以旧判据之失效自动换取升档（此正是 r50 所立「宁降不虚」之分寸）。本件依任务书口径二**不代升，`coord_certainty` 维持 `low` 一字未动**（机器反证），议升与否列 §4 上报一，**俟领队裁；裁定之日与其由须照留痕纪律记明**。

**供裁之两面（本件只陈，不主张）**：
- **可升之面**：点已按二书主流之指校准（北 13.3／西 2.7，合「北而稍西」），`certainty` 同批升 `medium`，两栏同向；照 §5 v1.24，`coord_certainty` 所答者是「点有没有戳准」，校准即是对这一问的正面回答。
- **不可升之面**：「三十余里」是约数，本库所取仍是两位小数之概位而非故城实址；杨注三处之文本身尚有「北而稍西」与「西北」之异（三分之一作「西北」），此异未清则点之精度仍有一档之疑。

---

## 4. 上报（本件不代裁，三条）

**上报一 · `L_HUAN.coord_certainty` 可否随修-E 议升 `medium`**：见 §3。**本件不代升**（口径二明令）。请领队裁；若准升，宜同时记明新判据与日期（留痕纪律），改动只及一栏。

**上报二 ·「异说」二字之去**：见 §2.3。请裁：① 照此去（现状）；② 留之而改其所指（如「（杨注三处之文略异）」）。

**上报三 · `coord_basis` 之纯追加是否可行**：见 §2.4。本件以为**必需**（旧文末三句已因本件而不实，不加注即成库内自相抵牾之文）；而该栏之动不在拟文明文之内，故上报。请裁：① 照此追加（现状）；② 不动该栏，本件所记之留痕只存于 `docs/changes/r51_fix51.md`。

---

## 5. `sources.Z018`／`Z125` —— 修-G／修-H 两项（纯追加，原文零删除）

两行**只动 `notes` 一栏**（其余七栏逐字全等，机器反证），且所加之文**与 `docs/changes/r50_diwang.md` §10 拟文逐字相同**（模拟设逐字全等断言，非「含关键词」式的宽检）。

**修-G · `Z018.notes` 追加**（120 字）：

> ——本篇另载「費伯帥師城郎，不書，非公命也」一节，系 r50 所立 `L_FEIBO`（费伯之费）之据（杨注·隐公元年页 10）；该点与 `L_FEIJISHI`（季氏之费）同名异地，两存而互见，判据链见二行之 `description`。

**其前提本件已实读复核**：`L_FEIBO.source_ids` 确挂 `Z018`（模拟设断言），而本行 `notes` 原只著录郑伯克段一事，不追记则读者不知本篇何以为一地点行作证。

**修-H · `Z125.notes` 追加**（137 字）：

> ——此语所记之状态已由 r50 解除：三邑经站长 2026-09-19 纸本核，俱已立点（郈 `L_HOU`、费 `L_FEIJISHI`、成 `L_CHENGYI`）；原文照留不删（conventions §7 v1.29），所解者是「材料不在手」之状态，非当日之判为误。

**其前提本件已实读复核**：`L_HOU`／`L_FEIJISHI`／`L_CHENGYI` 三点俱在库（模拟设断言）。

---

## 6. 修正建议（**只报不改，本节不动 `data/csv/`**）

**修正建议一 · `Z125.notes` 另有一句已失时效，本件依「照拟文办」不代加**：该行 `notes` 现另书「**子路（仲由）属孔门配角批（批丙）所议立行之人，本批不立行、不挂链**」——本轮批丙件（`data/incoming/round51_peijue/`）立 `P_ZILU` 并回挂 `E282`，此句于合入后即不实。
**本件不代改**，理由有二：① 口径一明令「照其拟文办，不自行改写」，r50 §10 修-H 之拟文只及三邑立点一事；② 该句之时效系于批丙之合入与否，属跨件之事。请领队裁：① 俟批丙合入后另件加注；② 准本件于同一追加段内一并加注（若准，本件可随改，**只动同一栏**）。

**修正建议二 · `L_HUAN.description` 之薄**：该栏现只一句「齐侯送姜氏、鲁桓公会齐侯，文姜至自齐的关键地点。」——而定公八年「陽虎入于讙、陽關以叛」、定公十年「齊人來歸鄆、讙、龜陰之田」两事（库内 `E280`／`E274`）亦系于此地，`coord_basis` 已详之而 `description` 未及。属只报不改，俟他轮。

---

## 7. 合并模拟与 `validate.py` 实测输出

`python data/incoming/fix51/sim_fix51.py`（2026-09-20 落笔当日实跑，主表基线 `bf4242c`）：

```
== 2. 逐表行数（本件纯替换，合入后九表全等） ==
   表                  合入前     合入后
   events             265     265  相符
   passages           506     506  相符
   sources            195     195  相符
   people             166     166  相符
   event_people       673     673  相符
   relations          289     289  相符
   places             104     104  相符
   archaeology          8       8  相符
   background          11      11  相符
== 3. 六表一字未动之反证 ==
== 4. L_HUAN 逐栏定点比对 ==
   新点 35.88／116.78 按 conventions §4 公式回校 → x=832、y=204（拟文所报 x=832、y=204）
   新点较宁阳县治：北 13.3 km、西 2.7 km
   旧点较宁阳县治：北 10.0 km、西 9.9 km（其形为正西北，即 r50 判『点未校』之由）
== 5. sources 两行 notes 纯追加之反证 ==
== 6. tools/validate.py（于副本上跑） ==
OK：全部校验通过
   exit code = 0

== 断言分布（实测） ==
   0. 表头与文件卫生                                       69
   1. 合并模拟（按 id 整行替换，零新增零删除）                         2
   2. 逐表行数（本件纯替换，合入后九表全等）                           18
   3. 六表一字未动之反证                                    305
   4. L_HUAN 逐栏定点比对                                 32
   5. sources 两行 notes 纯追加之反证                       29
   6. tools/validate.py（于副本上跑）                       1

456 PASS  0 FAIL  exit 0
```

**本件专属之四类断言**：① **九表行数全等**（纯替换件之本分）；② **六表＋两表其余各行一字未动之反证**（逐行逐字比对，305 条）；③ **坐标按 §4 公式实算回校与新旧两点方位实算**（不引拟文之数，自算而后比，并验「北向位移＞西向之两倍」这一「北而稍西」之形）；④ **两源 `notes` 之「纯追加」逐字反证 ＋ 追加之文与 r50 §10 拟文逐字全等**（不自行改写之机器保证）。

---

## 8. 交 Skipper 的合入要点

1. **手法**：`fixes_places.csv`（`L_HUAN` 1 行）与 `fixes_sources.csv`（`Z018`／`Z125` 2 行）**按 id 整行替换**；本件**无任何 `*_new.csv`**（模拟设断言反证其不存在）。
2. **合入后预期数**：**九表行数全不变**；`places` 仍 104、`sources` 仍 195。
3. **不触任何 QA 行数不变量**（195／104／506／265 与 506 五处），**与批丙所报之 `passages` 506→509 联动无涉**。
4. **三件互不相涉，合入次序不拘**：本件所触之三行（`L_HUAN`／`Z018`／`Z125`）与批丙、r51-B 所触之行无一重叠。
5. **`coord_certainty` 一字未动**——合入时请勿顺手同改（其议已列 §4 上报一，俟裁）。
6. **归档**：本文件归档为 `docs/changes/r51_fix51.md`，`sim_fix51.py` 归档为 `docs/changes/r51_fix51_sim.py`，归档在先、清空在后。
7. **conventions 不须升号**：本件未新造规范。

---

## 9. ★ 返工追记（2026-09-20，裁十八；**上文 §1–§8 原文一字未改**，照「判定史留原貌」）

> **本节之界**：`fix51` 首交之本经领队审定，**二事须改，余一字不动**（`team/round51_prompts.md` §二之四、§一之三 裁十四至裁十八）。上文 §1–§8 系首交当日之文，其中「`coord_certainty` 一字未动」「三条上报待裁」诸语系当日之状态，**照留不改**；其状态之变记于本节。

### 9.1 三条上报之裁（站长／领队 2026-09-20）

| 上报 | 裁 | 结果 |
|---|---|---|
| §4 上报一 · `coord_certainty` 议升 | **裁十四 准** | 由 `low` 升 **`medium`**，**本件须返工落实** |
| §4 上报二 ·「异说」二字之去 | **裁十五 准** | 判为「随修-F 之必然，非逾拟文」——**首交之本已如此，无须返工** |
| §4 上报三 · `coord_basis` 纯追加 | **裁十六 准** | 「拟文所拟者是『改什么』，留痕是『怎么改』之通则，不待拟文另许」——**首交之本已如此，无须返工** |
| §6 修正建议一 · `Z125` 子路句 | **裁十七 准其②** | 于同一追加段内一并加注，**本件须返工落实** |

**裁十四之要义须记明（免后人误读）**：升档之据是「**点之校**」，不是「**证之增**」——r50 裁十「证增而点未校则不升」之判据**于此得其正用，非绕过之**。领队明言「Sophia『前提消不等于可升、升档须有新判据』判得极对，**本裁即补其所缺之新判据**」。

### 9.2 返工所动（只此二事）

**其一 · `fixes_places.csv` 之 `L_HUAN`**——在首交之本之上，**再动两栏**：

| 栏 | 首交之本 | 返工之本 |
|---|---|---|
| `coord_certainty` | `low`（一字未动） | **`medium`**（裁十四） |
| `coord_basis` | 旧文 ＋ 追加段一 | 旧文 ＋ 追加段一 ＋ **追加段二**（**段一亦一字未改**，纯追加于其后） |
| `certainty`／`lat`／`lng`／`modern_location` | medium／35.88／116.78／「约在今山东省宁阳县北而稍西三十余里一带」 | **一字不动**（首交之本已核可，裁十八） |
| 其余六栏 | 一字未动 | **一字未动** |

**追加段二所含四事**（裁十四所命，逐事设机器断言，见 §9.4）：① **升档之裁与其日**（2026-09-20 领队裁十四）；② **升档之据是「点之校」非「证之增」**，并记旧点之正西北与新点之「北而稍西」为其对照；③ **不升 `high` 之由**（县域级概位、判据链末端未闭，照 `L_JIELIANGCHENG` r44d ㈣与 `r50_diwang.md` §11 自限二）；④ **一处未闭之端，如实记不略**——新点距宁阳县治约 **13.6 公里**（北 13.3／西 2.7，球面近似**实算**：√(13.3²+2.7²)＝13.59），依本库通行今折（一里≈0.5 公里，`r50_diwang.md` §11 自限三）折合约 **27 里**，**略短于杨注「三十餘里」**；此差落在两位小数概位之粗尺度内（0.02° ≈ 2.2 公里），且「里」之今折本身未经考订。**坐标本轮不再微调**——其差既在粗尺度之内，动之即是**以精度之名行无据之实**。

**其二 · `fixes_sources.csv` 之 `Z125`**——**于同一追加段内**补子路一句（裁十七）：原句照留不删，记明该状态已由 **r51 批丙**解除（`P_ZILU` 已立行、依 `Q471`「仲由為季氏宰，將墮三都」回挂 `E282`，`presence` 从严取「相关」），**所解者是「批次之界所限」之状态，非当日之判为误**（照修-H 之同型写法）。`Z125.notes` 之追加由 137 字增至 **532 字**；**`Z018` 一行一字不动**（机器断言反证）。

### 9.3 ★ 模拟中扫出并订正者一处 —— **harness 之错，非数据之错，留痕不抹平**

返工首跑 **1 FAIL**：断言「Z125 原 notes 含子路一句」不成立。**查因**：该断言初稿把任务书二之二〔二〕所引之「子路（仲由）……，**本批不立行、不挂链**」**连同其 markdown 强调一并写死**，而**实读 `Z125.notes` 原字并无星号**（`**` 系任务书行文之强调，非数据之字）。

**错的是断言，不是数据**（照 r49 §13.1「harness 之错，非数据之错」与本库「QA 基础设施诚实优先」之例）。**两处一并订正**：① 断言改取原字，并另加一条反向断言（「基线若带强调则本订正须重核」）以防日后误判；② **本件所拟之追加文中对该句之引用亦改从原字**，并于文内注明「任务书引作『**本批不立行、不挂链**』者系其行文之强调，非本栏之字」——**引用库内之文当照库内之字**。

**此事与本轮勘注七（领队以 `E295.summary` 之今译『楚子亲伐随』冒作传文）同出一源**：派件之文与库内之字，形近而非一物；§一之三 裁二十二所拟之通例「今译语不可代传文」，此为其第二例，**而这一次犯的是我**——如实记于此，不抹平。

### 9.4 返工后之模拟实测（断言由 456 增至 **487**，增 31 条）

`python data/incoming/fix51/sim_fix51.py`（2026-09-20 返工后实跑，主表基线 `bf4242c`）：

```
== 2. 逐表行数（本件纯替换，合入后九表全等） ==   九表逐表「相符」
== 4. L_HUAN 逐栏定点比对 ==
   新点 35.88／116.78 按 conventions §4 公式回校 → x=832、y=204（拟文所报 x=832、y=204）
   新点较宁阳县治：北 13.3 km、西 2.7 km
   旧点较宁阳县治：北 10.0 km、西 9.9 km（其形为正西北，即 r50 判『点未校』之由）
== 6. tools/validate.py（于副本上跑） ==
OK：全部校验通过
   exit code = 0

== 断言分布（实测） ==
   0. 表头与文件卫生                                       69
   1. 合并模拟（按 id 整行替换，零新增零删除）                         2
   2. 逐表行数（本件纯替换，合入后九表全等）                           18
   3. 六表一字未动之反证                                    305
   4. L_HUAN 逐栏定点比对                                 51
   5. sources 两行 notes 纯追加之反证                       41
   6. tools/validate.py（于副本上跑）                       1

487 PASS  0 FAIL  exit 0
```

**裁十八所命之「断言须随之增二」，实增两类十九条**：
- **`coord_certainty` 实得 `medium`** 一类（含原值须为 `low`、新值须为 `medium`、三级枚举、两栏同取 medium 之实测）；
- **`Z125.notes` 含子路句之追加且原句仍在** 一类（含原句在基线、原句在新值、追加段以修-H 拟文逐字开头、所补之句含 `P_ZILU`／`E282`／`Q471`／裁十七／「批次之界所限」诸著录、**引用照原字不带强调**、`Z018` 未被牵动）；
- 另增 `coord_basis` 返工段之四事逐事断言（升档之裁与日、「点之校」非「证之增」、不升 `high` 之由、一处未闭之端之六项细目）与「首交四栏未被返工改动」之反证。

### 9.5 返工件之上报（一条，不代裁）

**上报四 · 裁六「止血条」与本件两栏之追加文**：裁六所拟之 conventions 止血条作「**实渲之栏新写注文，markdown 标记由显示层承担；显示层未及之栏，本轮不得新增**」。落笔当日实读：该条**尚未落入 `docs/conventions.md`**（现仍 v1.43，工作区内该文件未改），系俟 Skipper 随 Vision 件合入时落笔。

**而 `places.coord_basis` 与 `sources.notes` 二栏俱属「显示层未及之栏」**（`app.js` 实渲之注文栏只 `passages.modern_note`／`events.summary`／`people.notes` 三者，见 §一之二 勘注二），**本件首交与返工之追加段俱有新增 `**` 标记**（r50 所立之旧文亦满是，属该账之 1234 处）。首交之本已经领队审可（裁十六），返工段**从其式以保行内一致**。

**请裁**：① 照此办（现状，行内一式）；② 止血条既立，追加段之 `**` 宜去——则其改**只在这两栏之追加段内**，不牵动他处、不牵动 r50 旧文（旧文照留不删）。**本件不代裁**。

---

## 附注区（§7 私有层 · 写作向备注，不入公开数据）

- **讙这个地方，本库为它写的字比它自己的历史还长**：`coord_basis` 一栏现已逾三千字，而经传写它只有三处——齐侯送姜氏于讙、阳虎入于讙阳关以叛、齐人来归郓讙龟阴之田。一个点的依据链长过它的事迹，这是考据之常，也是本库「宁降不虚」之价。
- **「北而稍西」与「西北」之间只差十公里**，而这十公里正是 r50 判「不升」、r51 判「可校」的全部内容。若哪天有人问本库的坐标精到什么程度，这一条可以当答案：精到「能分辨正西北与北而稍西」。
