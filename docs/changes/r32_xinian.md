# fix31_xinian · 息妫章并陈组落库备料（清华简《系年》第五章 · J 层首件）

任务书：【任务 For Sophia · 系年试点落地件】息妫章并陈组落库备料。
权威依据：`team/ruling_chutuwenxian.md`（出土文献层领队裁定书十一条，2026-08-15）；拟稿来源：`docs/kaoding_chutuwenxian.md`。

**本件性质：新增两行 ＋ 定点整行替换四行的小件。** 无删除行、无字段增删、无既有 ID 变动、无 `reliability` 变动、无坐标变动、无年份字段回填。所有替换件一律以**整行替换**形式提交（合入者按 `id` 覆盖主表同 ID 行即可，不必手工拼接长字段）。

**只备料，未合入。** `data/csv/` 主表一字未动，`tools/` 一字未动（含 `validate.py`，护栏归 Skipper，红线 1），`site/` 未动，未重生成 `site/data/`，未 `git add`／`commit`。

| 文件 | 内容 | 行数 |
|---|---|---|
| `CHANGES.md` | 本文件 | — |
| `sources_new.csv` | **新增** `J001`（《清华大学藏战国竹简（贰）·系年》第五章） | 1 |
| `passages_new.csv` | **新增** `Q442`（《系年》一方引文，挂 `E146`） | 1 |
| `fixes_passages.csv` | 整行替换 `Q161`（《左传》一方反向互指） | 1 |
| `fixes_events.csv` | 整行替换 `E146`／`E147`（`summary` 增句 ＋ `source_ids` 挂 `J001`） | 2 |
| `fixes_people.csv` | 整行替换 `P_XIHOU`（`notes` 增段，`death_year_bce` **不回填**） | 1 |

合计：**新增 2 行、整行替换 4 行**。合入后行数预期 `sources` 173→**174**、`passages` 424→**425**；`events` 235、`people` 153、`places` 91、其余各表**行数不变**。

---

## 0. 体例与口径（先立后用）

### 0.1 诚实留痕四项，照裁定书核可之口径不改口

裁定书末段核可记档者，本件一律照办：

1. **全电子、无纸本**——本轮备料者（Sophia）一处纸本亦未取得，`J001` 之整理本（中西书局，2011）**待纸本**；
2. **B 类三种只题录不引**（尤锐《從〈繫年〉虛詞的用法重審其文本的可靠性》、蔡莹莹、甯登国三种正文未能取阅）——本件注文**一字不引其结论**，故本件亦不为其建 `B###` 源行；
3. **拒套「页 N」末缀**——本件通篇无「页 N」引注，末缀二式（「，页码 YYYY-MM-DD 补核」／「，页码同次核」）**均不适用**，不机械照抄；只有一次核对时照抄第①式，等于凭空断言存在过一次并不存在的补核动作（conventions §7 v1.29）；
4. **不引 `〔页码待补〕` 入本件**——该标记是数据表专用占位、全库已于 r31 归零，本件不重新引入，`grep -n '〔页码待补〕' data/csv/*.csv` 合入后仍应为 **0**。

### 0.2 「待纸本」三处用字之口径（裁定 9）

`J001.notes` 与 `Q442.modern_note` 均明写：**转录本可证有无某事、次第如何，不可证某字作某**。故「新／莘」「弗訓／弗順」「命視／命見」三处一律标「**待纸本**」，未核前不得据以立用字异文之论断，本件不作训释、不作推定通假。

### 0.3 一处**本轮新增的自曝**：`url` 未能实测复验

`docs/kaoding_chutuwenxian.md` 成稿时（2026-08-15）网络可达，转录本系实取；**本次落库执行时网络已不可达**——实测 `zh.wikisource.org` 域名解析失败（`getaddrinfo ENOTFOUND`）。故 `J001.url` 系依上轮实际校核所据之**页名**（「清華大學藏戰國竹簡/系年」）照维基文库 URL 规则写出，**本轮未能实测复验**，已在 `notes` 中如实登记为复核项、不作已验证之断言（体例同 `S014`，依 conventions §7 v1.27「`sources.url` 体例从实」）。**引文内容本身不受影响**——`Q442.quote_original` 逐字取自考订文档 §3.0（甲）之上轮实核转录，非本轮重取。

### 0.4 未动之物（合入者核对用）

`E146`／`E147` 之 `reliability`（**维持 `high`**）、`title`／`year_bce`／`place_id`／`category`／`importance`／`sort_key`；`Q161` 之 `quote_original`（**一字未动**）／`quote_type`（维持「原文」）／`event_id`／`source_id`；`P_XIHOU` 之 `death_year_bce`／`birth_year_bce`（**均维持留空**）与除 `notes` 外全部字段；`E148`／`E149`／`E150`、`Q162`／`Q163`／`Q164`、`P_XIGUI`、`places` 全表 —— **一字未动**（已实读断言，见 §4.2）。

---

## 1. 逐行明细（五项）

### ① `J001` 源行（`sources_new.csv`，新增 1 行）

| 栏 | 值 |
|---|---|
| `id` | `J001` |
| `title` | `《清华大学藏战国竹简（贰）·系年》第五章` |
| `work` | `清华大学藏战国竹简`（**批次丛书名**，照 J 前缀提案 §2.4 之批次区分办法） |
| `section` | `系年·第五章`（**篇名·章次**，同上） |
| `category` | `出土文献`（**新值**，随裁定 11 落；现有六值 传世文献／诗歌/舆论／诸子说部／考古资料／考古/遗址／气候/自然地理 无可归者） |
| `source_type` | `primary` |
| `url` | `https://zh.wikisource.org/wiki/清華大學藏戰國竹簡/系年`（**本轮未实测复验**，见 §0.3） |
| `notes` | 见下（四项必录俱全） |

**`notes` 全文**（单元格内为连续一行，此处为便阅读分段引出，**分段符非数据内容**）：

> 战国中期楚地写本，出土文献层（\`J\` 前缀首条，分层纪律见 conventions §2 拟入新节）。
> **【入藏与发现性质】**清华简系 2008 年由校友捐赠入藏清华大学，\*\*非科学发掘出土品\*\*，其来源链有别于考古发掘资料；学界曾有辨伪之议（主流已认其真），本库不作论定、只如实著录其为入藏简。此项系 J 层单独支撑者默认取 \`medium\` 之理据之一，凡 J 行 \`notes\` 必录、不得省略——medium 默认的理据必须可见。
> **【整理本出版信息】**清华大学出土文献研究与保护中心编、李学勤主编《清华大学藏战国竹简（贰）》，上海：中西书局，2011 年 12 月出版；《系年》全篇（一百三十八简、二十三章）收入此册。
> **【本库所据底本】**维基文库「清華大學藏戰國竹簡/系年」页之\*\*电子转录本\*\*；该页凡例自述「原簡文古體字、異體字甚多，這裡一概改用通行字」，故其正文不等于整理本释文，整理本释文之通假括注在转录中已失，\*\*整理本待纸本\*\*。据此立本库口径：\*\*转录本可证有无某事、次第如何，不可证某字作某\*\*。
> **【核对状态】**电子转录本已核，Sophia，2026-08-15；整理本（中西书局，2011）\*\*待纸本\*\*。本条通篇无「页 N」引注——本轮无一处取得纸本页码，末缀二式（「，页码 YYYY-MM-DD 补核」／「，页码同次核」）均不适用，不机械照抄。本章「新（莘？）」「弗訓（弗順？）」「命視（命見？）」三处用字须俟整理本纸本核定，\*\*未核前不得据以立用字异文之论断\*\*。\`url\` 栏\*\*本轮未能实测复验\*\*：备料环境网络不可达（\`zh.wikisource.org\` 域名解析失败），本栏依上轮实际校核所据之页名照录，列复核项、不作已验证之断言（依 conventions §7 v1.27「\`sources.url\` 体例从实」）。
> **【文献性质】**学界尚无定论：宋镇豪主楚史官所作纪年大事之史书、夏含夷主多国综合比较之编年体、胡平生疑为摘抄或整理编纂本；三说系维基百科条目转述，本库未核其原文出处，只作「学界有此三说」之登记，不引为判据。
> **【与《左传》之关系】**二者为独立成书之两支记载，\*\*非祖本亦非抄本\*\*（本库结构性观察：《系年》二十三章与《左传》记事重叠不足其半，双向大面积互无，非传抄关系所应有）；对读取互证与见异，不以一裁一，异处一律并陈不裁。

**四项必录对照**：①入藏／发现性质 →【入藏与发现性质】段；②整理本出版信息 →【整理本出版信息】段；③本库所据底本 →【本库所据底本】段；④核对状态 →【核对状态】段。四项俱全，缺一即违裁定 8。

### ② `Q442` 新 passage（`passages_new.csv`，新增 1 行）

| 栏 | 值 |
|---|---|
| `id` | `Q442`（接台账尾号 `Q441` 而下；`Q419`／`Q420` 系 r28 留空之号，**非退役号**，本件不取用，不求编号稠密） |
| `event_id` | `E146` |
| `source_id` | `J001` |
| `quote_original` | `蔡哀侯娶妻於陳，息侯亦娶妻於陳，是息媯。息媯將歸于息，過蔡，蔡哀侯命止之，曰：「以同姓之故，必入。」息媯乃入于蔡，蔡哀侯妻之。息侯弗訓，乃使人于楚文王曰：「君來伐我，我將求救於蔡，君焉敗之。」`（逐字取考订文档 §3.0 甲之上半，繁体照录；转录本所标简号【23】–【25】**未入正文**，避免与编者层标【】混淆——简号已在考订文档留痕） |
| `quote_type` | `出土文献`（**新值**，随裁定 6 落，并请随立即开软检） |
| `modern_note` | 首字符起【出土文献层，与《左传》系统并陈不裁、二说不合一；不作史实骨架用，亦不据以改易经传骨架】——**答性质／用法，不复述类型**（层名由 `quote_type` 与前端徽标承担，见 conventions §7 v1.20）；正文并列陈伟说与成富磊·李若晖说、明书二说方向相反而学界未定，整理者注明标「**转引、非直引**」；三层细分见 ⑤ |

### ③ `Q161` 反向互指增补（`fixes_passages.csv`，整行替换 1 行）

- **`quote_original` 一字不动**（已实读逐字比对断言，见 §4.2），`quote_type` 维持 `原文`，`event_id`／`source_id` 不动。
- `modern_note` **原句原样保留在首**，其后接一句：

> 另按：清华简《系年》第五章于此作「蔡哀侯妻之」，与本条「弗賓」为\*\*独立成书之两支记载\*\*、非同一文本之版本异文，本库两说\*\*并陈不裁\*\*（其异属「事实相抵」之\*\*行动层\*\*；同章另有措辞层、次第层两处相抵，见 \`Q442\`）；《史记·管蔡世家》作「蔡侯不敬」，与本条同属传世系统，不构成独立第三方。见 \`J001\` 及其 \`passage\`（\`Q442\`）。本条 \`quote_original\` 一字未动，\`quote_type\` 维持「原文」。

- **依据**：裁定 10 特别核可——「只在一侧注异，等于把 J 层降成附注」，并陈是双向义务。

### ④ `E146`／`E147` `summary` 增句（`fixes_events.csv`，整行替换 2 行）

两条 `summary` 均**原句原样保留在首**，其后各接一句：

- **`E146`**（莘之役）：「《系年》所记与此有异，见引文对读（清华简《系年》第五章，J001／Q442，出土文献层）：止息妫之辞、蔡侯所为、灭息之次第三处相抵，两说并陈不裁，本条 reliability 维持 high 不因之改动。」
- **`E147`**（灭息以息妫归）：「《系年》所记与此有异，见引文对读（清华简《系年》第五章，J001；引文挂于 E146 之 Q442）：《左传》一次成事（如息、入享、遂灭息），《系年》则文王先为客于息而还、「明歲」始起师克之，相抵在次第层而不在其事之有无，两说并陈不裁，本条 reliability 维持 high 不因之改动。」

**`reliability=high` 维持不动**（裁定 10）——未裁定的异说不反噬骨架；莘之役与灭息、取妫两造皆书，相抵者在过程与措辞，不在其事之有无。

**另改一栏（本件唯一超出任务书字面之处，请领队／Skipper 复核）**：`source_ids` 由 `Z025` → **`Z025;J001`**（`E146`）、`Z027` → **`Z027;J001`**（`E147`）。理由与自限见 §5 上报 1。

### ⑤ `P_XIHOU`（`fixes_people.csv`，整行替换 1 行）

- **`death_year_bce` 不回填，仍留空**（裁定 4，Xu 与领队同判）；`birth_year_bce` 亦不动；除 `notes` 外全部字段一字未动。
- `notes` 原文（末句「灭国后事左传无明文，卒年留空。」）**原样保留在首**，其后接一段：

> 【《系年》所记，J 层独有，不据以补经传之阙】清华简《系年》第五章明书「明歲，起師伐息，克之，\*\*殺息侯\*\*，取息媯以歸」（\`J001\`，出土文献层；维基文库电子转录本已核，Sophia，2026-08-15，整理本待纸本）——息侯之死于 J 层有文，而《左传》于其下场一字不及，是《左传》略而不书、非与之相抵。本库\*\*不据以回填 \`death_year_bce\`\*\*，卒年仍留空，二由：其一，此说 J 层独有而经传无征，J 层独有者不补经传之阙；其二，《系年》通章不系月日、只以「明歲」标次年，本无绝对系年可推，回填即等于在无系年之处造出一个年份。故只记此线索于 notes，不入年份字段。

### ⑥ 三处用字「待纸本」之落点（任务书④）

| 用字 | 见于 | 落点 |
|---|---|---|
| 「新」／「莘」（地名） | 《系年》「文王敗之於新」／《左传》「敗蔡師於莘」 | `J001.notes`【核对状态】段（三处并列，标「須俟整理本纸本核定」） |
| 「弗訓」／「弗順」 | 《系年》「息侯弗訓」 | `J001.notes` ＋ `Q442.modern_note` 末句（该字在 `Q442` 引文正文之内，故双落点） |
| 「命視」／「命見」 | 《系年》「君必命視之」 | `J001.notes`（该句不在 `Q442` 所引前段之内，故只落源行） |

三处一律**不作论定**，口径统一为「转录本可证有无某事、次第如何，不可证某字作某」。

### ⑦ 「异之性质」三层细分之落点（任务书⑤）

顶层二分（**详略之别**／**事实相抵**）**不动**；「事实相抵」下分三层，考订文档判为事实相抵之三节照此细分落到注文：

| 考订文档节次 | 层 | 两造之文 | 落点 |
|---|---|---|---|
| 节 3 | **措辞层**（抵最浅） | 《系年》「以同姓之故，必入」／《左传》「吾姨也」 | `Q442.modern_note` |
| 节 4 | **行动层**（本章核心分歧） | 《系年》「蔡哀侯妻之」／《左传》「止而見之，弗賓」 | `Q442.modern_note` ＋ **`Q161.modern_note`**（双向） |
| 节 7 | **次第层** | 《左传》一次成事／《系年》两次、隔「明歲」 | `Q442.modern_note` ＋ **`E147.summary`** |

---

## 2. 并陈组「四项一体」落点对照（缺一即失并陈之实）

| # | 任务书所令 | 本件落点 | 文件 | 实读核验（详 §4.2） |
|---|---|---|---|---|
| 一 | `sources` 新行 `J001` | `J001`（`category=出土文献`、`notes` 四项必录、入藏简性质固定著录） | `sources_new.csv` | 四项标目齐、含「非科学发掘出土品」句、含三处用字待核 |
| 二 | `passages` 新行挂 `E146`、`source_id=J001`、`quote_type=出土文献`、【】层标答性质、`modern_note` 并列两说且标转引 | `Q442` | `passages_new.csv` | `event_id`／`source_id`／`quote_type` 逐栏比对；`modern_note` 以「【」起首且含三层名 |
| 三 | `Q161` **反向互指**（并陈是双向义务），`quote_original` 一字不动 | `Q161.modern_note` 增句 | `fixes_passages.csv` | `quote_original` 与主表逐字全等；`modern_note` 以原值为前缀；含 `J001`／`Q442` 互指 |
| 四 | `E146`／`E147` `summary` 增句，`reliability=high` 维持 | `E146`／`E147` | `fixes_events.csv` | 两条 `reliability` 实读为 `high`；`summary` 以原值为前缀；除 `summary`／`source_ids` 外逐栏全等 |

**一句话自查**：四项俱在，且第三项（反向互指）与第四项（事目层增句）保证了读者**从《左传》一方读入**（`Q161` 引文、`E146`／`E147` 事目）与**从 J 层读入**（`Q442`）两条路径上都能看见另一说——并陈之实由此成立，J 层未被降为附注。

---

## 3. 合并模拟：怎么模拟的、结果如何

### 3.1 模拟方法（**未改护栏**）

`tools/validate.py` 以脚本自身位置推定仓库根（`ROOT = Path(__file__).resolve().parent.parent`），故只需在临时目录中重建 `tools/`＋`data/csv/` 两级结构即可整体模拟：

1. 在会话临时目录（scratchpad，非本仓库内）建 `sim/`，将 `tools/validate.py` **逐字复制**（复制后以字节比对断言 `sim/tools/validate.py` 与仓库件二进制全同）、`data/csv/` 整目录复制；
2. 在副本上应用本件：`sources` append `J001`、`passages` append `Q442`、`passages`／`events`／`people` 按 `id` 整行覆盖 `Q161`／`E146`／`E147`／`P_XIHOU`（覆盖命中数逐表断言等于待覆盖行数）；
3. 跑 `python sim/tools/validate.py`。

**主表 `data/csv/`、仓库 `tools/` 全程只读**，`git status --porcelain` 实测仅两条 `??`（本件目录 `data/incoming/fix31_xinian/`；另一条 `docs/kaoding_chutuwenxian.md` 系上轮考订文档尚未提交，非本件产物，见 §5 上报 4）。

### 3.2 第一跑（护栏原样）——**预期内报错，全文如下**

```
exit code: 1

校验失败，共 1 处问题：
  - sources.csv 第175行：id 'J001' 不符合规范 ^[ZSGAPBYLT]\d{3}$
```

**这是本件唯一的报错**，且正是裁定 1（`J` 前缀设立）尚未落到护栏所致，属任务书预先声明的预期内错误。本件**不自行改 `tools/validate.py`**（红线 1，护栏归 Skipper）。

**另两项新值未报错，原因如下（实测，非推定）**：

- `sources.category` 新值「出土文献」——`validate.py` 对 `category` **无枚举校验**（全文仅 `CATEGORIES` 一个枚举，作用于 `events.category`），故不报错，改动面仅及撰写口径；
- `passages.quote_type` 新值「出土文献」——`quote_type` **亦无枚举校验**，仅在层标软检处以 `SOFT_CHECK_TIERS.get(qtype, False)` 查表，新档位未在表中即取默认 `False`，故既不报错**也不体检**。**这正是需 Skipper 补一档的理由**：不补则该档天然静默，裁定 6「随立即开软检、开档即净」落空。

### 3.3 第二、三跑（**诊断性**，仅在临时副本上改，仓库 `tools/` 一字未动）

为验证「除 J 前缀正则外其余全绿」，另在**临时副本**的 `validate_diag.py` 上作两处最小改动并各跑一次。两次改动**只存在于会话临时目录**，与交付无关，此处如实记录以备复核：

| 跑次 | 副本改动（逐行 diff） | 结果 |
|---|---|---|
| 第二跑 | `- "sources": re.compile(r"^[ZSGAPBYLT]\d{3}$"),`<br>`+ "sources": re.compile(r"^[ZSGAPBYLTJ]\d{3}$"),`（**1 行**） | `exit 0`，`OK：全部校验通过`，软检警告 **0 条** |
| 第三跑 | 第二跑之外再加 `SOFT_CHECK_TIERS` 一档 `"出土文献": True,` | `exit 0`，`OK：全部校验通过`，软检警告 **0 条** |

**基线对照**：合入前主表现状跑 `python tools/validate.py` 实测 `exit 0`、`OK：全部校验通过`、软检警告 0 条。**第三跑与基线同为零警告**——即裁定 6 所期的「**新档零历史缺口、开档即净**」经实测成立，`Q442` 之【】层标使新档在开启当日即无缺口，非推断。

---

## 4. 自查

### 4.1 红线与边界

| 项 | 结果 |
|---|---|
| 不改 `data/csv/`（红线 3 备料—合入分离） | **未改**（`git status` 实测） |
| 不改 `tools/`（含 `validate.py`，红线 1） | **未改**（字节比对断言） |
| 不动 `site/` | **未动** |
| `team/` 只读不改、其内容不复制入公开文档 | **未改**；本件与交付文档只**引其裁定条目编号与结论要点**，不转录裁定书正文 |
| 写作向内容不入公开数据（红线 5） | 本件**无写作向内容**，附注区为空，见 §7 |
| `〔页码待补〕` 未重新引入 | 本件全文与四个 CSV 均 **0 处** |

### 4.2 实读断言（**读合并后的副本逐字比对，非依记忆**）

**28 项**断言全部通过（逐项实读合并后副本，与主表原值逐栏比对），其中要害九项摘录：

- `Q161.quote_original` 与主表原值**逐字全等**（并陈不得动原文层）；
- `Q161.modern_note`／`E146.summary`／`E147.summary`／`P_XIHOU.notes` 四处均以主表原值**为前缀**（即原句原样保留、只在其后追加，无改写、无删节）；
- `E146.reliability` ＝ `E147.reliability` ＝ `high`（**未动**）；
- `P_XIHOU.death_year_bce` ＝ `""`、`birth_year_bce` ＝ `""`（**未回填**）；
- `E146`／`E147` 除 `summary`／`source_ids` 外全部字段与主表全等；`P_XIHOU` 除 `notes` 外全部字段与主表全等；
- `Q442.modern_note` 以「【」起首、且含「措辞层」「行动层」「次第层」三词（三层细分实际落地）；
- `J001.notes` 含四项标目全部四个；含「非科学发掘出土品」一句（入藏简性质固定著录）；含三处用字之待核并列；
- 合入后行数 `sources` 174／`passages` 425／`events` 235／`people` 153；
- 公开表头无 `novel*` 列。

---

## 5. 需 Skipper 同步的 `validate.py` 改动点清单（**不要自行改，本件未改**）

| # | 落点 | 现值 | 应改为 | 必要性 | 影响面 |
|---|---|---|---|---|---|
| 1 | `ID_PATTERNS["sources"]` 正则（第 75 行） | `^[ZSGAPBYLT]\d{3}$` | `^[ZSGAPBYLTJ]\d{3}$` | **阻断级**——不改则 `J001` 无法合入（第一跑报错即此） | 1 行；仅字母集补 `J`，不影响既有九前缀；实测第二跑 exit 0 |
| 2 | `SOFT_CHECK_TIERS` 开关表（第 51–59 行） | 无「出土文献」键 | 补 `"出土文献": True,` | **裁定 6 所令**——不补则该档静默、「随立即开」落空 | 1 行；实测第三跑 exit 0、警告 0 条（开档即净） |
| 3 | 上述两处的行内注释 | — | 建议各补一句注明 `J`＝出土文献层（v 升下一号，J 前缀）／新档零历史缺口故开档即净 | 体例（既有各处枚举扩充均带注释，如 `PRESENCE`「不在」） | 注释 2–3 行 |

**无须改动者（实测确认，供 Skipper 免于多改）**：

- `sources.category` 新值「出土文献」——`validate.py` 无 `category` 枚举，**无改动点**；
- `passages.quote_type` 新值「出土文献」——除上表第 2 项软检开关外，**无枚举校验、无其他改动点**；
- 年代边界、`PRESENCE`、`CATEGORIES`、`REL_TYPES` 等**均不涉及**（本件不新立事目、不动 `event_people`、不动 `relations`）。

**合入顺序建议**：先落 `validate.py` 两处改动 → 再合本件四个 CSV → `python tools/validate.py`（预期 exit 0、警告 0）→ `python tools/csv_to_json.py` → 一并提交。若先合数据后改护栏，中途 `validate` 必红，非数据之误。

---

## 6. conventions 待写条文（**升下一号**，勿写死版本号）

裁定书十一条须落规范者，逐条列落点如下（措辞可由领队定稿，本节只备条文要点与落位）：

| # | 落点 | 条文要点 | 出处 |
|---|---|---|---|
| 1 | §2 ID 规范表 `sources` 行 | 补 `J###`（出土文献层）；「九个前缀」计数改为**十**，注明 `J` 为本次新增 | 裁定 1 |
| 2 | §2 新节「`J###` 前缀分层纪律」 | ① J 为**独立见证层**，与 `Z` 平行、**不设高下**；② J 单独支撑者默认 **`medium`**；③ 两条理由入原文义——「**出土文献之早是载体之早、非记载之早**」与入藏简来源链之额外不确定；④ 四条尺度（互证→`high`／独有而属编年骨架→`medium`／独有且相抵或系争议关键字→`low`／国势总结语不作骨架、只作 `passage`）；⑤ **入藏简性质固定著录**——凡非科学发掘之入藏简，`sources.notes` 必著一句来源性质，medium 默认的理据必须可见 | 裁定 2、8 |
| 3 | §2（同节或紧邻） | **金文归 J（预防性）**：铭文是文本证言、器物是物——一物两录，器入 `A`、铭入 `J`，两行并立、`notes` 互指，不合并为一行；本库现无金文条目，先立规矩后遇事 | 裁定 7 |
| 4 | §7 新通例「出土文献与经传异文，并陈不裁」 | 通则灵魂一句入原文义：「**版本异文有善本可问，独立两支无正本可言，不得比照择一。**」附三种例外及其限定语（①独立第三方可断——第三方须与二说均无源流关系，注家之说、《史记》一类同源者不可；②异出于释读／转录层面者径作用字注——**须已取整理本核定**方可如此判；③一方与自身内证相抵者只降权重、不改并陈格局）。并注明：**并陈是双向义务**——「只在一侧注异，等于把 J 层降成附注」，两侧注文须互指（首例 `Q161`↔`Q442`） | 裁定 3、10 |
| 5 | §7「异之性质」条 | 顶层二分（详略之别／事实相抵）**不动**；「事实相抵」下分**措辞层／行动层／次第层**三层，保二分之简、得三层之实；三层相抵有深浅（措辞层最浅，行动层与次第层最深）。首例即本件三节 | 裁定 5 |
| 6 | §7 新口径句 | 「**转录本可证有无某事、次第如何，不可证某字作某。**」——电子转录本与整理本释文在引用效力上的分界；凡涉用字者标「待纸本」，不得以推理代核对（与 §10.2 自限同一道防线） | 裁定 9 |
| 7 | §7 软检分档 | `quote_type` 新值「**出土文献**」并**随立即开软检**（`SOFT_CHECK_TIERS["出土文献"]=True`）；开档前提（该档缺口已核定、已入有序修缮节奏）由**新档天生零历史缺口**自然满足，本轮实测开档即零警告——本库少有的「开档即净」 | 裁定 6 |
| 8 | §2 或 §7（`sources` 撰写口径） | `sources.category` 新值「**出土文献**」；同层内批次区分办法落在现有八栏内、**不加栏**：`work`＝批次丛书名、`section`＝篇名·章次、`title`＝含册次全称 | 裁定 11、J 前缀提案 §2.4 |
| 9 | §11 待决事项 | 登记试点排程：**第六章**（骊姬之乱至晋文入晋）为下一试点；第二、二十章押后；**越 −464 截断规则**入待议池 | 裁定书末「试点排程」 |

**版本号一律写「升下一号」**，本件不写死具体版本号。

---

## 7. 上报事项（依 §10 记录上报，不自行裁量）

1. **`E146`／`E147` 的 `source_ids` 加挂 `J001`——本件唯一超出任务书字面的改动，请复核。**
   - **为何必须加（至少 `E146`）**：conventions §7 v1.21 判例明立「某 `passage` 引文之源，必须出现在其所挂事件的 `source_ids` 中」（**正向包含**是硬要求）。`Q442` 挂在 `E146` 而源为 `J001`，若 `E146.source_ids` 不含 `J001`，即违此判据。此项**非可选**。
   - **`E147` 为何一并加（此项可商）**：`E147.summary` 增句确实引述了《系年》之说，而 `source_ids` 语义是「本条所引全部来源」；同判例又已明立「不得仅凭某源出现在 `source_ids` 中即推断其为史实骨架依据」，先例为 `E176` 之 `P011`（《陈风·株林》，P 层舆论材料在 `source_ids` 中而不作骨架）与 `E046` 之 `S001`（有源无 `passage`）。故加挂不致误升其层级。**但 `E147` 下并无引 `J001` 的 `passage`**，若领队认为「无引文之源不入 `source_ids`」更清爽，删去 `E147` 之 `;J001` 即可，`summary` 增句与并陈四项一体均不受影响（`E146` 一处**不可删**）。
2. **`J001.url` 本轮未能实测复验**（网络不可达，见 §0.3）。若领队或 Skipper 处网络可达，请顺手实测该页可否打开、页名是否作「系年」（另有可能作「繫年」），一句话回告即可清账；在此之前 `notes` 已如实登记为复核项，不作断言。
3. **本件目录名与轮次编号不一致**：任务书指定产出路径为 `data/incoming/fix31_xinian/`（`fix31`），而交付文档为 `docs/delivery_sophia_r32.md`（r32）。本件**照任务书字面**落在 `fix31_xinian/`，未自行改名。归档时 `docs/changes/` 的命名（`r31_xinian.md` 抑或 `r32_xinian.md`）请领队一句话定，以免归档与轮次台账错位。
4. **`docs/kaoding_chutuwenxian.md` 尚未入库**：`git status` 显示其为未跟踪文件（`??`），系上轮考订产物、非本件所产。裁定书正据此文成立，建议随本轮合入一并提交入库，否则日后裁定书与其依据之文档不同在库中。
5. **`Q419`／`Q420` 两个空号仍留空**（r28 裁定 a：空号非退役号、编号唯一不求稠密）。本件取 `Q442` 接尾号，**不填补空号**，特此留痕以免后人误以为遗漏。
6. **一处著录自限（非疑点，只留痕）**：`Q442.quote_original` 未录转录本所标简号【23】–【25】。理由：本库 `modern_note` 的编者层标亦用【】，简号入引文正文会与层标同形而生歧义；简号已在 `docs/kaoding_chutuwenxian.md` §3.0（甲）完整留痕，可随时回取。若领队认为简号应入正文（出土文献引用常例确有标简号者），可另定体例，本件依此重出一版即可。
7. **裁定书未及而本件已从严处置者**：《系年》第五章末「文王以北啟出方城，圾肆於汝，改旅於陳焉，取頓以贛陳侯」一节（考订文档节 12，属尺度④「国势总结语」），本件**未立 `passage`、未立事目**——尺度④只言「不作史实骨架用，只作 `passage`」，但该节释读三处未定（「圾肆」「贛」等，待纸本）、且非本试点章并陈之所系，本件从严不取。若领队认为该节宜一并作 `passage` 收入（以见《系年》体例之异），可下轮补一行，本件先行留痕。

---

## 附注区（写作向备注）

**无。** 本件为编年著录之数据备料，通篇引原文、校异文、断性质、注出处，未产生写作向内容（依 CLAUDE.md 红线 5 与 conventions §7，写作向内容不入公开数据）。
