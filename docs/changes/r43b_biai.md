# round43_final_biai · 「嬖」字出处勘误落地件（追加裁定 24）

任务书：【任务 For Sophia · r43 收官备料收尾】（`team/round40_41_prompts.md`「r43 收官件」节·任务 7）。
所据裁定：**追加裁定 24**（同节「领队追加裁定备案（r43 收官 · 2026-08-29）」第 24 条，逐字查得原文，无一处凭记忆推断）。
关联裁定：裁定 21（2026-08-18 备案，「嬖」入注备考之原令，其初记即本次所勘之误）、裁定 22（2026-08-18 备案，「并存之异」强度核可）、裁定 18（纸本扫描本视同纸本核字）。
权威规范：`docs/conventions.md`（**v1.34**，唯一权威）——§7「条件关闭之留痕，与判定反转之留痕同例」（v1.34 新增，r43）、§7 v1.29「判定反转须如实记录，不得抹平」。
先例与体例：`docs/changes/r43_zhiben2.md`、`docs/changes/r43_xinian6_annot.md`。

**本件性质：定点整行替换 2 行之著录勘误件，改动只落在注文栏。** 无新增行、无删除行、无字段增删、无新 ID、无既有 ID 变动、无 `reliability` 变动、无坐标变动、无 `presence` 变动、无年份／`place_id`／`source_ids` 变动、无 `quote_original` 变动。**九表行数逐表全等。**

**只备料，未合入。** `data/csv/` 主表**一字未动**（`git diff --stat -- data/csv/` 实测为空；`git status --porcelain` 实测仅 `M CLAUDE.md`〔领队所置，非本件〕与两路 incoming 目录之 `??`），`tools/` 一字未动（含 `validate.py`，护栏归 Skipper，红线 1），`site/` 未动，未重生成 `site/data/`，未 `git add`／`commit`。`docs/conventions.md` **一字未动**（本轮明令不改；§12.2 之收录由 Skipper 依追加裁定 26 于合入时执行）。

| 文件 | 内容 | 行数 |
|---|---|---|
| `CHANGES.md` | 本文件 | — |
| `fixes_passages.csv` | 整行替换 `Q443`（`modern_note` 一栏） | 1 |
| `fixes_sources.csv` | 整行替换 `J002`（`notes` 一栏） | 1 |
| `sim_biai.py` | 本件之合并模拟＋46 条实读断言脚本（供 Skipper 复跑；**不属数据，合入时不入 `data/csv/`**） | — |

---

## §1 · 逐行变更清单

两行，各只动一栏；**变动字段由合并模拟实读比对得出，非人工申报**。

| # | 表 | ID | 变动字段 | 旧长 | 新长 | 增量 | 所据 |
|---|---|---|---|---|---|---|---|
| 1 | `passages` | `Q443` | `modern_note`（**唯一变动栏**） | 2668 | **3174** | **+506** | 追加裁定 24 ＋ **27** |
| 2 | `sources` | `J002` | `notes`（**唯一变动栏**） | 3314 | **3725** | **+411** | 追加裁定 24 ＋ **27** |

### 1.1 `passages.Q443.modern_note`

改写落在原注文之 **④「嬖」异说入注备考**与 **⑤「转录本不可证某字作某」口径之实证**两节，另于【判定三变】末补一括注。逐项：

- **④ 出处改定为二**——原文只作「实核」之陈述（记「初记之出处与实测不合，已列上报，未据以入库，请复核」一类未决语气）；今依裁定 24 改定为**已定之出处二者**：㈠《左传·庄公二十八年》本文作「驪姬**嬖**」（`Q073`）；㈡ 本库 r40 备料注文所记「婢、嬖音近，或读为嬖」之通假说。**著录随改**，未决语气随之撤去。
- **④ 三步留痕新立**——增【出处之勘误·三步留痕，不抹平】一段，明写「**不写作「一直如此」**」，三步俱录：（一）**初记**——裁定 21 原文记「维基文库『嬖』为转录破读」（**原语照留不删**）；（二）**实核**——本库 2026-08-29 第三度实取该页 `action=raw` 源码复核（HTTP 200、22810 字节，与 2026-08-16、2026-08-17 两次实取字节数全同），实测第六章通行字正文与【原簡】隶定栏皆作「婢」，全页「嬖」三见俱在他章（第一章「褒姒嬖于王」、简 69 一带「齊三嬖大夫」及其隶定栏），第六章无「嬖」字；（三）**勘误**——领队采纳本库实核之结论，判初记**系误**，出处改定如上。
- **④ 界限三条**——明记此勘误**只涉「嬖」字出处之著录**：不涉本条之判定（「异·措辞层·并存之异」**不变**）、不涉底本（维基文库转录本**仍为 `J002` 之底本**）、不涉本章五字之核定结果。
- **⑤ 实证之内容改定**——「转录本不可证某字作某」口径之**实证价值仍成立**，惟实证之**内容**由「转录本之破读」改为「**偶合无证字之力**」：转录本此处与整理本偶合作「婢」，而同章「牙」字转录本作「牙」、整理本正读为「**與**」（`Q448`）——一处偶合、一处不合，**证字之力不可由偶合推得**。原「破读」之说明记已由领队裁定否定。
- **【判定三变】末补一括注**——声明本次系**著录勘误**、只涉出处不涉判定，**故不列为第五变**，其反转过程另于 ④ 留痕。判定三变（并定案共四判）之原文与婢案原回退条件之文字**一字未删**。

### 1.2 `sources.J002.notes`

改写落在【本章五处用字】之 **①「婢／嬖」**一目内。逐项：

- **出处改定**——「嬖」入注备考之出处，由原「实核（未决）」改定为《左传·庄公二十八年》本文「驪姬嬖」与本库 r40 所记「婢、嬖音近」之通假说。
- **【出处之勘误·留痕不抹平】新立**——三步俱记：**初记**（裁定 21 记「嬖」为**本页之转录破读**，原语照留不删）→ **实核**（第三度实取源码，实测本页第六章正文与隶定栏皆作「婢」，全页「嬖」三见俱在他章，第六章无「嬖」字；初记与实测不合，列为上报）→ **勘误**（领队采纳实核之结论，判初记系误，著录随改）。
- **【须辨明者】新立**——**本页仍为本条之底本**；本章五字之核定结果（婢／里之克／悼子／梁城／與）与口径句「转录本可证有无某事、次第如何，不可证某字作某」**一概不因之改动**；本案对该口径之实证价值仍成立，惟实证之内容为「**偶合无证字之力**」（互指 `Q443` ⑤）。

### 1.3 未动之行（列此供核，非无关）

- `passages.Q073`（《左传》一侧，并陈之对侧）**一字未动**——其现文已作「本条之『嬖』即『嬖』字之出处所在（《系年》一侧无此字，只作异说备考，实核详见 `Q443` ④）」，与裁定 24 所定之出处㈠**本已相合**，无可改者。已立断言核实。
- `passages.Q448`（牙／與）**一字未动**；`E076`／`E082`／`E083`／`E084` 等事目**一字未动**（裁定 24 明言不涉判定与骨架）。
- **全库范围检索**：注文中含「破读」一语者，实测**只 `Q443`（2 见）与 `J002`（1 见）两行**，无第三行遗留旧说——「著录随改」之覆盖面已实测封闭，非估计。

---

## §2 · 九表行数断言（**实测数，非预填**）

合并模拟以主表副本实读计行（`csv.DictReader` 计数，不含表头）。**实测与预期逐表相符**：

| 表 | 合入前 | 合入后（实测） | 预期 | 判 |
|---|---|---|---|---|
| `sources.csv` | 177 | **177** | 177 | 全等 |
| `places.csv` | 91 | **91** | 91 | 全等 |
| `passages.csv` | 436 | **436** | 436 | 全等 |
| `events.csv` | 236 | **236** | 236 | 全等 |
| `people.csv` | 153 | **153** | 153 | 全等 |
| `event_people.csv` | 612 | **612** | 612 | 全等 |
| `relations.csv` | 282 | **282** | 282 | 全等 |
| `archaeology.csv` | 8 | **8** | 8 | 全等 |
| `background.csv` | 11 | **11** | 11 | 全等 |

**断言：无新增、无删除、无新 ID，九表行数全等。** 另以 SHA-256 实测：未触及之七表（`archaeology`／`background`／`event_people`／`events`／`people`／`places`／`relations`）与仓库件**逐表全同**，`passages`／`sources` 二表已改（预期内）。

**改动只落在注文栏之声明**：本件全部改动**只落在 `passages.modern_note` 与 `sources.notes` 两栏**——即注文栏。除此二栏外，两行各栏及全库他行**一字未动**，`quote_original`、`reliability`、坐标、`presence`、年份、`place_id`、`source_ids` 一概未触。此声明非申报，系由模拟脚本逐栏实读比对后立断言核实（见 §3）。

---

## §3 · 合并模拟实测输出（`sim_biai.py`，**46/46 通过**）

脚本取**专名** `sim_biai.py`，临时副本落**专用子目录** `mergesim_biai/`——r43 两路曾因共用 `merge_sim.py` 一类通名被互相覆写，本轮两路脚本名与临时目录**各自独立、不共享路径**（另一路为 `sim_xutian.py` ／ `mergesim_xutian/`）。临时目录由 `tempfile.gettempdir()` 推得、可由环境变量 `CHUNQIU_SIM_TMP` 覆写，**不写死会话目录**；`ROOT`／`INC` 由脚本自身位置推得。

复跑：`python data/incoming/round43_final_biai/sim_biai.py`（仓库根目录）。实测：

```
== 合并动作 ==
  覆盖 passages.csv  Q443      变动字段 = ['modern_note']
  覆盖 sources.csv   J002      变动字段 = ['notes']
…
  —— 46/46 通过

== validate.py（临时合并副本）==
OK：全部校验通过
exit code = 0
```

**`python tools/validate.py` 于合并后之临时副本 exit 0、输出仅「OK：全部校验通过」一行，无任何告警。** 仓库根目录之 `python tools/validate.py`（主表未动之现状）亦实测 exit 0、无告警。

46 条断言分十组：① 改动范围（7 条：两行除注文栏外各栏未动、`quote_original` 未动、他行未动、`Q073`／`Q448` 未动）；② 出处改定为二（4 条）；③ 三步留痕与初记原语照留不删（8 条）；④ 实核之实测数（5 条：三度实取／HTTP 200／22810 字节／字节数全同／隶定栏亦作「婢」／「嬖」三见俱在他章）；⑤ 勘误界限四条（7 条：判定不变、底本不变、五字不变、只涉著录）；⑥ 口径实证价值成立而内容改为「偶合无证字之力」（4 条，含「牙／與」反证仍在）；⑦ 判定三变留痕未改写、本次不冒充第五变、婢案原回退条件文字照留（3 条）；⑧ 全库「破读」一语只余二行（1 条）；⑨ **裁定编号回补（5 条，7b 新增，详见 §5.1）**；⑩ 无新 ID、无占位遗留（2 条）。

> **一处 harness 自纠，如实记录**：首跑 40/41，一条 FAIL——断言「三次字节数全同」照裁定 24 之字面写，而注文实际作「与 2026-08-16、2026-08-17 两次实取字节数全同」。**实测所错者为断言、非数据**（同一事实、措辞不同，本次即第三次；且注文他处另有「三次字节数全同」一语）。故**改断言、不改数据**，并于脚本内留注说明「断言照注文实际措辞写，不照裁定字面套」。复跑 41/41（7b 回补后为 46/46）。**此处置已由追加裁定 28（2026-08-30 领队）核可**，并令该注**留存不删**——「QA 装置测错对象时，错的是装置」；本轮实测该注仍在脚本内。

---

## §4 · 与追加裁定 24 原文之逐项复核

| 裁定 24 所令 | 本件落实 | 判 |
|---|---|---|
| 初记「维基文库『嬖』为转录破读」**系误** | `Q443` ④、`J002` ① 俱明记「系误」 | 相符 |
| 实核三度实取、HTTP 200、22810 字节、字节数全同 | 两行俱载（措辞见 §3 自纠注） | 相符 |
| 第六章正文与【原簡】隶定栏**皆作「婢」** | 两行俱载 | 相符 |
| 全页「嬖」三见俱在他章（第一章褒姒、简 69 齊三嬖大夫） | `Q443` 逐一列举，`J002` 概述 | 相符 |
| 出处**改定为二**（㈠ `Q073` 本文「驪姬嬖」；㈡ r40 通假说） | 两行俱改定为此二者 | 相符 |
| **著录随改** | 两行注文即改后之现文 | 相符 |
| 不涉本条判定（「异·措辞层·并存之异」不变） | `Q443` 明写；判定三变原文未改 | 相符 |
| 不涉底本（维基文库转录本仍为 `J002` 底本） | `Q443`／`J002` 俱明写 | 相符 |
| 不涉本章五字之核定结果 | 两行俱明写（`J002` 并列五字） | 相符 |
| 裁定 21 原文与初记文字**照留不删**，三步留痕不写作「一直如此」 | 原语照留，三步俱记，「不写作『一直如此』」明文在 | 相符 |
| 口径实证价值仍成立，内容改定为「**偶合无证字之力**」 | 两行俱改定，「牙／與」反证在 | 相符 |

**逐项相符，无一项落空。** 唯有一处著录与裁定**现行文本**之称谓不一致，非实质不符，列于 §5 上报，本轮**未自行改动**。

---

## §5 · 上报待裁（本轮未自行取舍）

**⚑ 一项：裁定之称谓与编号，两行注文所记为「领队勘误裁定（r43 上报处置第①项，2026-08-29）」，而该裁定现已入档为「追加裁定 24」。**

- **事实**：两行注文写就时，本裁定之条文尚未回写 `team/round40_41_prompts.md` 之追加裁定序列（当时序列止于第 23 条），故本库依「不代拟裁定编号」之纪律，只记其描述性称谓。今该条文**已入档**，编号为**追加裁定 24**（见「领队追加裁定备案（r43 收官 · 2026-08-29）」第 24 条），本行所记之称谓遂成**未及回补之旧称**，非误记。
- **出现处**：`Q443.modern_note` **3 见**、`J002.notes` **2 见**，句式均为「领队勘误裁定（r43 上报处置第①项，2026-08-29）」。
- **修正建议（供领队／Skipper 裁夺，一次替换即可）**：将上开五处之「领队勘误裁定（r43 上报处置第①项，2026-08-29）」回补为「**追加裁定 24（2026-08-29 备案）**」，或作「**追加裁定 24（2026-08-29 备案，即 r43 上报处置第①项）**」以兼存旧称之线索。本库倾向后者——与库内既有「追加裁定 21（2026-08-18 备案）」之句式一律，且不失原称之可追。
- **本轮不改之由**：任务书本件明令「只补账目文书与合并模拟，不做新的史料判断」「落地文字已写」，回补编号非收尾四项之一；且改后须同步改动 `sim_biai.py` 之相应断言。依「拿不准即停、上报，不自行取舍」，**照原文留、列此上报**。
- **同型一项在另一路**，情形更重（`L_XUTIAN` 注文内有一句已随条文入档而失实），详见 `data/incoming/round43_final_xutian/CHANGES.md` §5。

### 5.1 结案（**追加裁定 27**，2026-08-30 领队裁定，任务 7b 落地）

**上开 §5 之上报原文照留不删**（写下过的岔路不因已走通而抹去）——领队已裁：**两路一并回补，照本库所拟**，并明记裁定 27 之起因即**领队回写迟滞**（追加裁定 24、25 作出于 08-29，回写任务书于 08-30）。

本路落实（**只回补编号，其余一字不动**）：

| 表 | ID | 栏 | 回补处数 | 旧称 | 新称 |
|---|---|---|---|---|---|
| `passages` | `Q443` | `modern_note` | **3 处** | 领队勘误裁定（r43 上报处置第①项，2026-08-29） | **追加裁定 24（2026-08-29 备案，即 r43 上报处置第①项）** |
| `sources` | `J002` | `notes` | **2 处** | 同上 | 同上 |

- **句式**与库内既有「追加裁定 21（2026-08-18 备案）」一律；**旧称原样嵌于括注内**（「即 r43 上报处置第①项」），线索可追，实测 `Q443` 3 见、`J002` 2 见，旧称零残留。
- **范围实测**：以回补前之快照作字符级 diff，两栏之变动**仅为该 5 处原位替换**（`Q443` +18 字、`J002` +12 字），**无第六处改动**；除该二注文栏外，两行各栏及全库他行一字未动。裁定 27「不得顺势清理、不得借此修饰精简既有留痕文字」**照办，未动一字**。
- **断言同步**：`sim_biai.py` 增 5 条（新称处数 3／2、旧称零残留、旧称线索仍可追、句式一律），41 → **46 条**，复跑 **46/46 通过**、`validate` exit 0 无告警。
- **本节 §5 之上报遂告结案。** 另一路之同型项由 `data/incoming/round43_final_xutian/CHANGES.md` §5.1 记。

---

## §6 · 附注（写作向与体例备忘，**不入公开数据表**）

- 本案之价值不在「嬖」字本身，而在**一条依据链如何留下自己的错误史**：初记写在裁定里、实核推翻它、勘误采纳实核——三步俱在同一段注文内可读。若径改为「出处一直是《左传》」，读者便无从知道本库曾误以转录本为出处，也无从知道这类误认还可能潜伏在何处。conventions §7 v1.29 所立者正是此义。
- 「**偶合无证字之力**」一语可作口径条之常备例句：同一转录本、同一章，「婢」偶合而「牙／與」不合——证据的力量不能由结果碰巧对上而事后追认。

（本节为附注区，依红线 5 与 validate 之 `novel*` 护栏，写作向内容不入公开数据表。）
