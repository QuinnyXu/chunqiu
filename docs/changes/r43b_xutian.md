# round43_final_xutian · 许田案关闭落地件（追加裁定 25）

任务书：【任务 For Sophia · r43 收官备料收尾】（`team/round40_41_prompts.md`「r43 收官件」节·任务 7）。
所据裁定：**追加裁定 25**（同节「领队追加裁定备案（r43 收官 · 2026-08-29）」第 25 条，逐字查得原文，无一处凭记忆推断）。
关联裁定：裁定 12（谭图逐点核对三分处置，「不合者改坐标」，并明列本点为已知两例之一）、裁定 23（否定性核字结果之效力自限）、裁定 16／17（谭图引注体例与两书版次基准）。
权威规范：`docs/conventions.md`（**v1.34**，唯一权威）——§7「**条件关闭之留痕，与判定反转之留痕同例**」（v1.34 新增，r43；实测该条现文在库，首例记为 `Q443` 婢案之回退条件关闭）、§7 v1.29「判定反转须如实记录，不得抹平」、§4 投影公式。
先例与体例：`docs/changes/r43_zhiben2.md`（本行坐标改点之原件）、`docs/changes/r43_xinian6_annot.md`。

**本件性质：定点整行替换 1 行之关闭记录件，改动只落在注文栏，且为纯追加（原文一字未删）。** 无新增行、无删除行、无字段增删、无新 ID、无既有 ID 变动、无坐标变动、无 `certainty`／`coord_certainty` 变动、无 `modern_location` 变动、无 `source_ids` 变动。**九表行数逐表全等。**

**只备料，未合入。** `data/csv/` 主表**一字未动**（`git diff --stat -- data/csv/` 实测为空；`git status --porcelain` 实测仅 `M CLAUDE.md`〔领队所置，非本件〕与两路 incoming 目录之 `??`），`tools/` 一字未动（含 `validate.py`，护栏归 Skipper，红线 1），`site/` 未动，未重生成 `site/data/`，未 `git add`／`commit`。`docs/conventions.md` **一字未动**（本轮明令不改）。

| 文件 | 内容 | 行数 |
|---|---|---|
| `CHANGES.md` | 本文件 | — |
| `fixes_places.csv` | 整行替换 `L_XUTIAN`（`coord_basis` 一栏） | 1 |
| `sim_xutian.py` | 本件之合并模拟＋24 条实读断言脚本（供 Skipper 复跑；**不属数据，合入时不入 `data/csv/`**） | — |

---

## §1 · 逐行变更清单

一行，只动一栏，且**只在栏末追加、栏内原有文字一字未删**；变动字段由合并模拟实读比对得出，非人工申报。

| # | 表 | ID | 变动字段 | 旧长 | 新长 | 增量 | 形态 | 所据 |
|---|---|---|---|---|---|---|---|---|
| 1 | `places` | `L_XUTIAN` | `coord_basis`（**唯一变动栏**） | 778 | **2570** | **+1792** | **纯追加**（字符级 diff 实测：主表原文为新文之前缀，两段俱在栏末） | 追加裁定 25 ＋ **27** |

### 1.1 所追加者：【★r43 收官 · 許田案关闭记录】一段

- **关闭之标的**——上文 ② 所立之待考实项（「谭图同幅同区另标『許』（`L_XU`，距新点约 5.6 km）……本库无从辨其所读者是否即許田本标」）**及**其后所附之「**列待裁**：俟站长复读一句『图上「許田」二字落于何处、与「許」二字是否并见』，再定两栏升否」一句，经领队裁定**予以关闭**。
- **原文照留不删**——上述两段**一字未删**，只在其后注明其已关闭，明引 conventions §7 v1.34「条件关闭之留痕，与判定反转之留痕同例」，并如裁定 25 所辨：该条首例为 `Q443` 婢案之**回退条件**关闭，本项为**待考实／待裁项**之关闭，**同族而非同物**，共通者只在「关闭须留痕、不得径删」一点。
- **【所关闭者何问】**——限定所关闭者只是「本库是否继续追问该一考实项」一问，**关闭 ＝ 本库不再追此项，非其他**。
- **【关闭之效力界限·三条】**——① **既有处置一字不变**（坐标维持 33.96／113.94，两栏维持 `low`／`low`，`modern_location` 维持「河南省许昌市东南一带」），**关闭不是升级的理由**：判 `low` 之理由 ① 本就独立于本项、不因关闭而消失，理由 ② 所陈之事实（二标相邻约 5.6 km、无从辨所读是否即許田本标）并未改变；② **不得读作「此疑已释」**——本项之关闭系**裁定所致，非新核所得**，自 2026-08-29 站长核对以来本库未取得任何新材料，「释与关闭是两回事」，故不作「已释」「已辨」「已核实」一类断言语气，**二标之疑其实仍在**；③ **他日有新材料仍可重启**，届时依 `L_BOJU` 先例一次判两栏，**关闭不是禁入**。
- **【证据等级】**——本段所记为**领队裁定**，**非任何一次核对之结果**，**不入本行依据链**，不与本行既有之「纸本，站长核对」（谭图落点）、「站长告知线索·待考实」等措辞混同（四种证据措辞不串之同一纪律）；末句仍如实答：图上究竟怎么标，**本库不知道**。
- **【裁定条文之著录状态·一处如实留白】**——写就当日条文尚未入档，故不代拟裁定编号。**该段原文照留不删**；其后另追加【编号回补（2026-08-30）】一段（依追加裁定 27），详见 §1.1a 与 §5.1。

### 1.1a 所追加者之二：【编号回补（2026-08-30）】一段（**追加裁定 27**，任务 7b）

栏末**再追加**一段，原文（含 1.1 末之留白段）**一字未删、未改写**：

- **回补条件已成就**——上文「条文尚未回写、序列止于第 23 条」系本段写就当日（2026-08-29）之实况，**照留不删**；今该条文已入档为 `team/round40_41_prompts.md`「领队追加裁定备案（r43 收官 · 2026-08-29）」之**第 25 条**，本项正式编号即「**追加裁定 25（2026-08-29 备案）**」。
- **迟滞如实记**——该裁定**作出于 2026-08-29、回写任务书于 2026-08-30，迟一日**；**不写作「即时入档」**，并申明上文之「尚未回写」正是这一日之间的实况，**非误记、亦非本库不查即书**。（此为裁定 27 明令须记之一项，其起因即领队回写迟滞。）
- **自记所依**——本段依**追加裁定 27**（2026-08-30 领队）追加，并记该裁定「只回补编号，其余一字不动」之令，本行照办。

**留痕之形态**：本行 `coord_basis` 至此叠了三层——原依据链（谭图改点）→ 关闭记录（裁定 25）→ 编号回补（裁定 27）。**三层俱为追加，无一层改写前一层**；读者顺栏读下去，看得见这库曾有过「条文未入档」的一刻，也看得见它何时补上。

### 1.2 未动之栏与未动之行（列此供核，非无关）

- `L_XUTIAN` 之 `lat`／`lng`（33.96／113.94）、`certainty`／`coord_certainty`（`low`／`low`）、`modern_location`（「河南省许昌市东南一带」）、`ancient_name`／`state`／`place_type`／`description`／`source_ids` **一字未动**——裁定 25 明令「两栏维持 `low`，坐标与 `modern_location` 照准不动」。已逐栏立断言核实。
- `places` 其余 **90 行**一字未动（含相邻之 `L_XU` 许国之都、`L_WULU` 五鹿、`L_YINGGU` 颍谷）。
- **presence 从严之附带说明**：本件不涉 `event_people`，`presence` 一栏全库未触。

---

## §2 · 九表行数断言（**实测数，非预填**）

合并模拟以主表副本实读计行（`csv.DictReader` 计数，不含表头）。**实测与预期逐表相符**：

| 表 | 合入前 | 合入后（实测） | 预期 | 判 |
|---|---|---|---|---|
| `sources.csv` | 177 | **177** | 177 | 全等 |
| `places.csv` | 91 | **91** | 91 | 全等 |
| `passages.csv` | 436 | **436** | 436 | 全等 |
| `events.csv` | 236 | **236** | 236 | 全等 |
| `people.csv` | 153 | **153** | 153 | 全等 |
| `event_people.csv` | 612 | **612** | 612 | 全等 |
| `relations.csv` | 282 | **282** | 282 | 全等 |
| `archaeology.csv` | 8 | **8** | 8 | 全等 |
| `background.csv` | 11 | **11** | 11 | 全等 |

**断言：无新增、无删除、无新 ID，九表行数全等。** 另以 SHA-256 实测：未触及之八表与仓库件**逐表全同**，`places` 一表已改（预期内）。

**改动只落在注文栏之声明**：本件全部改动**只落在 `places.coord_basis` 一栏**——即注文栏（地望依据链之著录栏）。除此一栏外，`L_XUTIAN` 各栏及全库他行**一字未动**；坐标、`certainty`／`coord_certainty`、`modern_location`、`reliability`、`presence`、`place_id`、`source_ids` 一概未触。此声明非申报，系由模拟脚本逐栏实读比对后立断言核实（见 §3）。

---

## §3 · 合并模拟实测输出（`sim_xutian.py`，**24/24 通过**）

脚本取**专名** `sim_xutian.py`，临时副本落**专用子目录** `mergesim_xutian/`——r43 两路曾因共用 `merge_sim.py` 一类通名被互相覆写，本轮两路脚本名与临时目录**各自独立、不共享路径**（另一路为 `sim_biai.py` ／ `mergesim_biai/`）。

> **一处工程改动，如实声明**：本脚本原将临时目录写死为上一会话之 scratchpad 路径（`…/2c7dcc76-f6b1-4107-b2fd-a792621f0973/scratchpad/mergesim_xutian`），换会话即不可复跑。本轮依任务书第 2 项改为：`INC`／`ROOT` 由脚本自身位置推得，`TMP` 取 `tempfile.gettempdir()` 下之 `mergesim_xutian`、并可由环境变量 `CHUNQIU_SIM_TMP` 覆写，**不写死任何会话目录**。`sim_biai.py` 照同一写法。**改动只涉路径推导，断言与合并逻辑一字未动。**

复跑：`python data/incoming/round43_final_xutian/sim_xutian.py`（仓库根目录）。实测：

```
== 合并动作 ==
  覆盖 places.csv: L_XUTIAN    变动字段 = ['coord_basis']
…
  —— 24/24 通过

== validate.py（临时合并副本）==
OK：全部校验通过
exit code = 0
```

**`python tools/validate.py` 于合并后之临时副本 exit 0、输出仅「OK：全部校验通过」一行，无任何告警。** 仓库根目录之 `python tools/validate.py`（主表未动之现状）亦实测 exit 0、无告警。

24 条断言分七组：① 既有处置不因关闭而变（4 条：坐标、两栏 `low`、`modern_location`、除 `coord_basis` 外全行未动）；② 原待考实项与「列待裁」一句照留不删、明引 §7 v1.34（3 条）；③ 关闭记录本体与三条效力界限（5 条）；④ 绝不写成「此疑已释」（3 条，含「已释／已辨／已核实」三词只出现于其被禁止之句内之计数核实）；⑤ 证据等级不串、不入依据链、末句仍答「本库不知道」（3 条）；⑥ **裁定编号：原留白照留 ＋ 编号回补（4 条，7b 由 1 条增补，详见 §5.1）**；⑦ 他行未受影响、无占位遗留（2 条）。

---

## §4 · 与追加裁定 25 原文之逐项复核

| 裁定 25 所令 | 本件落实 | 判 |
|---|---|---|
| 关闭 ② 所立之待考实项（`L_XU` 相邻、无从辨所读是否即許田本标） | 关闭记录明引该项原文并声明关闭 | 相符 |
| 关闭其后所附「**列待裁**」一句 | 同上，逐字引其原句 | 相符 |
| `certainty`／`coord_certainty` **两栏维持 `low`** | 实读均为 `low`，并明写「关闭不是升级的理由」 | 相符 |
| 坐标 33.96／113.94 **照准不动** | 实读 33.96／113.94 | 相符 |
| `modern_location`「许昌市东南一带」**照准不动** | 实读「河南省许昌市东南一带」 | 相符 |
| 所关闭者只是「本库是否继续追问此问」，**非断言二标必为一标** | 【所关闭者何问】限定明确；界限 ② 明记二标之疑其实仍在 | 相符 |
| 上述两段文字**照留不删**，只就地注明已关闭 | 字符级 diff 实测为**纯追加**，原文零删除 | 相符 |
| 引 §7「条件关闭之留痕，与判定反转之留痕同例」 | 明引，版本记为 v1.34（实测该条现文在库） | 相符 |
| 首例为 `Q443` 婢案之回退条件关闭，本项**同族而非同物** | 明写此辨 | 相符 |

**逐项相符，无一项落空。** 唯有一处著录之留白已随条文入档而失实，列于 §5 上报，本轮**未自行改动**。

---

## §5 · 上报待裁（本轮未自行取舍）

**⚑ 一项：`coord_basis` 末段【裁定条文之著录状态·一处如实留白】现已失实。**

- **原句**（`L_XUTIAN.coord_basis` 末，**1 见**）：
  > `team/todo_current.md`（2026-08-29 交接刷新）之交接注列本项为「④许田……已裁」，**惟其条文尚未回写** `team/round40_41_prompts.md` 之追加裁定序列（**该序列现止于第 23 条**），故本库**不代拟其裁定编号**，只记「**r43 收官裁定（领队转交，2026-08-29）**」；俟条文入档后可回补编号。
- **实况**（本轮实测）：该条文**已回写**——`team/round40_41_prompts.md`「领队追加裁定备案（r43 收官 · 2026-08-29）」现载**第 24、25、26 条**，本项即**追加裁定 25**。故「条文尚未回写」「该序列现止于第 23 条」两语**已不合实况**，且原句自设之条件「**俟条文入档后可回补编号**」**已成就**。
- **性质**：非误记，是写就当日之实况随后变动；**若照原样合入，公开数据将载一句关于内部档案状态的失实陈述**——此为本轮所见唯一实质待处置项，故列为上报之首。
- **修正建议（供领队／Skipper 裁夺）**：依「留痕不抹平」，**不改写原句**，于其后**追加**一句：
  > 【编号回补（2026-08-30）】上文「条文尚未回写、序列止于第 23 条」系本段写就当日（2026-08-29）之实况，**照留不删**；今该条文**已入档**为 `team/round40_41_prompts.md`「领队追加裁定备案（r43 收官 · 2026-08-29）」之**第 25 条**，本项之正式编号即「**追加裁定 25（2026-08-29 备案）**」，原句自设之回补条件于此成就。
- **连带**：`sim_xutian.py` 之断言「未代拟裁定编号，并记条文尚未回写任务书」须同步改写为核「原句照留 ＋ 回补段在」；本轮**未预改**，俟裁定。
- **本轮不改之由**：任务书本件明令「只补账目文书与合并模拟，不做新的史料判断」「落地文字已写」，回补编号非收尾四项之一。依「拿不准即停、上报，不自行取舍」，**照原文留、列此上报**。
- **同型一项在另一路**（`Q443`／`J002` 五处仍作「领队勘误裁定（r43 上报处置第①项，2026-08-29）」，其正式编号今为**追加裁定 24**），详见 `data/incoming/round43_final_biai/CHANGES.md` §5。**两路宜一并裁、一并改**，免半改半留。

### 5.1 结案（**追加裁定 27**，2026-08-30 领队裁定，任务 7b 落地）

**上开 §5 之上报原文照留不删**——领队已裁：**两路一并回补，照本库所拟**（本库所拟之「两路宜一并裁、一并改」亦获采）。裁定 27 并明记其起因即**领队回写迟滞**。

本路落实（**只追加、不改写**）：

| 表 | ID | 栏 | 形态 | 增量 | 所据 |
|---|---|---|---|---|---|
| `places` | `L_XUTIAN` | `coord_basis` | 栏末**追加**【编号回补（2026-08-30）】一段；原留白句**一字未删未改** | **+385 字**（本次）；对主表累计 **+1792 字** | 追加裁定 27 |

- **范围实测**：以回补前之快照作字符级 diff，本次变动为**单一 insert、位置在栏末**，**零删除、零改写**；对主表亦仍为**纯追加**（主表原文为新文之前缀，实测为真）。除 `coord_basis` 一栏外，`L_XUTIAN` 各栏（坐标 33.96／113.94、两栏 `low`、`modern_location`）及 `places` 其余 90 行**一字未动**。
- **拟文取用**：照 §5 所备拟文，并依裁定 27 之明令**增记回写迟滞一日之实**（原拟文未及此项，系本次补足）。
- **断言同步**：`sim_xutian.py` 原「未代拟裁定编号」1 条，改为 4 条——原留白照留、回补段在且编号为 25、迟滞如实记且不写作「即时入档」、回补段自记所依（裁定 27）；21 → **24 条**，复跑 **24/24 通过**、`validate` exit 0 无告警。
- **本节 §5 之上报遂告结案。** 另一路之同型项由 `data/incoming/round43_final_biai/CHANGES.md` §5.1 记，两路**已一并改，无半改半留**。

---

## §6 · 附注（写作向与体例备忘，**不入公开数据表**）

- 本案立下的是一个此前库内没有的动作：**关闭一个问题，而不假装回答了它**。三条效力界限里最要紧的是第 ②——「释者，疑得其解；关闭者，不再追问而已」。许田与许两标相距 5.6 km 的疑，今天仍然在图上；变的只是本库不再追。注文末句「本库不知道」是这段的落款，不宜在日后任何一次润色中被删。
- **地望从严之附带印证**：本项关闭而两栏不升，正是「宁降不虚」之实践——关闭一个待追项与升一级把握，是两件事，中间没有顺势可乘。

（本节为附注区，依红线 5 与 validate 之 `novel*` 护栏，写作向内容不入公开数据表。）
