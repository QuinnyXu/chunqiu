# 交付说明 · Sophia r32 —— 出土文献层试点落地件（息妫章并陈组）

日期：2026-08-15　执行：Sophia（史料研究员）
任务书：【任务 For Sophia · 系年试点落地件】息妫章并陈组落库备料
权威依据：`team/ruling_chutuwenxian.md`（领队裁定书十一条）　拟稿来源：`docs/kaoding_chutuwenxian.md`
增量路径：**`data/incoming/fix31_xinian/`**（含 `CHANGES.md` 与四个 CSV）

---

## 一、做了什么

出土文献层（`J` 前缀）首件落库备料。J 层自此有第一条源行、第一条引文，与《左传》一方**双向并陈**。

| 项 | 落点 | 文件 | 行数 |
|---|---|---|---|
| ① `J001` 源行 | `sources` **新增**《清华大学藏战国竹简（贰）·系年》第五章，`category=出土文献`，`notes` 四项必录俱全（入藏性质／整理本出版信息／本库所据底本／核对状态） | `sources_new.csv` | 1 |
| ② 并陈组·J 层一方 | `passages` **新增** `Q442`，挂 `E146`、`source_id=J001`、`quote_type=出土文献`，【】层标答性质不复述类型 | `passages_new.csv` | 1 |
| ② 并陈组·《左传》一方 | `Q161` **反向互指**增补，`quote_original` 一字未动 | `fixes_passages.csv` | 1 |
| ② 并陈组·事目层 | `E146`／`E147` `summary` 各增一句，`reliability=high` **维持不动** | `fixes_events.csv` | 2 |
| ③ `P_XIHOU` | 「殺息侯」入 `notes`，`death_year_bce` **不回填**、仍留空 | `fixes_people.csv` | 1 |
| ④ 三处用字 | 「新／莘」「弗訓／弗順」「命視／命見」一律标「**待纸本**」 | `J001.notes`／`Q442.modern_note` | — |
| ⑤ 异之性质三层细分 | 节 3 **措辞层**／节 4 **行动层**／节 7 **次第层**，落到 `Q442`／`Q161`／`E147` 注文 | 同上 | — |

合计：**新增 2 行、整行替换 4 行**。合入后 `sources` 173→174、`passages` 424→425；`events` 235、`people` 153、`places` 91 及其余各表**行数不变**。

**只备料，未合入**：`data/csv/`、`tools/`、`site/` 一字未动（`git status --porcelain` 实测仅本件目录一条 `??`，另一条为上轮考订文档 `docs/kaoding_chutuwenxian.md` 尚未入库，见五·4）。

## 二、依据

- **裁定书逐条落地**：J 前缀设立（1）／层级定位与四条尺度（2）／并陈不裁通则含三例外（3）／`P_XIHOU` 卒年不回填（4）／「事实相抵」下分三层（5）／`quote_type` 新值随立即开软检（6）／入藏简性质固定著录（8）／三处用字俟纸本（9）／`E146`·`E147` 增句而 `high` 维持、**`Q161` 反向互指特别核可**（10）／`sources.category` 新值（11）。金文归 J（7）本件无实例，只入规范条文清单。
- **并陈是双向义务**：`Q161`（左传一方）与 `Q442`（J 层一方）互指，`E146`／`E147` 事目层亦各增一句。读者无论从哪一侧读入都能看见另一说——J 层未被降为附注。
- **不反噬骨架**：莘之役与灭息、取妫两造皆书，相抵者在措辞、行动与次第，不在其事之有无，故两条 `reliability` 维持 `high`。
- **不于无系年处造年份**：《系年》通章不系月日、只以「明歲」标次年；J 层独有之「殺息侯」只入 `notes`，卒年留空。

## 三、validate 合并模拟结果

**模拟方法（未改护栏）**：`validate.py` 以脚本位置推定仓库根，故在会话临时目录重建 `tools/`＋`data/csv/` 两级结构——`validate.py` **逐字复制**（复制后字节比对断言与仓库件全同）、`data/csv/` 整目录复制，在副本上 append 两行、按 `id` 覆盖四行，再跑副本脚本。主表与仓库 `tools/` 全程只读。

| 跑次 | 对象 | 结果 |
|---|---|---|
| 基线 | 主表现状（未合入） | `exit 0`，`OK：全部校验通过`，软检警告 **0** |
| 第一跑 | 副本，护栏**原样** | `exit 1`，**1 处报错**（预期内，全文见下） |
| 第二跑（诊断） | 副本正则补 `J`（仅副本改 1 行） | `exit 0`，警告 **0** |
| 第三跑（诊断） | 再开 `SOFT_CHECK_TIERS["出土文献"]=True` | `exit 0`，警告 **0** —— 裁定 6 所期的「**开档即净**」经实测成立 |

第一跑报错全文：

```
校验失败，共 1 处问题：
  - sources.csv 第175行：id 'J001' 不符合规范 ^[ZSGAPBYLT]\d{3}$
```

这是本件**唯一**报错，即裁定 1 尚未落到护栏所致。本件**未改 `tools/validate.py`**（红线 1）。

**需 Skipper 同步的 validate 改动点（两处，各 1 行）**：

| # | 落点 | 改动 | 性质 |
|---|---|---|---|
| 1 | `ID_PATTERNS["sources"]`（第 75 行） | `^[ZSGAPBYLT]\d{3}$` → `^[ZSGAPBYLTJ]\d{3}$` | **阻断级**，不改则无法合入 |
| 2 | `SOFT_CHECK_TIERS`（第 51–59 行） | 补 `"出土文献": True,` | 裁定 6 所令；不补则该档静默、「随立即开」落空 |

**无须改动者（实测确认）**：`sources.category` 与 `passages.quote_type` 在 `validate.py` 中**均无枚举校验**，两个「出土文献」新值不触发任何报错；`quote_type` 仅在软检处以 `.get(qtype, False)` 查表，故除第 2 项外无其他改动点。**合入顺序**：先落护栏两处 → 再合数据 → `validate` → `csv_to_json` → 一并提交。

## 四、conventions 待写条文（**升下一号**）

九条落点清单已备于 `CHANGES.md` §6，要点：§2 补 `J###` 前缀（前缀计数九→十）＋新节「`J###` 分层纪律」（独立见证层、与 `Z` 平行不设高下、单独支撑默认 `medium`、四条尺度、入藏简性质固定著录）＋金文一物两录（器入 `A`、铭入 `J`、互指）；§7 补「并陈不裁」通则（灵魂句「版本异文有善本可问，独立两支无正本可言，不得比照择一」）与三例外及限定语、并陈之**双向义务**、「事实相抵」下分措辞／行动／次第三层、口径句「转录本可证有无某事、次第如何，不可证某字作某」、`quote_type` 新档「出土文献」随立即开；`sources.category` 新值与 J 层批次区分办法（`work`＝批次丛书名、`section`＝篇名·章次，不加栏）；§11 登记试点排程（下一试点第六章、越 −464 截断规则入待议池）。**一律写「升下一号」，未写死版本号。**

## 五、拿不准处与上报（不自行裁量）

1. **`E146`／`E147` 的 `source_ids` 加挂 `J001`——本件唯一超出任务书字面的改动**。`E146` 一处**不可不加**：conventions §7 v1.21 判例明立「`passage` 引文之源必须出现在其所挂事件的 `source_ids` 中」，`Q442` 挂 `E146` 而源为 `J001`。`E147` 一处**可商**（其下无引 `J001` 的 `passage`，只是 `summary` 增句引述了《系年》）：若领队认为「无引文之源不入 `source_ids`」更清爽，删去 `E147` 的 `;J001` 即可，不影响并陈四项。
2. **`J001.url` 本轮未能实测复验**：本次执行时网络已不可达（`zh.wikisource.org` 域名解析失败，考订成稿时尚可达），`url` 依上轮实际所据之页名照录，`notes` 已如实登记为复核项、不作断言。可达环境下一句话回告页名是「系年」抑或「繫年」即可清账。**引文内容不受影响**——`Q442.quote_original` 取自上轮实核转录，非本轮重取。
3. **目录名与轮次不一致**：任务书指定 `data/incoming/fix31_xinian/`（fix31），交付文档为 r32。本件照任务书字面落盘、未自行改名；归档命名（`docs/changes/r31_xinian.md` 抑或 `r32_xinian.md`）请领队一句话定。
4. **`docs/kaoding_chutuwenxian.md` 尚未入库**（`git status` 显示未跟踪）。裁定书即据此文成立，建议随本轮合入一并提交，免得裁定与其依据之文档不同在库。
5. **`Q419`／`Q420` 空号仍留空**（r28 裁定 a：空号非退役号，编号唯一不求稠密）。本件取 `Q442` 接尾号 `Q441`，不填补空号，留痕免误。
6. **`Q442.quote_original` 未录简号【23】–【25】**：本库层标亦用【】，简号入正文会同形生歧义；简号已在考订文档 §3.0 完整留痕。若领队另定「出土文献引文须标简号」之体例，本件可依此重出一版。
7. **第五章末「文王以北啟出方城」一节本件未收**：属尺度④「国势总结语」，且释读三处未定（待纸本），本件从严不立 `passage`、不立事目，只此留痕；若领队认为宜作 `passage` 收入以见《系年》体例之异，可下轮补一行。

## 六、验收自查

| 项 | 结果 |
|---|---|
| 合并模拟（临时副本，未改护栏） | 第一跑 `exit 1`，**唯一**报错为预期内的 `J001` 正则；诊断第二、三跑均 `exit 0`、警告 0 |
| 不改 `data/csv/`（红线 3） | **未改**（`git status` 实测） |
| 不改 `tools/`（红线 1，含 `validate.py`） | **未改**（字节比对断言） |
| 不动 `site/`、`team/` 只读不改 | **未动**；交付文档只引裁定条目编号与结论要点，不转录 `team/` 正文 |
| 写作向内容不入公开数据（红线 5） | 无写作向内容，`CHANGES.md` 附注区为空 |
| 断言类自查以**实读**为准（r30 教训） | **28 项**断言逐项实读合并后副本、与主表原值逐栏比对，全部通过 |
| 要害四项（实读） | `Q161.quote_original` 与主表**逐字全等**；`E146`／`E147` `reliability` 实读为 `high`；`P_XIHOU.death_year_bce` 实读为空；四处增补均以主表原值**为前缀**（原句原样保留、只追加） |
| `〔页码待补〕` 未重新引入 | 本件全文与四个 CSV 均 **0 处** |
| 一律简体中文；原文摘录保持原字 | 正文简体；`quote_original` 繁体照录 |
