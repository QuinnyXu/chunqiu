# 交付说明 · Vision r17 —— 播放引擎合一 · 速度档 · 交会显示修正

日期：2026-07-21　作者：Vision　团队轮次：r17
改动面：`site/app.js`、`site/index.html`、`docs/design/design_notes.md`（v1.8→v1.9）、本说明。
**数据零改动**（`data/csv/`、`site/data/*.json` 未动一字，`git status site/data/` 干净）；**站点零运行时依赖**（仍纯原生 JS/CSS/SVG；jsdom 仅为 scratchpad 内的测试工具，不入仓、不入站）。
`python tools/validate.py` → `OK：全部校验通过`（无数据变更，作红线门例行复核）；`node --check site/app.js` 通过。

---

## 一、引擎合一：合并后的结构（清偿 TD-r16-01，裁定 1）

### 1.1 一句话结论
把 r16b 判为「无法复用」的两套状态机（单人 `play/*` 距离驱动、并观 `cplay/*` 按年驱动）**合并为唯一状态机 `player`**；单人＝单轨模式、并观＝双轨模式，**只由注入的 `cfg` 区分**，`cplay` 已删除、`play` 旧函数（`togglePlayback/stepPlayback/announceStation/finishPlayback/stopPlayback`）与 `cplay` 旧函数（`cmpTogglePlay/cmpStep/cmpYearNow/cmpUpdateAt/cmpUpdateMarkersOnly/cmpFinish/cmpStop`）全部退役。`grep` 复核：全库已无这些标识符（仅一处注释提及旧名以志沿革）。

### 1.2 统一引擎的三个部件（app.js）
1. **主钟（`player` 状态 + `playerFrame`）**：与模式无关。核心改动是把 r16b 的「`startTs` 停表法」换成**增量累加时钟**——每帧 `elapsed += min(Δt, 100ms) × speed`。
   - `min(Δt,100ms)` 上限：切后台/长帧回来只补一帧，不突进。
   - `× speed`：速度档（见二）。
   - 暂停＝停 RAF 并 `lastTs=0`；续播首帧 `Δt=0`（不追赶）。
   - 交会自动暂停（`holdUntil`）期间**不推进 `elapsed`**，解除后从原位无缝续行。
2. **控制器**：`playerStart/Pause/Resume/Finish/Stop`，各自转调 `cfg` 的生命周期钩子（`onStart/onPause/onResume/onHoldEnd/onFinish/onStop`）与 `cfg.render(elapsed)`。终点由 `playerFinish` **显式** `render(dur)` 落位（并观也由隐式改显式，见 §四·1）。
3. **beats（交会/中断）**：`cfg.beats = [{key, at, fire}]`（按 `at` 升序）。`playerFrame` 检出跨越（`at∈(prev,elapsed]`）即**精确落位到该刻**（`elapsed=at`，两标记不越过交会点）、`fire()`、`holdUntil=now+1200ms`。单轨模式 `beats=null`（无中断，保持逐段观感）。

### 1.3 两种 cfg
| | 单轨 `singlePlayCfg`（单人地图） | 双轨 `comparePlayCfg`（并观） |
|---|---|---|
| 标记 | 1 枚 `#play-marker` | 2 枚 `cmp.markerA/B` |
| 进度→位置 | 归一化 elapsed → 按**段长（几何距离）**分配的段内推进，逐段 `easeInOut` | 归一化 elapsed → **绝对年** `axisMin→axisMax`；两标记各按年沿己轨插值（`cmpMarkerPos` 段内 `easeInOut`） |
| 段定义 | 相邻站点，段时长 ∝ 空间距离 | 年线性映射；标记在各自轨点间按年插值 |
| beats | 无 | 交会年（a、b **同权**）成组 |
| 字幕 | 到站播报「第 N/几 站」 | 「年份·甲乙在世态」；交会拍换免责字幕 |
| dur（1×基准） | `clamp(1200·(n−1),2000,8000)` | `clamp(span·130,4000,12000)` |

### 1.4 与 r16b「无法复用原因」的逐条对照（如何解决）
r16b §九列出七个「结构不同构」的维度，判为「泛化 ≈ 重写」。r17 的解法是**把差异全部外化进 cfg，主钟只认一个归一化时间**：

| r16b 列的差异维度 | r17 如何吸收 |
|---|---|
| 驱动量：距离 vs 绝对年 | 主钟只产 `elapsed`（ms）。「elapsed→位置」的换算下沉到各 `cfg.render`——单轨按段长表、双轨按 `axisMin+p·span`。主钟不关心语义。 |
| 标记数：1 vs 2 | 标记完全由 `cfg` 持有与摆放；主钟不碰标记。1 或 N 只是 render 内部循环次数。 |
| 「段」定义：站点 vs 轨点年 | 段定义封在 render 内；主钟只给一维 `elapsed`。 |
| 有无绝对年轴/游标 | 年轴游标/年号更新是双轨 render 的副作用；单轨 render 不做。主钟无关。 |
| 生卒态灰置 | 双轨 render 内 `placeMarker` 据生卒灰置；单轨无此维度、不做。 |
| **b 级交会自动暂停** | 抽象为**通用 beats 机制**——`{at,fire}` 列表。单轨传 `null`，双轨传交会年。主钟统一处理「跨越→落位→fire→hold」。**这条正是 r16b 认为最不可复用的「双人独有概念」，现被 beats 泛化吸收，且顺带支持 a 级同权（见三）。** |
| 私有状态字段（segs/lastStation vs lastYear/fired/holdUntil…） | 站序/字幕等私有量闭包在各 cfg 内（`singlePlayCfg` 的 `segs/lastStation`、`comparePlayCfg` 的 `byYear`）；主钟只持 `elapsed/holdUntil/firedBeats` 三个通用量。 |

r16b 末段「将来合并方向」预设的接口（归一化时间轴 t → 若干标记位置 + 中断事件列表 + 段定义作参数注入，单轨是 N=1、中断空、段按距离的特例）**即本轮落地形态**。r16b 要求的「同轮双向回归」已做（见三、四、六）。

---

## 二、速度与手感（裁定 2、5）

### 2.1 速度档 0.5×/1×
- 工具条各加一枚按钮：单人 `#btn-speed`、并观 `#cmp-speed`，文案「速度 0.5×／速度 1×」。
- **默认 0.5×**，`localStorage` 键 `cq_play_speed`（值 `"0.5"|"1"`）记忆，读写包 `try/catch`（隐私模式静默降级为不记忆，遵 conventions §9.1）。
- 两按钮**共用同一记忆值、互相同步显示**（切一个另一个随之更新；跨视图亦保持——jsdom 实测：并观切到 1× 后进单人地图，`#btn-speed` 显示「速度 1×」）。
- 速度只**缩放每帧 Δt、不改内容时长 `dur`**，故**切档即时生效**（`player.speed` 当帧更新），不打断正在进行的播放。

### 2.2 全局调慢
- `dur` 公式保持旧值作为 **1× 基准**，默认 0.5× ⇒ 运动段耗时恰为旧速的 **2 倍**（观感约旧速一半，落在任务「一半到三分之二」区间）；切 1× 即回旧速。
- 单人秦穆公线（8 站，1× dur=8000ms）实测：0.5× 全程 16.0s、1× 8.0s；单帧最大位移 0.5×=4.42px / 1×=8.82px（0.5× 每帧位移减半，明显更稳）。

### 2.3 「汇合点后突然加速」的复现与修复
- **复现（旧因）**：旧 `cmpStep` 用 `startTs += Δt` 逐帧推进停表来冻结；hold 结束那一帧**落回主流程时漏补了「最后一个 hold 帧→本帧」的间隙**，`elapsed` 会跳一小步；叠加旧版**只有 b 级暂停**、a 级同场密集串（文姜×齐襄公前694–687 七年窗仅占全轴约 18%）无暂停，标记在交会簇里线性疾走——两者合成「到点顿一下、随即窜过一串」的加速感。
- **修复**：增量累加时钟从根上消除停表漏补——hold 期间只是**不累加 `elapsed`**，解除后 `Δt` 照常且有 100ms 上限，**无补偿跳步**；再叠加「a 级同权暂停」（三），交会簇里逐点各停一拍，标记自然慢下来。
- **自验（纯逻辑仿真，镜像 `playerFrame`）**：文姜×齐襄公双轨，相邻帧年份跳跃 >1.2 年者 = **0**（无突进），0.5×/1× 各跑一遍均如此。

---

## 三、交会显示修正（裁定 3、4）

### 3.1 a 级同权自动暂停（修 III.1）＋ 漏亮根因
- **根因＝检测层到显示层的传递缺失**（与任务书预判一致）：检测器 `detectMeetings` 本就检出 a 级（r16b 已核 14/14），但旧 `cmpStep` 的暂停/脉冲逻辑写死 `if (m.level !== "b") continue`——**a 级同场在播放中既不暂停也不脉冲**，遂「检测通过但标亮不过」。
- **修复**：beats **按交会年成组**（`byYear`），**a、b 同权**触发；`cmpFireMeetingBeat(year, list)` 脉冲高亮**该年全部**交会地、只暂停一拍，并按该年是否含 a 级出「同场／同年同地（相邻记载）／同年同地」字幕＋B3 免责句。

### 3.2 文姜×齐襄公全程走查（修 III.2）
以 `event_people` 实况实算（`presence=相关` 排除，坐标齐全）。交会集 **6 处（同场 5 · 同年同地 1）**，落 **5 个地点**、**5 个交会年** ⇒ 播放连停五拍：

| 拍 | 年 | 地点（脉冲高亮） | 级别 | 依据 |
|---|---|---|---|---|
| 1 | 前694 | **泺 L_LUO ＋ 临淄 L_LINZI** | a 同场（两处同亮同拍） | E009 二人皆亲至；E010 二人皆亲至 |
| 2 | 前693 | 临淄 L_LINZI | b 相邻记载 | 文姜 E012（前693）× 齐襄 E010（前694），±1 年容差 |
| 3 | 前692 | 禚 L_ZHUO | a 同场 | E013 二人皆亲至 |
| 4 | 前690 | 祝丘 L_ZHUQIU | a 同场 | E014 二人皆亲至 |
| 5 | 前687 | 防 L_FANG | a 同场 | E016「两会于防、谷」二人皆亲至 |

- **泺（前694）判定**：按 `event_people` 实况，E009（鲁桓公会齐襄公于泺、携文姜入齐）文姜与齐襄公**均亲至** ⇒ a 级同场，与临淄同在前694 一并点亮、同一拍。（谷：E016 标题含「防、谷」，但该事件 `place_id=L_FANG` 单点落图，谷未单立地点，故随防同点，不另出一环。）
- **五处齐亮齐停验证**：纯逻辑仿真显示 5 个 hold 严格按年序 −694/−693/−692/−690/−687 触发、无遗漏；jsdom 端到端观测到 5 地点（泺·临淄·禚·祝丘·防）全部脉冲、按钮播完复位、末拍字幕「前687 · 防：同场 — 同城未必同时，不能仅据同年同地断定相遇。」

### 3.3 侧栏—地图一一对应（修 III.3）
- 地图交会环按**地点**去重绘制（`placed` Map），侧栏按**交会条**列出。文姜×齐襄公：**地图 5 环 / 侧栏 6 行**（临淄占 a、b 两行、共用一环，点击展开两事）。jsdom 实测「侧栏有而图上缺 = 0」——每条侧栏交会的地点在图上均有环。
- 全量复核（对照 Sophia a 级 14 对基准）：检测层 r16b 已验 14/14 无变；本轮**只改显示层传递**（beats 纳入 a 级），故 a 级标亮逻辑随之全量修正，不改一条检测口径。

---

## 四、回归与自验

1. **文姜×齐襄公全程**（0.5× 默认）：连停五拍（临淄/泺→临淄→禚→祝丘→防）、无突进（年份跳跃 >1.2 = 0）、免责句在场——✓（仿真＋jsdom 双通道）。
2. **秦穆公单人线（8 站，最长轨迹之一）**：0.5×（16.0s）与 1×（8.0s）各放一遍，单帧最大位移 4.42/8.82px、逐段经过、**终点像素级归位**（`render` 末刻显式落位守卫，修了旧浮点误差致末点差 ~0.01px、并稳定播报末站）——✓。
   - 附：并观终点亦由隐式改**显式落位**（`playerFinish` 显式 `render(dur)`），清偿 r16b §八·3 登记的「隐式落位靠数学不变量」隐患。
3. **手机端**：速度档按钮进 `.map-toolbar`（`flex-wrap:wrap`，窄屏自动换行、可点）；并观 ≤680px 侧栏折叠底部条逻辑未触碰、无退化；四档宽度沿用既有取景，本轮无新增几何。**真机点按手感待 Xiangtao 复测**（见六）。
4. **导航清理**：旧「离开视图 `stopPlayback()+cmpStop()`」并为单一 `playerStop()`（据 `cfg.onStop` 分派清理），jsdom 实测并观↔单人来回切换、播放中途离开均无残留、无抛错。

---

## 五、需上报领队/Xiangtao 的事项（conventions §10：只记录不裁量）

- **史学疑点：无新增。** 本轮数据零改动，交会检测口径一字未改（沿用 r16b 已对齐 `binguan_fixtures.md` 三护栏的实现）；`fixtures` §四登记的三条数据修正建议（E129/申舟一年差、P_ZIWEN 卒年、E118 弦高 presence）仍是 Sophia/Skipper 的既有待议项，与本轮无关，不重复上报。
- **排程建议 1（速度档默认值）**：任务定「默认 0.5×」，已照办。若上线后 Xiangtao 真机觉 0.5× 偏慢，微调只需改 `playSpeed()` 默认返回值或 `dur` 基准系数，属一行改动、无结构风险——是否调、调到 0.6×/0.66× 由领队按真机观感定。
- **排程建议 2（谷未单立地点）**：E016「两会于防、谷」现只落防一点。若日后想让「谷」在图上独立成环（并观与单人地图皆受益），需 Sophia 备料补 `L_GU` 坐标、Skipper 合入——属数据侧、跨角色，登记备考，不在本轮范围。

---

## 六、真机待复测清单（本环境浏览器扩展 Claude in Chrome 未接入，照 r16 惯例申报）

自验用三通道替代真机：`node --check`、**纯逻辑仿真**（镜像 `playerFrame`，验交会齐停/无突进/速度倍率/终点归位）、**jsdom 端到端**（真实 DOM 加载 `app.js`＋本地数据，实测渲染/播放/脉冲/速度记忆/跨视图，零运行期错误；jsdom 对 SVG 命名空间 `#id` 选择器的既有失配已在 harness 侧兜底，与生产浏览器无关）。以下留待 Xiangtao 真机复测：

1. **手感主观**：0.5× 默认速度是否舒适；汇合点暂停一拍（1.2s）时长是否合宜；缓入缓出是否顺滑无顿挫。
2. **触屏**：`#btn-speed`/`#cmp-speed` 在 ≤680px 的可点性与换行版式；播放中切档是否即时且不跳。
3. **交会脉冲观感**：文姜×齐襄公五处齐亮齐停的视觉节奏；前694 泺+临淄两点同拍高亮是否清晰；相邻年（694/693 临淄）连续脉冲是否重叠得当。
4. **并观侧栏无退化**：手机底部展开条 `#cmp-sheet-toggle` 与播放并存时的层叠。
5. **长轨迹极端**：秦穆公 1× 与 0.5× 的完整播放无卡死、无标记漂移。
6. **切后台回来**：播放中切走再回来，标记不突进（Δt 上限 100ms 的真机效果）。

---

# 交付说明续 · Vision r17b（返工：年位同步根修 · 全屏播放控件 · 状态机测试）

接 r17（同分支 `r17-play-engine-merge`）。**数据零改动、不新增运行时/构建依赖**（新测试仅用 Node 标准库＋项目自身 app.js）。改动文件：`site/app.js`、`site/index.html`、`site/styles.css`、`tools/test_binguan_play.js`（新增）、`docs/design/design_notes.md`（v1.9→v1.10）、本文件。`node --check` 通过、`python tools/validate.py` 通过、`site/data/` 未动。

## 一、年位同步根修：位置＝主钟进度的纯函数（裁定映射唯一化）

### 1.1 病因（r17 遗留，本轮复现并根治）
r17 双轨主钟是「纯绝对年 clockYear」，标记位置＝`cmpMarkerPos(traj, clockYear)`。但存在**多对一塌缩**：`buildTraj` 把同地相邻事件聚合（文姜临淄 E010@694＋E012@693 并为一站、年取最早 -694），且一人同年可有**多个不同地点**的交会（文姜前694 既在泺又在临淄）。纯年函数下「钟到 694」只能解析到一个地点，无法同时保证「钟到泺⇔人到泺」与「钟到临淄⇔人到临淄」——这是数学必然的塌缩，也是「检测过但标亮/落位不过」的病灶。

### 1.2 根修：以「交会锚故事进度 sc」为主钟唯一量
关键代码结构（`site/app.js`，均纯函数、无 DOM）：
- **`cmpBuildSyncs(trajA, meetings)`**：交会涉及的不同地点各成一个**锚点**，按 trajA 站序定序、编号即 `sc`（整数）。一人同年多交会地（泺694、临淄694）自然成**相继两锚点**，从源头拆开塌缩。
- **`cmpBuildWaypoints(traj, syncs)`**：把一个人的轨迹站点映射为按 sc 排布的路点 `[{sc,x,y}]`。交会站钉到整数 sc（`|站年−锚年|≤1` 匹配，容纳 b 级±1 与聚合取最早年）；非交会站在相邻锚间按序均分小数 sc（不与整数锚碰撞）。
- **`cmpPositionAt(wps, sc)`**：**标记位置＝sc 的纯函数**——在 sc 落入的路点段内按段内比例插值，`easeInOut` 只作视觉平滑、不改「某 sc 在某段」的映射。sc 恰为交会锚整数时返回该锚路点＝**交会地**（从构造上「钟到交会刻⇔人到交会地」）。
- **`cmpClockYearAt(sc)`**：clockYear＝sc 的**单调函数**（控制点＝年轴两端＋各交会锚年，分段线性）；年严格段内 sc↔year 一一对应，故等价于领队要的「年的纯函数」；同年多交会地由 sc 细分区分。
- **主钟（`comparePlayCfg`）**：`elapsed` 线性映射到 `sc∈[scMin,scMax]`；beats＝各交会锚整数 sc（`at = sc2e(idx)`）。两标记与 beats **共用同一主钟同一 sc 映射**——主钟精确落位交会刻，两标记天然同在交会地（停拍由既有 `holdUntil` 冻结机制触发，映射统一后无需任何显式覆盖/hack）。

单人模式不受影响（无交会概念，仍「按段长逐段」，`beats=null`）；**引擎仍是唯一的 `player`**，单/双轨只以 `cfg` 的 render/beats 区分，未回退出第二引擎。旧塌缩式 `cmpMarkerPos/placeMarker` 已删除。

### 1.3 五处连停逐一验证
状态机测试（下三）实测：文姜×齐襄公 5 交会锚 **泺(前694)→临淄(前694)→禚(前692)→祝丘(前690)→防(前687)** 逐一触发（sc 序 0→1→2→3→4），每锚触发帧两标记坐标精确等于交会地坐标。前694 的**泺与临淄同年双地**由 sc 细分为相继两锚，各自落位正确——正是根修要害。临淄锚兼含 a 级同场与 b 级相邻记载（前693 折入），字幕并述。

## 二、速度档补位（任务二）与全屏播放控件（任务三）

- **速度档补位**：`#btn-speed` 已在单人地图工具条渲染（r17 落地），与并观 `#cmp-speed` 同一组件（`bindSpeedBtn`）、同一状态源（`localStorage` 键 `cq_play_speed`、`playSpeed()/setPlaySpeed()`）；两处切档即时生效、记忆共享。本轮复核在案（jsdom 实测：并观切 1× 后进单人地图，`#btn-speed` 显示「速度 1×」）。
- **全屏播放悬浮控件（任务三）**：地图全屏浮层 `#map-overlay`（单人 `openOverlay`／并观 `openCmpOverlay` 同用）左下角新增 `#overlay-controls`＝「播放/暂停 `#ov-play`＋速度 `#ov-speed`」：
  - **播放委托**：`#ov-play` 点击委托对应模式的主按钮（`$("#btn-play").click()`／`$("#cmp-play").click()`），**复用其 onclick 闭包与播放状态**；文案经 `setPlayBtnText(mode,txt)` 主/浮层同写、始终一致。
  - **速度共享**：`#ov-speed` 并入 `refreshSpeedBtns` 三处同步组，切档即时且记忆。
  - **落位与无缝**：左下角浮起、触屏热区 ≥44px、不遮轨迹与图例；退出全屏 `#overlay-controls` 隐藏、播放不中断（jsdom 实测退出后主按钮仍「⏸ 暂停」，续播无缝）。无轨迹（主按钮 disabled）则控件不显。

## 三、动画域自验（本轮硬要求）：状态机测试

- **脚本**：`tools/test_binguan_play.js`（Node 标准库＋极简 DOM 桩，`require` 项目自身 `site/app.js`——app.js 末尾加 Node 守卫：`typeof document` 判断只在浏览器自启 `boot`，`module.exports` 仅 Node 下暴露引擎与映射）。
- **做法**：用合成 Δt 序列**手动驱动【真】`playerFrame`**（`requestAnimationFrame` 桩为 no-op），在每个 beat 触发帧读取两枚假标记的实际坐标，断言＝交会地坐标（投影公式独立算出）。这才真正驱动了引擎的**主钟＋beat 跨越检测＋sc→位置映射**全链路，弥补 r16b/r17「只测检测器没测动画」的盲区。
- **运行输出**（`node tools/test_binguan_play.js`，退出码 0）：

```
== 并观状态机测试：P_WENJIANG × P_QIXIANG ==
交会锚（syncs，按 sc 序）：
  sc=0  泺(L_LUO)  锚年=前694  含同场
  sc=1  临淄(L_LINZI)  锚年=前694  含同场·相邻记载
  sc=2  禚(L_ZHUO)  锚年=前692  含同场
  sc=3  祝丘(L_ZHUQIU)  锚年=前690  含同场
  sc=4  防(L_FANG)  锚年=前687  含同场

驱动帧数=397，触发交会锚序=0→1→2→3→4

交会锚触发帧断言（两标记须同在交会地）：
  ✓ sc=0 泺 前694 | 甲(847.8,143.1)在交会地 | 乙(847.8,143.1)在交会地 | 交会地(847.8,143.1)
  ✓ sc=1 临淄 前694 | 甲(939.5,129.9)在交会地 | 乙(939.5,129.9)在交会地 | 交会地(939.5,129.9)
  ✓ sc=2 禚 前692 | 甲(829.4,151.7)在交会地 | 乙(829.4,151.7)在交会地 | 交会地(829.4,151.7)
  ✓ sc=3 祝丘 前690 | 甲(945.9,272.2)在交会地 | 乙(945.9,272.2)在交会地 | 交会地(945.9,272.2)
  ✓ sc=4 防 前687 | 甲(868.2,245.0)在交会地 | 乙(868.2,245.0)在交会地 | 交会地(868.2,245.0)

结论：5 处交会锚，触发 5 处，断言全过=是
TEST PASS
```

**五断言全过**（每处「甲」「乙」两标记坐标均等于交会地坐标）。按裁定：**「年位同步」经此测试后不再属于真机不可验项。**

## 四、端到端复验（jsdom，本环境 Chrome 扩展仍不可用）

- 重构后并观渲染无回退：地图 5 交会环／侧栏 6 行（临淄一环含 a+b 两行）、五处地点播放中逐一脉冲、免责句在场、播完复位。
- 全屏控件：单人与并观全屏内 `#overlay-controls` 显现、`#ov-play` 委托播放使标记行进、`#ov-speed`／`#btn-speed`／`#cmp-speed` 三处切档同步、退出全屏无缝衔接；均零运行期错误。

## 五、验收偏差上报（有实测依据的字面异议，按惯例上报领队）

- **「position 为 clockYear 的纯函数」的字面在「同年多交会地」处不可行**——已由数据实证：文姜×齐襄公前694 同时有泺(E009)与临淄(E010)两处 a 级同场，纯 clockYear 函数在 694 处必塌缩到单一地点，无法令泺、临淄两锚同时「钟到年⇔人到地」。故按领队根修**意图**（映射唯一化、同一主钟、钟到交会刻⇔人到交会地）实现为「主钟＝故事进度 sc、位置＝sc 的纯函数、clockYear＝sc 的单调函数」：年严格段内 sc↔year 一一对应即等价于「年的纯函数」，同年多交会地以 sc 细分区分。此为忠实于意图的落地，特此报备，无阻断性问题。
- **五停拍口径**：本轮交会锚以「交会地点」为单位（5 处：泺·临淄·禚·祝丘·防），恰为 5 停拍，与验收「文姜×齐襄公五停拍」一致；b 级前693 折入临淄锚（同一地点，字幕并述「同场·相邻记载」），不再单列第 6 拍——如领队更希望 b 级独立成拍，可另议（属呈现口径，非映射正确性）。

## 六、真机待复测清单（年位同步已由状态机测试转为可验，余项仍待 Xiangtao 真机）

1. 文姜×齐襄公 0.5× 全程：五停拍节奏、**临淄交会时两点确在临淄**的视觉确认（状态机已证坐标，真机验观感）。
2. 全屏浮层控件触屏热区与左下角遮挡（是否碍图例/轨迹）、播放中切档即时性。
3. 同年双地（泺→临淄）相继两锚在真机上的移动/停顿观感是否自然。
4. 秦穆公长轨 1×/0.5× 全屏内播放无卡死；切后台回来不突进。
5. 手机并观底部展开条与全屏控件、播放三者并存的层叠。
