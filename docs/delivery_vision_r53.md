# delivery_vision_r53 —— ⚑H 扩施七栏 ＋ 反引号一路

**执行者**：Vision　**日期**：2026-09-25（本机时区，照裁十四①）　**件**：`team/round53_prompts.md` `## Task for Vision · r53-3`

**所读之节，及各节末次勘注之日**（本轮体例 5）：

| 所读之节 | 出处 | 末次勘注之日 |
|---|---|---|
| `## Task for Vision · r53-3`（五节全） | `team/round53_prompts.md` | **2026-09-25** |
| `## 三、本轮正件之裁` 之 **裁四十八**（本件所据）、**裁四十九**（其①款命 `prod_render_invariants.js` 不并入，界之所在） | 同上 | **2026-09-25** |
| `## 四、本轮之界`（三件共） | 同上 | **2026-09-25** |
| `## 一之十七、裁四十一 · 反引号随 ⚑H 余栏同批排`（站长 2026-09-22） | `team/round52_prompts.md` | **2026-09-22** |
| §7 ⚑H 止血条①②③④⑤款（含 2026-09-21 Skipper 就地加注「第十栏 `people.relations`」） | `docs/conventions.md` v1.45 | **2026-09-21**（该条加注之日） |
| §3.6／§3.6.1／§3.6.2／§7.1／§7.2／§7.3 | `docs/design/design_notes.md` v2.13 | **2026-09-20** |

**所读之节之戳**：`team/round53_prompts.md` 之 `## Task for Vision · r53-3` 一节，落笔毕重读得 **1906 字、sha256 `c4029bc3841ad845…`**（节界取自 `## Task for Vision · r53-3` 至 `## Task for Sophia · r53-4` 之前）。

★ **此处须辨其谓词，不得读作「逐字未变」**：该文件 mtime 落笔期间自我起手之时移至 **2026-09-25 22:07**（本轮三件并行，他人另立其节），而我**起手时未取该节之 sha**，故**不能声称其节逐字未变**——只能证其**所据之五句落笔毕仍逐字在节内**（程序检索，非目测）：「落点须自己查全，不取本块之列」／「共用之助手，24 处调用」／「停下上报、不自行落定」／「`:3369`／`:3374`」／「须证**无星号亦无反引号之值，其 DOM 与旧版逐位全等**」。

★ **一笔留与后人**：本轮体例 5 命「书明所读之节与其末次勘注之日」——**起手即取其节之 sha 并书之**，方能在并写之下真证「所读者未受他人之动」。本轮我未如此，是我之失，记之；上所书之 sha 即为下一执行者之起点。

**交件**：`site/app.js`｜`site/styles.css`｜`docs/design/design_notes.md`（**升 v2.14**）｜`tools/qa/vision_r53.js`（新立，**94 项断言，全过**）｜本文件。
**不动 `data/csv/` 一字，不重生成 `site/data/`。不提交、不推送。**

---

## 〇、前置之自核（★ 照纪律三「写前查库」，不取派件之复述）

| 核项 | 领队加注所书 | Vision 实读 | 判 |
|---|---|---|---|
| `HEAD` 与远端同哈希 | `ce708df` | `git log --oneline -1` 得 `ce708df`；`git ls-remote origin main` 得 `ce708df6765570d93634e4293b2dbd54add84e7a` | **符** |
| 工作区现有二项系他件之物 | `docs/delivery_skipper_r52.md`（改）、`docs/delivery_skipper_r53.md`（新增） | 起手 `git status --porcelain` 得恰此二项 | **符**，一字未动 |
| 本件之面 | `site/app.js`／`site/styles.css`／`docs/design/design_notes.md`／`tools/qa/vision_r53.js` ＋ 本文件 | 落笔止于此五者 | **符** |
| `tools/qa/` 之下只碰 `vision_r53.js` | —— | 旧四本、`prod_data_invariants.js`、`prod_render_invariants.js`（748 行）**一字未碰**；`prod_render_invariants.js` 只以 `QA_BASE_URL=local` **跑过**作回归 | **符** |
| 不切分支 | —— | 全程无 `git checkout`／`switch`／`stash`／`reset` | **符** |

★ **落笔过程中，Skipper（r53-5）与 Sophia（r53-4）之件先后落地**，工作区遂另见 `tools/qa/prod_data_invariants.js`（新增）、旧四本之删、`data/incoming/r53_kongzi_role/`（新增）——**俱系他件之物，本件一字未动**。

---

## 一、§〇 四条库内事实之复跑（**求法｜所期之果｜读法**，逐条附下）

**统一求法**：自 `data/csv/*.csv` **全表全栏**扫描；口径同 `conventions` §7 ⚑H ⑤款——星号只计**长度恰为 2 之游程**所成之**对**（`***`／`****` 之属不计），反引号**顺序两两成对**，行次取**去重行**。基线 `ce708df`。

| # | 事实 | 所期 | 实测 | 判 |
|---|---|---|---|---|
| 1 | ②款七栏之 `**` | 1703 对／217 行 | **1703／217**（逐栏：`coord_basis` 628/48｜`sources.notes` 426/39｜`people.notes` 290/36｜`events.summary` 172/39｜`places.description` 116/24｜`source_note` 65/30｜`archaeology.summary` 6/1） | **符** |
| 2 | 十栏之 `` ` `` | 2602 对／352 行 | **2602／352**（含①款二栏与第十栏 `people.relations` 6/2） | **符** |
| 3 | 两集合相减 | 两向俱空 | **两向俱空**——「有星无反引号之栏」`[]`、「有反引号无星之栏」`[]` | **符** |
| 4 | 二记法之相遇 | 嵌套 243、交叉 **0** | 粗体内含 code span **242**、code span 内含成对星号 **1**、**交叉 0** | **符** |

**读法**：第 3 条之「两向俱空」是本件「一函数治二记法」之依据（十栏同源，不必分账）；第 4 条之「交叉 0」是「顺序扫描之纯函数足用、不引第三方 markdown 库」之依据（红线六）——**若交叉非 0 即停下上报**，本轮无此情。

### ★ 一处自勘：首跑之数恰为两倍，系我之谓词写错

首跑得「星号 6090 对／反引号 5204 对」，**恰为所期之两倍**。查因：我的函数返回的是**记号之下标序列**（长度＝2×对数），而我径取其 `length` 当作对数。**数据无误，错的是我没书谓词就用了那个数。** 改除以二后逐项相符。

★ **此即本轮任务书所拟通例①「计数须随书谓词」之又一例**——同一口径之数，算法不同即不是同一个数。记之不删。

---

## 二、★ 反引号之渲法：**拟二案，陈其利害，不自行落定**（件二，候裁）

★ **Vision 未落定，亦不代裁**。下述二案同一 DOM、同一钩子，**其别只在 `site/styles.css` 一块三行**；`app.js` 两案全同，落定之后一字不动。**本轮 `styles.css` 内暂取案甲**，该块已加「★★ 候裁·案甲之三行（欲取案乙者，删本块即是；勿改 app.js）★★」之标。

### 二案之公共部分（已落，非候裁之项，其由附下）

| 项 | 取值 | 其由 |
|---|---|---|
| 元素 | `<code class="md-name">` | 类名写死为常量，不由数据决定（零 XSS 面之一环） |
| 字栈 | `--mono`（`ui-monospace, SFMono-Regular, Menlo, Consolas, Liberation Mono, monospace`） | **全用系统自带，不引 webfont**（红线六）。库内之名 99.9% 是拉丁与半角（含 CJK 者仅 3 处、0.1%，皆作 `quote_type=后出叙事` 之形），等宽正是其语域 |
| 字号 | `0.92em` | 等宽字之视觉字面大于中文同号，收 0.92 方与正文齐 |
| 色 | **一律承继上下文** | 照 §3.6.1 之律「不给 `strong` 另设颜色——改色即等于再开一档语态」。★ **故 §3.5 六档层色与 §2.1 十一国色诸矩阵一分未动、无须重跑** |
| 不取虚点下划线 | —— | 那一形已归通行字视图（r46 立，窖土褐虚点＝「此处经整理本回改」）。两种虚点下划线同屏，是把一个已有明确所指之形拿去说第二件事 |
| 不改 `letter-spacing` | —— | 改之则字幅变，§3.6.1 所立「盒零变」之证当场失守 |

### 二案之别，与逐项实测（`tools/qa/vision_r53.js` §五，两宽各跑一遍）

| | **案甲·淡底衬名**（本轮暂样） | **案乙·只易其字** |
|---|---|---|
| **其三行** | `background: rgba(46,42,36,0.06)`＋`border-radius: 2px`＋`padding: 0 0.2em` | **删去此三行** |
| **利** | 一望而知「此是库内之名」，正合裁四十八④之命；密行中可一眼数出几个名 | 盒几乎零变；密行不成补丁墙；不依赖任何「面」，在任何底色上表现一致 |
| **弊** | ① 盒会变，窄屏尤甚；② 密处成「补丁墙」；③ ★ **辨识随其所坐之底而变** | ★ **贴近「只去其符而已」**——裁四十八④正谓其不宜；辨识全压等宽字形，小字号处（层标 0.72rem）尤弱 |
| **盒变**（1440px／量器 640px） | `L_HUAN` +0.57px（0 行）｜`J002` +0.00（0 行）｜`P_CHUWU` **+23.77px（+1 行）**｜`events.summary` 最密者 +0.00（0 行） | 基准 |
| **盒变**（375px／量器 343px） | `L_HUAN` **+23.58px（+1 行）**｜`J002` **+47.72px（+2 行）**｜`P_CHUWU` **+23.77px（+1 行）**｜`events.summary` 最密者 +0.10（0 行） | 基准 |
| **补丁墙之量** | 最密之行实载库内之名 **6 枚**（`J002`）；有名之行：`L_HUAN` 41／`J002` 37／`P_CHUWU` 26／`events.summary` 19 | 不适用（无面可数） |
| **辨识 ΔL**（WCAG 相对亮度差，口径同 §2.8） | 素绢帛／卡面 **0.0920**｜引文卡页脚（层色淡底之上）**0.0851**｜编者层标（暖赭淡底之上）**0.0747**——最弱处比最强处**低 23%** | 不随底变 |

**求法**：把最密之四值（`L_HUAN.coord_basis` 64 枚／`J002.notes` 65 枚／`P_CHUWU.notes` 53 枚／`events.summary` 之最密者 34 枚）真渲进量器（宽 `min(640px, 100vw−32px)`，0.9rem／1.65），量其盒高、按 span 之盒顶归行数其密度；案乙以 CSS 覆盖等效关掉那三行后重量一遍。**所期**：二案之差只见于「面」所引之盒变与密度，不见于文字本身。**读法**：Δ高 > 0 即案甲之「面」把该值推高了；Δ行即多出之行数。

### ★ 三事请裁时权衡（Vision 之陈，不代决）

1. **裁四十八④之字面偏向案甲**——「不宜只去其符而已」。案乙之辨识确实薄，尤在层标 0.72rem 处。
2. **而案甲之代价集中在窄屏与密处**：375px 下最密三值各多 1～2 行；且其淡底**在引文卡与编者层标上最薄**（那两处本已各着一层淡底，再叠 0.06 之墨已近于无），而**引文卡恰是库内之名最密之处之一**。
3. ★ **故二案之外或有第三条路**（加深其底、或改取细边框而不填面）——惟那已越出「加不加面」之问，且会把「不另开语态档」这条律推到边上。**本轮不预拟，待裁后再议。**

★ **一处自记**：上第 2 条初稿系目视所得，作「层色淡底之上最小」；继以 ΔL 实测，**最小者乃暖赭层标（0.0747）而非引文卡（0.0851）**。与预判不符即改措辞，**不松动数据**。

---

## 三、落点：**自查所得，不取派件之列**（件一之 2 明命）

### 三之一、所改之文本流落点（十三处，八栏）

| # | 宿主（`app.js`） | 所渲之栏 | 原法 | 今法 |
|---|---|---|---|---|
| 1 | `personCardLi()` `:838`（`else if (ready)`） | `people.notes` | `p.textContent` | `p.appendChild(mdInlineFrag(…))` ★ **今日渲不出，见三之三** |
| 2 | `eventBodyNode()` 摘要 `<p>` | `events.summary` | `sm.textContent` | `mdInlineFrag` |
| 3–4 | `cmpMeetEvNode()`（**r53 新立**） | `events.summary` | ★ **`innerHTML` 字符串拼接**（⚑H ④款明禁） | 逐节点建之 |
| 5–6 | `buildPlaceContent()` 之 `row` 助手 | `places.coord_basis`／`places.description` | `d.textContent = dd` | `d.appendChild(mdInlineFrag(dd))` |
| 7–8 | 资料库详情之 `row` 助手 | `sources.notes`／`archaeology.summary` | 同上 | 同上 |
| 9 | `relDetailBody()` `.rel-note` | `relations.source_note` | `note.textContent` | `mdInlineFrag` |
| 10–11 | `eventQuotesFrag()` `p.q-caveat` ＋ `footer` | `passages.modern_note` | `mdBoldFrag`（只治星号） | `mdInlineFrag`（二记法一并治） |
| 12–13 | 役签胶囊 ＋ `p.evt-role-note` | `event_people.role_in_event` | `mdBoldFrag` | `mdInlineFrag` |

★ **两处 `row` 助手是本件最省之一改**：二者各是一个 `(dt, dd) => {…}` 局部箭头函数，**共 20 处调用**（地点 5 ＋ 资料库 15），一改覆其全。其余十五行之值**二记法俱零处**，经同一函数返回**恰好一个文本节点**，DOM 与旧版全等——故不分流，零影响有机械之证。

### 三之二、★ **与派件所列之异，照实报，不径改块**（领队加注 5 明命）

| 派件块所书 | Vision 实读 | 判 |
|---|---|---|
| 「`:2016` 与 `:3659` 两处 `row` 助手」 | **符**——二处行号逐位相符 | 符 |
| 「★ 共用之助手，**24 处调用**」 | ★ **实测 20 处**（`buildPlaceContent` 5 ＋ 资料库详情 15，求法：`grep -n "^\s*row("`，逐行列出可核）。若把两处 `const row = (dt, dd) =>` 之定义与另一处同名之 `const row = rows.find(…)` 一并数入，得 23；仍不得 24 | **异，已报**。★ 不径改块，亦不据块写断言 |
| 「`:838` `p.textContent = person.notes`」 | **符** | 符 |
| 「`:1211` `sm.textContent = evt.summary`」 | **符** | 符 |
| 「`:3369`／`:3374` 走 `innerHTML`，须一并改掉」 | **符**，已改（见三之四） | 符 |
| 「`:4517–4520` `note.textContent = rel.source_note`」 | **符** | 符 |
| 「`:3751`／`:4215` `source_note` 之串接（**请自核其落于文本抑或 `title` 属性**）」 | ★ **实读之答：二者俱落 SVG `<title>` 元素，非文本流**。`:3751` 之 `pairTip()` 由 `:4085`／`:4096`／`:4345`／`:4357` 四处用，皆作 `tip.textContent`，而 `tip = document.createElementNS(NS, "title")`；`:4213–4215` 同式 | **答其所问**：`title` 一路，**本件不治** |

### 三之三、★ 一桩落点之实读，与旧记不符——**`people.notes` 今日一行渲不出**

**病**：`:838` 落在 `else if (ready)` 一支上，**只在 `short_bio` 阙如时才渲注文**。而含记法之行**一律有小传**。

**求法**：`git show <ref>:data/csv/people.csv`，谓词「`notes` 含成对 `**` 或成对 `` ` ``，**且** `short_bio` 为空」。**所期**：若该栏真「读者可见」，其数当 > 0。

| 锚 | 其时 | `people` 行 | `notes` 含记法 | **其中无 `short_bio` 者** |
|---|---|---:|---:|---:|
| `bf4242c` | ⚑H 立条之基线（2026-09-19） | 166 | 34 | **0** |
| `ae3e800` | r51 收官 | 174 | 42 | **0** |
| `ce708df` | r52 收官 | 174 | 42 | **0** |

三点俱 **0**，且 `:838` 那一行代码在三点**逐字相同**（一并实读）。

**读法（须分两层，不可混作一句）**：
- **栏之层**：`people.notes` **是**一个实渲之栏（有一条渲染之路），故它**不是** conventions §7 ⚑H「本库不存在『写了没人看见』之注文栏」之反例——该条之首个反例仍是 `people.relations`（**连路都没有**）。
- **处之层**：其含记法之 42 行**无一行走得上那条路**，故 `design_notes` §3.6.1 所书「读者可见者 210 行 1564 处（…＋ `people.notes` 28 行 175 处）」**不实，且当日即已不实**，非其后所变。

**处置**：
1. `design_notes` §3.6.1 **就地加注**（原字一字未改，照 §7 v1.29／v1.41），把三点实测与两层之别写清；
2. 本件**仍把 `:838` 接上 `mdInlineFrag()`**——路该是对的，且新写之人若不填 `short_bio`，其注文即当场走上此路；
3. ★ **然本轮于此栏无读者可见之改善，如实记之，不以「已扩施八栏」掩之**。门为此栏立断言之法是**注入一行无 `short_bio` 之同型人物**（照 §7.2 按类反证之精神），**并在同节另立一条断言把「42 行含记法／0 行无小传」这个实数钉住**——注入之绿与实况之零，两条并列，谁也不许遮谁。

★ **上报（不在 Vision 之面，登记呈领队）**：conventions §7 ⚑H **②款之题曰「尚未由显示层承担者」**，而本栏今已由显示层承担其路却无一行实渲——**其款属之描述与实况之别，属体例之事**，请裁其当入②款、①款，抑或另立。

### 三之四、`innerHTML` 一路之绝（⚑H ④款明禁）

**原文两句**（`cmpShowMeetings()` 内）把**三样数据**（人名、事目题、摘要）拼进 HTML 串：

```
evA.innerHTML = '<i style="background:' + cmp.colorA + '"></i><b>' + personName(cmp.A) + "</b> · " +
  m.ea.title + "<br><span>" + (m.ea.summary || "") + "</span>";
```

改为逐节点建之（`cmpMeetEvNode()`）。★ **DOM 之形一字未改**（仍 `<i style=…>` ＋ `<b>名</b>` ＋ `" · "` ＋ 题 ＋ `<br>` ＋ `<span>摘要</span>`），故 `.cmp-meet-detail-ev` 一族选择器**无须动**；`<i>` 之底色改走 `style.background = …` 赋值（属性值不再由字符串拼接而成）。

**其病有实证，非推论**：门 §三之二以一条含 `<b>` 之摘要注入旧版那句，**当场生出 1 枚真 B 元素**；同一注入施于新版，B 元素 **0** 枚、`<b>` 以字面出场、`code.md-name` 1 枚。

**源码实读之证**：`.innerHTML =` 由旧版 **16 处**减为 **14 处**，恰减此 2 处。

### 三之五、★ `title` 之落点清单（件一之 3 明命「须列出，供下轮排期」）

**求法**：全文搜 `.title = `、`createElementNS(NS, "title")`、`setAttribute("title"`、`setAttribute("aria-label"`，逐处实读其所载之栏。**所期**：凡载 ⚑H 十栏者列出，余者剔除。

| `app.js` 之处 | 类 | 所载之栏 | 读者可及否 | 本轮实测 |
|---|---|---|---|---|
| `:1278` `b.title` | HTML `title` 属性 | `event_people.role_in_event` | ★ **可及** | ★ **裁二十六之问，本轮验到其答：漏**。其宿主 `eventPeopleNode()` 只在 `opts.people` 时出，而该 opts **只有编年视图**用（`eventBodyNode(evt, { people: true })`，`:1583`）。实测 **696 枚人物签之 `title` 内共载裸反引号 182 枚、裸星号 236 枚**（逐事目直调生产函数建之，不扫分批渲之页面）。★ **星号那一半自 r52 起就漏着** |
| `pairTip()` → `tip.textContent`（**四处用**：全景边 `:4085`、边徽记 `:4096`、ego 边 `:4345`、ego 边徽记 `:4357`） | SVG `<title>` 元素 | `relations.source_note` | 可及（悬停关系线） | 未量（同类） |
| ego 节点之 `<title>`（`:4213`–`:4215`） | SVG `<title>` 元素 | `relations.source_note` | 可及（悬停节点） | 未量（同类） |

★ **另须记明一处「一栏两态」**：`relations.source_note` 之**文本流**落点（`relDetailBody()` `.rel-note`）本件已治，而其 `title` 三处未治——故该栏今日「**点开见其名、悬停见其裸符**」。此系 `title` 一路未治之直接后果，**登记呈裁**。

★ **其治之难不在工而在体例**：`title` 只承字符串，欲去其记号须**另立一条「去记号」之纯函数**（与 `mdInlineFrag()` 同判据而不同出口）；而去符之后「此是库内之名」这层意思在 tooltip 里**无处可着**（tooltip 无排版）。故其治**须先裁「tooltip 内之库内之名如何自处」**——属体例之事，本件只列不办。

★ **不载 ⚑H 栏而剔除者**（列之以证「查过」）：`:393`／`:822` `XSNOTE`、`:811` `FLOW_CHIP_NOTE`、`:1096` 释文原貌、`:1177` 切换钮、`:1319`／`:1536` 事目分类、`:1555` 编年地望签、`:1857` 无地望计数、`:2696` 并观提示、`:3201` 交会标记、`:708` 首页徽记（人名）、`:1438` 年份刻度（事目题）——其所载或是写死之界面文字，或是 `name`／`title`／`category` 一类二记法俱零处之栏。

---

## 四、零影响之证（件四）

★ 判据**不与「我觉得没动它」对读**，而是真把改动前那一版（`OLD_REF = ce708df`）之 `app.js`／`styles.css` 于内存另起一个源端跑起来（同一份 `site/data/`，**不切分支、不 stash、不动仓库任何文件**），两版对读。

| 证 | 所期 | 实测 |
|---|---|---|
| **单一文本节点**（机械之证，非抽样） | 二记法俱无之值**逐个**落成恰好一个文本节点 | **15110 / 15110** |
| **引文卡逐卡对读**（509 张） | 无反引号之卡 `outerHTML` **逐位全等** | **390 张零差异** |
| 同上 | 含反引号之卡，两版之差**只有 `` `x` ``→`<code class="md-name">x</code>` 一式** | **119 张，无他差** |
| 两类相加 | 509，无卡漏检 | **390＋119＝509** |
| **`roleParts()` 694 行** | 与旧版**逐字全等** | **零差异**（694 行） |
| 数据未动 | `event_people` 694／`events` 265／`passages` 509／`places` 104／`people` 174／`sources` 195／`relations` 309／`archaeology` 8 | **逐项相符** |
| `quote_original` 一路 | Q442 引文正文两版 `outerHTML` 全等（r44d 之训） | **全等**，释文原貌切换钮两版俱在 |

### ★ 一处比法之误，首跑即现、已改——**记之以免后人再踩**

`OLD_REF`（`ce708df`＝r52 收官）**已含 r51 之 `mdBoldFrag`**，故旧版卡内**本就有 `<strong>`**。我初照 `vision_r51.js` 之式，把新版 `<strong>` 也还原成 `**` 去比旧版——**比的是「新版之星号记法」对「旧版之 `<strong>` 元素」，两边根本不同物，必假红**：首跑 137 张假红，其长差恰 **25×13**＝`<strong>` 标签之字符数，一算便知。

**改法**：**本轮新增者只有 `<code>` 一式，故只还原它**；分桶之判据亦由「二记法之有无」改为「**反引号之有无**」——只有星号之卡两版全同，正该落进「逐位全等」那一桶。

★ **此事之训**：**旧版源端之锚一换，逆函数就得跟着换**。r51 之锚在 ⚑H 之前（旧版无 `<strong>`），r53 之锚在 ⚑H 之后（旧版有 `<strong>`）——照抄前轮之式而不问锚之位置，就会拿两个不同的东西对读。

### ★ 另一处 harness 之误，亦首跑即现、已改

§五之量器初把宽度**写死 640px**，于是「375px 那一行」量的还是 640px，两宽之数**逐位相同**。**那种「相同」是 harness 测错了对象，不是实况**——若留着，它会读起来像「窄屏无差别」这个结论。已改为 `min(640px, 100vw − 32px)`，375px 一路方真（改后见案甲于最密三值各多 1～2 行）。

★ 同族之误另有一处：§四取卡初用 `document.querySelectorAll("blockquote.quote")` 扫编年页，**只取到 69 张**（编年是分批渲的，扫 DOM 必漏且**静默地漏**）。改照 `vision_r51.js` 之式**逐事目直调 `eventQuotesFrag()`**，得 509 张。

---

## 五、按类反证（件三，必备）

★ **每一记法、每一类落点各配一条反证**：同一量法施于**已知会红**之对照（锚定旧版 `ce708df`），**测不出红即抛错停门**。

| 反证 | 所期 | 实测 |
|---|---|---|
| 旧版「事目卡摘要」 | 裸反引号 > 0、`code` 元素 0 | **裸符 16 枚／code 0** |
| 旧版「地点卡·坐标依据」 | 同上 | **裸符 128 枚／code 0** |
| 旧版「关系详情·依据」 | 同上 | **裸符 2 枚／code 0** |
| 旧版「引文卡·编者层标」 | 同上 | **裸符 4 枚／code 0** |
| 旧版 `innerHTML` 一路 | 注入含 `<b>` 之摘要，**当场成真标记** | **B 元素 1 枚** |
| 新版同一注入 | B 元素 0、`code.md-name` 1、`<b>` 字面出场 | **B 0／code 1／文本逐字相符** |
| `people.notes` 之落点（**库内无一行走得上**） | 注入一行无 `short_bio` 之同型人物，须渲出 `code.md-name` | **2 枚 code、2 枚 strong、零裸符**（并另立一条钉住「42 行含记法／0 行无小传」之实数） |
| 锚定失效之闸 | 旧版内 `mdInlineFrag` 0 次、`.md-name` 0 次、「`innerHTML` 拼 summary」2 处 | **0／0／2**，不符即抛错停门（本轮首跑即因我正则写错而抛过一次，闸有效） |

★ **无此一节，§二§三跑绿只证明「脚本会打印 ✓」，不证明「脚本测的是裸反引号」**（design_notes §7.2）。

---

## 六、★ 验收偏差上报（★ 照角色护栏，等裁定，不擅自扩大改动面）

### 偏差一：`vision_r51.js` 转红 3 项、`vision_r52.js` 转红 4 项 —— **红在二门之判据以「星号」为唯一记法而立，不在站点**

**实况**（改动前后各跑一遍，基线可核）：

| 门 | 改动前 | 改动后 | 转红之项 |
|---|---|---|---|
| `vision_r51.js` | **97 项，0 红** | **97 项，3 红** | §四之三条：「不含成对星号之卡…逐位全等」「含成对星号之卡…两版之差只有 `**x**`→`<strong>`」「两类相加＝509」 |
| `vision_r52.js` | **57 项，0 红** | **57 项，4 红** | 「486 字之全文落卡内…往返还原逐字全等」×2 宽；「叔向 9 行胶囊与卡体 `outerHTML` 逐位相同」×2 宽 |

★★ **本节系 2026-09-25 领队复核后改写。原归因作「红在二门之逆函数」——经领队指出 `vision_r52.js` 之 `E206`／`E271` 二行不为其所覆，Vision 两源端实测重查，确认原归因 *不完整*：其所述只是两形之一。照「错的若是归因，就改归因，不要把实测往归因上凑」，此处改写归因，不改实测。原句存档于本节末「原归因之原字」。** ★★

**根（一）**：二门之判据——**分桶、逆函数、所比之域**——**一律以「星号」为唯一记法而立**（立条之时本库只治星号）。r53 引入第二种记法，遂把这些判据之域**撑破**。其表现有**两形，机理不同，不可含糊并作一因**：

**形㈠·逆函数之形**（还原式只识 `**`）：
- `vision_r51.js` `:180`／`:200`／`:248`／`:295`／`:377` 一律作 `"**" + n.textContent + "**"` 或 `.replace(/<strong>/g,"**")`；
- `vision_r52.js` `:80` 之 `restore()` 把非 `STRONG` 之元素节点一律记作 `"?" + textContent`。
故凡含反引号之值，其**还原必不等**。中此形者：r51 §四「含成对星号之卡…两版之差只有 `**x**`→`<strong>`」**1 项**；r52「486 字之全文落卡内…往返还原逐字全等」**2 项**（两宽各一，`E295`／`P_CHUWU` 之 `role_in_event` 含 8 对反引号）。

**形㈡·所比之域之形**（★ **根本不经逆函数，是裸比 `outerHTML`**）：
- `vision_r51.js` §四「不含成对星号之卡 N 张…逐位全等」——其**分桶判据**按「有无成对星号」划，含反引号而无星号之卡遂被划进「应逐位全等」那一桶；
- `vision_r52.js` §四「叔向 9 行胶囊与卡体 `outerHTML` 逐位相同」——★ **此条之红与逆函数全无干系**。
中此形者：r51 **1 项**（＋派生之「两类相加＝509」1 项）；r52 **2 项**（两宽各一）。

**（二）★ `E206`／`E271` 之补证**（领队 2026-09-25 交还，Vision 两源端实测；旧版锚照 `vision_r52.js` 门内所钉之 **`ae3e800`**，非 `HEAD`、非本门之 `ce708df`）：

r52 该条断言比的是 `chipHTML` **与 `bodyHTML` 二者**，而 `.event-body` **含事目摘要与引文卡**（`eventQuotesFrag`）——**不止 `role_in_event` 一栏**。实测：

| 卡 | `chipHTML` | `bodyHTML` | 其 `role_in_event` | 其 `events.summary` | 其引文 `modern_note` | 新版 `code.md-name` |
|---|---|---|---:|---:|---|---:|
| `E206` | ★ **逐位相同** | 3678 → 3840 | 0 对 | 0 对 | **`Q495` 6 对** | **6 枚** ✓ |
| `E271` | ★ **逐位相同** | 1575 → 1926 | 0 对 | **5 对** | **`Q441` 8 对** | **13 枚** ✓ |

- ★ **胶囊本身两版全等**——领队所查之 `role_in_event`（27／41 字、二记法俱零）**确实一字未变**，r52 该断言之**本意**（`role_in_event` 一栏零影响）今日仍成立；红的是**该断言所比之域内另外两栏**。
- **首处不同即是** `` `Q281` ``→`<code class="md-name">Q281</code>`（`E206`，在 `Q495` 之 `modern_note` 内）、`` `E221` ``→`<code class="md-name">E221</code>`（`E271`）。
- ★ **枚数逐条对得上**：`E206` 6 对→6 枚；`E271` 5＋8＝13 对→13 枚。
- ★ **站点无非预期之变**：把新版 `<code class="md-name">x</code>` 还原成反引号后，与旧版**逐位相同＝`true`**（二行俱是）。**故不是回归**，无须停下上报之情。

★ **另报一事（我与领队俱被其误导）**：r52 门所印之 `["E206","E271"]` 系 `domDiff.slice(0, 2)` 之**截断**——**实际有差者 4 行**（`E206`／`E210`／`E221`／`E271`），无差者 5 行（`E194`／`E196`／`E209`／`E219`／`E220`）。**「细目只有两行」是打印之限，不是实况之数**；此亦一条通例：**报红之细目若经 `slice`，须连其截断一并书**。

---

### ★★ （四）第三次改写：**两形之所据，须连「记法」一并分列** ★★

**2026-09-25 领队第二次交还补证**：四行之中 **`E210` 一行，数据侧四处俱无反引号**（其 `summary`、其 5 张引文卡逐栏、其 3 枚人物签之 `role_in_event`，并已排除 `quote_original` 之虞——全库该栏含反引号 0 行）。**以上（一）（二）之归因解释不了它。**

**实测（旧版锚仍取 `ae3e800`）**：

```
E210  bodyHTML 4468 → 4481   Δ=13
  首处不同：旧「…**本条为晏婴、叔向共用事件**：ID 由本件…」
            新「…<strong>本条为晏婴、叔向共用事件</strong>：ID 由本件…」
  新版 code.md-name 0 枚｜strong 新 1 / 旧 0（Δstrong=1）
  只还原 <code> 后与旧版逐位同？ false
  ★ 二式并还原（<code>→反引号、<strong>→星号，两版同施）后逐位同？ true
  算术核：0×27 ＋ 1×13 ＝ 13，实测 Δ=13   ✓
  其值：events.summary **星号 1 对**、反引号 0 对；五张引文卡二记法俱 0
```

★ **其差是 `<strong>` 之式，不是 `<code>` 之式。**

**何以仍属预期、不是回归**——**有源码实证，非推论**：r52 门之锚 `ae3e800` 之 `app.js` **`:1150`** 作 `sm.textContent = evt.summary || "";`，**`events.summary` 根本未经 `mdBoldFrag`**（同版 `mdBoldFrag(` 调用点 3 处，施于 `evt.summary` 者**无**）。即**该栏之星号在 `ae3e800` 时亦是裸出的**，而本件 ⚑H ②款扩施该栏（**172 对／39 行**）正是要治它。故 `<strong>` 亦是**本件之所生、之所欲**。

★ **领队未漏栏——漏的不是栏，是记法；而把领队引偏的是我。** 领队所查四处俱无反引号，**那是对的**；其所以只查反引号，是因我在（一）里写「形㈠／形㈡**二者俱系『反引号成 code』所致**」——**把星号那一半漏掉了**。

**根因（我须写下来，因为这是我第三次在同一处失之于窄）**：★ **我一直拿本门之锚 `ce708df` 想问题，而 r52 门之锚是 `ae3e800`。相对不同的锚，「本轮新增了什么」是不同的集合**：

| 相对之锚 | 其时 `events.summary` 之渲法 | **本件新增之式** |
|---|---|---|
| `ce708df`（**本门之锚**，r52 收官） | `textContent`（星号裸出） | **只有 `<code>` 一式**——因本门 §四所比者是引文卡（`modern_note`），该栏之星号在 `ce708df` 时**已**是 `<strong>`（r51 所治） |
| `ae3e800`（**r52 门之锚**，r51 收官） | `textContent`（星号裸出） | ★ **两式**：`<code>`（八栏）**＋ `<strong>`（`events.summary` 等本轮新扩之栏）** |

**故（一）之两形，其所据之记法须补列**：

| 形 | 机理 | 所据之记法（相对 `ae3e800`） | 中之者 |
|---|---|---|---|
| **形㈠·逆函数** | 还原式只识 `**` | **反引号**（`<code>` 还不回去） | r51 1 项、r52 2 项 |
| **形㈡·所比之域** | 裸比 `outerHTML`，其域只按星号划／只当作一栏之域 | ★ **反引号 *与* 星号俱可致之**——`E206`／`E271`／`E221` 由反引号，**`E210` 由星号** | r51 1 项＋派生 1 项、r52 2 项 |

**四行之算术俱对上**（★ 二式并还原后逐位相同 **`true`**，四行无一例外）：

| 卡 | Δ | 拆 | 源 |
|---|---:|---|---|
| `E206` | +162 | 6×27 | `Q495.modern_note` 反引号 6 对 |
| `E271` | +351 | 13×27 | `E271.summary` 5 ＋ `Q441.modern_note` 8 |
| `E221` | +540 | 20×27 | `Q515` 11 ＋ `Q516` 9（★ 领队所查 20 对，**自洽，已解**；只还原 `<code>` 即逐位同 `true`） |
| `E210` | **+13** | **1×13** | ★ **`E210.summary` 星号 1 对**（27＝`<code class="md-name">`＋`</code>`－2 反引号；13＝`<strong>`＋`</strong>`－4 星号） |

★ **停下上报之交代**（领队命第 3 条）：领队所命「若**不是** `<code>` 之式，即系非预期之变，立即停下上报、不自行修补」——**我照办了：未改一字，报在此处。** 惟须同时指出，**那条判据之前提是我给窄的**：「本件所生之变只有 `<code>` 一式」**只在本门之锚 `ce708df` 下成立**；相对 `ae3e800`，`<strong>` 同是本件所生。故此行**不是**非预期之变。★ **然判据窄了是我之失，不是领队之失**——**据窄了的归因立判据，判据必跟着窄**，这一条比本行本身更值得记。

> **改写史（★ 照「依据链留错误史」，不抹平成「一直如此」）**：本节归因**三稿**——
> ① **初稿**：「红在二门之逆函数」→ 漏**形㈡**（裸比 `outerHTML` 者根本不经逆函数）；
> ② **二稿**（领队第一次交还后）：拆出两形 → **两形皆系于反引号**，漏**星号**；
> ③ **本稿**（领队第二次交还后）：两形之**所据之记法**分列，并记明其根因＝**我拿本门之锚想 r52 门之事**。
> ★ 三稿之失同出一源：**每次都只补了被问到的那一处，没有回头问「这个归因还漏了什么维度」**。三稿原字俱照留于本节，不删。

**（三）总数仍可核**：r51 §四红之 **11 ＋ 108 ＝ 119 张**，恰等于 `modern_note` 含反引号之 **119 行**——**一张不多、一张不少**（本门 §四已把 119 钉成断言）。★ 惟须辨明：其 **11 张属形㈡、108 张属形㈠**，二者只是总数同归一根，**机理不同**。

★ **二门之红是「站点变了」之正确反映，不是回归**：叔向诸卡之 DOM 本就该变（`events.summary` 与 `modern_note` 之反引号已成 `<code>`），那正是本件之目的。

> **原归因之原字（2026-09-25 改写前，照留不删）**：「**因**（★ 量化归因，非推测）：二门之**还原式只识 `**`、不识 `` ` ``**……r52 之红同理（`E295/P_CHUWU` 之 `role_in_event` 含 8 对反引号；叔向 9 卡之 `.event-body` 含引文卡，其 `modern_note` 与 `events.summary` 含记法者已改渲）。」
> ★ **其失有二**：① 把**两形**笼统归作「逆函数」一因，而 r52 叔向二项、r51 「逐位全等」一项**根本不经逆函数**；② r52 之因只在括号里一笔带过、无一个数支撑，遂经不起复核之问。**记之不删。**

**本轮不治之由**：r53-3 件之界明书「**`tools/qa/` 之下你只碰 `vision_r53.js` 一个文件**」。故本件**不动二门一字**，只把红在何处量清。

**所拟之治（三途，Vision 不代决）**：
- ㈠ **改二门之判据为识二记法**——★ 须**两形并治**：逆函数补识 `` ` ``（形㈠），**分桶／所比之域**亦须随之扩（形㈡，如 r52 叔向一条须改断「还原 `<code>` 后逐位相同」而非裸比）。只改逆函数**治不了形㈡那 3 项**。惟须加授二文件；
- ㈡ **把二门之锚各自前移一版并冻结其域**——治标而不治本，下次扩施仍要再来一次；
- ㈢ **裁定「历轮之门只作史，不作现行回归门」**，其红登记不修——★ **此途最须慎**：二门内另有大量与本件无涉之断言（r51 97 项中 94 项仍绿、r52 57 项中 53 项仍绿），弃之可惜。
  ★ **Vision 之见**：倾向㈠，且宜与 r53-5 之 QA 归并同批排（其时正在动 `tools/qa/`）。**呈领队裁。**

### 偏差二：⚑H ②款之星号数，**须随书一个谓词**

★ **裁四十八改变了「有多少对星号」这个数**，其量恰为 **1 对**：

| 谓词 | 数 | 算法 |
|---|---:|---|
| **原口径，不问反引号** | **3045 对／406 行次** | 即 r51 之 `mdBoldMarks()`，＝ conventions §7 ⚑H ⑤款**现行所载之数** |
| **施反引号优先之后**（即真渲成 `<strong>` 者） | **3044 对** | `mdInlineMarks().stars` |

**差之所在已逐条定位**：`people.notes` 之 **`P_JILIANG`（季梁）**行，18 对 → 17 对——其 `` `is_protagonist=**0**` `` 内那一对星号落在 code span 内，照裁四十八**不解析、原样照出**。

★ **裁四十八②书「本条不伤任何一处现有之文」——就渲染而言为真**（那一处正该字面照出，合其意）；**就计数而言则不然**。二数**俱不是「错的那个」**，错的是不书谓词而径引其一。本门 §二**两数并断**，并把那唯一一处钉成断言。

**上报**：conventions §7 ⚑H ②款／⑤款所载之 3045（及②款七栏之 1703）**是否须随书此谓词**，属体例之事，**不在 Vision 之面**，呈领队裁。★ **本件未动 conventions 一字。**

### 偏差三（登记，非本件之病）：`.innerHTML =` 尚余 3 处拼数据

| 类 | 处数 | 行 | 所注者 | 判 |
|---|---:|---|---|---|
| 徽记／分类图标／底图 SVG 之注入 | **8** | `:509`／`:794`／`:1393`／`:1445`／`:1665`／`:642`／`:1832`／`:3158` | 本站自有之 SVG 资源，非数据之字 | 不在 ④款之域 |
| 写死之静态串 | **2** | `:3387`／`:3390` | 零数据 | 同上 |
| 代码算出之文字 | **1** | `:3312`（`yearLabel()` 之出） | 非库内之字 | 同上 |
| ★ **仍拼数据者** | **3** | `:3445`／`:3450`（句内直见 `personName()` ＋ `events.title`）；`:3379`（句内只作 `+ name +`，**人名经参数传入**：`item(cmp.colorA, "A", personName(cmp.A), …)`） | `people.name`／`events.title` | ★ **与本件所治者同类**，惟其所载非 ⚑H 十栏（二栏之记法俱零处、亦无 `<`），故不在 ④款之域。**本件不扩其面**（准则六），**登记呈领队排期** |

★ **谓词须书明其所照与所不照**：门以「整句内找 `personName(`／`.title`」为谓词，**只照得到 2 处**；第三处（`:3379`）之数据经参数传入，该谓词照不到。故门以**两条断言分断之，不硬凑其数**——若硬写 3，测出来的就是自己设的靶。

---

## 七、QA 全帐（2026-09-25，本机 Chromium／Windows）

| 门 | 结果 | 备 |
|---|---|---|
| `python tools/validate.py` | **OK：全部校验通过** | 红线一 |
| `tools/qa/vision_r53.js`（**新立**） | **94 项，FAIL 0** | 本件之门 |
| `tools/qa/regress20.js` | **页面错误：无** | 改动前后各跑一遍，降级清单不变 |
| `tools/qa/prod_render_invariants.js`（`QA_BASE_URL=local`） | **48 项，FAIL 0** | ★ **一字未动**（裁四十九①）；本地源端跑之作回归 |
| `vision_r20`／`r21`／`r21b`／`r22`／`r28` | **俱 0 红** | |
| `vision_r32` | **29 项，失败 0** | 须外起 `python -m http.server 8791` |
| `vision_r46` | **82 项，全过** | 同上 |
| `vision_r24a` | **512 通过／1 未过** | ★ 系**既有** ⚑J 之 FAIL（「无地望事件 11 条一律中性签（实测中性 12）」，待裁池，conventions v1.45 明书「照留不改」）——**非本件所生** |
| `vision_r51` | **97 项，3 红** | ★ 见偏差一 |
| `vision_r52` | **57 项，4 红** | ★ 见偏差一 |
| `r43_prod_check`／`r44_`／`r45_`／`r43_prod_render_check` | **未跑** | ★ 其靶在生产（未含本件之改），且**本轮正由 Skipper（r53-5）归并移走**——不跑、不碰 |

---

## 八、工作区之状（交件时）

```
 M docs/delivery_skipper_r52.md      ← 他件（r53-2），一字未动
 M docs/design/design_notes.md       ← 本件（升 v2.14）
 M site/app.js                       ← 本件
 M site/styles.css                   ← 本件
 D tools/qa/r43_prod_check.js        ← 他件（r53-5 Skipper），一字未动
 D tools/qa/r43_prod_render_check.js ← 同上
 D tools/qa/r44_prod_check.js        ← 同上
 D tools/qa/r45_prod_check.js        ← 同上
?? data/incoming/r53_kongzi_role/    ← 他件（r53-4 Sophia），一字未动
?? docs/delivery_skipper_r53.md      ← 他件，一字未动
?? tools/qa/prod_data_invariants.js  ← 他件（r53-5），一字未动
?? tools/qa/vision_r53.js            ← 本件（新立）
```

`HEAD` 仍 `ce708df`，与远端同哈希。**未提交、未推送。**

**改动量**（`git diff --numstat`）：`site/app.js` **196 增／27 删**｜`site/styles.css` **44 增／0 删**｜`docs/design/design_notes.md` **170 增／2 删**。

★ **2026-09-26 r53-6 之后改作**（原数照留于上，就地加注）：`site/styles.css` **41 增／0 删**（44 − 3，删去案甲之三行）｜`docs/design/design_notes.md` **187 增／2 删**（其 2 删仍只是 v2.13 两行版本头，见 §十）｜`site/app.js` **196 增／27 删，逐位未变**。

★ **一桩自误，已回正，记之**：落笔中以 Python 改写 `design_notes.md` 与 `vision_r53.js`，其默认写入在 Windows 上把行尾由 **LF 变成了 CRLF**（`git diff` 因自动归一而未显其形，故不易察觉）。核仓库惯例——`site/app.js`／`docs/conventions.md`／`tools/qa/vision_r51.js`／`vision_r52.js`／`prod_render_invariants.js` **一律 LF**——遂把二文件逐字节回正（`b"\r\n"` → `b"\n"`，design_notes 1273 行、vision_r53.js 782 行），回正后 `file` 实测无 CRLF、`node --check` 仍过、门重跑仍 **94 项 0 红**。**非本意之改，不留与后人对着 diff 猜。**

★ **会话在制品之交代**（红线四附则②）：落笔中曾于 `tools/qa/` 下置一目视核验用截图脚本 `_tmp_shot_vision_r53.js` 并留来源标记 `_tmp_shot_vision_r53.SOURCE.json`（记会话、日期、目的），**用毕即由本会话自删**（系本会话自己落盘之物，非他人产出，故径删不入 `_to_delete_`）。交件时 `tools/qa/` 下无任何 `_tmp_` 残留。

---

## 九、候裁与承接（汇总，**Vision 不代决**）

1. ★ **反引号之渲法：案甲抑或案乙**（件二明命停下上报）——二案、其利害、逐项实测见 §二。落定只须换 `styles.css` 一块三行，`app.js` 一字不动。
   ★ **2026-09-26 已裁：取案乙**（裁五十），r53-6 执行完毕——见 §十。**本条了结**（原字照留）。
2. ★ **`vision_r51.js`／`vision_r52.js` 之红如何治**——三途见 §六偏差一，Vision 倾向㈠（改其逆函数），宜与 QA 归并同批排。
3. ★ **⚑H ②款之数是否须随书「反引号优先」之谓词**（§六偏差二）——体例之事，不在 Vision 之面。
4. ★ **`people.notes` 之款属**（§三之三）——其路已治而无一行实渲，②款之题「尚未由显示层承担者」与实况有别，请裁。
5. ★ **`title` 一路之治**（§三之五）——`:1278` 之漏本轮验到其数（182 裸反引号／236 裸星号）；`relations.source_note` 今日「点开见其名、悬停见其裸符」之一栏两态。其治**须先裁 tooltip 内之库内之名如何自处**。
6. **`.innerHTML =` 余 3 处拼数据**（§六偏差三）——同类隐患，非 ⚑H 十栏，登记排期。
7. **时序**（§四.2 之界）：Sophia r53-4 之 19 行备料**须俟本件渲染层落定后再合入**，其实效（胶囊首句是否成短语、切点是否仍腰斩）须在新渲染层上验。★ 本件**未为迁就其未定之文而改任何判据**：`roleParts()` 之门槛 60、句读 `[。；！？]`、首句之取法**一字未动**，其 694 行之出与 r52 **逐字全等**（§四已证）。新增者只有「截点避 code span」一闸，**今日 103 个分者无一触发**。

---

## 十、r53-6 追记（2026-09-26）：**反引号取案乙**（裁五十）

### 十之一、所读之节，与该节末次勘注之日（体例第 5 条）

| 所读之节 | 该节末次勘注之日 | 备注 |
|---|---|---|
| `team/round53_prompts.md` `## Task for Vision · r53-6 — 反引号取案乙（裁五十）`（四条，`:580`–`:589`） | ★ **该节内无署日**——体例第 4 条云「已派之件其改动追加并署日」，本节自派出以来**无追署之痕**；故其日以所据之段与派件加注为准（见下二行） | 自读现行文本，不取派件之复述（体例第 2 条） |
| 同上 §六 **裁五十**（`:478`–`:491`） | **2026-09-26**（节头署「§六 r53 三件之裁（裁五十至六十二，2026-09-26 Co站长）」`:476`；节内无更晚之追署） | 本件之所据 |
| 领队加注（随派件投递，五条） | **2026-09-26 09:28 -0400**（其自书「实读」） | ★ 照纪律三自核一过，结果见十之二 |
| `docs/design/design_notes.md` §3.6.3「渲法之裁」 | 改前 **2026-09-25**（r53-3 本人落笔，未提交）；**本件加注 2026-09-26** | 本件之交件之一 |

### 十之二、前置之自核（★ 纪律三：不凭派件之复述）

| 所核 | 求法 | 所期之果 | 实读 |
|---|---|---|---|
| `HEAD` 与远端同哈希 | `git log --oneline -1`／`git ls-remote origin main` | 二者同 | **`ce708df`／`ce708df6765…4e7a`——同**，符 |
| 工作区 13 项 | `git status --porcelain`（计其行） | 13 行，且皆 r53 三件之在制品 | **13 行**（`M` 4／`D` 4／`??` 5），符 |
| 续改于 r53-3 之成果上，不回退不重建 | 落笔前 `cp site/styles.css` 存一本于会话暂存区，改后对读 | 只见本件之改 | 符（十之三①） |

★ **本件未用 `git checkout／switch／stash／reset`**（三件共用工作区）；亦未动他件之面（`docs/conventions.md`、`tools/qa/prod_data_invariants.js`、`data/incoming/r53_kongzi_role/`、`docs/delivery_skipper_r53.md` 一字未碰）。

### 十之三、所改（二文件 ＋ 本文档）

1. **`site/styles.css`——删案甲之三行**：`background: rgba(46, 42, 36, 0.06);`／`border-radius: 2px;`／`padding: 0 0.2em;`。
   ★ `--mono` 字栈、`0.92em`、`font-style: normal`、`font-variant-ligatures: none`、**承继上下文之色**俱留（件之第 1 条明命）。★ `site/app.js` **一字未动**。
2. **`docs/design/design_notes.md`——记其裁、其据与登记项**（件之第 2 条）：§3.6.3 末新立「★★ 裁定（2026-09-26）★★」一节（四由 ＋ 落定后之实测 ＋ 登记 ＋ 一事候令），并于节头与版本行 ⑤ **就地加注**「已裁：取案乙」。
   ★ **原字照留**：二案之陈、「暂样」「暂取案甲」「候裁」诸字**一字未删**——所记者是**本人实测之数**（ΔL 三数、两宽之行数），非「领队选了乙」。
   ★ **版本号未升**（仍 v2.14）：v2.14 即本轮（r53）之号且尚未提交，本件系同轮同一交付之续，故照「就地加注」之律加注于 v2.14 行内，不另开 v2.15。★ 若领队以为当升号，一行可改，候令。

### 十之四、自验三条（★ 求法｜所期之果｜读法，逐条附实数）

**① 案乙之盒变对基准：Δ高 0、Δ行 0（两宽）**
- **求法**：`node tools/qa/vision_r53.js` §五——把最密之四值（`L_HUAN.coord_basis` 64 枚／`J002.notes` 65 枚／`P_CHUWU.notes` 53 枚／`events.summary` 最密者 34 枚）真渲进量器（宽 `min(640px, 100vw−32px)`，0.9rem／1.65），量其盒高与行数；第二遍以 `YI_CSS`（`background:none!important;border-radius:0!important;padding:0!important`）覆盖后重量。**改前亦跑一遍存证**（会话暂存区 `pre.txt`／`post.txt`）。
- **所期之果**：三行既删，二遍之量必同 → **Δ高 0.00、Δ行 0，两宽八格无一例外**。
- **读法**：Δ高 > 0 即「面」把该值推高了；★ **更强之一证**——改后之「甲·高」须与**改前之「乙·高」逐位相同**，否则「Δ=0」只证两遍一致，不证所落定者确是案乙。

| 宽 | 栏 | 改前 甲·高／行 | 改前 乙·高／行 | 改后 甲·高／行 | 改后 Δ高／Δ行 |
|---|---|---|---|---|---|
| 1440px（量器 640） | `places.coord_basis` | 1786.27／75 | 1785.70／75 | **1785.70／75** | **0.00／0** |
| | `sources.notes` | 2902.88／122 | 2902.88／122 | **2902.88／122** | **0.00／0** |
| | `people.notes` | 1048.13／44 | 1024.36／43 | **1024.36／43** | **0.00／0** |
| | `events.summary` | 786.05／33 | 786.05／33 | **786.05／33** | **0.00／0** |
| 375px（量器 343） | `places.coord_basis` | 3402.80／143 | 3379.22／142 | **3379.22／142** | **0.00／0** |
| | `sources.notes` | 5518.13／232 | 5470.41／230 | **5470.41／230** | **0.00／0** |
| | `people.notes` | 1975.83／83 | 1952.06／82 | **1952.06／82** | **0.00／0** |
| | `events.summary` | 1475.63／62 | 1475.53／62 | **1475.53／62** | **0.00／0** |

★ **改后之「甲·高」与改前之「乙·高」八格逐位相同**——故所落定者确是案乙。门印之两行总结：`案甲对案乙之盒高差：宽 [0,0,0,0] px／窄 [0,0,0,0] px`。
★ **一处读法之提醒**（免他日误读）：表末二列「一行最多几枚／有名之行」于案乙**已无意义**（无面可数，其数只是 span 盒顶归行之副产物，改后两宽各有升降），门之读法②本已声明「案乙此数无意义」。
★ **§五之 ΔL 一段改后仍可复算**：其量法以 `rgba(46,42,36,0.06)` **逐层合成所坐之底**，不读 `.md-name` 之实底，故三数改后不变（引文卡页脚 0.0851／编者层标 0.0747／地点卡 dd 0.0920，最弱低 23%）——**本裁之据日后仍可重跑核对**，非一次性之数。

**② `vision_r53.js` 全过**
- **求法**：`node tools/qa/vision_r53.js`（本机 Windows／Chromium，`tools/qa/node_modules` 之 playwright；旧版源端仍锚 `ce708df`）。
- **所期之果**：`=== 合计 94 项，FAIL 0 ===`、exit 0。
- **读法**：FAIL 非 0 即停下上报；项数少于 94 亦是异常（有段未跑）。
- **实读**：改前 **94 项／FAIL 0／exit 0**；改后 **94 项／FAIL 0／exit 0**。★ 含 §三之二按类反证（旧版须当场测出裸反引号：事目卡 16／地点卡 128／关系依据 2／层标 4）、§六「375px 无一枚库内之名越界、页面无横向溢出」俱仍绿。

**③ `git diff --numstat`：`styles.css` 之删恰 3、`app.js` 0 改**
- **求法**：`git diff --numstat site/styles.css site/app.js`；另以改前存本 `git diff --no-index <改前之本> site/styles.css --numstat`。
- **所期之果**：件之第 3 条书「删行恰 3」；`app.js` **0 改**。
- **实读**：`site/app.js` **196 增／27 删——与改前逐位相同，本件 0 改**（`git diff --numstat site/app.js` 改前改后两读同字）；`site/styles.css` 对 `HEAD` 为 **41 增／0 删**（改前 44／0，**减恰 3**）；对**改前之本**为 **0 增／3 删**（删行恰 3）。

### 十之五、★ 验收偏差上报（★ 照角色护栏，等裁定，不擅自扩大改动面）

**偏差一（量法之偏，非结果之偏）：「`git diff --numstat site/styles.css` 之删行恰 3」在对 `HEAD` 之读法下不可得，其数必 0。**
- **其由**：`.md-name` 全块系 r53-3 **新增**之行（对 `HEAD` 之 styles.css 为 44 增／**0 删**）。删去三行新增之行，只使**增数减 3**，不生删行——故对 `HEAD` 之 numstat 改后为 **41／0**，其「删」恒为 0。
- **本件之处置**：两法并报（十之四③）——对 `HEAD` **41 增／0 删（44−3）**；对**改前之本** **0 增／3 删**。二者俱指同一事实：**恰三行去矣，别无他改**。**不改门、不动件之字**，请裁其后此类自验之读法当以何为准（拙见：**取「改前之本对改后」之差**，方与「只删三行」之命同一量纲）。
- ★ **2026-09-26 领队答：此判得对**——「是件之自验措辞与量纲不合，不是你没办到」；并拟**升为通例呈站长**：「**凡自验之数，须书明其所对之基准（对 `HEAD`、对改前之本、拑或对某锚）；基准不书，其数即无谓词。**」——与本轮已立「计数随书谓词」同族，是其在 diff 上之一面。★ **本节十之七即照新例落笔**：二组之数各书其基准。

**偏差二：`styles.css` 内该块之标与其空壳未删——本件不擅自扩大改动面，拟治如下，候令。**
- **实况**：三行既删，其处今存
  ```
  /* ★★ 候裁·案甲之三行（欲取案乙者，删本块即是；勿改 app.js） ★★ */
  .md-name {
  }
  ```
  且其上之长注（`:133`–`:141`）仍作「★★ **候裁之块** ★★ ……**案甲·淡底衬名**（本块所取，**暂样**）」。**裁既下，此二处之字与实况相左**（空壳无功用，长注之「候裁」「暂样」已非实）。
- **★ 为何本件不办**：件之第 1 条命「**只删三行**」，第 3 条又以「删行恰 3」为闸；删其标与空壳即再去 3 行（注 1 ＋ 选择符 1 ＋ 右花括号 1），当场破该闸。照角色护栏第 6 条，**记此上报，等裁定**。
- **拟治（一行可落，不涉 `app.js`）**：① 删「候裁·案甲之三行」之注与其空壳三行；② 上方长注之「候裁之块／本块所取／暂样」改作「**已裁·案乙（2026-09-26 裁五十）**：其形＝等宽 ＋ 0.92em ＋ 承继上下文之色，**不加底、不加圆角、不加内边距**」，并存其所去之三行于注内（供他日第三路再议时对读）。★ 落之后 `vision_r53.js` 须重跑一过（其 §五 之 `YI_CSS` 覆盖仍为空操作，Δ 仍全 0，不受影响）。
- ★ **2026-09-26 领队裁：当办——已办（上文原字照留）**。其裁之理：「**该闸所量者是『案甲之三行已去』，不是『文件只减三行』**；收尾之删与长注之改属**同一裁之落实**，非另一件、非扩大改动面。」——**所办四条与其果、二组之数见下十之七。**

### 十之六、界与状（交件时）

- **本件只动**（★ **收尾之后之数见十之七**，下数为收尾前之状，原字照留）：`site/styles.css`（−3 行）、`docs/design/design_notes.md`（加注 ＋ 新立一节）、`docs/delivery_vision_r53.md`（§八／§九 加注 ＋ 本节）。
- **`site/app.js` 0 改**；`tools/qa/vision_r53.js` **未动一字**（非本件交件之列，其 94 项断言无一系于那三行）。
- **他件之面一字未碰**：`docs/conventions.md`、`tools/qa/prod_data_invariants.js`、`data/incoming/r53_kongzi_role/`、`docs/delivery_skipper_r53.md`、`docs/delivery_skipper_r52.md`。
- **`HEAD` 仍 `ce708df`，与远端同哈希。未提交、未推送。**
- **会话之在制品**（红线四附则②）：只于会话暂存区（`…/scratchpad/`）留 `styles.before.css`／`pre.txt`／`post.txt` 三本，**不落仓库**。
- **附一验**（非件之所命，聊以证未破）：`python tools/validate.py` → `OK：全部校验通过`、**exit 0**（本件未动 `data/` 一字，此数只作旁证）。
- **行尾**：三文件逐字节核过——`CRLF 0／LF n`（`docs/delivery_vision_r53.md` 522／`design_notes.md` 1290／`styles.css` 2394），未复 r53-3 之行尾之误（§八）。

### 十之七、收尾之办（2026-09-26 领队裁「偏差二当办」，同日续办）

★ **其裁之理，记之**：「**该闸所量者是『案甲之三行已去』，不是『文件只减三行』**；收尾之删与长注之改属**同一裁之落实**，非另一件、非扩大改动面。」——故照办，惟使闸仍可读：**二组之数各书其基准**（照领队同日所立之通例，见十之五偏差一之加注）。

#### 所办四条，与其果

| # | 所命 | 已办 |
|---|---|---|
| 1 | 删 `:146` 之「候裁·案甲之三行」标与 `:147–148` 之空壳 `.md-name { }` | **已删三行**（注 1 ＋ 选择符 1 ＋ 右花括号 1） |
| 2 | `:133–:141` 长注改作已裁之文，**并存所去三行之原字** | 改作「★★★ **已裁·案乙（2026-09-26，`team/round53_prompts.md` §六 裁五十）** ★★★」，内载裁之据之摘（ΔL 三数、375px 之行数）、**所去三行原字**与**登记**（0.72rem 层标、第三路须带实测）；今为 `:133–:154`，22 行 |
| 3 | 案甲二案之陈、其实测之数、「候裁」当日之原字，一律照留 | **照留**：`design_notes` §3.6.3 之二案表、「暂样」「候裁」「暂取案甲」诸字与 `delivery_vision_r53.md` §二**一字未删**；所去者只是 `styles.css` 内**已失效之活文** |
| 4 | 重跑 `vision_r53.js` 与 `validate.py`，列其果 | `vision_r53.js` **94 项／FAIL 0／exit 0**，§五 Δ高 `宽 [0,0,0,0]／窄 [0,0,0,0]`、Δ行 全 0，且八格之「甲·高」与**首跑之乙·高逐位相同**（1785.70／2902.88／1024.36／786.05；3379.22／5470.41／1952.06／1475.53）；`python tools/validate.py` → `OK：全部校验通过`、**exit 0** |

#### ★ 三行原字之存，逐字节核过（求法｜所期之果｜读法）

- **求法**：以会话暂存区之**改前之本**（`styles.before.css`，即 r53-3 交件时之 styles.css）取其 `:148`–`:150` 三行，与今注内三行逐字符比——判据取 `注内之行 == " *     " + 改前之行`。
- **所期之果**：三条俱 `True`（即注内除注之续行前缀外**一字未增损**，连 `rgba(46, 42, 36, 0.06)` 之空格与原有之二格缩进俱存）。
- **读法**：有一条 `False` 即是「引文之内有增损」，当场停下改回——此律（摘引不得增损一字）系本轮体例一所立，于 CSS 注内同守。
- **实读**：`[True, True, True]`。改前之三行 `repr`：`'  background: rgba(46, 42, 36, 0.06);'`／`'  border-radius: 2px;'`／`'  padding: 0 0.2em;'`；注内之三行 `repr`：`' *       background: rgba(46, 42, 36, 0.06);'`／`' *       border-radius: 2px;'`／`' *       padding: 0 0.2em;'`。

#### ★ 二组之数，各书其基准（★ 领队所命之分列；基准不书，其数即无谓词）

| 组 | 所量者（谓词） | 基准 | 增／删 |
|---|---|---|---|
| **第一组** | **案甲之三行已去** | 改前之本（r53-3 交件时之 `styles.css`）→ 收尾前 | **0 增／3 删**（即「恰三行去矣」） |
| **第二组** | **收尾：标、空壳、长注** | 收尾前 → 现本 | **22 增／10 删**（10 删＝旧「候裁之块」注 7 行 ＋ 标 1 ＋ 选择符 1 ＋ 右花括号 1；22 增＝已裁之注 22 行） |
| **二组相加** | **本件对 `styles.css` 之全量** | 改前之本 → 现本 | **22 增／13 删** |
| （旁列） | 同上，另一基准 | `HEAD`（`ce708df`）→ 现本 | **53 增／0 删**（44 − 3 − 10 + 22 = 53；其删恒 0 之由见十之五偏差一） |

★ **二数不相混**：「恰三行去矣」是第一组之数（0/3），「本件共改几行」是二组相加之数（22/13）；★ 二者各有其谓词与基准，正是本轮「凡计数随书其谓词」在一处小事上的用法。

#### 界（收尾之后，复核一过）

- `site/app.js` **`196 增／27 删`——与本件起手时逐位相同，仍 0 改**；`tools/qa/vision_r53.js` **未改一字**（只跑，二次）。
- `docs/design/design_notes.md` 对 `HEAD` **189 增／2 删**——★ 其 **2 删仍只是 `v2.13` 两行版本头**（r53-3 升 v2.14 所替），**本件与收尾未抹平一字**（`git diff -U0 … | grep '^-'` 实读为那两行）。
- 他件之面一字未碰：`docs/conventions.md`、`tools/qa/prod_data_invariants.js`、`data/incoming/r53_kongzi_role/`、`docs/delivery_skipper_r53.md`、`docs/delivery_skipper_r52.md`。
- 未用 `checkout／switch／stash／reset`；本件未新增一件未跟踪之物。★ **工作区今为 14 项**（起手时 13）——多出之 `M docs/conventions.md` 系 **Skipper r53-7 并行之作**，非本件所动（本件一字未碰）；此一笔照实记之，免他日误计于本件（暂存区另留 `styles.before.css`／`styles.mid.css`／`pre.txt`／`post.txt`／`post2.txt` 五本，**不落仓库**）。
- **`HEAD` 仍 `ce708df`，与远端同哈希。未提交、未推送。**
