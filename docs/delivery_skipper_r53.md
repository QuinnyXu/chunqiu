# 交付说明 · Skipper r53（r53-2：`docs/delivery_skipper_r52.md` 追十二之就地加注；**不提交、不推送**）

**件**：`team/round53_prompts.md` `## Task for Skipper · r53-2 — docs/delivery_skipper_r52.md 追十二之就地加注`
**所据**：`team/round52_prompts.md` **裁四十五④**（2026-09-23 Co站长）
**落笔之日**：2026-09-25（照裁十四取本机时区；落笔时本机时刻 **2026-09-25 08:19 -0400**）

---

## 〇、所读之节与其末次勘注之日（r53 新体例第 5 条）

| 所读之节 | 末次勘注之日 | 自核之法 |
|---|---|---|
| `team/round53_prompts.md` `## Task for Skipper · r53-2` | **2026-09-25**（本文件之立日；该节内**无署日之追加**——节内正则搜 `2026-\d\d-\d\d` **零命中**） | 落笔前后各读一过，节之 sha256 = `2f3645a02485c6168c1e7ed66c177edb41f01d5116da92f4233ab9cb2cf1bfc2`（UTF-8、节界取自 `## Task for Skipper · r53-2` 至 `## 二、承接登记`），**两读相同** |
| `team/round53_prompts.md` §一 **裁四十七** | **2026-09-25**（节题自署「2026-09-25 Co站长」） | 全节读毕，以知本件之来历 |
| `team/round52_prompts.md` §一之二十三 **裁四十五**（①②③④四款全文） | **2026-09-23**（节题自署；文件 mtime `2026-09-23 17:53:20 -0400`） | 全款读毕，未取派件块之复述 |
| `docs/conventions.md` §7 v1.29／v1.41 通例、§10.3 | v1.29 条 r31 立、v1.41 条 r47 立；§10.3 v1.44 新增、r51 立 | 读其原条以定加注之体例 |
| `docs/delivery_skipper_r52.md` 追十二、追二十四之二 | 追十二：本件加注**前**无勘注，**本件即其首次加注（2026-09-25）**；追二十四之二：**无勘注，本件亦不动之** | 逐字读原文，加注前后取 sha256 对校 |

★ **一事须报（非病，只记）**：`team/round53_prompts.md` 于我落笔期间由领队**另立 §三**（r53-1 之复跑列果），文件 mtime 自 `08:15` 变至 `08:19:27 -0400`。**我之节未受其动**——重读之 sha256 与初读逐字相同（上表）。合新体例第 1 条「另立节、不改他人之节」与第 4 条「已派之件只增不改」。

---

## 一、做了什么（一处，只此一处）

`docs/delivery_skipper_r52.md` **追十二**之末、**追十三**之前，插入一段以〔…〕括起之**就地加注**，五款：

| 款 | 所治 | 对任务书第 1 条之要点 |
|---|---|---|
| 其一 | 「移入本会话暂存区」之法**已被裁二十二推翻**；今日正法为 `_to_delete/` **一律置于 `team/` 下**（`CLAUDE.md` 红线四附则①、`docs/conventions.md` §10.3） | **要点一** |
| 其二 | 该枚锁其后由领队自暂存区取回、改置 `team/_to_delete_20260920_index_lock/`；并书上文末句「该文件仍在」所指之暂存区副本**今已不在** | **要点二** |
| 其三 | 指向**裁四十五②之细则**：零字节之工具自遗锁**径删可也**，惟须记**四事**（路径、字节数、时间戳、当时有无该工具之进程）；**四事缺一或非零字节者回附则①常法** | **要点三** |
| 其四 | 明书本加注**不改上文一字、不动追二十四之二**（其处置照细则本即合规，只是当日细则未立） | 第 2 条「不得为之」之自证 |
| 其五 | 本加注之界：只改此一处，不动 `data/`／`site/`／`tools/`／`DATA_LICENSE.md`／`README.md`，不重生成、不升版本号 | 第 2 条之自证 |

**体例**：照 `docs/conventions.md` §7 v1.29「判定史留原貌，不得抹平」／v1.41「事实错就地勘正加注」，**与 2026-09-21 `docs/conventions.md` ⚑H 止血条第十栏之加注同式**（〔署日＋署人＋所据＋「上文原字一字未改」之声明＋分款〕）。

---

## 二、怎么验（三条，可一键复跑；照新体例第 6 条，求法／所期之果／读法并书）

### 验一·**追十二原字一字未改**（本件之第一红线）

- **求法**：取 `## 追十二、一事上报` 起、至加注首行 `〔**2026-09-25 Skipper 就地加注` 止之全文，以 UTF-8 取 sha256。
- **所期**：`d7bbcc988043c19ac5fc82d15e253784752b24fdea0f1f34cb4a9df2545faee2`（**645 字节**）——与加注**前**同一节界之值**逐位相同**（实测，加注前后各取一次）。
- **读法**：哈希相同即证原字一字未增、未删、未改；**连强调记号亦未动**（记号在字节之内，哈希自然覆之）。

### 验二·**本件只增不删、只动一文件**

- **求法**：`git diff --numstat`
- **所期**：`17	0	docs/delivery_skipper_r52.md`（**17 增、0 删、一文件**）；`git status --porcelain` 只一行 ` M docs/delivery_skipper_r52.md`。
- **读法**：删数为 0 即证未改他人既有之字；文件数为 1 即证未越界。**任务书第 2 条所禁之五处**（`data/`、`site/`、`tools/`、`DATA_LICENSE.md`、`README.md`）另以 `git status --porcelain data/ site/ tools/ DATA_LICENSE.md README.md` 单核，**所期无输出**（实测：无输出）。

### 验三·**质量门与数据面未动**

- **求法**：`python tools/validate.py`
- **所期**：`OK：全部校验通过`，exit **0**。
- **读法**：本件不动数据，此跑非为证数据之变，而为证**数据一字未动而门仍绿**——若此跑不绿，即说明本件越界到了它不该碰的地方。（实测 exit 0，无告警。）

### 附·加注内两条**库内断言**之自验（随书求法，照第 6 条）

| 断言 | 求法 | 所期之果 | 读法 |
|---|---|---|---|
| 该枚锁今在 `team/` 下，非在暂存区 | `ls -la team/_to_delete_20260920_index_lock/` | `stale_index.lock_20260920_2001` **0 字节**、mtime `2026-09-20 20:01:53 -0400`；`README.md` **1297 字节** | 见此二者即证其物已改置。★ **此目录已由裁四十七判「可清」、候站长一语**（`team/round53_prompts.md` §三之三）——**清后此求法将无输出，非断言失效**，其账另存于加注其二之表所列六处 |
| 暂存区副本已随会话而去 | 本机 `…/AppData/Local/Temp/claude` 下 `find . -name "stale_index.lock*"` | **无输出** | 无输出即证裁二十二之由（「会话暂存区随会话去而失」）**是实证非推论**。2026-09-25 08:19 -0400 实跑，无输出 |

### 附·加注内**四处引文**之逐字核

加注内凡引他文者四处，俱经程序核为**其源之精确子串**（非凭记忆、非凭复述）：

| 引文 | 其源 | 核法与果 |
|---|---|---|
| 「移入本会话暂存区」 | `docs/delivery_skipper_r52.md` 追十二原文 | 子串命中 |
| 「领队若需复核其来历，该文件仍在」 | 同上 | 子串命中（全文内计 2 次＝原文 1＋加注 1） |
| 「会话暂存区随会话去而失，账留在那里即等于没留」 | `team/round52_prompts.md` 裁二十二 | 源内命中 4 次，加注内 1 次 |
| 「办得对，其理由尤对」 | `team/round52_prompts.md` 裁二十二 | 源内命中 2 次，加注内 1 次 |

★ 照 r52 自校所立之律：**引文之内不得有一字之增损，连强调记号也是增损**——故四处引文一律**不加粗、不加记号**，其外之强调皆在引号之外。

---

## 三、本件所增改之文件全列（并行面复核用）

| 文件 | 改动 | 备注 |
|---|---|---|
| `docs/delivery_skipper_r52.md` | **17 增 0 删**（追十二之后、追十三之前插入就地加注） | 原字一字未改（验一） |
| `docs/delivery_skipper_r53.md` | **新增**（本文件） | 任务书第 3 条「本轮交付文档记其事」 |

**未动者**（逐一实核，无输出即未动）：`data/`、`site/`、`tools/`、`DATA_LICENSE.md`、`README.md`、`docs/conventions.md`、`docs/design/design_notes.md`、`team/` 下一切文件。
★ **`team/` 下四个 `_to_delete_*` 一个未动、一个未删**——r53-1 系领队之件，与本件交件面不相交。

---

## 四、停在提交之前（任务书第 3 条）

- **不提交、不推送。** 工作区留此二文件之改动，交 **Co站长复看**。
- 本件起手时 HEAD = `ce708df`，与 `git ls-remote origin main` **同哈希**，工作区**干净**（自核一过，不凭领队之注）；本件所改者**只有上表二物**。
- 任务书第 3 条末句「若本轮另有须提交之件，可并入其提交，不必独成一提交」——**本轮我未受他件**，故**未提交，不自行择其并法**，候 Co站长与领队定。

---

## 五、已知限度（如实）

1. **加注只治其法，不治其事。** 追十二当日之处置（移入暂存区）**已然发生**，本加注不能使其复原，只能使**后来者不再照此行事**。其账今分存三处：追十二原文（当日之法）、本加注（其法已废、其物今在何处）、`team/_to_delete_20260920_index_lock/README.md`（来源标记本身）。**第三处候站长一语即清**，清后前二处即是全部之账。
2. **裁四十五之细则「呈站长」，尚未立为常条。** 加注其三已照裁文原样书明其为**裁四十五②所立、呈站长**，未书作已定之常条；若他日站长另裁，此加注须再加注，不得径改。
3. **加注未及第二枚锁之四事复述。** 其四事本已全记于追二十四之二，本加注只指其处、不复述——**复述即是第二份账，两份账即有分歧之机**。
4. **本件未验「执行者是否真会读到」。** 追十二在公开仓内、裁二十二在 `team/` 内——此一分野系裁四十五④之判据，我照其判而加注，**未另行验证其读法之实**（如新执行者是否果真先读交付文档）。此非本件之域，只记。

---

## 六、上报事项（不代裁）

1. **`docs/conventions.md` §10.3 之一处勘，本件未办。** `team/round53_prompts.md` §一裁四十七末已登记：§10.3 书 `HANDOVER_r50.root_copy.md` 系「逐字节相同之未跟踪副本」，**今日已不实**（1608 字 vs 15834 字），措辞须改为「其文全数仍存于现行本内」；裁四十七明书「**俟他日动 conventions 时顺带加注，不为此另开件**」。**本件不动 `conventions`，故未办**——只在此登记，免其失落。
2. **追十二之标题仍作「已移置留痕」。** 其字在标题内，属上文原字，**本件照令未改**；读者今须读至加注方知其「移置」之落点已非当日所书之处。★ 若 Co站长以为标题亦须加注（如于标题后缀一「（见下 2026-09-25 加注）」），**此系改上文原字，须另令**，我不自裁。
3. **交接备注**：本轮 r53 之正件尚未定（`team/round53_prompts.md` §二末「正件俟站长定」）。本件落地后，`docs/delivery_skipper_r52.md` 之末次勘注之日为 **2026-09-25**；下一执行者若再动该文件，**须自此加注之后另起，不得并入其内**。

---
---

# 追记 · Skipper r53-5（四本 QA 归并为 `tools/qa/prod_data_invariants.js`；**门不动**；不提交、不推送）

> ★ **本追记只增不改**：上文 r53-2 之文（追记前 **10895 字节**、sha256 `ab9260591089b99d094b4b2bbdaaf847085033bc2178da0f745ea088ba7d3691`）**一字未动**，本节自其末另起（照 r53 新体例第 4 条，及 r53-2 §六之交接备注「下一执行者须自此加注之后另起」）。

**件**：`team/round53_prompts.md` `## Task for Skipper · r53-5 — 四本 QA 归并为 prod_data_invariants.js（门不动）`
**所据**：`team/round53_prompts.md` §三 **裁四十九**；`team/round52_prompts.md` **裁三十一**
**落笔之日**：**2026-09-25**（照裁十四取本机时区；落笔时本机 **2026-09-25 21:54 -0400**）

---

## 〇、所读之节与其末次勘注之日（r53 新体例第 5 条）

| 所读之节 | 末次勘注之日 | 自核之法（★ 照纪律三，不凭派件之复述） |
|---|---|---|
| `team/round53_prompts.md` `## Task for Skipper · r53-5`（七条） | **2026-09-25**（节内**无署日之追加**——正则搜四位年月日**零命中**；其立日即本文件之立日） | 全节读毕；节界取自 `## Task for Skipper · r53-5` 至 `## 四、本轮之界`，**1810 字节**，sha256 `465f6507261cfbc905c71f148a27c842f953a9d96f60466ccf606968c1a6da0c` |
| 同上 §三 **裁四十八、裁四十九**（其所据） | **2026-09-25**（节题自署「2026-09-25 Co站长」；裁四十九内无另署之追加） | 裁四十九全文 **2100 字节**读毕，五款逐款照办 |
| 同上 §四 **本轮之界**（三件共） | **2026-09-25**（同署；节内无另署之追加） | sha256 `9fe864db4b50810c2346bb7a980f3c19a9a9da1241717cf27ce322f4f2692460`；其三款俱读 |
| 同上 §一 裁四十七、「领队之一·追一」 | **2026-09-25** | 读以知 `team/` 下四个旧 `_to_delete_*` 已清之实，免与本轮新建者混为一事 |
| `team/round52_prompts.md` **裁三十一** | **2026-09-21**（其所属大节 `## 一之六、…（2026-09-21 Co站长实核）`；裁三十一内 **938 字节**，无另署之追加。★ 该文件 mtime 为 `2026-09-23 17:53:20`，系他节之后改，非此节） | 全款读毕——其③「新立之门是唯一一本不随数据回改的，归并时不得反以旧本之式同化之」即本件之硬界 |
| `docs/conventions.md` §4（地图投影规范）、§7 v1.29 | §4 v1.8 立（2026-07-19 西扩第①批）；v1.29 条 r31 立 | §4 之公式用以**跑时回校**五鹿投影（代旧本写死之 x≈708／y≈198）；v1.29「判定史留原貌，不得抹平」用以处置四条已勘之断言 |

★ **一事须报（非病，只记）**：本件落笔期间 `team/round53_prompts.md` 之 mtime 为 `2026-09-25 21:30:35`（本机），早于我起手；**我之节前后两读 sha256 逐字相同**（上表）。合新体例第 1、4 条。

---

## 一、做了什么（三物）

| 物 | 状 | 数 |
|---|---|---|
| `tools/qa/prod_data_invariants.js` | **新增** | 778 行 |
| `tools/qa/r43_prod_check.js`／`r44_`／`r45_`／`r43_prod_render_check.js` | **移出**（入 `team/_to_delete_20260925_qa_merge/`，**不径删**） | 78／74／78／59 行，合 14750 字节 |
| `docs/delivery_skipper_r53.md` | **追记本节**（上文一字未动） | —— |

★ **`tools/qa/prod_render_invariants.js` 一字未动**（裁四十九①）：sha256 `8a31fdcb95eeb00d093def7b23f18b842e8397e3dc3b2ee54bdb48261f40a6dc`，与 `git show HEAD:tools/qa/prod_render_invariants.js` **逐位相同**；并实跑一过仍绿（`合计 48 项，FAIL 0`）。
★ **`tools/qa/vision_r53.js` 一概未碰**——其系 Vision r53-3 本轮之物。★ **照实记其时序**：我起手时 `tools/qa/` 下不见其名；**交件前之终态核对时其已落**（未跟踪，`git status` 作 `?? tools/qa/vision_r53.js`）。**其落在我落笔期间，非我所置、我未读未改一字**——凭据：本件所动之 `tools/qa/` 只四删一增（`git status` 之 ` D` 四行与 `?? prod_data_invariants.js` 一行），其文件不在我之改动之内。
★ `tools/qa/` 之 `.js` 由 **28 本降至 25 本**（−4＋1）。

---

## 二、逐条判其去留，并书其由（件 3 之答；**不得一并抹去**）

旧四本共 **72 条断言**（r43 21＋r44 24＋r45 21＋r43渲 6，移前逐本实跑逐条数得。★ 「r43渲 6」系其 6 个 `ok` 之数，分属该本自题之「断言一至四」四题——**题四而判六**，本节一律以判计），本件之处置**无一条被默然抹去**：

| 类 | 条数 | 处置 | 其由 |
|---|---|---|---|
| **（留）锁数据之内容者** | **68**（数据层 62 ＋ 渲染层 6） | 原样入合本 §二之一／§二之二，逐条署其所出之轮（`[r43]`／`[r44]`／`[r45]`／`[r43渲]`） | 那是真断言——某 id 某值、某串在否、某行有无。裁四十九③明书「宜留」 |
| **（并）锁数据之多少者** | **4** | 撤出断言、归 §一之机制（`sources`／`places`／`passages`／`events` 四表之行数，俱出 r43 之「附加：全库不变量」） | 正是裁四十九③所治之债：此四条每扩一表即须回改。**撤者非抹**——其所问「生产是否与本仓同数」正是 §一逐表所问，且由**四表扩至全表九表**，并加「生产 meta 之记」一重比对，覆盖比旧本**更宽** |
| **（并之二）投影之所期** | 上列 68 之内 1 条 | 旧写死 x≈708／y≈198，今改为**跑时自本仓 `places.json` 取 `L_WULU` 经纬、照 conventions §4 公式回校** | 所锁者本是「投影之法在位」，非那两个数。改点亦不必回改本文件 |
| **（合）同一判两处重出** | **−2** | 合为一条并记其所出之两轮 | `E084.summary` 含「简 34」（r43 断言三、断言四各书一次）；`Q073` 之「存在」（r43、r44 各书一次，今署 `[r43＋r44]`） |
| **（简）恒等之或运算** | 7 处 | 只留其实者 | 旧式以「带反引号之串」或「裸串」两项相或——前项是后项之子串，其或运算**恒等于后项**。**判之所及一字未减** |
| **（分）一条含两判以上者** | **＋11** | 拆为一判一条 | r43 三处：`B002 不在任何 source_ids`（`events`／`places` 各一条，逐表对位）、`Q073 存在且非空`（存在／非空各一条）、`Q448 锁「与（與）」且不锁「牙」`（has／hasNot 各一条）；r45 八处：`Q454`–`Q457`「存在且挂 E274」各拆二（＋4）、`Q454`–`Q456`「quote_type＝原文，source＝Z118」各拆二（＋3）、`Q457`「quote_type＝后出叙事，source＝S015」拆二（＋1）。拆之所以：**逐条反证须有单一之对照，合条则红处指不明** |
| **（勘）所锁之状已被后轮之裁推翻者** | **4** | 改其所期为今之实，**原所期照录于 `sup` 栏、跑时逐条打印** | 见下——**本件唯一改动「所期之值」之处，已列上报** |

**合**：68 − 2（合）＋ 11（分）＝ **77 判**，其中数据层 **71**（§二之一：`[r43]` 18＋`[r43＋r44]` 1＋`[r44]` 23＋`[r45]` 25＋`[r45→r50]` 4）、渲染层 **6**（§二之二）；§二之二另加「三页零 pageerror」1 判，故该节共 7 判。

### ★ 二之一、四条已勘之断言（**本件之首要上报，不代裁**）

**实测在先，处置在后**：旧 `r45_prod_check.js` 今日对生产实跑 **exit 1、4 条红**——

| 旧所期 | 今之实 | 其被推翻之由 |
|---|---|---|
| `L_JIAGU.lat` 为空 | `36.1` | **r50 之自限解除**（2026-09-19 站长纸本核杨伯峻《春秋左传注》定公十年注**页 1576**、谭图第一册·春秋·**齐鲁幅页 26–27**），坐标由留空改填 |
| `L_JIAGU.lng` 为空 | `117.8` | 同上；其偏移量系本库自拟（`coord_basis` 已自限） |
| `L_JIAGU.coord_certainty` 为空 | `low` | r50：「留空与 `low` 之别在有没有取点，今既取点，自当填值」 |
| `L_JIAGU.modern_location` 含「未定」 | 「今山东莱芜（今济南市莱芜区）东南一带（夹谷峪）」 | 莱芜说落定，「未定」二字已不在其内 |

**我之处置**：四条**不删、不留其红**——改其所期为 r50 落地之实，并于每条之 `sup` 栏照录 r45 当日之原所期与其被推翻之由，**跑时逐条打印**（照 `docs/conventions.md` §7 v1.29「判定史留原貌，不得抹平」：依据链要留得住它拐过的弯）。`certainty=low` 一条**原样留不动**（r50 裁十明书「维持 low，不升」）。
★ **请领队与站长复看此一处之分寸**：改「所期之值」非我常权。若以为当留其红、或当径删该四条、或当另开一件走勘正之式，一语即改。**我未改一个字之数据，只改了断言之所期。**

---

## 三、所期之数不再写死（件 2 之答）

**法**：`versionDiff(expected, prodMeta, prodRowCount)` 一函数，**其内一处数字字面量也没有**，所期全数由入参带入：
- `expected` ＝ 本仓 `site/data/meta.json` 之 `tables`（跑时实读）；
- `prodRowCount` ＝ 生产各表 `*.json` 之**实际行数**（跑时逐表实取、当场数其行）；
- `prodMeta` ＝ 生产 `meta.json` 之 `tables`（第二重比对：生产之**记**与生产之**实**亦须相符）。

**其所答之问**：「**本次所推之本，与生产所服之本，是否同一本**」。**数据扩表时本文件一个字也不必回改。**

★ **与门之界不冲突**（裁四十九③末句）：门锁「不随数据变者」，本本锁「本次所推与生产所服是否同一本」——**二者所答之问不同**，故并立而不相并。

★ **一处只报不判，其由明书**：生产与本仓之 `generated_at` **不入断言**，只打印其同异。其戳**重生成即变**（同一批数据重生成亦换戳），与「数据之同异」不是一事；入断言则每逢本地重生成而未推即假红。今日实测二者**相同**（俱 `2026-09-21T01:09:11+00:00`）。★ 此照 r52 裁二十九③「降为只报不红」之先例。

★ **另立一条「所期之料自洽」之判**：`meta.tables` 之每一表皆须有其 `site/data/<表>.json`，否则**抛错中止**——所期之料自身漏了一张表，其时之绿是空的。今日 **9 表俱在**。

---

## 四、固定等待之治（件 4 之答；裁三十一）

旧 `r43_prod_render_check.js :45` 之 `waitForTimeout(800)`，其前只有 `networkidle` 之 `goto`、无数据就绪条件。今照门之就绪闸之式立**两闸**：
1. **数据闸**（三页共用，与门同一形）：候 `DATA.event_people` 可迭代且**非空**（以「迭代一步即止」验之）、`PROTAGONISTS` 是数组且非空；
2. **地图闸**（代那 800ms）：候 `#map-canvas svg` 已挂、`#layer-anchors` 内至少一枚 `.anchor`。

★ 二闸**只判形与非空，不判量**——不与任何数字相比、不写任何行 id，故数据扩表、缩表、改行俱不须回改本闸一个字。
★ **超时不默然放行**：抛错中止（退出码 2），并把当场探得之状况一并报出。
★ 实测候时：`P_JINWEN/timeline` **388ms**、`P_QINMU/timeline` **15ms**、`P_JINWEN/map` **45ms** ＋ 地图闸即过；`READY_MS` 取 **30000**（同门，照裁三十「宁长勿短」），余量约七十倍。

---

## 五、按类反证（件 5 之答；**测不出红即抛错停门**）

合本之反证**三面**，其料一律跑时取、跑时拼：

| 面 | 法 | 所期 | 实测 |
|---|---|---|---|
| **§一 · 逐表扰动**（三形） | 逐表：①所期 +1；②生产实数 −1；③撤其 `meta` 之键。**施于同一个** `versionDiff()`，非另一套码 | 每形每表**各自见红**，一表不红即中止 | **9 表 × 3 形俱当场红** |
| **§一 · 真旧本**（领队所举之例） | 自 git 历史回溯 40 版 `site/data/meta.json`，取**第一个与今本 `tables` 相异者**——跑时求得，**不写死其哈希** | 其相异之表逐表红，其**相同之表一处不误红** | 得 **`0a57f8a`**（r50 合入本：`event_people` 673／`passages` 506／`people` 166／`relations` 289）→ **4 表逐表红（8 处）、5 表零误红** |
| **§二 · 逐条对位** | 每条断言各派生一个**已知当红**之对照（去其所含之串／植其所禁之串／缀其值／倍其数／空其栏／撤其行／补其行／去其一枚字符），对照之法**自其 `op` 派生，非手写** | **71 条逐条各有一个当场红之对照**，一条不红即中止 | **71 条俱红** |

★ 照 **r52 裁二十四**「不得以集合之性质代其成员之性质」：§二不取「逐类一枚样本」而取**逐条**；§一不取「整批见红即算过」而取**逐表**。

### 五之二、四型注入之实测（证「反证之绿不是空的」）

反证自身亦须能失效——故以**四型同型破坏**注入副本（副本置会话暂存区，**本体一字未动**）：

| 注入 | 所期 | 实测 |
|---|---|---|
| 令 `versionDiff` 对行数之异视而不见 | §一之反证当场失效、**中止**（退出码 2） | **exit 2**，报「反证失效（§一 · 截其实数）」并列出 9 表全名 |
| 令 `has` 之判恒真 | §二之逐条反证当场失效、中止 | **exit 2**，报「反证失效（§二 · 逐条）」 |
| 令 `absentRow` 之判恒真 | 同上 | **exit 2**，同报 |
| 植入一行写死之表行数（死码不跑） | §六之自证**甲判见红**（退出码 1） | **exit 1**，✗ 甲判并指出其行号 447 |

★ 前三型证「反证咬得住」，第四型证「自证不是自述」。**此即 r52 所立之式，本件照之。**

---

## 六、自证：本本读自己的源码（件 2 之机器凭据）

两条机器判据（非自述，可复核之扫描；**表名集与行数一律跑时自本仓 `meta.json` 求得，本文件内不枚举一个表名**）：
- **甲** · 源码内不得有「表名与数字字面量相比」之形 → **零处**；
- **乙** · 源码内不得有「`.length` 与非零数字面量相比」之形 → **零处**（与零相比者在禁外，那是「零不符」之判）。

并**逐条打印源码内全部「与数字字面量之比较」**（今 **24 处**）供人眼复核——不只报「过了」。

★ **二判之网有二形之漏，漏写在明处**（同门之自述）：其一，行数先落于变量而后比较；其二，累加器之属其名既不带表名亦不带 `.length`。二形恃上述逐条打印兜之。
★ **且须辨一层**：本本之所期之料一律取自 `meta.json`（`versionDiff` 之入参），**并无一处「所期之数」落在码内**，故此二漏于今日之形势下无处落脚——**此是形势之实，不是网之密**，二者不可混。

---

## 七、怎么验（可一键复跑；照体例第 6 条，求法／所期之果／读法并书）

| # | 求法 | 所期之果 | 读法 |
|---|---|---|---|
| 一 | `node tools/qa/prod_data_invariants.js` | `=== 91 过 / 0 红（共 91 判）===`，**exit 0** | 全本（数据层 85 ＋ 渲染层 6）。**三跑俱同**（exit 0／0／0，91 判／91 判／91 判）——就绪闸代固定等待之后，无 r52 所遇之「同机同日五跑一败」 |
| 二 | `QA_SKIP_RENDER=1 node tools/qa/prod_data_invariants.js` | `=== 85 过 / 0 红 ===`，**exit 0** | 只跑数据层。★ 跳过之声明会印在报里（「其四条之绿本轮不成立——跳过是声明，不是绿」），**不冒充绿** |
| 三 | `QA_BASE_URL=local node tools/qa/prod_data_invariants.js` | **exit 0**，且当场印「以本仓比本仓，自明而无力」 | 本地源端模式下 §一「同一本」之问自明；本本自己声明其无力，**不得以其绿充「生产已核」** |
| 四 | `sha256sum tools/qa/prod_render_invariants.js` 与 `git show HEAD:tools/qa/prod_render_invariants.js \| sha256sum` | 二者**逐位相同**＝`8a31fdcb95eeb00d093def7b23f18b842e8397e3dc3b2ee54bdb48261f40a6dc` | 哈希相同即证门一字未动（裁四十九①），**连一处注亦未增** |
| 五 | `node tools/qa/prod_render_invariants.js` | `=== 合计 48 项，FAIL 0 ===` | 门复跑仍绿，证其未因本件而受动。★ 门跑之源端是**生产**，故不受 Vision r53-3 在制品之影响 |
| 六 | `python tools/validate.py` | `OK：全部校验通过`，exit **0** | 本件不动数据，此跑非为证数据之变，而为证**数据一字未动而门仍绿**——若不绿即说明本件越了界 |
| 七 | 旧四本移前移后 `sha256sum` | 逐本首 16 位：`b9c47a1e5d4cb565`（r43）／`2ca001267dd62f2f`（r44）／`4dd874a09da33cdd`（r45）／`345c7b5862dc9632`（r43渲），**移前移后相同** | 哈希相同即证移而未损一字节 |
| 八 | `git --no-optional-locks status --porcelain data/ docs/conventions.md DATA_LICENSE.md README.md` | **无输出** | 本件不动数据面、不动 conventions、不动许可三口 |
| 九 | `ls team/ \| grep _to_delete` | 只 `_to_delete_20260925_qa_merge` 一目 | 本轮之物；旧四目已于 2026-09-25 由领队清毕（`team/round53_prompts.md` 「领队之一·追一」），**不混为一事** |

### 附 · 本节所书库内断言之求法（体例第 6 条）

| 断言 | 求法 | 所期 | 读法 |
|---|---|---|---|
| 旧四本共 80 条断言，r45 今日红 4 | 移前逐本实跑 `node tools/qa/r4X_prod_check.js`，数其 PASS／FAIL 行与退出码 | r43 `21 PASS / 0 FAIL` exit 0；r44 `24/24` exit 0；**r45 `17/21`、4 FAIL、exit 1**；r43渲 `6 PASS / 0 FAIL` exit 0 | ★ r45 之红**非生产之病**，系其所锁之状已被 r50 推翻而断言未随之勘。**先实测，后处置** |
| 合本 §二之一 71 条、按轮之分布 | 读 `SPECS` 之 `r` 栏逐值计数 | `r43` 18／`r43＋r44` 1／`r44` 23／`r45` 25／`r45→r50` 4，合 71 | 与上表「留／并／合／简／分／勘」之加减相符：r43 17−1合＋3分＝19、r44 24−1合＝23、r45 21＋8分＝29 |
| 合本 778 行；`tools/qa/` 之 `.js` 28→25 | `wc -l tools/qa/prod_data_invariants.js`；`ls tools/qa/*.js` 计其数 | 778；25（移前 28） | 四本合一之后本数净减三 |
| 真旧本之取得非写死 | 读 `oldMetaFromGit()`：`git rev-list -n 40 HEAD -- site/data/meta.json` 逐版比其 `tables` | 今日得 `0a57f8a`（r50 合入本）；若近 40 版无一相异，则**当场声明「未得真旧本，不冒充已验」** | 其哈希不写死，故他日库再扩表，本条自己会找到新的对照 |

---

## 八、顺带修正一处（照「顺带修正三步范式」：修正＋声明＋可回退）

**其病**：旧四本取 JSON 时**未设编码**，Buffer 逐块 `toString()` 拼接——一个多字节字符若恰跨两块即坏一字。本库之值多长（`Q073.modern_note` 实测 2041 字），其病不常现（块大而字小），**现则成假红**；而假红最难查，因其只在某次某块界上现。
**其治**：`fetchJson()` 内加一行 `res.setEncoding("utf8")`，由 `StringDecoder` 接管块界，此形之坏字不复可能。
**可回退**：删那一行即复旧；其注已就地书于函数之上。
★ **声明**：此非本件所受之命，系合并时读旧码所见。**只治合本，未回改旧四本**（旧四本已移出，回改其将废之物无益）。

---

## 九、已知限度（如实）

1. **地图闸之分寸有代价。** 地图闸候「锚层非空」——若他日生产地图一枚点也不落，本本**中止（exit 2）而非报红（exit 1）**。此是照门之式所取之分寸（闸判形与非空、不判量），**其代价须明记**：exit 2 读作「本本自身出错」，而那一刻其实是站点之病。他日若以为不妥，改在地图闸，不在别处。
2. **渲染层恃 `playwright`。** 取不到即 **exit 2**，**不默然跳过**；欲只跑数据层须明示 `QA_SKIP_RENDER=1`，其跳过会印在报里。
3. **网络瞬断即 exit 2。** 今日首跑一次 `getaddrinfo ENOTFOUND chunqiu.timechorus.com`，复跑即通（其后五跑俱通）。本本**无重试**，沿旧四本之式。★ 加重试之利弊请裁：重试可去此噪，然亦会掩真故障。
4. **渲染层 6 条无逐条反证。** §二之一之 71 条俱有；渲染层 6 条所锁者是**同一批数据事实**「亦可见于屏上」，其数据面已在 §二之一逐条反证之内，而「可见于屏上」这一层之反证须在生产页上植入对照，**本件未办。不假装办了。**
5. **`regress20.js` 本件未跑，其由须明书。** 其所验者是 `site/`，而 `site/app.js`／`styles.css` 现有 **Vision r53-3 之在制品**（实测 196 增 27 删／44 增 0 删）——跑之所得非我之件所致，**故不跑、亦不以其果充数**。门跑之源端是生产，不在此限。
6. **本本之绿只及「同数」与「其内容在位」两事。** §一逐表同数，**不能**证「表内每一行都同」（那须逐行哈希，本件未办，亦非裁四十九所命）。**此界须明**——「同数」不等于「同本」之全部。

---

## 十、上报事项（不代裁）

1. ★ **四条已勘之断言，请复看其分寸**（详见 §二之一）——改「所期之值」非我常权。若以为当留其红、或当径删、或当另开一件走勘正之式，一语即改。**我未改一个字之数据。**
2. **归并之旧账（裁三十一②）本件结**：其账自本轮由「四本 ＋ 新立之门」变为「**合本一 ＋ 门一**」，而门**一字未动**（裁四十九①守住）。`docs/conventions.md` **§9.3**（QA 工具之节）今仍只书旧本之属，**是否须升号以记合本之立与旧四本之退**——本件之界只及 `tools/qa/`，**故未动 conventions，只在此登记，免其失落**（同 r53-2 §六之一 §10.3 一勘之处置）。
3. **`team/_to_delete_20260925_qa_merge/` 候站长一语方删。** 其内四本与 `README.md`（来源标记三事：会话、日期、目的）俱在，并附逐本字节与 sha256 之清单。★ **此移与前数例不同型，须记明**：四本系**已跟踪**之文件，其全文仍在 `HEAD`（`ce708df`）之内，`git show ce708df:tools/qa/r43_prod_check.js` 之属可逐字取回——**故此移可逆**，不可以待「未跟踪之孤本」之分寸待之。
4. **本轮工作区现有三人之物**（`git --no-optional-locks status --porcelain` 实读）：我之 `tools/qa/` 四删一增 ＋ `docs/delivery_skipper_r53.md`；Vision r53-3 之 `site/app.js`／`site/styles.css` ＋ `tools/qa/vision_r53.js`（后者于我落笔期间落地，**我未碰**，详见 §一之注）；Sophia r53-4 之 `data/incoming/r53_kongzi_role/`；并 r53-2 之 `docs/delivery_skipper_r52.md`。★ **我未 `checkout`／`switch`／`stash`／`reset` 一次**，未碰他二人之文件一字。
5. **交接备注**：`HEAD` 起手至交件仍 `ce708df`，与 `git ls-remote origin main` **同哈希**。本文件末次勘注之日今为 **2026-09-25**；下一执行者若再动本文件，**须自本追记之后另起，不得并入其内**。

---

## 十一、停在提交之前（件 7）

- **不提交、不推送。** 工作区留上列改动，候领队复核、Co站长复看、**站长一语**（`team/round53_prompts.md` §四本轮之界第 3 款）。
- 本件所动者**只三物**（§一之表）：`tools/qa/` 之四删一增，＋本文件之追记。`data/`、`docs/conventions.md`、`DATA_LICENSE.md`、`README.md` **逐一实核无输出**；`site/` 之改动**全属 Vision**，非我所动。
- ★ 合入之序（§四第 2 款）系 Sophia 之备料俟 Vision 之渲染层落定——**与本件不相涉**，本件不含任何数据合入。

---

# 追记 · Skipper r53-7（conventions 四条落笔 ＋ 门头一句；裁五十一、五十二、五十六、五十七、五十八、六十一；**不提交、不推送**）

> ★ **本追记只增不改**：上文 r53-2 与 r53-5 两节之文（追记前**共 34682 字节**、sha256 `e7a853af76fb59142029f9ab3fd5e455b3c7d059a4aefb8fbf18b4976c94cb0e`）**一字未动**，本节自其末另起（照 r53 新体例第 4 条，及 r53-5 §十之 5「下一执行者须自本追记之后另起」）。

**件**：`team/round53_prompts.md` `## Task for Skipper · r53-7 — conventions 四条落笔 ＋ 门头一句`
**所据**：同文件 §六 **裁五十一、五十二、五十六、五十七、五十八、六十一**（2026-09-26 Co站长）
**落笔之日**：**2026-09-26**（照裁十四取本机时区；落笔时本机 **2026-09-26 09:51 -0400**）

---

## 〇、所读之节与其末次勘注之日（r53 体例第 5 条）

| 所读之节 | 末次勘注之日 | 自核之法（★ 照纪律三，不凭派件之复述） |
|---|---|---|
| `## Task for Skipper · r53-7`（五节俱读） | **2026-09-26** | ★ **节内四位年月日之串零命中**（正则 `20\d{2}-\d{2}-\d{2}` 全节搜），故无署日之追加，**其立日即 §六 之日**；节界取 `## Task for Skipper · r53-7` 至 `## Task for Sophia · r53-8`，**2879 字节**，sha256 `283797b4313a9ce9e83914285074465190fd1961f72d9f334fe5d6ea4090917b`（起手与交件两读逐字相同） |
| 同文件 §六（裁五十至六十二全节） | **2026-09-26**（节题自署「2026-09-26 Co站长」；节内除此一处外无另署之日） | 节界取 `## 六、` 至 `## Task for Vision · r53-6`，**9825 字节**，sha256 `6947555cc28cc3c90960f4476971886187cad22b7903f69ce110b74c3f978346` |
| 同 §六 **裁五十一** | **2026-09-26**（其内零日期串，从节题之日） | **801 字节**，sha256 `ef54263e7d5a5093db31f6eb2eceb46bfa3ce35f5354ed8da92fe1c7966ab725` |
| 同 §六 **裁五十二** | **2026-09-26**（同上） | **811 字节**，sha256 `f8c4e09242b25e99428c3fc30fa298e0e85a667ade394023b8813bc5b77683ad` |
| 同 §六 **裁五十六** | **2026-09-26**（同上） | **209 字节**，sha256 `6674f65724ff4bc3c346a95f4161c24f5f0054bf8addd29730e0997100de352a` |
| 同 §六 **裁五十七** | **2026-09-26**（同上） | **457 字节**，sha256 `a077657f4b1b3bfd4883be0c9055b239afa495ae395273a1ca9750a782a176ca` |
| 同 §六 **裁五十八** | **2026-09-26**（同上） | **323 字节**，sha256 `123f375f07ea53022ce7f81d0cbc104caf0917d8ce688275f4e017e8178a67a5` |
| 同 §六 **裁六十一** | **2026-09-26**（同上） | **1782 字节**，sha256 `ec1db098f83f3f5ec704942d7b478acc6366b150860be6c4b0c78de4929f23b1` |
| **旁读（不在本件之面，为核其所系之实而读）** | —— | `team/round53_prompts.md` `## Task for Vision · r53-3`、`领队之五`（r53-3 复核）、`## Task for Sophia · r53-8`；`docs/delivery_vision_r53.md` §三之一／三之二／三之三／§六偏差二；`docs/conventions.md` §7 ⚑H 全条与 §9.3；`site/app.js` `mdInlineMarks()`／`mdInlineFrag()` 之实装与八处落点 |

★ **一事须报（非病，只记）**：`team/round53_prompts.md` 之 mtime 为 **2026-09-26 09:22:20**（本机），早于我起手（约 09:30）；我之节前后两读 sha256 逐字相同（上表）。合体例第 1、4 条。
★ **另一事**：领队派件之聊天块内有一段自署「2026-09-26 09:28 -0400 实读」之加注（八条），而**任务书 r53-7 节内并无该段之文**（上表零日期串即其证）。照纪律四与体例第 2 条，**我以任务书现行文本为准**，并把该加注八条一并当作领队之中途校正照办（其八条与任务书五节无一处相冲，逐条落实见下）。**此只记不判，供领队照「先入文、后发件」之律自处。**

---

## 一、做了什么（二物，六事）

| # | 物 | 事 | 据 |
|---|---|---|---|
| 1 | `docs/conventions.md` | **版本升号 v1.45 → v1.46**（旧 v1.45 行**原字照留**，只易其首二字为「历史」） | 件一之首 |
| 2 | 同上 §7 ⚑H 通例 | **二数并书之加注**：谓词甲 3045 对／406 行次、谓词乙 3044 对／406 行次，并钉死其差之唯一一处 | 裁五十一 |
| 3 | 同上 §7 ⚑H ①②③⑤款 | **`people.notes` 由②款移入①款**（①成三栏 1629／224；②余六栏 1413／181），并就地记明「路已治而今无一行实渲」之实；③款记其轮不记其日（不预填）；⑤款口径再增二事 | 裁五十二、裁五十一 |
| 4 | 同上 §7 | **新立体例一·摘引之标点** —— 其字不得增损、标点从摘引之语境；首例 `E290`→`Q508`／`E297`→`Q500`，判为体例非误、库行不改；并配量器（去标点后比） | 裁六十一 |
| 5 | 同上 §7「交付体例·实测口径」 | **新立常条二** —— ①「凡入条文之库内计数，一律随书其谓词与核法」；②「凡取其前 N 而报者须书明『此系前 N，非全量』；凡入条文之数一律取全量」 | 裁五十一、裁五十八 |
| 6 | 同上 §9.3 | **就地加注** —— `tools/qa/` 之实况四类并列（合本／门／逐轮走查门／截图出图），并记红线 6 不因此松、旧四本之退可逆、合本无重试 | 裁五十六（并裁六十、裁五十七） |
| 7 | `tools/qa/prod_data_invariants.js` | **门头新立一段【本门无重试】** —— 明书其无重试与其由；**不加重试** | 裁五十七 |

**改动之量**：`docs/conventions.md` **+29 行／−1 行**（`git diff --numstat`），所删之一行即版本行；`tools/qa/prod_data_invariants.js` **只增一段注释**（`node --check` 通过，一行码未动）。

★ **落笔之式（本件之骨）**：五事**俱是加注与并书，不是改写**。为使「旧文一字不删」有机械之证，**五处加注一律另起一行，不在原行内追加**——若在行内追加，`git diff` 必以「删一行、增一行」示之，自验①之闸即读不出「有无抹平」。**落位之改不改其文一字。**

---

## 二、自验（件四之三条，逐条书其求法／所期之果／读法）

### 自验① · **非版本行之删即是抹平**（本件最要紧之闸）

- **求法**：`git diff -U0 docs/conventions.md | grep '^-' | grep -v '^--- ' | wc -l`
- **所期之果**：**0 或仅版本行**
- **实测**：**1**，其文即 `-版本：v1.45（2026-09-20：r52 合入——…`
- **读法**：非版本行之删为零，**无一处抹平**。
- ★ **更进一层之证（本件自加，机械可跑）**：取 `git show ce708df:docs/conventions.md` 之**每一旧行**，逐行问其是否仍见于新文——**未见者恰 1 行，即版本行**；且该行之内容**逐字照留**于新之「历史：v1.45（…」行（比其第 4 字起全等，只易首二字「版本：」→「历史：」，实测 `True`）。旧 433 行 → 新 461 行。
  **读法**：此法较自验①更严——自验①只数删行，本法证**旧文之每一字都还在**。

### 自验② · 三处新数与自跑之实测相符

- **求法**（标准库，`data/csv/*.csv` 逐表逐栏逐 cell）：**谓词甲**取正则 `/\*+/g` 中长度恰为 2 之游程，每 cell 内其数为奇者舍其末，成对计；**谓词乙**先求该 cell 内反引号之配对下标（奇者亦舍其末），凡星号游程之**起始下标严落于某一对反引号之间**者不入之，余者同甲成对计——★ 此二谓词系**照 `site/app.js` `mdBoldMarks()` 与 `mdInlineMarks()` 之实装逐句复现**，非另拟一法；**行次**取「含至少一对之 (表, 栏, 行) cell 数」。
- **所期之果**：甲 **3045 对／406 行次**；乙 **3044 对／406 行次**；差恰一处。
- **实测**：甲 **3045／406**；乙 **3044／406**；差 **1 处**，落 `people.notes` 之 `P_JILIANG`（`data/csv/people.csv` 第 **166** 行），甲 18 对 → 乙 17 对；其文实含 `` `is_protagonist=**0**` ``（该行反引号 31 对）。**全库他处无第二例。**
- ★ **谓词之外更自求其算法**（领队加注 4 所命，**我自跑自判，未取其代核**）：**上二数系配对数**；同一扫描之**长度恰为 2 之星号游程数为 6090**，`6090 == 3045 * 2` 实测 `True`——**游程数恰为配对数之二倍**。故本件入条文者一律书「**对**」并明书其为配对数。**领队所戒之滑处，我之求法从起手即取配对数，未经游程数一步。**
- ★ **一处领队未点而我自见之实**：**处数减一而行次不减**（406 → 406）——其差所在之行于乙谓词下仍余 17 对，其 cell 仍计一行次。**此正是「同一口径之数，算法不同即不是同一个数」之现成实例**，已并书入条文。
- **读法**：若差不止一处、或落他栏，则 `data/csv/` 已变或渲染器之判据已变，**停下上报，不得径改条文之数**。

### 自验③ · 质量门

- **求法**：`python tools/validate.py`｜**所期**：exit 0｜**实测**：`OK：全部校验通过`，**exit 0**，无告警。
- **附**：`node --check tools/qa/prod_data_invariants.js` 通过（门头之改未伤其语法）。
- **`regress20.js` 本件未跑，其由同 r53-5 §九之 5**：其所验者是 `site/`，而 `site/app.js`／`styles.css` 现有 Vision r53-3／r53-6 之在制品，跑之所得非我之件所致。**本件一行 `site/` 未动。**

### 自验④ · 本件所书他条库内断言之自核（体例第 6 条）

| 断言 | 求法 | 所期 | 实测 |
|---|---|---|---|
| `people.notes` 含记法之 42 行**俱有 `short_bio`** | 谓词「`notes` 含成对 `**` 或成对 `` ` ``，**且** `short_bio` 为空」，施于 `git show <锚>:data/csv/people.csv` | 三锚俱 **0** | `bf4242c` 0（166 行／34 含记法）｜`ae3e800` 0（174／42）｜`ce708df` 0（174／42）。★ 并自求一反向之数：`ce708df` 全表 `short_bio` 为空者 **29 行**，而其中注文含记法者 **0**——**不是「没有空小传之人」，是「有记法之人都有小传」** |
| 该落点之行号与其分支 | 实读 `site/app.js` 与 `git show ce708df:site/app.js` | 落 `else if (ready)` 一支 | 现行 **`:842`** 作 `else if (ready) p.appendChild(mdInlineFrag(person.notes \|\| ""));`；`ce708df` **`:838`** 作 `else if (ready) p.textContent = person.notes \|\| "";`。★ **Vision 交付文档作 `:838`，系其落笔时之行号**，二者非异，本件二号并书 |
| 余六栏之路亦已治 | 实读现行 `site/app.js` 之落点 | 六栏俱经 `mdInlineFrag()` | `places.coord_basis`／`places.description` 由 `buildPlaceContent()` 之 `row` 助手 `:2152`；`sources.notes`／`archaeology.summary` 由资料库详情之 `row` 助手 `:3827`；`events.summary` 由 `:1336` 与 `:3490`；`relations.source_note` 由 `:4689` |
| `people.relations` 仍无一读者 | `grep -n "\.relations" site/app.js` 去其 `DATA.relations` 者 | **零命中** | 零命中（五处 `.relations` 尽是 `DATA.relations`）——**⚑H 条之首个反例其状未变** |
| ②款六栏之和复得全库 | 逐栏相加 | 1413／181；并①款 1629／224 与第十栏 3／1 复得 3045／406 | 628+426+172+116+65+6 = **1413**；48+39+39+24+30+1 = **181**；1629+1413+3 = **3045**；224+181+1 = **406** ✓ |
| ②款六栏**谓词甲＝谓词乙** | 二谓词之差只一处且落 `people.notes` | 六栏无一处星号落 code span | 符（差之全集只一处） |
| 裁六十一之首例二处 | 取注文内直角引号所包之段，与其挂链 `quote_original` **各去标点**（Unicode 类目 P／Z／C，并 `**`／`` ` ``）后比 | 二处去标点后为底账之子串 | `E290`·`P_KONGZI` 之「季孫問諸仲尼仲尼曰丘聞之火伏而後蟄者畢今火猶西流司厤過也」**在** `Q508` 去标点之文内；`E297`·`P_KONGZI` 之「仲尼聞之見於郯子而學之既而告人曰吾聞之天子失官學在四夷猶信」**在** `Q500` 去标点之文内。**其字逐字相同，异者只一枚标点**（`Q508`「丘聞之**：**」→ 库行「丘聞之**，**」；`Q500`「吾聞之**：**」→ 库行「吾聞之**，**」），并其内层引号之剔与句末句号之省 |
| ★ Sophia 之「设错靶」其实可复见 | 以「凡直角引号内者皆传文之引」为谓词扫 `E290` 之 `role_in_event` | 假阳丛出 | 实得 13 段，其中只 2 段真属传文之引，余为本库判语（「异」「相关」「亲至」「问」「见」「訪」之属）。★ **此非旁证而是同一病之复现**，故其自记一并入条文 |
| `data/csv/` 未动 | `git status --porcelain -- data/csv/` | 无输出 | 无输出（`data/incoming/r53_kongzi_role/` 系 Sophia 之备料，非本件所动；**条文内之措辞已按此精确改过一次**，初稿误写作 `-- data/`） |

---

## 三、领队加注八条之逐条落实（★ 其加注不在任务书之内，见 §〇；照办并列其果）

| # | 加注所命 | 本件之办 |
|---|---|---|
| 1 | 前置：`HEAD` 仍 `ce708df` 与远端同哈希；13 项在制品；本件在其上续改，不得回退或重建；`conventions` 今干净 | **自核过**：`git log --oneline -1` = `ce708df`，`git ls-remote origin main` = `ce708df6765570d93634e4293b2dbd54add84e7a`，**同哈希**；起手 `git status --porcelain` **13 项**，与其所列逐项相同；`git diff --stat docs/conventions.md` 起手**无输出**（干净），故其改即本件之物。我之 `tools/qa/prod_data_invariants.js` 与 `docs/delivery_skipper_r53.md` **在其上续改，未回退、未重建** |
| 2 | 三件并行；我之面只三物；不得 checkout／switch／stash／reset | 我所动**只三物**：`docs/conventions.md`、`tools/qa/prod_data_invariants.js`、本文件。★ **一次 `checkout`／`switch`／`stash`／`reset` 也未用**——落笔中途须回退一版加注之落位（行内 → 另起一行），**以脚本逐块搬之，未借 git 还原**，正为守此界 |
| 3 | 自验①是最要紧之闸；非版本行之删即是抹平 | 见 §二自验①，**实测 1，即版本行**；并自加一层「旧行逐行是否仍在」之机械证 |
| 4 | `3045`／`3044` 之差；★ 仍须自跑自判，并书其谓词（配对数，非游程数） | 见 §二自验②，**自跑自判**；配对数 3045、游程数 6090，`6090 == 3045*2` 实测 `True`；条文内一律书「对」并明书配对数 |
| 5 | §7 ⚑H ②款之分野在「路是否已治」；`people.notes` 路已治而今无一行实渲；宜存其实，不宜写成「已扩施故已见效」 | ①款之加注内**并书二事**：「入①款之由」与「本栏今日无一行实渲」，并明书「**本轮于此栏之改善是『路对了』，不是『读者看见了』**——不得以『已扩施』读作『已见效』」，且标明此实系 Vision r53-3 如实上报所得 |
| 6 | 门头一句不加重试，并陈其由 | 见 §一之 7；**一行重试之码也未加**（`grep -in "retry\|重试" tools/qa/prod_data_invariants.js` 实测 5 处命中，**逐一落在新增之 `:56`–`:63` 注释段内**，码内零命中） |
| 7 | 记账之日照裁十四取本机时区 | 全件署 **2026-09-26**（本机 09:51 -0400 落笔） |
| 8 | 不提交、不推送；拿不准即停、上报 | 见 §五、§六 |

---

## 四、一处**未办而登记**（★ 本件最要紧之上报，不代裁）

**其实**：Vision r53-3 **扩施之栏是八栏**（②款旧七栏 ＋ ①款二栏之一路改经 `mdInlineFrag()`），六栏之渲染之路**与 `people.notes` 同治于一件**（求法与实测见 §二自验④第三行，逐栏行号俱列）。而 **§六 之裁只及 `people.notes` 一栏**（裁五十二）。

**本件之处置**：**只移 `people.notes` 一栏，余六栏一律留在②款**，其数照实更（1413／181）而**其款不动**；并于②款与③款之加注内**明书此事**：余六栏之路已治、其款属未裁、本件不移，③款之欠账**照旧记为未清**。

**其由二事**：
1. **裁只及一栏**，我不代裁其余六栏之款属——照 `CLAUDE.md` 角色纪律「史学疑点与体例之事一律记录上报，不自行裁量」。
2. **③款所命之移栏以「落地」为凭**，而本轮之件（Vision r53-3／r53-6）**尚未合入**；`people.notes` 之移系裁五十二明命，故从裁；余六栏无裁可从，**从旧款而记其实**。

**呈站长一问（不代裁）**：余六栏是否随 `people.notes` 一并移入①款？若裁其同移，**合入之日一并移之并逐栏记其日**（届时②款将空，③款之欠账亦当随之结账，此二事之措辞亦须一并裁）。
★ **本件已把「扩施之实」与「移栏之名」之落差写在条文里，不以任一方掩另一方**——同 r52 追十八「款属未定，待裁」之式。

---

## 五、本件所动之文件全列（并行面复核用）

| 文件 | 状 | 量 |
|---|---|---|
| `docs/conventions.md` | 改（v1.45 → **v1.46**） | +29／−1（所删即版本行） |
| `tools/qa/prod_data_invariants.js` | 改（**只增门头一段注释**） | **778 → 789 行**（+11 行注释），**一行码未动**（`node --check` 通过） |
| `docs/delivery_skipper_r53.md` | 改（**只增本追记**） | 上文 34682 字节 sha256 `e7a853af…4cb0e` 一字未动 |

**未动者，逐一实核**：`data/csv/`（`git status --porcelain -- data/csv/` 无输出）｜`site/`（其改**全属 Vision**，`site/app.js`／`styles.css` 我一字未碰）｜`site/data/`（无输出）｜`DATA_LICENSE.md`、`README.md`（无输出）｜`tools/` 之他物（除上表一件外无输出）｜`data/incoming/r53_kongzi_role/`（**Sophia 之面，未碰**）｜`docs/design/design_notes.md`、`tools/qa/vision_r53.js`（**Vision 之面，未碰**）。
★ **四条已勘之断言**（r53-5 §二之一）**现状未动一字**（裁五十五已照准，件三明禁）。

---

## 六、已知限度（如实）

1. **条文内所引 Vision／Sophia 之数，凡系我可自跑者俱已自跑**（§二自验②④）；其**不可自跑者二**，照实标其所出而不冒作自测：① 裁六十一所书之「**其余 46 处传文之引俱命中**」——此系 Sophia r53-4 之实测，我只自核其**二处之异**（去标点后比），**46 之数未自求**（其谓词系 Sophia 之量器，量器本身正是 r53-8 在改之物）；② Vision 之 ΔL 实测与二门之红，本件未引入条文。
2. **§9.3 之加注所述「旧四本自本轮退」，其删今在工作区而未提交**——加注内已就地明书此事（「★ 落笔之时其删已在工作区、尚未提交，本行随本轮合入生效」），**不预填其已成之状**。
3. **`people.notes` 之「落地之日」未填**，只记其轮 `r53`——照「不得预填」之律，俟本轮合入实测补记。★ **此是一处有意之空格，不是漏**；合入者请勿以为忘填。
4. **条文之数俱系 `ce708df` 之基线**。若 Vision r53-6 或 Sophia r53-8 于合入前改及 `data/csv/`（今二件俱不改数据，故不当有变），则 §7 ⚑H 诸数须重跑自验②再定。
5. **本件未跑 `regress20.js` 与二门**（其由见 §二自验③）。本件一行 `site/`、一行数据未动，二门之锚亦未动。
6. **§9.3 之加注取「四类」之分，是我之归类，非裁所命**——裁五十六只命「随本轮升号」。其分类若不合站长之意，改之只须动此加注一段，不牵他处。

---

## 七、上报事项（不代裁）

1. ★ **余六栏之款属**——见 §四，**本件只记不移，呈站长一语**。
2. **领队加注与任务书之别**——见 §〇末，该八条之文不在任务书 r53-7 节内；照「先入文、后发件」之律，宜回写任务书。**我以任务书现行文本为准，并已把八条一并照办。**
3. **`team/_to_delete_20260925_qa_merge/` 仍候站长一语方删**（裁六十：可清，本轮不办）。其可自 git 历史取回之法**已入 §9.3 之加注**（裁六十所命「清时于交付文档记明」，本件更进一步写进了条文，使其不随交付文档之久远而失落）——★ 若以为条文不宜载此，一语即移回交付文档。
4. **本轮工作区现有三人之物**（`git --no-optional-locks status --porcelain` 实读，**14 项**）：我之 `docs/conventions.md`＋`tools/qa/` 四删一增＋本文件；Vision 之 `site/app.js`／`site/styles.css`／`docs/design/design_notes.md`／`tools/qa/vision_r53.js`；Sophia 之 `data/incoming/r53_kongzi_role/`；并 r53-2 之 `docs/delivery_skipper_r52.md`。★ **我未 `checkout`／`switch`／`stash`／`reset` 一次。**
5. **交接备注**：`HEAD` 起手至交件仍 `ce708df`，与 `git ls-remote origin main` **同哈希**。本文件末次勘注之日今为 **2026-09-26**；下一执行者若再动本文件，**须自本追记之后另起，不得并入其内**。

---

## 八、停在提交之前（件五）

- **不提交、不推送。** 工作区留上列改动，候领队复核、Co站长复看、**站长一语**（`team/round53_prompts.md` §四本轮之界第 3 款：三件俱不含推送口令）。
- 本件**不含任何数据合入**；合入之序照裁五十九（Sophia 之备料俟 Vision r53-6 落定，合入之日重跑 `sim_r53_kongzi_role.py` 一过）。
- ★ **`conventions` 之 v1.46 一号已用**——若本轮三件之合入另有条文须升号，**请并入 v1.46 而不另起 v1.47**（裁五十六之意是「随本轮合入升号」，一轮一号）。
---

# 追记 · Skipper r53-9（conventions 续裁并入 v1.46；裁六十三至六十八；**不提交、不推送**）

> ★ **本追记只增不改**：上文 r53-2、r53-5、r53-7 三节之文（追记前**共 56339 字节**、sha256 `c2d447f2b1e3dbdc70af2f435ca11a4845496753d6cb9a635c92df8614e7a381`）**一字未动**，本节自其末另起（照 r53 体例第 4 条，及 r53-7 §七之 5「下一执行者须自该加注之后另起」）。

**件**：`team/round53_prompts.md` `## Task for Skipper · r53-9 — conventions 续裁并入 v1.46（裁六十三至六十八）`
**所据**：同文件 §七 **裁六十三至六十八**（2026-09-26 Co站长），并「**领队之九**」之领队加注六条（★ 该节系派件前所立、已在文内，非补记）
**落笔之日**：**2026-09-26**（照裁十四取本机时区；起笔时本机 **2026-09-26 10:33 -0400**）
**所改之文件**：**只一物**——`docs/conventions.md`（并本文件追一节）。`data/`、`site/`、`tools/` 一行未动。

---

## 〇、所读之节与其末次勘注之日（r53 体例第 5 条）

| 所读之节 | 末次勘注之日 | 自核之法（★ 照纪律三，不凭派件之复述） |
|---|---|---|
| `## Task for Skipper · r53-9`（五条俱读） | **2026-09-26** | ★ **节内四位年月日之串零命中**（正则 `20\d{2}-\d{2}-\d{2}` 全节搜），故无署日之追加，**其立日即 §七 之日**；节界取 `## Task for Skipper · r53-9` 至 `## Task for 领队 · r53-10`，**1404 字节**，sha256 `3fb1bd4e36d220d555fc8de9c0de6685d5194a0f0426212aa4217cebbe1ab709` |
| 同文件 **§七**（裁六十三至六十八全节） | **2026-09-26**（节题自署「2026-09-26 Co站长」；节内除此一处外无另署之日） | 节界取 `## 七、r53 续裁` 至 `## Task for Skipper · r53-9`，**4313 字节**，sha256 `af2bd49d5e702bdde370bc2dc13b84960c8155ff8e756568eadb94f060c87c1b` |
| 同 §七 **裁六十三** | **2026-09-26**（其内零日期串，从节题之日） | **1826 字节**，sha256 `e7818769fc3847cc6e74d712b147240d4ded4ac84b25e2c0a7e904cc41a2a49f` |
| 同 §七 **裁六十四** | **2026-09-26**（同上） | **814 字节**，sha256 `d70ea70736d398813198015761aa8864972df6be8ff3e061a9836577c4875418` |
| 同 §七 **裁六十五** | **2026-09-26**（同上） | **472 字节**，sha256 `b4126c597c21cfe53394b2fcda49ffd73ce6fe2d2cbcfc5912fe41188d7c1cc7` |
| 同 §七 **裁六十六** | **2026-09-26**（同上） | **494 字节**，sha256 `afadbb2c69816a5e58b6fc833003748abf72b23a4120cf532c002b82275c2884` |
| 同 §七 **裁六十七** | **2026-09-26**（同上） | **148 字节**，sha256 `21d3a05824d3c009f0d10f25eba39676bf29024aaef345334534f683d5130cf7` |
| 同 §七 **裁六十八** | **2026-09-26**（同上） | **481 字节**，sha256 `8431b2a1b5fc5bda3e79c1c192a7001da525de2100ea808fb103b5d0ff99bd1b` |
| 同文件「**领队之九**」（r53-9 之领队加注六条） | **2026-09-26**（节题自署「2026-09-26 领队」，节内另有「10:17 -0400 实读」一处） | ★ **该节实在文内，非聊天块之补记**——此即件之抬头所命之自核，**得其证**；节界取 `## 领队之九、` 至次一 `## ` 之前（今为 `## 领队之十、`） |
| 同文件「**领队之十**」（r53-10 交件 · 星号入引之数之谓词） | **2026-09-26** | ★ **此节系我起手之后方入文**（见下「一事须报」）；**读之而后校我条文一句**（§四） |
| **旁读（不在本件之面，为核其所系之实而读）** | —— | `docs/conventions.md` §7 ⚑H 全条（①至⑤款并其六处加注）与「交付体例·实测口径」全节；`site/app.js` 之 `mdInlineFrag()` 与②款六栏之全部落点（逐处实读，见 §二）；`docs/delivery_vision_r53.md` §三之五及其 `title` 清单；`docs/delivery_skipper_r53.md` r53-7 节（§〇、§一、§四、§七） |

★ **一事须报（非病，只记；★ 但足以改一句条文，故必书）**：**任务书自我起手至交件之间长了一节。**
- **起手实读**：全文 **817 行**，`## Task for Skipper · r53-9` 落 **:797**，`## 领队之九、` 落 **:480**，`## 领队之二、` 落 **:511**。
- **交件实读**：全文 **876 行**，r53-9 落 **:856**，`## 领队之九、` 仍落 **:480**，而 `## 领队之二、` 已落 **:570**；mtime **2026-09-26 10:20:41**（本机）。
- **其增系新立之「领队之十」**（r53-10 交件，59 行，落 :511–:569）——**在「领队之九」之后、「领队之二」之前插入**。
- ★ **我之件与「领队之九」二节之文，前后两读逐字相同**（内容重读一过，无一字之异；其 sha 与字节见上表，系交件时所取）。★ **一处须自白其谓词**：上表诸 sha **系交件时一次所取**，非「起手／交件两读相同」之二次比对（我起手只实读其文、未取 sha）；**故此凭只能证「交件时其文如此」，不能证「其间一字未变」**——**其间未变之凭是内容之重读，非 sha**。照本轮方立之常条「计数／自验之数须随书其谓词与基准」，此处不敢含糊。
- ★ **此正是「先入文、后发件」之律的一个正面之证**：因领队之新节入了文，我**读得见**，遂能据现行文本校我条文一句（§四）；若其只在聊天里，我便不会知道。

---

## 一、做了什么（**一物，四事**；俱以就地加注／另起新行之式，旧文一字不删）

| 事 | 所据 | 落于 `docs/conventions.md` |
|---|---|---|
| ① **新立体例一·引文之内不得择字加粗** | 裁六十三 | **:307–:308**（紧接「摘引之标点从其语境」一条之后，与「今译语不可代传文」同族相邻） |
| ② **新立常条三** | 裁六十五／六十六／六十八 | **:328**（`conventions` 一轮一号）／**:330**（自验之数须书明其基准）／**:332**（领队加注入文之二法），俱入 §7「交付体例·实测口径」，接裁五十八之常条之后 |
| ③ **⚑H ②款之整理**（五栏移入①款，②款余一栏） | 裁六十四 | ①款加注 **:292**（逐栏之验＋①款成八栏之数）／②款加注 **:295**（余一栏，并其「一栏两态」之实）／③款加注 **:298**（②款未空，其款不预用；五栏落地之日不预填） |
| ④ **版本行记续裁四事** | 裁六十五（一轮一号）＋裁六十七（确认） | **:3**——**并入 v1.46，不另升 `v1.47`**；其内并书裁六十七之确认（`slice` 之通例**已在本轮前批之落笔内**，本件**无新落笔**） |

★ **体例一之落笔，其三事俱备**（件之 1 所命）：① **其由**——⚑H 之前提是「`**` 系编者之记法」，落引外读者读作编者在说话，落引内而择字则「编者之声穿了史料之衣」；并明书其与 r51「今译语不可代传文」同族（**那条禁以今语冒作传文，本条禁以编者之轻重冒作传文之轻重**）；② **其二类之别**——通引之粗**可**（例 `E277`）／择字之粗**不可**（例 `E283`，其极形 `E282` 之加粗传文一字）；③ **存量未改、登记候排、其数之谓词待对齐**——见 §五。

---

## 二、②款之整理 · **逐栏之验**（件之 3 明命「不得只报结论」）

**所据之本**：**工作区现行 `site/app.js`**（`ce708df` ＋ r53 在制之改，未提交）。★ **其行号与 `docs/delivery_vision_r53.md` §三所书者相差约 163 行**——系其后 r53-6「案乙」落笔所致，**非另一份文件**；引者须连其时点一并引。

| 栏名 | 其落点（实读之行号） | 经何函数 | 路已治否 | 本件之处置 |
|---|---|---|---|---|
| `places.coord_basis` | **一处**：`:2158 row("坐标依据", pl.coord_basis)` | `:2152` `buildPlaceContent()` 之 `row` 助手 → `mdInlineFrag()` | **已治** | **移入①款** |
| `places.description` | **一处**：`:2159 row("说明", pl.description)` | 同上助手 | **已治** | **移入①款** |
| `sources.notes` | **一处**（显示）：`:3852 row("说明", r.notes)`（`sources` 页签分支） | `:3827` 资料库详情之 `row` 助手 → `mdInlineFrag()` | **已治** | **移入①款** |
| `archaeology.summary` | **一处**（显示）：`:3844 row("概要", r.summary)`（`archaeology` 分支；`:3835` 之同名 `row` 系 `background.summary`，不在本栏） | 同上助手 | **已治** | **移入①款** |
| `events.summary` | **三处**：`:1336`（`eventBodyNode()` 事目卡摘要，人物时间线与编年两视图共用）｜`:3490`（`cmpMeetEvNode()`，并观交会弹卡两侧各用一次，故计二落点） | 三者俱 `mdInlineFrag()`（其二其三原走 `innerHTML` 拼接，r53 已照④款改掉） | **已治** | **移入①款** |
| `relations.source_note` | **文本流一处**：`:4689`（`relDetailBody()` 之 `.rel-note`，内嵌右卡与全屏抽屉共用）｜★ **SVG `<title>` 五处**：`pairTip()`（`:3914` 定义）之四用处 `:4249` 全景边／`:4260` 边徽记／`:4509` ego 边／`:4521` ego 边徽记，并 ego 节点提示内联之一处 `:4377` | 文本流经 `mdInlineFrag()`；`title` 五处俱 `tip.textContent = …`，**不经任何解析** | ★ **一栏两态：文本流已治、`title` 未治** | ★ **留②款**，并书其未治之落点 |

★ **非落点者亦须书明，免后人再查一遍**：`:4884`（`sources` 组之全站搜索索引 `searchNorm([… s.notes])`）与 `:4857`（`events` 组之 `e.summary`）**只供匹配、不渲于页**，其 `label`／`sub` 二栏不取此二栏之值，**故非落点**。

★ **谓词之辨，须报（不强求同数）**：Vision 报「其 `title` **三处**未治」（`docs/delivery_vision_r53.md` §三之五及其清单），我报**五处**——**二数谓词不同、不可相权**：其按「**清单之目**」计（`pairTip()` 一函数作一目、ego 节点一目），我按「**调用点**」计。照本轮之常条，**二数各书其谓词即可**；**我不改其数，亦不以我之数覆其数**。

★ **结论之形（照裁六十四逐条落）**：
1. **五栏已治 → 移入①款**；②款之逐栏之数照实更，**旧数、旧栏之文一字不删**。
2. **一栏两态 → 留②款**，并书其未治之落点（上表）。★ **其数不可拆作「已治之数」「未治之数」二堆**——本栏每一对记法**二处俱渲**（点开见其粗体、悬停见其裸符），**同一对既在已治之面、又在未治之面**；欲据之排期者须另设谓词分计。
3. ★ **②款未空，故裁六十四所设「其条存而其目空」之款本件不预用**——**照实记其未空**；他日 `title` 一路之治落定，②款方空，那时方照此记之。**条本不删**（裁六十四明命）。
4. **移入①款之五栏，其轮记 `r53`；其落地之日俟本轮合入实测补记，本件不预填**（照 r38 立之「不得预填」通例，并照裁十四取本机时区之日）。
5. ★ **「路已治」与「有实渲」之分，逐栏记其实**（领队加注第 4 条所重申）：此五栏之落点**俱无条件分支**（`row()` 助手只跳空值，`:1336`／`:3490` 直渲其文），**故其路既治即有实渲之果**；此与 `people.notes` 之「路已治而今无一行实渲」（其落点在 `else if (ready)` 一支上、`short_bio` 阙如方渲）**不同，条文内已明书其别，不混为一谈**。

---

## 三、自验（件之 4 之三条，逐条书其**求法／所期之果／读法**）

### 自验① · **非版本行之删即是抹平**（本件最要紧之闸）

- **求法**：`git diff -U0 docs/conventions.md | grep '^-' | grep -v '^--- ' | wc -l`
- **实测**：**1**；其一行即 `-版本：v1.45（2026-09-20：r52 合入……`。
- **读法**：**此一删系 r53-7 之版本行下移（v1.45 移入「历史：」一列），非本件所为，亦在「仅版本行」之许内**；★ **本件自身之非版本行之删＝0**。
- ★ **更强之一证（其形，非只其数）**：以 `difflib.SequenceMatcher` 比 `git show ce708df:docs/conventions.md` 与现文，**全部 opcode 为 `insert`，唯一之 `replace` 落在 `old[3:4] → new[3:5]`（即版本块）**——**旧文无一行被改、被删**。
- ★ **其所以能如此**：照领队加注第 2 条与 r53-7 自立之范式，**加注一律另起一行，不在原行内追加**；行内追加会使 `git diff` 呈「删一行、增一行」，此闸即读不出有无抹平。本件十一行新文**俱另起一行**，唯版本行一处系「其自身之行」，不在此律之内。

### 自验② · **版本号仍 v1.46，未出第二号**

- **求法**：`grep -n "^版本：\|^历史：" docs/conventions.md`；并 `grep -o "v1\.4[5-9]"` 数其现形。
- **实测**：行首「版本：」**只一行**（`:3`，`v1.46`）；「历史：」自 `:4` 起（`v1.45` 以下，一行未增未删）。`v1.47` 全文现形 **3 次**，★ **三处俱系否定之提及**——「不另升 `v1.47`」「引者见 v1.46、v1.47 二号，无从知其为同一轮之两批」「二批俱系 v1.46，不出 `v1.47`」。
- **读法**：若行首「版本：」现二行，或 `v1.47` 现于行首之「版本：」，即违裁六十五，**停下上报**。

### 自验③ · **质量门**

- `python tools/validate.py` → `OK：全部校验通过`，**exit 0**。
- ★ **未跑 `regress20.js` 与二门，书其由**：本件 `data/`、`site/`、`site/data/` **一行未动**（`git status --porcelain -- data/csv/` 无输出），二门之锚亦未动；跑之只是重跑他人之在制品，测不出本件之事。

### 自验④ · **本件所书库内之数之自核**（体例第 6 条；★ 并照新立之「自验之数须书明其基准」自守）

- **基准**：`ce708df`（`data/csv/` 工作区未动，实核 `git status --porcelain -- data/csv/` 无输出，故工作区之数即该基线之数）。
- **求法**：自 `data/csv/*.csv` 逐表逐栏逐 cell 扫之——**谓词甲**取 `/\*+/` 中长度恰为 2 之游程，每 cell 内其数为奇者舍其末、成对计；**谓词乙**先求该 cell 内反引号之配对下标（奇者亦舍末），凡星号游程之起始下标严落于某一对反引号之间者不入，余者同甲；**行次**取「含至少一对之 (表, 栏, 行) cell 数」。
- **所期之果／实测**（逐栏，甲对／甲行｜乙对／乙行）：`passages.modern_note` 1221／137｜1221／137｜`event_people.role_in_event` 118／51｜同｜`people.notes` **290／36｜289／36**（★ 甲乙之差唯此一栏）｜`places.coord_basis` 628／48｜同｜`sources.notes` 426／39｜同｜`events.summary` 172／39｜同｜`places.description` 116／24｜同｜`relations.source_note` 65／30｜同｜`archaeology.summary` 6／1｜同｜`people.relations` 3／1｜同。**全库合计：甲 3045／406，乙 3044／406**——与 §7 ⚑H 条 r53-7 所立之二数**分毫不差**。
- **本件所新书之和**：①款八栏 1221＋118＋290＋628＋426＋172＋116＋6 ＝ **2977**，行次 137＋51＋36＋48＋39＋39＋24＋1 ＝ **375**（谓词甲）；谓词乙 **2976／375**。
- **读法（可复核之恒等式）**：①款 **2977／375** ＋ ②款 `relations.source_note` **65／30** ＋ 第十栏 `people.relations` **3／1** ＝ **3045／406** ＝ 全库（谓词甲）；乙同式得 **3044／406**。★ **三者之和不合全库之数，即有一栏未入账，停下上报**（2026-09-21 追十八所勘之病正是此型）。

---

## 四、**一句之校**（★ 因任务书中途增「领队之十」一节；照「拿不准即以现行文本为准」）

- **原拟文**（我起手时所写，其时 r53-10 尚未交件）：「领队另报一数，其求法、限域与谓词**待其自书**（`team/round53_prompts.md` 之 r53-10 正办此事）。**俟二者对齐之后，另件补其数于本条**。」
- **校后之文**（据「领队之十」实读改写，落 `:308`）：**谓词已各自书明，而二数仍未对齐**——① Co站长之数：判据「成对星号落 `「…」` 之内」、域全库全栏、单位星号对、通引与择字分计；② 领队所报之数：源出 Sophia r53-8 之量具，限域只 `event_people.role_in_event` 之 `P_KONGZI` 诸行、单位系**摘引段**（非星号对）、且**须先判为传文之引**；★ **二者单位即不同，不可相减相较**；而 Co站长之数领队照其判据自跑**未能复现**，其差最可能在「**通引**」之界如何画，**待其书明其界方能定**。**故本条不书一数**，俟对齐后另件补之。并录二事：★ **另有一数至今无人求过**——「全库『传文之引』之内之择字之粗」，当由一件专求、其谓词随书；★ **排期之轻重，领队有见而不代裁**——宜先治「已判为传文之引」之域，**呈站长**。
- ★ **此校之性质须书明**：所校者是**我自己本轮尚未交件之在制文字**（未提交、未交人），**非他人之文、非已入档之文**，故**直改而不加注**；此与「他人之文原字照留、就地加注」之律不相冲。★ **然其改之过程记于本节，不抹平成「一开始就这么写的」**（照 §7 v1.29「判定史留原貌」之意）。

---

## 五、★ **本件一处数也未写入体例条**（件之 1 末句／领队加注第 3 条——「本件最易失守处」）

- **求法**：取 `docs/conventions.md` `:307`–`:308`（体例一全条），搜其内全部阿拉伯数字之串。
- **实测所得之数串**（`grep -o "[0-9]\+" | sort -u` 之全集，**一个不漏**）：`09`／`1`／`10`／`2026`／`26`／`277`／`282`／`283`／`44`／`46`／`51`／`53`／`54`／`7`／`8`——其属：`2026`／`09`／`26`（日）｜`1`／`44`／`46`（`v1.44`、`v1.46`）｜`53`／`54`／`51`（轮次 `r53`／`r54`／`r51`）｜`8`（**件号 `r53-8`**）｜`7`／`10`／`1`（`§7`、`§10.1`）｜`277`／`282`／`283`（`E277`／`E282`／`E283` 三例之 ID）。★ **此一集系机器所出、非我所举**——初稿之列漏了 `8` 一枚（`r53-8` 之件号），自核时以 `sort -u` 之全集补齐；**记此一漏，不抹平**（本轮之律：举例式之自陈最易漏，故自验须取全集而非凭举）。
- **读法**：★ **无一处系「星号入引之计数」**——`177`／`166`／`343`／`23` 之属**零现形**（可以 `grep -o` 逐一验之）。**宁其条无数，不可其数无谓词**——本条之数俟 r53-10 之对齐落定后另件补之。
- ★ **例证之 ID 不是数**：`E277`／`E283`／`E282` 系**所引之行**，非**所数之量**；其所以入条，是件之 1 明命「记其二类之别」，而二类之别**非举例不可明**。

---

## 六、领队加注六条之逐条落实（「领队之九」；★ 该节系派件前所立、已在文内，我实读得其证）

| 条 | 所命 | 本件之果 |
|---|---|---|
| 1 | 前置自核；`conventions` 现是 r53-7 之 v1.46（在制），**在其上续改，不得回退／重建，不另起 v1.47** | ★ **自核一过**：`HEAD` `ce708df` 与 `git ls-remote origin main` **同哈希**；工作区 **14 项**未提交（`M`×5、`D`×4、`??`×5）。本件**在 r53-7 之 v1.46 上续改**——其证即自验①之形（全部 `insert`，旧文零改零删），**未回退、未重建**；**未出 v1.47**（自验②） |
| 2 | 自验①是最要紧之闸；**加注一律另起一行，不在原行内追加** | ★ **照办**：十一行新文**俱另起一行**（①②③款加注各一行、体例一二行、常条三含空行共六行）；唯版本行一处系其自身之行。自验①因此**读得出**：非版本行之删 0 |
| 3 | 件之 1 末句最易失守；**条文内一处数也不要写**，俟对齐后另件补之 | ★ **守住**：见 §五之逐字自查。★ **并因「领队之十」已交件而校其一句**——**谓词已书而二数未对齐**，故仍**不书一数**（r53-10 §十之四.1 亦明言「其数不必俟对齐，宁其条无数」，与本件所守同） |
| 4 | ②款之整理须**逐栏列其验之果**；**分野在「路是否已治」，不在「是否有实渲之果」**；`people.notes` 之型须照存 | ★ **照办**：逐栏之表见 §二（栏名｜落点｜经何函数｜路已治否｜处置）。★ **分野守住**：五栏以「路已治」入①款；★ **并书其与 `people.notes` 之别**——此五栏落点无条件分支、路治即有实渲，而 `people.notes` 路已治而今无一行实渲，**条文内明书其别，不以「已扩施」读作「已见效」** |
| 5 | 常条三之「领队加注之二法」系由领队本轮之失所生；**落笔时不必为领队讳**，直书其由 | ★ **直书**：条内明书「**其条之由即领队本轮之失**——领队违『先入文、后发件』之纪律三轮，由 Skipper r53-7 指出，遂立此条」，并指其自记与补记之所在（「领队之七」「领队之八」「领队之九」）。★ **并收 r53-7 自立之一则**——「加注一律另起一行、不在原行内追加」，注明其系 Skipper r53-7 自立、领队记为范式 |
| 6 | 记账之日照裁十四取本机时区；**不提交、不推送**；拿不准即停 | ★ **本机落笔之日 2026-09-26**（起笔 10:33 -0400），条文与本节诸日俱取此。★ **未提交、未推送**（§九）。★ **一处「拿不准」已停而上报，未自裁**——见 §八之 1 |

---

## 七、已知限度（如实）

1. **②款六栏之路，我只验「其落点是否接 `mdInlineFrag()` 一路」，未验其渲染之果于浏览器**——本件不动 `site/`，亦未起浏览器；其渲染之实测在 Vision r53-3／r53-6 之交付内，我只核其码之路。
2. **`relations.source_note` 之 `title` 五处，我未量其内实有几处裸记号**——只验其路未治。★ **该栏每一对二处俱渲**，故「未治之处数」须另设谓词方可求，**本件不求、不猜**。
3. **条文之数俱系 `ce708df` 之基线**。若 Vision r53-6 或 Sophia r53-8 于合入前改及 `data/csv/`（今二件俱不改数据，故不当有变），则 §7 ⚑H 诸数（含本件新书之①款八栏之和）须重跑自验④一过。
4. **本件之行号（`site/app.js` 诸落点）系工作区在制之码**，未提交；合入之后行号将变。★ **条文内已书其时点与其与 Vision 交付之差（约 163 行）**，但**合入后若有人以行号复核，须自行重定位**——此系在制品之常，不是错。
5. **裁六十七之「确认」我只核其文在否，未重判其文之当否**——其文系 r53-7 依裁五十八所落（§7「交付体例·实测口径」之「凡取其前 N 而报者……」），本件**确认其已在、并入 v1.46**，未改一字。
6. **常条三之落点是我之取**（俱入 §7「交付体例·实测口径」）——裁六十五、六十六、六十八未命其落于何节。其由：该节之题正是「交付文档自身之实测/推断之辨」，三条俱属此域。★ **若站长以为「领队加注之二法」当另立一节（其事在协作纪律而不在实测口径），改之只须移此一行，不牵他文。**

---

## 八、上报事项（不代裁）

1. ★ **`relations.source_note` 之款属（本件最要紧之一报）**——我依裁六十四之通则（「未治者留②款并书其未治之落点」）**留其在②款**；★ **然其实是「一栏两态」，非「全未治」**：文本流已治、`title` 五处未治，故②款之题「尚未由显示层承担者」于此栏**不尽合其实**。**此属体例之事，本件只记不代裁**——若站长以为当另立一款「**部分已治之栏**」，或以为 `title` 一路不入⚑H 之域（其为 tooltip，无排版可言），**则其款属须另裁**；本件之加注届时只须改一段，不牵他文。
2. ★ **`title` 一路之治须先裁其体例**（Vision 实报，我核可其判）：SVG `<title>` 只承字符串、装不了元素节点，欲去其记号须另立一「去记号」之纯函数；而去符之后「此是编者之强调」一层意思于 tooltip 内**无处可着**。**故其治须先裁「tooltip 内之库内之记法如何自处」。**
3. ★ **②款遂空之日，其条不得删**（裁六十四明命）——今②款未空，其款未用；**待 `title` 一路落定，②款方空**。我已把这句写进③款之加注，**免后人见其空而删条**。
4. **择字之粗之治，其数至今未对齐**（§四）——r53-10 已书二数之谓词并如实报「未能复现」；★ **更有一数无人求过**：全库「传文之引」之内之择字之粗。**此数当由一件专求、其谓词随书**，不可以现有二数之任一代之。
5. **任务书之变（§〇「一事须报」）**——「领队之十」于我工作期间入文（mtime 10:20:41）。★ **此非病，反是「先入文、后发件」之效**；只须记明：**我之条文有一句系据其而校**，故本件之交付须与该节并读。
6. **`conventions` 一轮一号之律今已成文**（裁六十五）——★ **r53-7 §八末所拟之「请并入 v1.46 而不另起 v1.47」，今由裁六十五升为常条**；本件即其首次照办。
7. **交接备注**：`HEAD` 起手至交件仍 `ce708df`，与 `git ls-remote origin main` **同哈希**。`docs/conventions.md` 末次勘注之日今为 **2026-09-26**（本件）；本文件末次勘注之日亦为 **2026-09-26**。★ **下一执行者若再动此二文件，须自本追记（与 `:307`–`:308`／`:328`–`:332`／`:292`／`:295`／`:298` 诸加注）之后另起，不得并入其内。**

---

## 九、停在提交之前（件之 5）

- **不提交、不推送**（件之抬头与领队加注第 6 条俱明命）。工作区留本件之改，候领队复核、Co站长复看、**站长一语**。
- **本件所动之文件全列（并行面复核用）**：`docs/conventions.md`（**+11 行新文，＋版本行一处续写；非版本行之删 0**）、`docs/delivery_skipper_r53.md`（本追记，只增不改）。★ **`data/`、`site/`、`tools/`、`DATA_LICENSE.md`、`README.md` 一行未动。**
- **工作区现有三人之物**（`git status --porcelain` 实读，**14 项**，与领队前置之数同）：我之 `docs/conventions.md`＋`tools/qa/` 四删一增＋`docs/delivery_skipper_r53.md`；Vision 之 `site/app.js`／`site/styles.css`／`docs/design/design_notes.md`／`docs/delivery_vision_r53.md`／`tools/qa/vision_r53.js`；Sophia 之 `data/incoming/r53_kongzi_role/`；并 r53-2 之 `docs/delivery_skipper_r52.md`。
- 本件**不含任何数据合入**；合入之序照裁五十九。

---

## 十、r53 推送之实测（追记 · 2026-09-26；件之所据：`team/round53_prompts.md` **§九、r53 推送**）

> ★ **本节系推送之后所追记，非预填**（照 §7「交付体例·实测口径」r38 立之通例）。本机落笔时刻 **2026-09-26 12:08 -0400**（记账之日照裁十四取本机时区）。
> ★ **§九 之来历，照其节首之注并书**：该节系**领队** 2026-09-26 据站长所发之派件块**立于任务书内**（其块引「任务书 §九」而本文件内原无此节），其正文〇至五系站长原字一字未改，领队之加注七条**另列于节末、派件前已在文内**（非补记）。

### 〇、所读之节与其末次勘注之日（r53 新体例第 5 条）

| 所读之节 | 末次勘注之日 | 自核之法 |
|---|---|---|
| `team/round53_prompts.md` **§九、r53 推送**（含节末「领队加注」七条） | **2026-09-26**（节内署日实测：`2026-09-26` **4 处**、`2026-09-25` **1 处**〔即 `.git/index.lock` 所遗之日〕；末次勘注系「领队加注」自署 **2026-09-26 12:01 -0400**） | 节界取 `## 九、r53 推送` 之行至其末之 `---` 前（任务书行 598–661，下一节题 `## 领队之二` 在 663）；节之 sha256 = `e2f83cee58facda31aebace411b92fc7550286a837d4b19b96039c2764352b62`（UTF-8、含行末），**落笔前后各读一过，两读相同** |
| `team/round53_prompts.md` **§八、r53 收官之裁与 Co站长复核**（裁六十九，含甲乙丙三目） | **2026-09-26**（节题自署） | 全节读毕，以知「推前二嘱」与「口令归站长」之实 |
| `team/round53_prompts.md` 卷首**新体例六条** | **r52 议定**（无另署之日） | 逐条读毕；本节即照其第 2 条（不凭派件之复述）与第 5 条（书所读之节与末次勘注之日）而作 |
| `docs/conventions.md` 版本行（v1.46）与 §7「交付体例·实测口径」 | **2026-09-26**（r53-9 之落笔，即本轮） | 读其现行文本以定本节之体例；★ **本节未改 `conventions` 一字** |

★ **一事并记**：任务书行 598 之 §九 系领队于 **12:02:25 -0400** 入文（文件 mtime 实读），我于 **12:03 -0400** 起读——**其文已在，非我落笔后补入**。合「先入文、后发件」。

### 一、〇节「先清陈旧锁」——**落笔之时无物可清，不编一份四事之记**

| 项 | 实测 |
|---|---|
| 命 | §九〇节命先清 `.git/index.lock`（据其文：0 字节、2026-09-25 21:25 EDT 所遗、系 Co站长之 shell 所留），照裁四十五记四事（路径｜字节数｜时间戳｜当时有无 git 进程）后删；**进程仍在者不删** |
| 我之实读（**2026-09-26 12:03 -0400**） | `ls -la .git/index.lock` → **`No such file or directory`**；`tasklist` 过滤 `git` → **无 git 进程** |
| 领队之实读（**同日 12:01 -0400**，见领队加注 1） | 同——`No such file or directory`、无 git 进程 |
| 处置 | ★ **照领队加注 1 之②：如实书「派件之时其在（据 §八丙之嘱与 §九〇节之文），落笔之时已不在」**。**四事（路径｜字节｜时戳｜有无进程）无从记**——★ **不因「本该有」而编一份四事之记**。 |
| 副证 | 五个 `git commit` **无一撞锁**（俱一次而成，见下表），此即「锁已不在」之行为之证 |

★ **此正是本轮所立之律之用：账记其实，不记其应然。**

### 二、一节「提交之分法」——**照议之五提交，分法未改**

| # | 提交哈希 | 题 | 文件（实测 `--stat`） |
|---|---|---|---|
| 一 | `27717c3` | `feat(r53 Vision 件): ⚑H 扩施七栏 ＋ 反引号一路取案乙（r53-3、r53-6）` | `site/app.js`（+196 −27）／`site/styles.css`（+53 −0）／`docs/design/design_notes.md`（+189 −2）／`tools/qa/vision_r53.js`（新 782 行）＝ **4 文件 +1220 −29** |
| 二 | `86d418d` | `refactor(r53-5): 四本 QA 归并为 tools/qa/prod_data_invariants.js——门不动` | 新 `prod_data_invariants.js`（789 行）＋删 `r43_prod_check.js`／`r43_prod_render_check.js`／`r44_prod_check.js`／`r45_prod_check.js` ＝ **5 文件 +789 −289** |
| 三 | `74b1114` | `docs(conventions): 升 v1.46——r53 九事就地加注，旧数旧文一字不删（r53-7、r53-9）` | `docs/conventions.md`（**+40 −1**；其 −1 即版本行一处续写，**非版本行之删 0**） |
| 四 | `8c78b03` | `docs(delivery): skipper r52 追十二之就地加注（r53-2）` | `docs/delivery_skipper_r52.md`（**+17 −0**） |
| 五 | `12c0557` | `docs(delivery): r53 交付文档二本——skipper 与 vision` | `docs/delivery_skipper_r53.md`（646 行）／`docs/delivery_vision_r53.md`（565 行）＝ **2 文件 +1211 −0** |

★ **分法与 §九 一节所议逐项相同，故无「书其由」之事**（其条许我自定分法，我取其议）。
★ **`data/incoming/r53_kongzi_role/` 未提交**——照裁五十九（Sophia 备料俟 Vision 落定、合入之日重跑一过）。

### 三、★ **一节之末命「书其据上报」——我自核之果与领队加注 6 之据不同，据实勘正**

领队加注 6 代查而书：「**前例曾提交 `data/incoming/` 之备料**（如 `round51_peijue`／`round50_diwang` 诸批，**其合入之件内 `data/incoming/` 与 `data/csv/` 同批入库**）……**惟其俱是「合入之件」，备料与合入同批**。」
★ **一节之末并命「执行者仍请自核一过」。我自核之，其实与此不同**——

| 项 | 领队加注 6 之述 | 我之实测（求法随书） |
|---|---|---|
| 所举之二批 | `round51_peijue`／`round50_diwang` | ★ **全史无其物**——`git log --oneline --all -- 'data/incoming/round50_diwang*' 'data/incoming/round51_peijue*'` **零命中**；`git log --pretty=format: --name-only --diff-filter=A -- 'data/incoming/*'` 去重后所列之 31 路径内**无此二名**。**此二批从未入库。** |
| 真有之前例 | —— | `relations`（`ba4e65a`）／`round5`（`5ce8853`）／`round7`（`220a826`）／`fix7`（`b79ddd8`）／`round9`（`e7a126d`），**共 5 批**（求法：`git log --oneline --diff-filter=A -- 'data/incoming/*'`） |
| 其形 | 「备料与合入同批」 | ★ **反是**——其五批俱系**备料单独一提交**，且**提交信息内自书「本轮不合入」／「本轮待合入」**（`5ce8853`「Sophia，本轮不合入」、`220a826`「本轮不合入」、`b79ddd8`「本轮待合入」、`e7a126d`「本轮待合入」）；合入乃**另一提交**（`relations`→`c7ecb58`、`round5`→`c132489`、`round9`→`33e2ac9`），**且于合入之提交内将其备料之文件删去**（求法：`git log --oneline --diff-filter=D -- 'data/incoming/round5/*'` 得 `c132489`，`round9` 同得 `33e2ac9`） |
| 今存跟踪 | —— | `git ls-files data/incoming` 只 **`data/incoming/.gitkeep`** 一项 |

★ **故前例之实是：备料确曾单独提交（未合入即入库），其后于合入之提交内删去。** 与领队所述之「同批」相反。
★ **然一节之命本身不因此变**：其文明书「**若前例曾提交 incoming 之备料，书其据上报，本轮仍不提交**」——**前例既确曾提交，正落此款，我照其命「书其据上报、本轮仍不提交」**，未自行改办。
★ **只记不判**：前例之式（备料未合入即入库）与今之式（备料留工作区、俟合入之日同批）**何者当为常法**，属体例之事，**呈领队与站长裁**；裁五十九于本轮之办法已明，我守之。

### 四、二节「推前三核」——**俱符方推**

| # | 所命 | 所期 | 实测（五提交之后、推送之前） |
|---|---|---|---|
| 1 | `git ls-remote origin main` 仍 `ce708df` | 同哈希 | ✓ `ce708df6765570d93634e4293b2dbd54add84e7a` ／ `refs/heads/main` |
| 2 | `git status --porcelain` 只余 `data/incoming/r53_kongzi_role/` | 恰 1 项 | ✓ **恰 1 项**：`?? data/incoming/r53_kongzi_role/` |
| 3 | `git diff --stat ce708df HEAD -- data/` | **0 行** | ✓ **0 行**（求法：其输出之行数计得 `0`）。★ 照领队加注 4 之读法：**本轮十件无一动数据**，其非 0 即是越界——**今为 0，未越界** |

★ **并加一核（我自加，非所命）**：`python tools/validate.py` → **`OK：全部校验通过`、exit 0**，**推前推后各跑一过，二次俱同**（红线一：质量门任何合入前必过）。
★ **前置之核（推前，与领队加注 3 逐项对得上）**：`HEAD ce708df` ＝ `git ls-remote origin main`；工作区 **14 项**（`M`×5：`docs/conventions.md`／`docs/delivery_skipper_r52.md`／`docs/design/design_notes.md`／`site/app.js`／`site/styles.css`；`D`×4：旧四本 QA；`??`×5：`data/incoming/r53_kongzi_role/`／`docs/delivery_skipper_r53.md`／`docs/delivery_vision_r53.md`／`tools/qa/prod_data_invariants.js`／`tools/qa/vision_r53.js`）。

### 五、三节「推送与回填」——**实测，不预填**

| 项 | 实测 |
|---|---|
| 推送时刻（本机时区，照裁十四） | 起 **2026-09-26 12:06:59 -0400**，讫 **12:07:01 -0400**（`date` 前后各一读） |
| 推送之果 | `To https://github.com/QuinnyXu/chunqiu.git` ／ `ce708df..12c0557  main -> main`，**exit 0** |
| 远端哈希（推后实读） | `git ls-remote origin main` → **`12c0557050ed968196a7364b5cb6eb7b469e37f2`**，与本地 `HEAD` **同** |
| Actions run（**第一次**） | **id `36254304809`**，workflow `Deploy site to GitHub Pages`，event `push`，头 `docs(delivery): r53 交付文档二本——skipper 与 vision`；`createdAt 2026-09-26T16:07:03Z` → `updatedAt 2026-09-26T16:07:22Z`（**19 秒**）；**status `completed` / conclusion `success`** |
| Actions run（**第二次**） | ★ **系本追记之提交所触**——**其 id 与其果，俟本节提交并推送后于本节之末回填，本表不预填** |
| Actions 之告（非病） | 一条 annotation：`The ubuntu-latest label will migrate to Ubuntu 26 beginning October 19, 2026`（runner-images#14748）。**只记，本轮不动 workflow** |

### 六、四节「生产复验（带参破缓存）」——**二门俱过，未遇 ENOTFOUND**

| 门 | 跑法 | 所期 | 实测 |
|---|---|---|---|
| `tools/qa/prod_data_invariants.js` | `env -u QA_BASE_URL node tools/qa/prod_data_invariants.js` | **91 过 / 0 红** | ✓ **`=== 91 过 / 0 红（共 91 判）===`**，exit 0 |
| `tools/qa/prod_render_invariants.js` | `env -u QA_BASE_URL node tools/qa/prod_render_invariants.js` | **48 项、FAIL 0** | ✓ **`=== 合计 48 项，FAIL 0 ===`**，exit 0 |

★ **「不带 `QA_BASE_URL`」之实**：我以 `env -u QA_BASE_URL` **显式抽去**该变量（不倚赖其本不在），故二门俱走其默认之 `PROD`（`prod_data_invariants.js:78`、`prod_render_invariants.js:70` 俱 `const PROD = "https://chunqiu.timechorus.com"`；取基址之处在 `:504`／`:378`，`QA_BASE_URL` 空即取 `PROD`）。
★ **带参破缓存之实**（读其码而书，非凭其门头之自述）：二门俱于起手算一 `BUST`（`Date.now()` 连一随机串；`:79`／`:72`），文档之 URL 带 `?v=<BUST>`（`prod_data_invariants.js:706`、`prod_render_invariants.js:418`），数据之 URL 亦逐个带参（`:543` `meta.json?v=`、`:548` 各表 `?v=`）；渲染门并**逐个子资源改写其 URL 加参**（其门头 `:58` 所述）。
★ **未遇 `getaddrinfo ENOTFOUND`**——**二门俱一次而成，无重跑**。故领队加注 5 所重申之「遇 ENOTFOUND 重跑一次并记其事」**本轮无从用**；★ **门内未加一行重试之码**（裁五十七之分寸守住，门头 `:56`–`:63` 之「有意为之，勿补」未动）。
★ **数据门内二条「勘」之印记照留**（非病，系 r45→r50 之改判史）：`L_JIAGU.coord_certainty` 由「期空」改锁 `low`、`modern_location` 由「含『未定』」改锁「莱芜」——**其原所期之文照留于输出内**，合「判定史留原貌」。
★ **渲染门之反证一路仍活**：七层俱「注入对照当场红、撤去注入复归在位」，**证其所量者系真渲染之果，非门之自说**。

### 七、已知限度（如实）

1. **Actions 之第二次系本追记所触，其果我须推后方能读**；若其非 `success`，**我停下上报，不自行重推**。
2. **生产复验之二门跑于第一次部署完成之后**（`updatedAt 16:07:22Z`，我跑于其后），故其所量者系 `12c0557` 之本；★ **本追记只动 `docs/`，不改 `site/` 与 `data/`**，故二门之果**不当因之而变**——但**我不预断第二次部署后之果**，若站长欲其证，可于第二次 run 成功后再跑一过（其法同上）。
3. **`.git/index.lock` 之「派件之时其在」我未亲见**——其据系 §八丙之嘱与 §九〇节之文（俱书 2026-09-25 21:25 EDT、0 字节、Co站长之 shell 所留）。★ **我所亲见者只「12:03 -0400 其已不在」**；**二者之别本节已分书，未混为一谈。**
4. **§三之勘正我只核「其批是否入过库」与「其形是否同批」二事**，未核领队何以记得 `round50`／`round51` 之名（或系与 `team/` 内之未跟踪备料相混——`team/` 不入公开仓库，其内之物不在 `git log` 内）。★ **此一猜我不写入断言，只列于此为限度。**
5. **本轮未合入任何数据**，`data/csv/` 一字未动（`git diff --stat ce708df HEAD -- data/` 得 0 行）；Sophia r53-8 之备料仍留工作区未跟踪，**其合入照裁五十九另议**。

### 八、上报事项（不代裁）

1. ★ **领队加注 6 之据有误，已据实勘正**（见 §三）——所举 `round50_diwang`／`round51_peijue` 二批**全史未入库**；真前例五批之形**与所述相反**（备料单独提交、其后于合入之提交内删去）。★ **其命（本轮仍不提交）我照办未变**；**惟「备料之提交之常法」当由站长定其体例**：是取前例之式（备料单独入库、合入时删）抑取今之式（备料留工作区、俟合入同批，裁五十九）。★ **此正是「派件之事实须实读」之一例**——领队代查之据，执行者自核而勘之，账上留其错误史。
2. **Actions 之第二次未回填**（§五之表已明标）——本节推送之后当即于本节之末补。
3. **`_to_delete_20260925_qa_merge/`（裁六十，可清、不急）本轮未清**——其在 `team/` 下，`team/` 不入公开仓库，**与本轮推送无涉**；★ 只记其仍在，俟领队排期。
4. **交接备注**：远端 `main` 今为 **`12c0557`**（推后实读），部署 run `36254304809` **success**。`docs/delivery_skipper_r53.md` 末次勘注之日 **2026-09-26**（本节即其末）。★ **下一执行者若再动本文件，须自本节之后另起，不得并入其内**（照 §八之 7 所立之则）。
5. **r54 之候排诸项**（照 §八乙之表并 §九之外诸裁，我只转录不增减）：`b.title` 之治（裁五十三）、`.innerHTML` 余 3 处（裁五十四）、穿井获羊补录（裁六十二）、择字之粗之全库专求（裁六十三，俟谓词对齐）、Sophia 备料之合入（裁五十九）、`title` 一路未治之第三款填数（裁六十九③）。
