# 交付说明 · Vision 第三轮（三屏静态原型）

日期：2026-07-17。交付物：`docs/design/design_notes.md`（风格定稿）、`site/assets/icons/`（15 分类图标 + 6 主角徽记）、`site/assets/map/base_map.svg`（分层底图）、`site/`（index.html / app.js / styles.css 三屏原型）。原临时预览页已整体替换。

## 前置说明：数据合入

Vision 开工前，Sophia 增量仍在 data/incoming/，places 无坐标，"地图真实可用"验收无法达成。故本轮先以 Skipper 身份执行了 CHANGES.md 全部合入与修正（单独提交 `1d7973e`），随后 site/data/ 重新生成。合入后六位主角数据全部到位，选人屏的"资料整理中"占位态因此在当前数据下不会出现，但逻辑仍在（人物缺席时自动降级）。

## 验收对照

- `python -m http.server` 后访问 `/site/` 即用；全部资源本地化，无外部依赖，字体走系统栈。
- 六位主角（不止四位）时间线与地图均真实可用：事件数 文姜12 / 桓公7 / 齐襄10 / 昭公9 / 庄公11 / 隐公6。
- 投影三样例逐一验证：临淄→(831,180)、曲阜→(699,312)、新郑→(373,442)；app.js 内置 console.assert 校验。
- URL hash 全状态（如 `#person=P_WENJIANG&view=map`），前进后退可用；`<details>`、按钮化卡片与锚点保证键盘可达；680px 以下单列布局、地图横向滚动。
- 史料文本全部来自 site/data/*.json；代码中仅界面文案与设计配置（人物-颜色-徽记映射、分类-图标映射）。

## 已知问题

1. **列国底色为艺术示意**，用椭圆色块表大致势力范围，非考据疆界（地图脚注已声明）。郑/许、齐/纪 存在有意重叠。
2. **水系为大势简化**：河水取春秋故道（东北入海）、济水经泺临淄之北，走向对照通说，但转折点为手绘平滑，未逐点考订。
3. **轨迹语义**：地图轨迹连接"相关事件"地点，含人物未必亲至的事件（如文姜在 E036 拟婚条挂临淄）；如需严格"亲至"，需在 event_people 增加到场性字段，留待下轮。
4. **轨迹播放**为逐站跳转（1.6s/站）而非平滑移动动画；已含暂停。
5. **同年多事件排序**按事件 ID 兜底，个别同年事件先后（如 E037/E038/E039）依赖 ID 顺序恰好正确，未做月序字段解析。
6. 图标为一版线描初稿，「弑杀/丧葬」「会盟/礼俗」的近义区分建议下轮让领队过目定稿。
7. 无暗色主题（设计决策，见 design_notes 第7节）。
8. 数据侧遗留：E002 importance 是否升 1（隐公线主线关键）仍待领队裁定；「未定位地点」侧栏当前无数据触发（全库地点均已有坐标），逻辑已备。
