# 经纬春秋 · 视觉设计定稿（design_notes v2.1）

作者：Vision。适用范围：site/ 各屏、site/assets/icons/、site/assets/map/base_map.svg。
版本：**v2.1（2026-08-02：r24a 补批两项，领队裁定 ②）**——① **诗歌层功能色换色相（§2.4 结案）**：新立史料层专用变量 `--poem` = `#63722F`「**菉**」（《诗经·小雅·采绿》「終朝采綠」，毛传「綠，王芻也」＝荩草，先秦染黄绿之草），取代旧值青绿 `--bronze` `#44766B`。旧值与郑国色 `#35706A` 仅 ΔE76 **4.39**，郑人物页的国色与《诗经》引文左线/徽标同屏读作同色；换色后**对郑 37.52**，且对「九国色 ＋ 其余全部功能色」共 30 项**最紧 22.39（陈）、全部 ≥13.2**，双底线 对绢帛 4.53 / 对白 5.28 皆达标。**只换色相不换重量**：L\* 45.5（旧 46.0）、灰阶 108（旧 109）。复算脚本 `tools/qa/color_matrix_r24a2.js`（色值从源文件实读，不另抄）。② **全景图默认改 27 主角环 ＋「显示全部」开关（§2.7 结案，即 r24a 三选项之 C）**：默认只画 27 主角，槽距由 **12.87 升至 58.55**（＞节点直径 30），同弧徽记不再压边；勾选「显示全部」回到 123 人全环，两态截图与实测入 §2.7。③ 其余低于 13.2 的国色×功能色对（齐×朱砂 8.14、秦×玄墨 8.83、鲁×暖赭 9.21，及本批扩大清单后新见的关系线/悬停态诸对）**按裁定记观察项、本批不改**，清单见 §2.4。④ **版本记录体例增补**（同裁定 ②）：**同轮补批归入同一版本条目，跨轮才起新号**，全文见下方「版本记录体例」段——本条自身即依此例并入 v2.1，不另起 v2.2。⑤ **新立 §7「前端体例：DOM 关系锚定与自验方法」**（r24a-fix2 收尾裁定 ②③）：§7.1 立「同族三相」条目——**病根都是 DOM 关系未锚定在身份与状态上**，三相各举本族已发生之实例与正解（按 id 取节点／状态为唯一真源／派生式重挂），并记明本族**已断根**；§7.2 把「按类反证 ＋ 交叉验证」写成可复用方法。原「§7 明确不做」顺延为 **§8**（全文一字未改）。本项属同轮（r24a）补批，故回填本条、不另起版本号。详见 delivery_vision_r24a §十一（补批）、§十八（体例增补）、§二十一（收尾落档）。
历史：v2.0（2026-08-02：色彩制度换血——「两级国色系」退役，立「国色制」（r24a，Xu 提案·领队裁定）**——① **制度全文见 §2.0**：「**国色定色、徽记定人、纹理定轨**」，一国一色、国内所有人物同色，个人身份由徽记承担，多轨同屏由线型＋端点形状＋徽记小签承担。② **九国色定调（§2.1）**：按裁定 3 新判据（九色两两 ΔE76 ≥13.2；对绢帛 ≥3.5、对白 ≥4.1 双底线；不再有族内人色判据）全量实测，**九色现值全部达标且余量充裕**（最紧对 郑×陈 17.32、最低对绢帛 4.90、最低对白 5.71），故**定调为零位移**——九色一字不改，读者认知迁移成本为 0。ΔE76 全矩阵与双底线逐色报数入 §2.1。③ **旁通道实测（§2.2）**：九色在三型色觉模拟与灰度轴上必有重合（红色盲 齐×晋 2.61、绿色盲 卫×宋 1.85、蓝色盲 郑×陈 5.16；去色后 郑≡鲁 灰阶 102、宋≈晋 77/78、楚≈秦 60/61），并给出**物理不可能证明**：双底线夹出的可用灰阶带仅 [45,125] 共 80 阶，九色两两 Δ灰阶 ≥13 需跨度 104 > 80。**这正是制度把辨识职责交给徽记与线型的根据，不是取色失误。** ④ **个人色退役（§2.3）**：`styles.css` 26 个 `--p-*` 变量全数删除，收敛为 9 个 `--state-*`；退役映射表（人→原色→现国色→位移 ΔE76）逐人留档供回溯，26 人位移均值 12.41、6 人零位移（原个人色即族基色）。⑤ **附带收益**：旧制个人色灰阶跨 33–125，鲍叔牙 33 低于正文玄墨 42、在细轨迹上读作黑线（v1.15 已记明的代价）；新制九色灰阶收在 60–102，**全部高于玄墨，无一读作黑线**。⑥ 并观线型改制见 §6.6（甲长划／乙珠点）、并观选择交互重构见 §6.7、全屏播放控件移位见 §6.8；第 16 类「政制」图标与 og-card 重出见 §4、§5.7。详见 delivery_vision_r24a。**推导依据文档：`docs/design/qi_palette_expansion.md`（齐系色板扩容调研，本制度的直接前置论证，随本轮入库存档）。**）
历史：v1.15（2026-07-31：**三贤上线（鲍叔牙/曹刿/介之推）＋齐系容量见底＋分享卡改按国色家族计（r22）**——① 三新阶：**鲍叔牙**`--p-baoshuya`=`#361814`（齐系第六阶，灰阶33）、**曹刿**`--p-caogui`=`#695713`（鲁系新阶，灰阶88，赭黄向黄扩谱至47°）、**介之推**`--p-jiezhitui`=`#9C6E56`（晋系族最亮阶，灰阶120）；三色对全库最紧 ΔE76 分别 17.6／17.0／18.3，**皆优于基准线 13.2**，26 人跨族最紧对仍是既有的鲁庄公×晋文公 13.2（未被本轮打破）。② **齐·红系容量见底的实测证明**（§2.1 新立）：亮端受「对白 ≥4.1」物理封死、暗端仅余 [13,34]、中间无 ≥26 空隙——第七位齐主角无解，须先扩谱或重排。③ 三枚徽记全取《左传》层：鲍叔牙＝**税械**（及堂阜而税之）、曹刿＝**辙**（下视其辙、视其辙乱）、介之推＝**文之渐隐**（言，身之文也；身将隐，焉用文之）；介之推**明确不取焚山/寒食**（非左传层且与左传情节互斥）。④ **分享卡色签由「每主角一枚」改为「每国色家族一枚」**（§5.7 改写）：增长源由「主角数」变为「国色家族数」，属构图承重改动、非排版微调。详见 delivery_vision_r22。）
历史：v1.14（2026-07-31：**陈/宋两国色家族新设＋夏姬/宋襄公上线＋编者层标前端体系（r21）**——① **陈·宛丘泽碧系**（`--state-chen` `#3B6A48`，色相137°/灰阶96/对绢帛5.40）与**宋·玄鸟绀系**（`--state-song` `#4F457F`，色相250°/灰阶77/对绢帛7.24）新设，两族各一主角、色取族基色（同晋文公/秦穆公/楚成王「单人族」先例），主角计23、国色家族计九。② 两枚新徽记：**夏姬＝四徙之途**（五点四折之途，起止各缀一珠——郑→陈→楚→郑→晋，只记其行止不作评断）、**宋襄公＝泓上之旆**（旗杆一竿、旆三游右扬，杆下泓水一道横流）。③ **编者层标 `.q-caveat` 体系**（§3.6 新立）：史料研究员写在 `modern_note` 首段的【…】分层处置说明（【归罪话术层·非事实判断】【P 层舆论材料·不作史实用】），由前端提取为引文**之上**的暖赭告示条——旧渲染把它拼进 footer 小字，致归罪话术以正文体量呈现、免责标注以脚注体量呈现，主次倒置。④ **presence 措辞分寸订正**：站内解释性文案一律改「史文无其在场明文」，不再写「本人不在场／本人未到」——「相关」是史文没说他在，不是史文说他不在。⑤ **人物卡流向 chip 由「首末两国」升级为 state 全链**（夏姬「郑→陈→楚→晋」、息妫「陈→息→楚」）。详见 delivery_vision_r21。）
历史：v1.13（2026-07-28：**管仲上线——第21主角，齐系第五阶（r20）**——齐·红系新增第五阶 `--p-guanzhong`=`#C24A3A`（灰阶≈114/对绢帛≈4.16，族最亮阶），与齐组已有四人皆深浅可辨：Δ灰阶 文姜14／僖公31／桓公47／襄公67，齐系五阶 47/67/83/100/114（齐组现5人，两级色系迄今最挤一族，插第五阶只能向上扩展——偏暗方向自襄公47再降仅得Δ9不达标且近黑失红相，故取族最亮阶）。徽记＝**衡·轻重**（天平梁＋双盘，扣管仲「轻重之术」权衡轻重之义；`badge_guanzhong.svg`，48×48 圆框线描 currentColor，形远于齐桓连环三盟与会盟之鼎，一眼可分）。孔子论管仲随 E046/E068/E071 入时间线，为「后世评价分层」（T 层「经义异闻」乌梅紫徽标）首秀。详见 delivery_vision_r20。）
历史：v1.12（2026-07-27：**卫国色相家族新设＋五女主角上线＋轨迹降级机制＋阵营着色对齐（r19b）**——① **卫·缟素靛青系**新设（`--state-wei` `#2F5480`），庄姜/宣姜居之，靛青偏蓝一相与郑之青绿拉开；息妫入楚系新阶、骊姬入晋系新阶、穆姬入秦系新阶，同族深浅 Δ灰阶≥13 照旧（见 §2 廿人色值总表）。② 五枚新徽记（庄姜=双燕·燕燕于飞、宣姜=新台临河、息妫=桃花·桃花夫人、骊姬=觚·鸩·鸩酒嫁祸、穆姬=薪火·登台履薪），48×48 圆框线描 currentColor，与旧十五枚同语言、形各异。③ **轨迹降级通用机制**：亲至可落图地点<2 者，单人地图与并观（双方皆<2 时）播放按钮替换为静态说明「亲至可考不足两地，暂无轨迹可播——已知地点如图。」（左朱线胶囊），单点定位与事件正常显示；现库降级三人：穆姬/庄姜/宣姜。④ **首页卫分区**新设（`HOME_BADGE_POS.卫`），卫色块可点、落庄姜/宣姜双徽簇；跨国卡片流向改「出身→归宿」首末国（息妫「陈/息/楚」→「陈→楚」）。⑤ **关系全景阵营着色对齐**：`panoStateKey()` 按「主要发生国」（meta.home 覆盖）排位，使节点色与阵营弧一致（庄姜/宣姜归卫弧、息妫归楚弧、穆姬归秦弧、武姜归郑弧，修正旧武姜郑色落申槽）。详见 delivery_vision_r19b。）
历史：v1.11（2026-07-26：**交会表现层简化＋速度定稿＋全屏控件定稿（r18）**——① 交会由「自动停拍＋脉冲闪烁」改为「常亮留痕」：播放中标记经过的交会点点亮并常亮至结束/重置，未经过者呈待亮态（淡、细、无标签），行进全程不打断；`holdUntil` 停拍机制退役、脉冲动画移除。② 速度定稿为唯一 0.5×：删除 0.5×/1× 档位按钮（`#btn-speed`/`#cmp-speed`/`#ov-speed`）与切档代码、清理 `localStorage` 速度键 `cq_play_speed`；单人与并观同速。③ 全屏播放控件定稿为仅「播放/暂停」（`#ov-play`，左下角、触屏热区 ≥44px），委托主按钮、文案主/浮层同写、全屏内外无缝双向同步；单人×桌面/手机、并观×桌面/手机四组合覆盖。详见 §6.6 与 delivery_vision_r18。）
历史：v1.10（2026-07-21：**并观年位同步根修＋全屏播放悬浮控件（r17b 返工）**——双轨播放主钟由「纯绝对年」改为「交会锚故事进度 sc」：标记位置成 sc 的纯函数，每个交会地＝一整数 sc 锚点、两人在该 sc 均钉到交会地，从构造上保证「钟到交会刻⇔人到交会地」，根治旧纯年映射在**同年多交会地**（文姜×齐襄公前694 泺＋临淄）处的塌缩；配套 `tools/test_binguan_play.js` 状态机测试（合成 Δt 驱动真引擎，五交会锚断言两标记落位全过）。地图全屏浮层加播放/暂停＋速度悬浮控件（单人并观同用，委托主按钮、并入共享速度组）。详见 §6.6 与 delivery_vision_r17（r17b 段）。）
历史：v1.9（2026-07-21：**播放引擎合一（清偿 r16b 技术债 TD-r16-01）**——单人地图播放与双人并观播放合并为唯一状态机 `player`（app.js），单人＝单轨模式、并观＝双轨模式，仅由注入的 `cfg` 区分；删除旧 `cplay`。主钟改「增量累加」（每帧 `elapsed += min(Δt,100ms) × speed`）：暂停冻结主钟、续播不追赶、交会自动暂停期间同样不推进——从根上消除「汇合点后突然加速」与切后台回来突进。**速度档 0.5×/1×**（默认 0.5×，`localStorage` 键 `cq_play_speed` 记忆；单人 `#btn-speed`／并观 `#cmp-speed` 共用同值、互相同步显示），只缩放每帧 Δt、不改内容时长，切档即时生效；默认观感约为旧速一半，1× 即旧速。**交会显示修正**：a 级同场与 b 级同年同地**同权**自动暂停一拍并脉冲高亮——旧版仅 b 级触发致 a 级同场在播放中漏亮；改为按交会年成组触发，一年内多处交会一并高亮、只停一拍，交会侧栏与地图标亮一一对应。文姜×齐襄公全程走查：泺·临淄（前694）→临淄相邻记载（前693）→禚（前692）→祝丘（前690）→防（前687）连停五拍。见 §6.6 与 delivery_vision_r17。）
历史：v1.8（2026-07-21：**双人并观（compare）上线**——第七种视图，hash `#compare=P_A,P_B`。两处入口：关系图两人关系卡「并观其迹」（内嵌右卡＋全屏抽屉两处）、人物地图工具条「添加对照人物」小面板。视觉：两人各用其国色主题色，同国深浅相近者副轨叠**点划线**纹理＋方点（甲圆·乙方，形状冗余对齐 §2 色盲双通道原则）；交会两级——**同场**（同事件同地，暖赭金环）与**同年同地**（异事件，朱砂虚环）；绝对年轴含两人生卒条＋生卒刻度＋交会标记＋播放游标；免责口径「同城未必同时，不能仅据同年同地断定相遇」常驻交会侧栏顶部与弹卡内。全屏、抽屉沿用「容器统一律」（复用 `#map-overlay`＋`bindZoomGesture`）；手机端交会侧栏折叠为底部可展开条。播放为「按绝对年推进」，复用 `easeInOut` 缓动与 RAF/字幕/暂停语义，未改动既有单人播放引擎。检测细则严格对齐 `docs/binguan_fixtures.md` 三条护栏，见 delivery_vision_r16b。见 §6.6。）
历史：v1.7（2026-07-20：站名更为「经纬春秋」（副题「左传为经，诸书为纬，牵系有据」，头部时间双标＝春秋纪年时段静态＋考订前沿动态小字）；楚庄王线上线——第15主角，楚系绛紫提亮一阶 `--p-chuzhuang`=`#8A4062`（灰阶≈90/对绢帛≈6.0，与楚成王 63/9.5 深浅可辨、Δ灰阶27）、徽记＝**问鼎**（三足两耳宝鼎、腹饰一带——「问鼎中原」，`badge_chuzhuang.svg`，形别于成王凤纹）；「重看引导」落位修复（隐藏视图残留元素致左上角 bug，改按「元素已布局出真实盒」判就绪）。见 Vision r15 交付说明）。
历史：v1.6（2026-07-20：两级色彩系统（裁定1）——十四主角色改按六国色家族分阶「国色定相、人色定阶、徽记定形」，全部人物色集中为 styles.css :root 的 `--p-<id>`/`--state-<国>` 变量单点管理；关系全景按国着色＋国别底晕形成齐鲁郑晋秦楚阵营分区；见 §2 十四人色值总表与 delivery_vision_r14）。
历史：v1.5（2026-07-19：西扩秦楚上线——秦穆公=西土赭+崤函之险徽记、楚成王=江汉苍绿+凤纹徽记（十四主角）；引文分层徽标体系（§3.5）；西部地形与秦楚色块）。
历史：v1.4（2026-07-18：首页地图导航（详见 home_map_notes.md）；武姜/祭仲/齐僖公主题色与徽记；人物子导航升格；底图增晋与汾水）。
历史：v1.3（全站搜索；分享卡生成器构图定稿；底部分享行与邮件反馈入口）；v1.2（导航两级层级与人物子导航；hash 新结构；关系并线与数字徽记；桌面全屏抽屉浮卡）；v1.1（晋文公主题色与徽记定稿；选人页按国分组与国别选项卡；姓名行显示样式）；v1.0。

**版本记录体例（r24a-2 领队裁定 ② 立）**：**同轮补批归入同一版本条目，跨轮才起新号。** 版本记录是给读者看的粗粒度叙事，git 才是逐笔账本，两者本就不该同粒度。本文件头部的「版本／历史」行与关于页 `#/about` 的版本列表同守此例：一轮之内无论分几批提交、补几项裁定，一律**回填**同一条目，不因补批另起号——r24a 补批两项（`--poem` 换色相、27 主角环）即并入本文档 **v2.1** 与站点 **v0.14**，不另起 v2.2／v0.15。要逐笔账去 `git log` 与 `docs/delivery_vision_r<轮次>.md` 查，那才是该细的地方。**本条自身依此例**：它是 r24a 同轮的体例增补，故就地并入 v2.1 条目（见其 ④），不另起新号。

## 1. 风格立场

**"简帛与青铜"，不是"仿古滤镜"。** 春秋气质来自三样东西：绢帛的底、墨与朱的书写、青铜的线。我们只取材质的色与线条的秩序，不做纹理贴图、不做描边金框、不做毛笔字特效。整体原则：**克制、清晰、留白**；装饰只出现在徽记与图标的线条本身。

## 2. 色板

| 角色 | 色值 | 出处意象 |
|---|---|---|
| 绢帛底 | `#F4EDDF` | 出土帛书底色 |
| 卡面/弹层 | `#FBF7EC` | 生绢 |
| 玄墨（正文） | `#2E2A24` | 墨书 |
| 次级文字 | `#7A7166` | 淡墨 |
| 界线 | `#DCD2BC` | 帛书栏线 |
| 暖赭（主强调） | `#B4652F` | 漆器赭 |
| 青绿（次强调） | `#44766B` | 青铜锈 |
| 朱砂（警示/加重） | `#BC4433` | 朱批 |
| 菉（诗歌层专用，v2.1） | `#63722F` | 荩草（《诗经》染草） |

### 2.0 国色制（v2.0 立系，r24a）—— 「国色定色、徽记定人、纹理定轨」

**这是全站色彩制度的一次性换血，取代 v1.6–v1.15 的「两级国色系」。个人色（`--p-<id>`）自本版起全数退役。**

三句制度：

| 通道 | 承担者 | 说明 |
|---|---|---|
| **国色定色** | 9 个 `--state-<国>` 变量 | 一国一色，**国内所有人物同色**。凡涉人物着色处（选人卡顶条与徽记、人物子导航、时间线加重、地图轨迹、关系 ego/全景节点、并观双轨、分享卡色带）一律取所属国色。 |
| **徽记定人** | 27 枚 48×48 圆框线描徽记 | **个人身份的唯一色外锚点**，形状恒定不变、不随色改。旧制中徽记已是「辨识第一锚点」，新制把它升为**唯一**逐人通道。 |
| **纹理定轨** | 线型＋端点形状＋徽记小签 | 多轨同屏（并观）的区分职责，由色彩移交纹理：甲轨长划线＋圆端点、乙轨珠点线＋方端点（见 §6.6）。 |

**制度动因**：旧制把「逐人可辨」压在色彩一条通道上，而色彩容量有物理天花板——`docs/design/qi_palette_expansion.md` 实测证明齐系第七人在旧判据下无解（可行解全部落在灰阶 ≤20 的近黑区）。国色制不是绕过这个天花板，是**取消这条约束**：色彩此后只回答「哪一国」，不再回答「哪一个人」，容量问题随之永久消解——新增主角不再消耗任何色彩额度，唯新立一个国色家族才占一色。

**代价与其接受依据（领队裁定 1，Xu 已明示接受）**：同国两人并观时双轨色相同。此代价由 §6.6 的线型＋端点形状＋徽记小签三重冗余承担，验收判据为「同国对（文姜×齐襄公）**灰度截图**下双轨肉眼立辨」。

**旧制退役条款**（勿沿用）：「人色定阶」「族内相邻 Δ灰阶 ≥13」「族内 ΔE76 判据」「郑的青—绿双相」「鲁的赭黄向黄扩谱」等旧判据**全部失效**，不再作为新增主角的前置条件；新增主角**不需要任何取色工作**，只需一枚徽记。

> 地图示意国色（`base_map.svg` 的 `layer-states` 淡色块）是另一套「示意疆域」层，与本节人物国色系**同名不同用、不同源**（如齐示意色为青灰、楚为苍绿），本轮不改。

### 2.1 九国色定调与判据实测（裁定 3）

**判据（裁定 3，取代 v1.15 §2.1 旧文三条）**：① 九国色**两两** ΔE76 ≥ **13.2**；② 对绢帛 ≥ **3.5**；③ 对白 ≥ **4.1**（`.person-nav button[aria-current="true"]` 国色底白字所系）。**不再有族内人色判据。**

**定调结论：九色现值全部达标且余量充裕，故取零位移——九色一字不改。** 任务书授权「可尽量沿用各族现有代表色相以降低读者认知迁移成本，数据说话」；数据说的是现值已过，则最优解即是不动（迁移成本 0）。九色逐色报数：

| 国色家族 | 变量 | 色值 | 人数 | 灰阶 | 色相 | 饱和 | 对绢帛 | 对白 |
|---|---|---|---|---|---|---|---|---|
| 齐 · 红系 | `--state-qi` | `#A5322A` | 6 | 91 | 3.9° | 59% | 5.83 | 6.80 |
| 鲁 · 赭黄系 | `--state-lu` | `#97561F` | 4 | 102 | 27.5° | 66% | 4.92 | 5.73 |
| 郑 · 青绿系 | `--state-zheng` | `#35706A` | 5 | 102 | 173.9° | 36% | 4.90 | 5.71 |
| 晋 · 栗褐系 | `--state-jin` | `#74402C` | 3 | 78 | 16.7° | 45% | 7.17 | 8.36 |
| 秦 · 玄灰系 | `--state-qin` | `#423C39` | 2 | 61 | 20.0° | 7% | 9.31 | 10.84 |
| 楚 · 绛紫系 | `--state-chu` | `#5E2B45` | 3 | 60 | 329.4° | 37% | 9.45 | 11.01 |
| 卫 · 缟素靛青系 | `--state-wei` | `#2F5480` | 2 | 82 | 212.6° | 46% | 6.67 | 7.78 |
| 宋 · 玄鸟绀系 | `--state-song` | `#4F457F` | 1 | 77 | 250.3° | 30% | 7.24 | 8.43 |
| 陈 · 宛丘泽碧系 | `--state-chen` | `#3B6A48` | 1 | 96 | 136.6° | 28% | 5.40 | 6.29 |

**双底线实测**：对绢帛最低 **4.90**（郑）＞底线 3.5，余量 1.40；对白最低 **5.71**（郑）＞底线 4.1，余量 1.61。九色无一贴线。

**ΔE76 全矩阵**（CIE Lab D65/2°；判据 ≥13.2）：

| | 齐 | 鲁 | 郑 | 晋 | 秦 | 楚 | 卫 | 宋 | 陈 |
|---|---|---|---|---|---|---|---|---|---|
| **齐** | — | 26.43 | 75.83 | 28.45 | 54.77 | 44.27 | 75.65 | 69.25 | 73.12 |
| **鲁** | 26.43 | — | 61.98 | 22.33 | 47.36 | 50.60 | 74.02 | 73.99 | 54.41 |
| **郑** | 75.83 | 61.98 | — | 49.15 | 29.23 | 50.87 | 35.33 | 50.32 | **17.32** |
| **晋** | 28.45 | 22.33 | 49.15 | — | 27.64 | 29.11 | 54.22 | 53.43 | 46.15 |
| **秦** | 54.77 | 47.36 | 29.23 | 27.64 | — | 26.06 | 32.85 | 38.89 | 32.31 |
| **楚** | 44.27 | 50.60 | 50.87 | 29.11 | 26.06 | — | 35.71 | 28.12 | 56.78 |
| **卫** | 75.65 | 74.02 | 35.33 | 54.22 | 32.85 | 35.71 | — | **17.97** | 50.38 |
| **宋** | 69.25 | 73.99 | 50.32 | 53.43 | 38.89 | 28.12 | **17.97** | — | 63.45 |
| **陈** | 73.12 | 54.41 | 17.32 | 46.15 | 32.31 | 56.78 | 50.38 | 63.45 | — |

36 对全部 ≥13.2；**最紧三对**为 郑×陈 17.32、卫×宋 17.97、鲁×晋 22.33，最紧者仍高出判据线 4.12。

**附带收益（旧制代价的清偿）**：旧制个人色灰阶跨 33–125，其中鲍叔牙 `#361814` 灰阶 33 低于正文玄墨（灰阶 42），v1.15 已记明「在并观地图的细轨迹上读作黑线」。新制九色灰阶收在 **60–102**，**全部高于玄墨**，无一在细轨迹上读作黑线；亮端亦无一逼近 125 的对比悬崖（最亮 102）。**这不是本轮刻意求得的，是取消族内分阶后灰阶自然收拢的结果，记此备核。**

### 2.2 旁通道实测：色觉模拟与灰度容量 —— 判据为何只到这里

裁定 3 只用正常色觉下的 ΔE76 与双对比底线，**不含灰度轴、不含色觉模拟**。这不是遗漏，是实测后的必然。

**a. 三型色觉模拟**（Viénot-Brettel-Mollon 1999 线性 sRGB 矩阵，模拟后再算 ΔE76）——九色 36 对中低于 13.2 者：

| 类型 | 低于 13.2 的对数 | 最紧三对 |
|---|---|---|
| 红色盲 protan | 4 / 36 | 齐×晋 **2.61**、卫×宋 8.01、齐×陈 11.52 |
| 绿色盲 deutan | 3 / 36 | 卫×宋 **1.85**、秦×楚 6.57、齐×鲁 7.74 |
| 蓝色盲 tritan | 6 / 36 | 郑×陈 **5.16**、秦×宋 9.14、晋×楚 10.07 |

**b. 灰度轴**——九色去色后的灰阶序列 楚60 / 秦61 / 宋77 / 晋78 / 卫82 / 齐91 / 陈96 / 郑102 / 鲁102，三处近乎重合：**郑≡鲁 Δ0、宋≈晋 Δ1、楚≈秦 Δ1**。

**c. 物理不可能证明（关键）**——上述重合**无论怎样重排九色都消不掉**：

- 双底线（对白 ≥4.1 且 对绢帛 ≥3.5）夹出的灰阶上限为 **125**；下限取正文玄墨之上（灰阶 42，低于它即在细轨迹上读作黑线），实用下限 **45**。可用带 **[45, 125]，跨度 80**。
- 九色两两 Δ灰阶 ≥13（旧制的色盲双通道底线）需要跨度 8×13 = **104 > 80**，**物理不可能**。
- 逐档实算容量 `floor(80/step)+1`：step=13 → 7 色、step=12 → 7 色、step=11 → 8 色、step=10 → 9 色（恰好，且须精确等距、两端顶死，无任何容差）。

**结论**：九个国色挤在一条 80 阶的灰度带上，旁通道重合是数学必然而非取色失误。**故辨识职责必须、也只能移交给形状与纹理通道**——这正是「徽记定人、纹理定轨」两句的根据。反过来说：**任何日后试图把「逐国灰度可分」重新写回判据的提议，都必须先回答这 80 阶怎么装下九色。**

复现脚本口径与 §2.1 同源（sRGB→线性→WCAG Y→回伽马×255 计灰阶；ΔE76 取 CIE Lab D65/2°），与 `qi_palette_expansion.md` §1.1 一字不差。

### 2.3 个人色退役映射表（供回溯）

`site/styles.css` `:root` 的 **26 个 `--p-<id>` 变量全数删除**；`site/app.js` `resolveProtoColors()` 改为按人物所属国（`panoStateKey()`，即 `meta.home` 覆盖优先、否则 `state` 首国）解析国色。下表留档旧值，供回溯与历史截图比对：

| 国 | 人物 | 退役变量 | 原个人色（灰阶） | 现国色（灰阶） | 位移 ΔE76 |
|---|---|---|---|---|---|
| 齐 | 文姜 | `--p-wenjiang` | `#B23A2F` (100) | `#A5322A` (91) | 4.34 |
| 齐 | 齐襄公 | `--p-qixiang` | `#571A15` (47) | `#A5322A` (91) | 29.97 |
| 齐 | 齐桓公 | `--p-qihuan` | `#7B2519` (67) | `#A5322A` (91) | 14.76 |
| 齐 | 齐僖公 | `--p-qixi` | `#963026` (83) | `#A5322A` (91) | 5.88 |
| 齐 | 管仲 | `--p-guanzhong` | `#C24A3A` (114) | `#A5322A` (91) | 9.61 |
| 齐 | 鲍叔牙 | `--p-baoshuya` | `#361814` (33) | `#A5322A` (91) | **47.04**（最大） |
| 鲁 | 鲁隐公 | `--p-luyin` | `#A5692A` (118) | `#97561F` (102) | 7.99 |
| 鲁 | 鲁桓公 | `--p-luhuan` | `#97561F` (102) | `#97561F` (102) | **0.00** |
| 鲁 | 鲁庄公 | `--p-luzhuang` | `#5E3F19` (69) | `#97561F` (102) | 23.50 |
| 鲁 | 曹刿 | `--p-caogui` | `#695713` (88) | `#97561F` (102) | 23.59 |
| 郑 | 郑庄公 | `--p-zhengzhuang` | `#204A3F` (67) | `#35706A` (102) | 16.05 |
| 郑 | 郑昭公 | `--p-zhengzhao` | `#3F7A6C` (112) | `#35706A` (102) | 6.05 |
| 郑 | 武姜 | `--p-wujiang` | `#47859B` (125) | `#35706A` (102) | 19.21 |
| 郑 | 祭仲 | `--p-jizhong` | `#2C5E6A` (87) | `#35706A` (102) | 13.73 |
| 郑 | **子产** | （从未有个人色） | — | `#35706A` (102) | — |
| 晋 | 晋文公 | `--p-jinwen` | `#74402C` (78) | `#74402C` (78) | **0.00** |
| 晋 | 骊姬 | `--p-liji` | `#9B4E38` (100) | `#74402C` (78) | 14.45 |
| 晋 | 介之推 | `--p-jiezhitui` | `#9C6E56` (120) | `#74402C` (78) | 18.34 |
| 秦 | 秦穆公 | `--p-qinmu` | `#423C39` (61) | `#423C39` (61) | **0.00** |
| 秦 | 穆姬 | `--p-muji` | `#6E6560` (103) | `#423C39` (61) | 17.70 |
| 楚 | 楚成王 | `--p-chucheng` | `#5E2B45` (60) | `#5E2B45` (60) | **0.00** |
| 楚 | 楚庄王 | `--p-chuzhuang` | `#8A4062` (89) | `#5E2B45` (60) | 15.24 |
| 楚 | 息妫 | `--p-xigui` | `#9E5B7C` (112) | `#5E2B45` (60) | 22.52 |
| 卫 | 庄姜 | `--p-zhuangjiang` | `#3A6693` (99) | `#2F5480` (82) | 7.39 |
| 卫 | 宣姜 | `--p-xuanjiang` | `#2A4A70` (72) | `#2F5480` (82) | 5.37 |
| 宋 | 宋襄公 | `--p-songxiang` | `#4F457F` (77) | `#4F457F` (77) | **0.00** |
| 陈 | 夏姬 | `--p-xiaji` | `#3B6A48` (96) | `#3B6A48` (96) | **0.00** |

26 人位移合计 322.7、**均值 12.41**；**6 人零位移**（鲁桓公、晋文公、秦穆公、楚成王、宋襄公、夏姬——其个人色原即族基色，这是旧制「单人族取族基色」先例的遗产）；位移最大者鲍叔牙 47.04（从近黑的緅色迁至齐红本色）。**位移不是缺陷指标**：制度换血之下，人色本就要归入国色，此列只作回溯与历史截图比对的量尺。

### 2.4 九国色与功能色的距离（r24a 上报 → r24a-2 领队裁定：动诗歌层一色，余记观察项）

裁定 3 的三条判据只管**九国色两两**与**两条对比底线**，不含站内四个功能色。实测九色对最近功能色的 ΔE76：

| 国色 | 最近功能色 | ΔE76 | |
|---|---|---|---|
| 郑 `#35706A` | 青绿 `#44766B` | **4.39** | ⚠ |
| 齐 `#A5322A` | 朱砂 `#BC4433` | **8.14** | ⚠ |
| 秦 `#423C39` | 玄墨 `#2E2A24` | **8.83** | ⚠ |
| 鲁 `#97561F` | 暖赭 `#B4652F` | **9.21** | ⚠ |
| 宋 `#4F457F` | 乌梅紫 `#6E5A86` | 13.46 | ✓ |
| 陈 `#3B6A48` | 青绿 `#44766B` | 15.13 | ✓ |
| 卫 `#2F5480` | 乌梅紫 `#6E5A86` | 18.89 | ✓ |
| 楚 `#5E2B45` | 乌梅紫 `#6E5A86` | 24.73 | ✓ |
| 晋 `#74402C` | 淡墨 `#7A7166` | 28.34 | ✓ |

四对低于 13.2。**此四对全部是既有状态、非本轮引入**（九色未动一字），但国色制**提升了它们的显著度**：旧制下 `--state-*` 只用于 0.10 透明度的阵营底晕、首页分区色块与分享卡色点，新制下国色出现在每个人物页的每一处主色面。典型可见处：郑人物页的国色子导航 vs 诗歌层引文的青绿徽标／左线；鲁人物页的国色 vs 全站暖赭焦点环与编者层标。

**我不自行调色**：判据由裁定 3 划定，扩不扩到功能色是制度问题，裁定权在领队与 Xu。若领队要求拉开，最小改动面是只动郑一色（4.39 最紧、且郑是 5 人大族），可在郑的青相带内下压灰阶取值，届时须重跑 §2.1 全矩阵复验。**r24a 本轮维持现值，此条只作留痕上报。**

#### 2.4.1 r24a-2 裁定与落地：**动诗歌层一色，不动国色**

领队裁定取的**不是**上文所拟「只动郑一色」，而是**换诗歌层这一侧的功能色色相**——判断更优：郑是数据侧的国色（5 人大族、已在 §2.3 退役映射表中留档、且分享卡/og 图/首页色块皆已按此值出图），动它牵连面远大于动一个只服务于单一史料层的界面色。

落地：新立 `--poem` = `#63722F`「**菉**」，`.quote.layer-shige` 的左线／淡底／徽标三处改取之，`--bronze` 仍留作次强调色与「拥立」关系线，不动。

- **取材**：《诗经·小雅·采绿》「終朝采綠，不盈一匊」，毛传「綠，王芻也」——菉即荩草，先秦染黄绿之草。**诗歌层的色出于《诗经》自身所咏之染草**，与「青绿＝青铜锈、暖赭＝漆器赭、朱砂＝朱批」同属材质取意，不破 §2 色板的取意逻辑。
- **只换色相、不换重量**：L\* 45.5（旧 46.0）、灰阶 108（旧 109）、对绢帛 4.53（旧 4.45）、对白 5.28（旧 5.19）。引文左线与徽标的**视觉分量与旧版几乎不变**，变的只是色族——这是刻意的，层色体系的层级关系（诗歌与经义异闻同为「非经传层」的并列两色）不应因换色而移位。
- **全复验（脚本 `tools/qa/color_matrix_r24a2.js`，色值从 `styles.css`／`app.js` 实读，脚本内不另抄一份）**：菉色对「九国色 ＋ 其余全部功能色」共 **30 项**，**最紧 22.39（陈）**，其次 师友关系线 22.55，**低于 13.2 者 0 项**；对郑由 **4.39 → 37.52**。九国色两两 36 对不受影响，最紧仍 郑×陈 17.32。

#### 2.4.2 观察项（本批不改，留痕待日后合并处理）

r24a-2 把复验清单由「四个功能色」扩到**全部 31 色**（界面基色 3、界面强调 3、史料层 2、悬停态 2、关系线 8、图面中性 4、国色 9），故清单较 §2.4 原表长。换色后仍低于 13.2 的**国色×非国色**对共 11 对：

| 对 | ΔE76 | 是否同屏可见 | 备注 |
|---|---|---|---|
| 齐 × 打赏钮 hover `#a63a2b` | 3.54 | 否（页脚打赏钮悬停态，与国色面不同屏区） | 既有 |
| 郑 × 青绿 `#44766B` | 4.39 | 是（`.chip.rel-high`、`.cmp-enter`、许可行链接等） | **诗歌层已摘出，余下诸处未动** |
| 郑 × 「拥立」关系线 | 4.39 | 是（关系图：郑节点旁的拥立边） | 既有 |
| 郑 × 资料库返回 hover `#3a685d` | 5.18 | 否（窄屏资料库返回条悬停态） | 既有 |
| 鲁 × 「亲属-直系」关系线 `#A9622B` | 5.71 | 是（关系图：鲁节点旁的直系边） | 既有 |
| 秦 × 「敌对」关系线 `#35302A` | 6.01 | 是 | 既有 |
| 秦 × 层名注记 `#4E4338` | 6.61 | 是（地图层名） | 既有 |
| 齐 × 朱砂 `#BC4433` ／「婚姻」关系线 | 8.14 | 是 | 既有，r24a 已报 |
| 秦 × 玄墨 `#2E2A24` | 8.83 | 是 | 既有，r24a 已报 |
| 鲁 × 暖赭 `#B4652F` | 9.21 | 是 | 既有，r24a 已报 |

**为什么本批不顺手一起改**：其中「关系线八色 vs 国色」这一组是**同一张图上两类语义（节点＝人／边＝关系）的撞色**，要拉开须重排关系线全套八色，属另一件事、另一个改动面；悬停态两对不与国色同屏，属噪声项。**按裁定「本批不改」，此处只留实测清单，供日后若立「功能色判据」时一次性处理。** 复算随时可跑：`node tools/qa/color_matrix_r24a2.js`。

### 2.5 徽记总录 —— 「徽记定人」的唯一逐人通道

国色制下徽记是个人身份的唯一色外锚点，故其**形状恒定不变**一条自 v1.6 起的老规约，此后升为制度硬约束（旧制中它是「第一锚点」，新制中它是**唯一**锚点）。各枚徽记的取材与形制如下（形与器物意象一律保留，旧版按个人意象所定的色值理由并入国色制后不再沿用）。


徽记（形不变，仅着色随国色系更新）：鲁庄公=鼓纹（一鼓作气），齐桓公=连环三盟（九合诸侯之约的几何化），晋文公=济河之舟（一舟一旆浮于水波之上，取"济河入晋、返国为君"之意——流亡与复国之间隔一水；v1.1 定稿，`badge_jinwen.svg`）。
v1.4 新增三徽记：**武姜=掘地及泉**（地平一线、隧道下拱、泉波三叠——「不及黄泉，无相见也」而终隧而相见，`badge_wujiang.svg`）；**祭仲=执圭秉政**（一圭居中、掌托在下——圭在其手，君由其立，`badge_jizhong.svg`）；**齐僖公=载书入坎**（圭形载书立于盟坎之中，上有书痕三行——僖公小霸，成于盟会，`badge_qixi.svg`）。三者皆 48×48 圆框线描、currentColor 着色，与旧九枚同一语言。
v1.5 新增两徽记（秦楚双主角上线）：**秦穆公=崤函之险**（双峰夹峙、中通一线下有隘门——「据崤函之固，西霸戎狄」，亦记崤之战之创，`badge_qinmu.svg`；亦曾拟车马/西陲意象，取崤函更切其地理与败创双重）；**楚成王=凤纹**（昂首、三翎扬冠、垂尾一卷——楚人崇凤、筚路蓝缕以启山林，`badge_chucheng.svg`）。二者仍 48×48 圆框线描、currentColor，与旧十二枚同语言。
v1.7 新增一徽记（楚庄王上线）：**楚庄王=问鼎**（三足两耳之宝鼎、腹饰一带——「问鼎中原」，其霸业以问周鼎轻重为标目；`badge_chuzhuang.svg`，48×48 圆框线描 currentColor，与旧十三枚同语言）。取问鼎而非「一鸣惊人」之鸟，以别于楚成王凤纹——同楚二鸟必混，鼎与凤形相远、一眼可分。鼎虽与「会盟」24px 类别图标同为鼎器，但徽记为 48px 圆框纹样、语境（个人徽记 vs 事件分类）与尺度俱异，不相扰。
v1.12 新增五徽记（卫二女＋楚息妫＋晋骊姬＋秦穆姬上线）：**庄姜=双燕**（燕燕于飞、差池其羽——《邶风·燕燕》咏其送归，二燕相逐，`badge_zhuangjiang.svg`）；**宣姜=新台临河**（河上一台、下叠河水二叠——《邶风·新台》「新臺有泚，河水瀰瀰」，`badge_xuanjiang.svg`）；**息妫=桃花**（五瓣一蕊——世号「桃花夫人」，`badge_xigui.svg`）；**骊姬=觚·鸩**（喇叭口酒觚、右上一鸩毒滴——置堇于肉、置鸩于酒以谗杀申生，`badge_liji.svg`）；**穆姬=薪火**（一焰之上、下架交薪——韩之战闻兄将至「登臺履薪」以死争，`badge_muji.svg`）。五者皆 48×48 圆框线描、currentColor，与旧十五枚同语言、形各异；庄姜双燕别于楚成王凤纹（一为双简燕形、一为单繁凤形，且卫靛青／楚绛紫异色），骊姬觚别于问鼎之鼎与会盟之鼎（喇叭口沙漏形，非三足两耳）。徽记选题与其人诗篇/本事呼应（庄姜双燕↔《燕燕》、宣姜新台↔《新台》），入时间线诗经 P 层徽标可两相印证。
v1.13 新增一徽记（管仲上线，第21主角）：**管仲=衡·轻重**（天平之梁横列、中悬提纽、两端各垂一浅盘——扣管仲「轻重之术」权衡轻重、通货积财之政，`badge_guanzhong.svg`，48×48 圆框线描 currentColor，与旧廿枚同语言）。取「衡」而非「盐铁/轨里连乡」诸拟，以其一眼可读且几何最简；天平双盘对称，形远于齐桓连环三盟（三圆相扣）与会盟 24px 类别之鼎（三足两耳），亦别于骊姬觚（喇叭口沙漏），同齐系而以最亮阶＋衡形双重区分于文姜涡纹、桓公连环、僖公载书、襄公雷纹。
v1.14 新增两徽记（宋襄公＋夏姬上线，第22、23主角）：
- **宋襄公＝泓上之旆**（旗杆一竿自上贯下，旆三游向右扬、右缘作一燕尾之凹；杆脚下横过泓水一道，`badge_songxiang.svg`）。取「泓」而不取「鼓」——「不鼓不成列」之鼓已为鲁庄公鼓纹所据，同形必混；旗为主形、水为一道横流，别于晋文公「济河之舟」之舟为主形＋四连拱密波，二者主形迥异、色系亦一栗褐一绀紫。
- **夏姬＝四徙之途**（一条四折之途自左下折向右上，起止两端各缀一珠，`badge_xiaji.svg`）。取意其一生四徙——郑（出）→陈（嫁）→楚（归襄老）→郑（归）→晋（奔），只记其行止、不作评断，故**刻意避开一切艳色符号与归罪意象**（花、镜、妆，以及「株林车马」之驱驰——车马实为《陈风》刺灵公的舆论层意象，画之即等于替舆论层作画，与本库「只录其行止与出处，不采其评」的编纂基调相违）。折线为几何抽象，与既有廿一枚（鼎、凤、燕、桃花、觚、薪火、衡、圭璧、雷纹……）无一形近。
  - **不把 presence 编进徽记**：曾拟以实心／空心点区分「亲至二地」与「相关三地」，因徽记规约要求「形状恒定不变」，而 presence 是可随核订变动的数据，二者耦合将使徽记随数据改形，故弃之；亲至／相关之别一律由地图实心／空心与时间线 presence 标签承担。

v1.15 新增三徽记（鲍叔牙＋曹刿＋介之推上线，第24–26 主角）。**三枚取材层级一律《左传》**（本批为分层示范批次，徽记不得取后出叙事层意象）：
- **鲍叔牙＝税械**（左右两片木枷各带一穿孔，沿对角错开、中缝张开成一道斜口，`badge_baoshuya.svg`）。取《左传·庄公九年》「管仲請囚，鮑叔受之，及**堂阜而稅之**」——「税」通「脱」，解其桎梏。取此而不取「知人」「分财」，因二者皆出《史记·管晏列传》（S 层后出叙事）、非左传明文。形用**方折木枷**而不用圆环，正为避开同族齐桓公「连环三盟」（三圆相扣）；两片**错开**而非对齐，是「已脱」与「未脱」之别。
- **曹刿＝辙**（双辙自左平行而来，中经一道车轴短竖，右段折乱，`badge_caogui.svg`）。取《左传·庄公十年》「下視其**轍**，登軾而望之」与「吾視其**轍亂**，望其旗靡，故逐之」（r22 裁定 3 指定的取材依据即在 Q217／Q218 两条）。**不取鼓**——「一鼓作气」之鼓已为鲁庄公鼓纹所据，同役同形必混；且辙属"察迹"、鼓属"作气"，正是曹刿战术判断的两半，分予二人恰好。
- **介之推＝文之渐隐**（左缘一道竖界，其右三道横纹自上而下递减，末纹之后余一点，`badge_jiezhitui.svg`）。取《左传·僖公二十四年》「**言，身之文也。身將隱，焉用文之**」——纹饰即是求显，身既将隐则纹样自当消去。
  - **明确不取焚山／寒食／割股**：此三者皆非左传层（焚死之说今见最早出《庄子·盗跖》，本库作 Q227 挂 E095、层标「经义异闻」；寒食挂钩更在汉魏以后），且其情节与《左传》所记方向相反（见 Q227 分层说明「绝不可与经传并置」）。**徽记是人物辨识的第一锚点、恒定不变，若取自互斥的后出传说，等于让本库以图像认可该说**——与「只录其行止与出处」的编纂基调相违，与 v1.14 夏姬徽记刻意避开「株林车马」舆论层意象同理。

v2.0 新增一徽记（子产上线，第 27 主角，随 r23b 入库、r24a 前端首秀）：**子产＝壞館之垣**（一段砖垣，自左端满高向右作两级阶梯退落，垣身横列两道砖缝、竖列错缝五道，垣基一道通长地线，`badge_zichan.svg`，48×48 圆框线描 currentColor，与旧廿六枚同语言）。取《左传·襄公三十一年》「（子產）使盡壞其館之垣，而納車馬焉」——晋人卑我，则自毁其馆垣以纳车马，而后从容陈辞于士文伯；小国之礼争在此一垣。

- **不取「刑鼎」，理由是实测撞形、非取材之疑**。初稿定为「刑鼎之铭」（铸刑书，《左传·昭公六年》），文本上最切；**动手前比对 `badge_chuzhuang.svg`（楚庄王＝问鼎）几何后否决**：二者同为 48×48 圆框内的三足两耳之鼎，即便一作圆腹一作方肩、一饰一带一列三铭，在全景图 20–22px 的呈现尺度上silhouette 仍同为「有足之器」，而国色制下徽记是**唯一**逐人通道，撞形的代价比旧制高一个量级。**辨识性是本制度的承重项，取材贴切度是第二位的**——此判据可供后续新徽记沿用。
- **不取「乡校」「天道远人道迩」**：乡校之门与「出奔＝出门之矢」的门框形近，且若加「门内议论」势必画人，破 §4「不画人脸、不画情节」；「天道远，人道迩」无可径画之器物，画星象则与「灾异＝星孛」相撞。
- **与既有形近者的分辨**：宣姜「新台临河」亦有横竖成band 的台体，但其下压两道密水波、整体重心在下半；子产之垣为**左高右低的阶梯断口**，重心在左上，且无水波。「内乱＝城垣裂」为 24px 类别图标（城垣带雉堞＋一道折裂），与本枚尺度、语境俱异，且断法不同（一为裂、一为拆）。
- **「铸刑书」并未落空**：其事目 `E188` 归入本轮新立的第 16 类「政制」，在时间线上由「政制」类别图标呈现（§4）——**人以垣记、事以制记，两不相夺。**

**徽记形状恒定不变**（v1.6 立「徽记定形」，**v2.0 国色制下升为唯一逐人通道**），仅其 `currentColor` 随所属**国色**更新（不再有「国色阶」——族内分阶已随个人色一并退役）；旧版按个人意象所定的色值理由（西土赭、江汉苍绿、乌梅紫等）在两级色系时已不再逐一沿用，国色制下一律作废，形与器物意象保留。

### 2.6 关系全景阵营分区（v1.12 立，v2.0 国色制下强化）

节点按国色着色、环形按 `STATE_ORDER` 排布使同国成弧；九主角国各铺一段极淡国色环弧底晕（`fill-opacity` 0.10）。排位/底晕键用 `panoStateKey()`＝「主要发生国」（主角取 `meta.home` 覆盖，否则 `state` 首国），令**节点色与所在阵营弧一致**——庄姜/宣姜归卫弧（非齐弧）、息妫归楚弧（非陈）、穆姬归秦弧（非晋）、武姜归郑弧、夏姬归陈弧。非主角仍按 `state` 首国排位。

**v2.0 变化**：国色制下同国节点**同色**，阵营弧因此读作一整块纯色而非同相深浅的渐层——阵营分区较旧制更清晰，但**节点间的逐人区分完全落到节点内嵌徽记上**。故本轮按 conventions §9.3 把「27 人级全景图中 30px 节点内徽记的可辨性」列为硬性截图实测项，结论与线宽处置见 §2.7 与 delivery_vision_r24a。

**v2.1 变化**：全景**默认只画 27 主角**（`panoPeople()`），阵营弧的排位键与铺法一字未改，仅落在其上的人少了——九主角国仍各铺一段弧，配角在默认态下不出现（勾「显示全部」即回全库）。见 §2.7.1。


### 2.7 §9.3 硬性项：27 人级全景图节点徽记可辨性（v2.0 实测）

国色制下同国节点同色，逐人区分完全落到节点内嵌徽记，故本条自「建议」升为**硬性验收项**。**本轮跑了，且跑出了问题**，实测记录如下。

**实测几何（r24a，1440 宽）**：环上 **123 人**（27 主角＋96 配角），环半径 R=252，相邻槽距（弦长）**12.87**，主角节点 r=15 即**直径 30**。

> **槽距 12.87 ＜ 节点直径 30 —— 主角盘面本就相互叠压。** 这是 123 人同环的既有密度问题（r23b 即如此，非国色制引入），但**国色制使它显影**：旧制同族五阶深浅不同，盘缘尚能靠明暗差看出；新制同国一色，一段弧遂并作一块无分界的色团。与 r23b 同图的对照截图（同槽距、同 r，仅填充色由五阶变一色）见 delivery_vision_r24a §四。

**处置三条（皆由截图实测得出，非观感臆断）**：

1. **徽记提到独立顶层**（`badgeTop`）。旧法把徽记塞进各节点自己的 `<g>`，而 `<g>` 按环序依次追加——**后一节点的盘面正好盖住前一节点的徽记**，同弧六人只有最后一枚徽记露得出来。提到顶层后 27 枚徽记一律叠在所有盘面之上。这是本轮收效最大的一条，且属纯缺陷修复、无观感取舍。
2. **主角节点的绢帛分隔环 2 → 3.4**（`PANO_RING_W`）。绢帛色描边是**与填充色无关**的分隔线，加宽它即可在同色相邻处重新划出盘缘——这才是「同国同色」这一症状的对症之药。
3. **徽记呈现尺寸 20 → 22、呈现线宽 2 → 2.6**（`PANO_BADGE` / `PANO_BADGE_SW`，以 CSS 覆盖 SVG 呈现属性）。即任务书所说的「上调一档」。

**规约**：

- **徽记 SVG 源文件一律 `viewBox="0 0 48 48"`、`stroke-width="2"`**，与旧廿六枚同语言，本轮新增子产一枚照此。**源文件线宽不因呈现尺寸而改，改的只是呈现端**——呈现参数集中在 `PANO_*` 三个常量，改一处即全景同步。
- 此后凡新增国色家族或主角批次，全景图截图复验为 conventions §9.3 范围内的必做项。

**⚠ 验收偏差上报（r24a 时未彻底解决，候裁定）**：上述三条把「同弧只有一枚徽记可见」改善为「27 枚皆可见、盘缘可分」，但**槽距 12.87 ＜ 徽记 22 的根本矛盾仍在**——同国相邻两枚徽记仍互相压边，「只凭徽记分辨」在最挤的齐（6 人）、郑（5 人）两弧上只能说勉强。彻底解决须改环布局（加大 R 并扩 viewBox、或按主角/配角分配不等角槽、或主角节点径向错排成双环），三者皆属布局重做、超出「线宽/尺寸上调一档」的授权范围，**故 r24a 不擅自动手**，costed 选项列于 delivery_vision_r24a §四候领队裁定。

#### 2.7.1 r24a-2 结案：**默认 27 主角环 ＋「显示全部」开关**（选项 C）

领队裁定取三条 costed 选项中的 **C**。落地：`relView.showAll` 默认 `false`，`panoPeople()` 按之过滤；工具条新增 `#rel-show-all`「显示全部 123 人」勾选框。

**A/B 两态实测（1280 宽，`node tools/qa/vision_r24a.js` §5 自动测得并出图）**：

| 态 | 环上人数 | 槽距（弦长） | 与节点直径 30 | 与徽记边长 22 | 关系条数 | 「仅主角边」 | 截图 |
|---|---|---|---|---|---|---|---|
| **A · 默认** | **27 主角** | **58.51** | **＞，不叠压 ✓** | **＞，不压边 ✓** | 41 | 隐去（恒为空操作） | `r24a2_06_pano_A_27proto.png` |
| B · 显示全部 | 123 人 | 12.87 | ＜，叠压 | ＜，压边 | 239 | 出现 | `r24a2_07_pano_B_showall.png` |

A⇄B 往返实测通过（取消勾选回 27）；全屏「⛶ 放大查看」克隆体随当前态同步（A 态克隆 27 节点 27 徽记）；四档宽度（1440/1024/768/390）关系视图无横向溢出。

**为什么 C 优于 A、B**：C 是三者中**唯一不动环几何、不改视觉性格**的一条——A 要把 viewBox 由 680 扩到约 800（全景图整体变大），B 把「清爽单环」改成双环（观感取向变更）。而 C 被改掉的信息量（全库 123 人一览）由一个勾选框**原样取回**，属默认值之争而非能力之失。

**记实两条，不含糊**：

1. **根本矛盾并未被消灭，只是移出了默认态。** B 态的槽距仍是 12.87，勾选后同弧徽记照旧压边。若日后「全库全环」要成为常用视图，A/B 两条布局选项仍须重提。自验脚本对此保留 ⚠ 记实一行，不因默认态达标而删去。
2. **`PANO_RING_W` 3.4 / `PANO_BADGE` 22 / `PANO_BADGE_SW` 2.6 一律不回退。** 它们是 r24a 为 123 人环所加，A 态其实已不需要那么大的余量；但 B 态仍需要，且这三个值在 27 人环上并不过分（截图实测）。为一个仍可达的状态保留其对症处置，胜过按默认态把它们调回去。

**截图入库口径**：按仓库既有惯例（`.gitignore` 排除 `tools/qa/screenshots/*.png`），A/B 两图**不作二进制入库**，此处以「文件名 ＋ 实测数 ＋ 重跑命令」留档，随时可由 `node tools/qa/vision_r24a.js` 一键重出。若需要 PNG 实体入库存证，请领队裁定后另行加入白名单。

## 3. 排版

- 标题/年号/引文：`"Songti SC","Noto Serif CJK SC","STSong","SimSun",serif`（衬线/宋体系，接近刻本气质）。
- 正文/界面：`system-ui,"PingFang SC","Microsoft YaHei",sans-serif`。
- 原文引文竖排？——**否**。竖排在 680px 下不可控，改为：引文用衬线、加左侧朱线、字距略宽（`letter-spacing:.05em`），示意简帛而不模仿简帛。
- 数字纪年双标：`前694 · 鲁桓公十八年`，公元前在前、鲁君纪年在后，中圆点分隔。

### 3.5 引文分层徽标（v1.5）

事件展开后的原文引文按 `passages.quote_type` 分层，各层给**专属色徽标＋同色左线＋极淡同色底**，一眼别层、风格统一（右上角胶囊徽标，`.q-layer`）。层与色的对应（呼应数据凡例「后出叙事分层通例」与来源可靠度）：

| 层（quote_type） | 徽标/左线色 | 含义 |
|---|---|---|
| 原文 | 朱砂 `#BC4433`（无徽标） | 《春秋》经传当代记事，骨架基准 |
| 言论 / 评论 | 淡墨 `#7A7166`（左线仍朱） | 经传之内的话语（言论）与断语（君子曰之评论），仍属经传层 |
| 后出叙事 | 淡墨 `#7A7166`，**左线灰虚线** | 《史记》等晚出戏剧化情节，虚线示「非当代记事」 |
| 诗歌 | **菉 `#63722F`（v2.1，旧为青绿 `#44766B`）** | 《诗经》舆论/阐释层（如秦风·黄鸟悼三良）。换色相理由与全复验见 §2.4.1 |
| 经义异闻 | 乌梅紫 `#6E5A86` | 公羊/穀梁传注异说层（如公羊记崤之战），专属紫，与上列并列 |

样式统一（同一胶囊几何、同一淡底逻辑），仅换色相；未知 quote_type 走通用纯文本回退（不加徽标、不报错）。自验样本：崤之战 `E118` 公羊条 `quote_type=经义异闻`（紫徽标）、三良殉葬之黄鸟条 `诗歌`（v2.1 起为菉色徽标）。

### 3.6 编者层标 `.q-caveat`（v1.14，r21 新立）

史料研究员对某些材料另写有**本库的分层处置说明**，写法是在 `passages.modern_note` **开头**加一段 `【…】`，如夏姬线的 `【归罪话术层·非事实判断】`（巫臣两谏「是不祥人也」、叔向之母「殺三夫、一君、一子」）与 `【P 层舆论材料·不作史实用】`（《陈风·株林》）。前端以 `splitCaveat()` 取出首段【…】，单独成条渲染。

- **位置：引文之前，不是脚注里。** 旧渲染把整条 `modern_note` 拼进 footer（`—— 《来源》 · <全文>`），层标遂夹在长今译中间、且位于原文之下——**归罪话术以正文体量呈现、免责标注以脚注体量呈现，主次恰好倒置**。r21 改为提到 `<blockquote>` 顶部：读者读到「殺三夫、一君、一子」之前，先见「归罪话术层·非事实判断」。这是编纂基调在前端的落点，位置本身即是态度。
- **色：一律暖赭 `#B4652F`，不随所在层色变。** 与 §3.5 层徽标分工明确——**层徽标**（右上小胶囊、随层着色）答「这段引文属哪一层」；**编者层标**（通栏、暖赭、界面 sans 字体）答「本库对该层如何处置」。暖赭是全站界面/编者语态色，故读者一眼即知这是编者的话，不是史料本身。
- **不夺层色**：`.quote.has-caveat` 只把左线**加粗一档**（3px→5px）示「另有编者处置」，左线颜色仍是各史料层原色。初版曾把左线一并改暖赭，自验截图发现那会抹掉《株林》诗歌层的层色左线（当时为青绿）——恰恰在最需要分层的条目上让 §3.5 体系失效，遂撤回。现《株林》条呈现为：菉色左线＋菉色淡底＋右上「诗歌」菉色徽标＋顶部暖赭编者层标（v2.1 换色相前为青绿三处），P 层与其上两条史料层（朱线朱底）一眼可分。
- **文字一字不改**，只挪位置、去外层【】（容器本身已是标记）；`role="note"`；footer 显剩余今译，不再残留【】。四档宽度（1440/1024/768/390）实测均不截断。
- **通用而非特例**：凡 `modern_note` 以【…】开头者自动生效，现库三条（Q180/Q188/Q193）全在夏姬线；未来任何批次照此写法即自动获得同一呈现，无需改代码。

## 4. 图标语言（16 分类 + 27 徽记）

- 24×24 网格，线描 stroke 1.6，圆头圆角，`fill="none"`，`stroke="currentColor"`——颜色永远由使用处文字色/主题色决定。
- 意象取自器物与仪式，不画人脸、不画情节：即位=冕旒、战争=交戈、会盟=鼎、相会=月下二人、婚嫁=同心双环、生育=嘉禾、出奔=出门之矢、弑杀=倒悬之匕、薨卒=玉璧、丧葬=封土、外交=圭、内乱=城垣裂、灾异=星孛、礼俗=豆、**政制=矩与绳墨**、其他=简牍。

**第 16 类「政制」图标（v2.0，r24a 补齐 conventions v1.21 登记的待办）**：`zhengzhi.svg`。形制取**矩（直角尺，竖臂两道刻度、横臂两道刻度）＋绳墨（一道垂绳，下悬一枚圆锤）**。

- **典据**：《管子·七法》「尺寸也，繩墨也，規矩也，衡石也，斗斛也，角量也，謂之**法**」——「规矩绳墨」正是先秦论「法/制度」的本喻，且出自《管子》，而本类现归的三件事目之一即管仲之政（`E187`），典源与事目同源。
- **为何不取领队建议的「简册/鼎铭」**：建议已收到并实测比对后另择，理由是**同尺度撞形**——「会盟」类别图标已是 24px 之鼎（双耳、口沿、圆腹、三足），「政制」若再取鼎（哪怕加铭），两类事件在时间线同一列表里以同尺寸并排出现，读者难分；纯「简册」则与「其他＝简牍」（三竖两横的编简）几乎同形。**类别图标之间必须在同一尺度下可分，这条比与徽记的跨尺度关系更硬。**
- **另一层理由：类别图标须对全类通用，不可为一件事目定制**。「刑鼎」只切子产 `E188` 一件，套在 `E187`（叁国伍鄙、作内政而寄军令）与 `E131`（孙叔敖为相与治绩）上皆不称；「规矩绳墨」是三件共有的「治国之制」本身。
- **与既有形近者的分辨**：管仲徽记「衡·轻重」同为量具，但它是 48px 圆框内的**天平**（横梁＋提纽＋双垂盘，左右对称）；本图是 24px 的**直角尺＋垂绳**（L 形＋单垂线，左右不对称），尺度、语境、对称性三重相异。「外交＝圭」为上尖下方的单片玉版，与 L 形无涉。
- 现归此类者：`E187`（管仲叁国伍鄙、作内政而寄军令）、`E131`（孙叔敖为相与治绩）、`E188` 系（子产铸刑书一幕）。
- **上报**：本图与领队建议意象（简册/鼎铭）不同，理由如上（同尺度撞形＋类别图标须通用）。若领队仍倾向鼎铭一路，可改，但须同时重画「会盟」以拉开二者——那是更大的改动面，故本轮先按可分性取矩绳，记此候裁。
- **会盟 vs 相会**：会盟是"器"（鼎，对称、庄重、直线多）；相会是"时"（残月与相向二人，弧线多、不对称、私密）。一眼可分。
- 徽记（48×48）走几何纹样抽象：文姜=涡纹、鲁桓公=圭璧、齐襄公=雷纹（方折回纹）、郑昭公=翎矢三叠、郑庄公=蟠虺双曲、鲁隐公=鱼纹（观鱼于棠）、鲁庄公=鼓纹、齐桓公=连环三盟、晋文公=济河之舟（余十八枚见 §2.5 总录）。圆框统一，纹样各异；同样 currentColor，**v2.0 起由所属国色着色**。

### 4.1 og 分享协议图（v2.0 重出，r24a）

`site/assets/og/og-card.png`（1200×630，预生成静态资源，属 conventions §9.2 例外清单第 1 项）。**构图沿用旧版**：绢帛底＋青铜双线框、左侧涡纹徽记（甲版 brand mark，暖赭）、站名「经纬春秋」大字、副题「左传为经，诸书为纬，牵系有据」（现行版）、下方一行色点色带。

- **本轮唯一实质改动：色带由 15 枚旧个人色签改为 9 枚国色圆点**，与分享卡 §5.7、首页九分区、关系全景阵营底晕同源同序（齐/鲁/郑/晋/秦/楚/卫/宋/陈）。旧图色带是 r15 期间生成的，其后经 r19b/r21/r22 多轮色板扩充与 r22 分享卡改制，早已与站内实况不符（旧图 15 枚、库内当时 26 人、现制 9 色）——这正是 `qi_palette_expansion.md` §8 第 5 条上报的问题，本轮一并清账。
- **增长源与分享卡同律**：色带宽度只随**国色家族数**增长，不随主角数增长；新增主角不改此图，唯新立国色家族才需重出。
- **生成方式**：`tools/qa/make_og_card.js`（Playwright 渲染 1200×630 后截图，与 §9.3 QA 工具链同一依赖，非站点运行时依赖）。产物入库、脚本入库，可重跑。

## 5. 地图

- viewBox `0 0 1200 700`，投影严格按 conventions.md 公式；底图所有落点由脚本按同一公式生成（构建时校验：临淄→831,180、曲阜→699,312、新郑→373,442）。
- 图层顺序（自下而上）：海域 → 列国淡色底 → 水系 → 山地 → 古名标注 → 城邑锚点（空层，JS 填充）。
- 水系用青灰 `#7FA3A8`：河水取春秋故道（荥泽以下东北行，至今津沽入海），济水自荥泽分流东行经泺、临淄之北入海，另有汶、泗、颍、洧、淮。走向对照谭其骧图集的大势，线条做了平滑简化。
- 列国底色为低饱和差异色、透明度 0.35，只表"势力大致范围"，**不是考据疆界**（图例中注明）。
- 底图只标古名；今地名、坐标依据、certainty 全部进弹窗。
- 锚点样式：普通地点=空心小圆；当前人物相关=主题色实心；importance=1 事件地点在轨迹上加重。轨迹为主题色虚线按年序连线，端点标序号。

## 5.5 导航层级与 hash 结构（v1.2）

- **两级导航**：主导航只留全局项「选人 · 关系 · 资料库 · 关于 · 反馈」；选定人物后头部下方出现**人物子导航**「〔徽记+人物名〕· 时间线 | 地图 | 关系 | ✕ 换人」，次级样式（绢帛底、虚线下边、胶囊小按钮、主题色高亮），视觉从属于主导航。站名＋徽记整体为回选人页链接。
- **hash 新结构**：`#/`（选人·地图模式）、`#/?home=list`（选人·列表模式，v1.4）、`#/p/<PID>/timeline|map|relations`（人物视图，relations=ego）、`#/relations`（全景）、`#/library[/<tab>][?q=…]`、`#/about`。旧格式 `#person=X&view=…` 由 `legacyToNewHash` 就地 `history.replaceState` 改写，外部旧链接永不失效。
- **人物语境**（`personCtx`）：进过人物视图后记住；逛资料库/关于时子导航仍在、主题色保持，「✕ 换人」清除。全局视图的 hash 不携带人物（分享资料库/关于链接是干净的）。
- **关系视图语义**：主导航「关系」已选人→该人物 ego 图，未选人→全景；首页「全景关系图谱」入口恒指全景。**v2.1 起全景默认只画 27 主角**，工具条「显示全部 123 人」勾选后回到全库全环、「仅主角边」过滤器随之出现（默认态下它恒为空操作，故隐去）——见 §2.7.1。（旧文作「56 人全景」，系 r13 时的库容，此后随入库人数变动，故此处不再写死人数。）
- **关系并线**：同一对人物的多条关系合并为一条边，取类型序（REL_COLORS 键序）首条定边色；多重关系在边中点加**数字徽记**（生绢底小圆＋计数，点击同边）；虚线=该对关系可靠度皆中/低（规则详注入关系卡）。关系卡按类型排序列出全部关系（rel_label、可靠度、依据）。
- **地点抽屉**：全屏态一律走底部抽屉；>680px 收为限宽浮卡（`min(520px, 100vw−2rem)` 居中、圆角 12px、底距 18px、浮起投影），≤680px 保持整宽贴底。全屏地图的指针捕获延迟到确认拖移/捏合才设置，保证桌面鼠标 click 可达锚点。

## 5.6 全站搜索（v1.3）

- **位置**：页眉站名右侧、主导航之前；680px 下收纳为放大镜图标，点开占整行展开输入框。
- **口径**：纯前端包含匹配（小写化＋去空格），字段间以「|」隔断防跨字段误连；输入防抖 200ms。四组：人物（name/alt_names/姓/氏/名/字）、地点（古名＋今地名）、事件（标题＋摘要＋事发地古今名——搜「临淄」应见发生于临淄之事）、原文（quote_original＋modern_note，白话注多为简体，兼济繁简检索）。
- **直达语义**：人物（主角）→其时间线；人物（非主角）→其 ego 关系图（hash 落 `#/relations`，同「以人为中心」游走态）；地点→地图并高亮（人物语境优先，否则主角序中首个于此地有事件者；活动范围外自动切全图视野）；事件→所属主角时间线定位展开（朱砂描边一闪 `spotlight`）；原文→事件详情内定位到引文块。
- **展示**：每组先显 8 条＋「更多（还有 N 条）」组内展开；无结果一句温和提示（`SEARCH_EMPTY`，copy_r8 无此项故自拟）。
- **键盘**：combobox/listbox 语义，上下键循环选中（aria-activedescendant）、Enter 直达（未选中即首条）、Esc 关闭。

## 5.7 分享卡与底部分享行（v1.3）

- **构图**（canvas 运行时合成，零依赖）：绢帛底＋青铜双线框＋上下回纹带（雷纹钥匙纹，几何绘制，非贴图）＋站名（衬线逐字排距）＋**九枚国色家族色点一行**（v1.15 改，见下）＋邀请语（SHARE_COPY.invite，一行放不下优先在「——」处分行）＋二维码（生绢衬块、整数倍缩放关平滑保扫码）＋域名。两版：1080×1440（3:4 竖图，微信/小红书）与 1080×1080（方图）。
- **字体**：canvas 用系统栈（SHARE_SERIF/SHARE_SANS），letterSpacing 兼容性不齐故逐字绘制。
- **入口**：页脚「生成分享卡 · 复制链接 ·（直接分享）」与反馈行（Issues＋邮箱）相邻；「直接分享」仅在支持 Web Share API 的环境出现，优先携图（canShare files），退化为链接分享。复制链接写入主站 URL，成功 toast 用 N5。
- **对话框**：尺寸切换胶囊、预览、下载 PNG（toBlob）；Esc/遮罩/✕ 关闭，Tab 简易圈定。

**色签改按国色家族计（v1.15，r22）——这是构图承重改动，不是排版微调，勿改回按人计。**

- **旧法**：每主角一枚色签（`tw=26, gap=14`），一行宽 = `40×主角数 − 14`，**随主角数线性增长**。23 主角时 906px，距内框净宽 992px 只余 43px；26 主角时 **1026px**，起点 x = 27——**不但越过内框（x=44）17px，连外框（x=30）也已越出 3px**。r21 曾预警"再加 3 人触框"，r22 实测即已触框。
- **新法**：每**国色家族**一枚圆点（`r=10`，中心距 44），一行宽 = `44×(家族数−1) + 20`。九族现为 **372px**，左右各余 310px。
- **改动的实质是换掉增长源**：由「主角数」改为「**国色家族数**」。新增主角不再加宽一分，唯**新立一个国色家族**才 +1 枚；而家族数增长远慢于主角数（现九族 26 人，且新族须经跨族可辨性实测方能设立，见 §2）。此前每轮加人都在逼近框线、迟早要再撞一次，换源之后这条线才真正稳住。**若日后有人以"点太少、显不出人多"为由改回按人计，即是把这个已解决的问题重新引入。**
- **色序与色值同源 `STATE_FAMILY_VAR` / `familyColor()`**，与首页九分区、关系全景阵营底晕共用同一份，不另写一份（新增国色家族时只改 `STATE_FAMILY_VAR` 一处，三处呈现同步扩展）。现序：齐/鲁/郑/晋/秦/楚/卫/宋/陈，与首页分区序实测一致。
- **v2.0 追认**：国色制立后，「按国色家族计」由 r22 的一项构图取舍**升为制度必然**——个人色已不存在，按人计在数据上已无从谈起。此条至此不再是可回退的选择。
- **不另加人数字样**（Vision 判断，已报备）：分享卡是品牌门面而非数据仪表盘；旧的 23–26 枚小色签本也无人数得清，并未真正承担"计数"职能。且人数逐轮变动，若印在图上会使每轮都需重出分享图。九枚国色点读作一串"印珠"，与「简帛与青铜」语汇相合，作纯装饰色带即可。

## 5.8 首页地图导航（v1.4）

定稿全文见 `home_map_notes.md`。要点：首页默认为海报化列国底图（色块饱和提档、国名放大、增晋与汾水），有主角之国落主题色徽记簇，点国出该国人物菜单（桌面右侧面板 / 手机滚动定位下方分组），无主角之国点出一句气质注＋「人物线整理中」；「列表模式」按钮切回分组卡片视图，模式记忆走 hash（`#/?home=list`），不用 Web Storage。人物卡全站统一尺寸（min-height 8.5rem、简介单行截断）。武姜 state 为「申/郑」，首页归郑分区（其人物线全在郑），卡上仍标「申→郑」流向（PROTAGONISTS 配置 `home` 覆盖项）。**v1.12 卫分区新设**：卫色块（底图 `data-state="卫"`，河淇之间）成键盘可达热区、落庄姜/宣姜双徽簇（`HOME_BADGE_POS.卫=[692,214]`，居卫块中）；分区归属按「主要发生国」（conventions §6.6）——庄姜/宣姜 `home:"卫"`、息妫 `home:"楚"`、穆姬 `home:"秦"`、骊姬随 state 首国归晋。

**v1.14 九分区（宋/陈新设）**：宋、陈色块底图既有（`layer-states` 内，同仿射），换算后宋 中心(755.3,317.8)半径(62.1,41.9)、陈 中心(698.8,380.6)半径(40.9,30.3)。两块在 x 693–740／y 350–360 一带相邻，且陈块左半 658–693 与蔡块重叠，故徽记分置对角、且均落在「纯陈／纯宋」区：`HOME_BADGE_POS.宋=[770,340]`（国名右下，块内归一化半径 0.34）、`HOME_BADGE_POS.陈=[732,384]`（国名右侧相切不压字，左缘 718.5 恰在蔡块右缘 693 之外），两簇心间距 58px＞两徽记直径 27px。夏姬 `home:"陈"`（其 `state` 首国为郑，须覆盖）；宋襄公 `state="宋"` 无需覆盖。

**流向 chip 规则修订（v1.14）**：跨国人物卡的流向标注由 v1.12 的「只取 `state` 首末国」改为**列 `state` 全链**。起因是夏姬 `state="郑/陈/楚/晋"`——首末规则给出「郑→晋」，恰好吞掉陈、楚两段，而这两段正是其人物线的主体（陈夏氏之乱、楚归连尹襄老），隐去它反而遮蔽内容。改动只影响两人：夏姬「郑→陈→楚→晋」、息妫「陈→楚」⇒「陈→息→楚」；四档宽度实测 h3 无溢出。
**并加一条语义护栏**：chip 的 `title` 明说「人物线所历之国（据 people.state 出身→归宿诸段），非亲至轨迹；何处为亲至、何处史文无在场明文，见其地图与时间线」——chip 是国别流向，亲至与否一律以地图实心/空心与时间线 presence 标签为准，两者不可混读。

## 6. 三屏结构与交互

- 屏1 选人（#/）：v1.4 起默认地图导航（见 5.8），列表模式保留如下：按 state 首国分区（v1.1），组头=国名大字+一句话气质注（界面文案，配置于 app.js `STATE_EPITHET`）；顶部国别选项卡（全部/齐/鲁/郑/晋…随分区自动生成）滚动定位到分区，sticky 吸顶，680px 下横滑。卡=徽记+姓名+姓名行小字（姓/氏/名/字压缩形式）+生卒+一句话简介，主题色作卡顶条与徽记色；跨国人物（state 含「/」）在姓名旁标流向（如文姜「齐→鲁」）。数据缺席的主角显示"资料整理中"占位卡（不可进入）。新国加入只增分区，布局自动扩展。
- 人物子导航（v1.4 升格）：人物名主题色衬线加粗、字号升一档（1.08rem），徽记 24px；未激活视图标签用正文玄墨（此前淡墨对比不足，Xiangtao 反馈 3）；激活态维持主题色深底白字；子导航在场时 main 顶部留白加 0.5rem 拉开层次。
- 屏2 时间线（#/p/<PID>/timeline）：顶部年份刻度尺（min→max，十年一格），主体为 `<details>` 纵列：图标+双标年份+标题；importance=1 加粗左线并放大图标；展开显示摘要、地点、可靠性、原文引文（passages，附出处）。
- 屏3 地图（#/p/<PID>/map）：底图+该人物轨迹；点击/回车锚点开弹层（古名、今地、坐标依据、certainty、该人物相关事件）；无坐标地点列在侧栏"未定位地点"；"按时间播放"按钮做逐段轨迹动画（可暂停）。**轨迹降级（v1.12 通用机制）**：亲至可落图地点<2（连不成任一段轨迹）者，播放按钮以静态说明「亲至可考不足两地，暂无轨迹可播——已知地点如图。」替代（`setPlayDegrade`，左朱线胶囊、按钮 `hidden`+`disabled` 故全屏浮层播放控件亦隐），单点定位与其事件正常显示，状态行改「亲至可考一地：X」；并观（compare）双方皆<2 时同句降级，只要一轨可动即保留播放（单点者定点、可动者行进）。现库降级三人：穆姬、庄姜、宣姜（各仅一亲至落点）。

**「相关」的措辞分寸（v1.14，Sophia 定，全站执行）**：`相关` 的准确含义是**史文无其在场明文**，不是**史文书其不在场**。站内任何解释性文案（tooltip、aria-label、图例、状态行、凡例）**不得写成或暗示「其实不在场」**。r21 据此改三处旧措辞：地图锚点 aria-label「相关地点，本人不在场」⇒「相关地点，史文无其在场明文」；关于页「怎么读」段「与其相关而本人未到之处」⇒「事与其相关而史文无其在场明文之处」；数据凡例段「事与其相关而本人不在场」⇒「事与其相关，而史文无其在场明文」，并补一句「**「相关」是"史文没说他在"，不是"史文说他不在"**」把分寸显式化。**建议 conventions §6 的 presence 定义（现作「相关＝事件与其相关但本人不在场」）同步订正**，见交付说明上报事项。

**状态行补注（v1.14）**：地图状态行在轨迹句之后，若该人另有「相关」落点，追加一句「另有 N 处相关地点（史文无其在场明文），空心示之、不入轨迹。」——通用于全部人物（现库 17 人触发），使「史文所系之地」与「亲至可考之地」在同一行里可分。**夏姬线是这一句最见分量之处**：状态行读作「亲至轨迹共 2 站，前596 起。另有 3 处相关地点……」，图上郑、晋两实心点连一段虚线，陈（宛丘）、株林、楚（郢）三点空心散在南方——四国皆有其事，史文能确证她在场的只有两处。**这张薄地图不是数据缺口，是内容本身，不得为「好看」而把陈、楚补进亲历轨迹。**
- URL hash 全状态可分享；浏览器前进后退可用。
- 键盘：卡片与锚点均可 Tab 聚焦、Enter 激活；`<details>` 原生可达；焦点环用暖赭双线。

### 6.6 双人并观（compare，v1.8）

第七种视图，与「时间线/地图/关系」并列但不入主导航（经入口进入）。hash `#compare=P_A,P_B`（须在 `legacyToNewHash` 之前拦截，否则含 `=` 会被当旧 `#person=` 链接误改写）。

- **两处入口**：①关系图任意两主角的关系卡内加「并观其迹 →」（内嵌右卡 `#rel-panel` 与全屏抽屉两处，仅当成对卡且二人皆主角时出现，青绿描边区别于既有操作）；②人物地图工具条「＋ 添加对照人物」弹小面板（其余十四主角，色点＋名），择一进并观。
- **双轨同图（v2.0 改制，裁定 1a）**：两人各用其**国色**（同国者色相同，这是国色制的既定代价）；区分改由线型与端点形状承担：

  | 轨 | 语义 | 线型 | `stroke-dasharray` | 端点 | 行进标记 |
  |---|---|---|---|---|---|
  | **甲** | 当前页主人物（hash 中在前者） | **长划虚线**（＝单人地图现行样式，原样不动） | `6 5` | 圆点 r=4 | 圆环 r=8 |
  | **乙** | 添加的对照人物 | **珠点线**（近零段长＋圆线帽，视觉为一串圆珠） | `0.5 8`＋`stroke-linecap="round"` | 方点 7×7 | 方框 14×14 |

  - **单人轨迹一律不动**（裁定 1a 首条）：`renderMap()` 的 `6 5` 长划虚线**零改动**，已有读者认知不受扰动。并观甲轨与单人轨迹是同一样式，故「从人物地图点进并观」时主轨在视觉上是连续的。
  - **乙轨珠点线为无条件**：旧制按 `cmp.similar`（同国且明暗相近）**条件性**启用点划线；国色制下同国即同色，条件判断已无意义，故珠点线改为**乙轨恒用**。`cmp.similar` 保留但语义改为「双方同色」，只用于文案提示，不再决定线型。
  - **三重冗余**：线型（长划／珠点）＋端点形状（圆／方）＋图例中的徽记小签。灰度截图下三者皆不依赖色彩。
  - **`vector-effect="non-scaling-stroke"`**：dasharray 随之在屏幕坐标下求值，故珠点在任意缩放级别下保持恒定的珠径与珠距，不会在放大后拉成长划、缩小后糊成实线。
  - 图例（`#cmp-legend`）同步标注两式：甲一段长划线样＋圆点＋姓名，乙一段珠点线样＋方点＋姓名，另附「同场／同年同地」两环例。图下 `.map-note` 一句同步改写为「**长划线＋圆点＝甲；珠点线＋方点＝乙**；环＝交会点。轨迹只连亲至。」
  - **验收**：同国对（文姜×齐襄公）灰度截图下双轨肉眼立辨，截图见 delivery_vision_r24a §三。

### 6.7 并观选择交互（v2.0 重构，裁定 1b）

旧交互是「一次性择人进入 → 之后只能靠『⇄ 对调』换主轨、靠浏览器后退退出」。r24a 改为**开关式**：

- **「＋ 添加对照人物」按钮常驻不变形**，在**单人地图**与**并观**两视图的工具条上同时在场，文案与形态两处一致（并观视图此前无此按钮，本轮新增 `#cmp-btn-compare`／`#cmp-compare-pick`）。
- **面板为开关式，实时反映当前组合**：
  - 单人态点某人 → 加入并观（甲＝当前页主人物，乙＝所点者）；
  - 并观态点**当前乙** → **移出，自动回单人模式**（落回甲的地图视图 `#/p/<甲>/map`）；
  - 并观态点**另一人** → 直接换乙（甲不变）。
  - 已选中者带**选中态**（`aria-checked="true"`＋对勾＋国色描边），面板每次打开按当前 hash 重建。
- **「⇄ 对调」按钮退役**（`#cmp-swap` 及其 CSS 一并删除）。等价路径：想以对方为主，自其人物页进入并观即可——`#compare=甲,乙` 的语义保持不变（**甲＝当前页主**），角色随入口自明，不需要一个额外按钮来解释它。
- **播放按钮文案两模式统一为「▶ 轨迹按时间播放」**（旧并观作「▶ 按年并观」）：同一个动作在两处不该有两个名字；暂停态两模式亦统一为「⏸ 暂停」「▶ 继续播放」。
- **Ctrl 多选（多人并观）本轮不做**，已记入候选池；第三线型（点划等）留待其时再扩，本轮不预留视觉位。

### 6.8 全屏播放控件移位（v2.0，网友实测反馈）

`#overlay-controls`（全屏浮层播放/暂停）由**左下角**移至**左上角、关闭钮之下**。

- **动因**：左下角与地点抽屉/站点卡的弹出区重叠，手机全屏播放中点开任一站点卡即被遮挡（网友实测反馈）。
- **落法**：控件 DOM 移入 `.map-overlay-body`（该容器已 `position:relative`，且位于顶部工具条之下），CSS 由 `bottom:14px` 改 `top:14px`，`left` 不变。桌面同步移位，两端一处定义。**热区仍 ≥44px**（`min-height:44px` 不动）。
- **腾出的左下角归图例**：全屏态下地图图例/字幕条占左下，与控件不再争位。
- **验收**：手机全屏播放中弹出任意站点卡零遮挡，截图见 delivery_vision_r24a §三之三。
- **候选池（r24a-2 记入，本轮不做）**：**播放中点站点卡自动暂停并展开**。现行行为是 `app.js` 锚点 `open()` 内 `if (player.raf) return;`——**播放进行中站点卡根本不打开**（r18 起既有设计：播放期间画面聚焦地图）。故 r24a 任务书所写「手机全屏**播放中**弹出站点卡」的字面状态在现行产品行为下不可达，实测按可达态（暂停后开卡）完成。若要改为「点卡即自动暂停并展开」，属交互语义变更（播放的中断权从读者的暂停键扩展到任意点击），须领队另行下达。
- **绝对年轴**：跨度取两人生卒/活跃并集（结构化 `birth/death` 优先，缺则以 `active_years_bce` 文本中「前N」与其事件年兜底）；两条生卒条＋生卒刻度（有结构化生卒标「生/卒」，否则「活跃自/止」）；交会标记（三角）；播放游标＋当前年读数。
- **按年播放（双轨模式，r17 起接入统一引擎 `player`；r17b 主钟改故事进度 sc；r18 交会常亮·速度定稿）**：与单人地图播放共用同一状态机与主钟（增量累加、段内 `easeInOut`、暂停/续播、终点归位），并观是「双轨＋交会锚主钟＋交会 beats」的一种 `cfg`。**年位同步（r17b）**：标记位置是「故事进度 sc」的纯函数 `cmpPositionAt(waypoints, sc)`；每个交会地＝一个整数 sc 锚点，两人在该 sc 均被钉到交会地——从构造上保证「主钟到交会刻 ⇔ 两人到交会地」。`clockYear` 是 sc 的单调函数（年严格段内 sc↔year 一一对应，等价「年的纯函数」）；缓动只作段内视觉平滑。根治旧「纯 clockYear」在**同年多交会地**（文姜×齐襄公前694 泺＋临淄）处的塌缩。**交会表现层（r18 裁定1）**：标记经过交会点即**点亮并常亮**（加 `.lit`）留痕至结束/重置，未经过者呈**待亮态**（播放态下 `.cmp-play-active` 降透明、隐标签，视觉可区分）；同年多处交会自然连续点亮、**行进全程不打断**（`holdUntil` 停拍退役、脉冲移除）。**速度定稿 0.5×（裁定2）**：唯一速度、无档位。交会一览侧栏、点击跳转、免责句、「相邻记载」分辨全部保留（在侧栏与弹卡）。状态机测试见 `tools/test_binguan_play.js`。
- **全屏播放控件（r17b 引入·r18 定稿）**：地图全屏浮层（`#map-overlay`，单人与并观同用）左下角悬浮「播放/暂停 `#ov-play`」（速度定稿后无速度按钮）——委托主工具条按钮（复用其 onclick 闭包与播放状态），文案由 `setPlayBtnText` 主/浮层同写，故**全屏内外播放状态无缝双向同步**（全屏中暂停→退出仍暂停；反之亦然）；触屏热区 ≥44px、左下角不遮轨迹与图例，退出全屏隐藏。覆盖单人×桌面/手机、并观×桌面/手机四组合。
- **交会两级与免责**：a 级同场（暖赭金环、徽标「同场」）；b 级同年同地（朱砂虚环，徽标据同链与否作「相邻记载」或「可能相遇」）。B3 免责句「同城未必同时，不能仅据同年同地断定相遇」常驻交会侧栏顶部、交会弹卡内再重复一句。跨年事件在场判断容忍 ±1 年并在卡上注明。检测口径逐条对齐 `docs/binguan_fixtures.md`（生死过滤、±1 跨年容差、同链「相邻记载」三护栏），实现细节与自验见 delivery_vision_r16b。
- **容器统一律与手机**：全屏走 `#map-overlay`＋`bindZoomGesture`（与地图/关系图全屏同机制，移动而非克隆 svg 以保播放标记活性）；交会详情桌面走侧栏内嵌卡、手机走底部抽屉；交会侧栏在 ≤680px 折叠为底部可展开条（`#cmp-sheet-toggle`）。
- **数据零改动**：并观全部派生自现有 `event_people.presence`／`events`／`people` 生卒，无新增表列、无新增依赖。

## 7. 前端体例：DOM 关系锚定与自验方法（v2.1 补，r24a-fix2 收尾裁定 ②③ 立）

本章不谈色与形，谈**界面代码的结构纪律**与**自验的咬合力**。两条都不是推演出来的，是 r16／r24a-fix／r24a-fix2 三次实测（其中一次为 Xu 的生产报障）付学费换来的，凡改动 `site/` 者须先读。

### 7.1 同族三相：DOM 关系必须锚定在身份与状态上

**领队定性（r24a-fix2 收尾裁定 ②，原话）**：

> 「靠文档顺序认亲」与「被 move 的单例」「无重建路径」确是同族三相——**病根都是 DOM 关系未锚定在身份与状态上**。

**规约一句话**：凡代码要回答「**是哪个节点**」「**它该在哪**」「**它现在什么样**」，答案只能取自**身份**（`id`／`data-*` 标记）与**状态**（内存里的唯一真源）；**不得取自文档顺序，不得取自「上次它还在那儿」的记忆**。DOM 是投影，可弃、可重建；真源不在 DOM 里。

三相各举本族已发生的实例与对应正解：

| 相 | 病灶 | 本族实例 | 正解 |
|---|---|---|---|
| 一 · **靠文档顺序认亲** | 用**位置**指认节点——`querySelector(".map-frame")` 取的是「文档里第一个」，节点关系遂系于 DOM 排布 | **`.map-frame` 顺序认亲**（r24a-fix2 走查发现）：`#cmp-frame` 也带 `class="map-frame"`，单人字幕条归谁全靠「谁排在前」；`<section>` 顺序一调，单人字幕条即被搬进并观图框 | **按 id 取节点**：单人图框补 `id="map-frame"`，`captionHost` 与 `closeOverlay` 两处选择器改 `$("#map-frame")`。**身份取代位置** |
| 二 · **被 move 的单例** | 一个静态节点在容器间**搬来搬去**，其存活依赖「每一条退出路径都记得把它搬回来」——一条漏搬即永久失联 | **`#play-caption`／`#cmp-caption`**（r24a-fix2 断根）：开浮层 move 进 `#map-overlay-body`、关浮层 move 回内嵌图框。走查实测**尚无可达销毁路径**（故是断隐患、非修事故），但只要再添一处 `textContent = ""` 型入口即死 | **状态为唯一真源**：`captionState` 存文本与显隐，DOM 只是其投影；归属容器**当场派生**（`captionHost`），节点随时可弃、用时**自愈重建**（`captionEl`／`mountCaption`） |
| 三 · **无重建路径** | 节点被当作「一定还在」的既有物：函数只会**找它、改它**，不会**造它**；节点一旦没了便静默空转，连告警都没有 | **`#overlay-controls`**（r24a-fix，Xu 生产实测报障）：r24a 移位把它放进了会被清空的 `#map-overlay-body`；旧 `setupOverlayControls` 首行 `if (!box) return` **不是守卫过严、是根本没写重建**，故控件消失后须整页刷新才复原 | **派生式重挂**：`mountOverlayControls(mode)` 每次开浮层先清残留再新建，显隐与文案**当场从主工具条按钮读出**，不依赖任何跨浮层存活之物 |

**第四例与一处订正（绑定同理）**：本族最早一例是 **r16 关系图全屏「点边/点节点无反应」**——`cloneNode(true)` 只复制结构与属性、**不复制 `addEventListener` 注册的处理器**，克隆体成了哑元素；正解亦是**派生式重挂**：绘图时给边/节点打 `data-detail`／`data-node` 身份标记（相一的正解：身份），克隆后 `rebindRelClone()` 按身份重绑（相三的正解：重建）。**绑定与节点同理，都不该指望跨容器存活。** ⚠ 此例在 `delivery_vision_r24a` §19.2 与 `app.js` 旧注释中曾误记为「r17b」，实为 **r16**（实据：`docs/delivery_vision_r16.md` §根因①、`app.js` `openRelOverlay` 上方注释「r16 两处修复」）；误记始于我，已一并订正，见 §二十一之四。

**本族已断根（实据，非估计）**：`#overlay-controls`（r24a-fix）与两条字幕条（r24a-fix2）清完之后，**`#map-overlay-body` 内不再有任何跨浮层存活的静态节点**——浮层内容此后一律派生式重建，关系图全屏的克隆体本就每次新建。`index.html` 中相应静态节点已删除、原位留注释指向 JS 唯一出处。**隐患清点已尽，无需排后续巡查**（此为 r24a-fix2 §20.9 条 3 的结论，随本次裁定落档）。

**此后的做法**（新写或改写浮层相关代码时照办）：

1. 节点取用一律 `$("#id")`；同类多份容器**必须各有 id**，不得靠 class ＋文档顺序区分。
2. 凡挂进 `#map-overlay-body`（或任何会被 `textContent = ""` 清空的容器）之物，**一律 `mountX(mode)` 幂等重建**：首行先 `unmountX` 清残留，再 `createElement`。
3. 显隐、文案、进度等**状态不得存在 DOM 上**；DOM 只读一次真源、写一次投影。真源改了就重投影，容器换了就重挂。
4. `mount` 调用**必须排在浮层状态位改写之后**（`mapState.overlay`／`cmpZoom.active`）——容器由状态派生，顺序错了就派生错。
5. 代码注释里写明「**为何每次重建而非静态单例**」并附本次报障，否则后人见其"多此一举"会改回单例。现行两处注释（`mountOverlayControls`、字幕条块首）即此例。

### 7.2 自验方法：按类反证 ＋ 交叉验证 —— 没有活 bug 也能把断言磨到咬得住

**领队评价（r24a-fix2 收尾裁定 ③，原话，作方法留档）**：

> §4c 那个「注入同型清空、旧码 16 FAIL 新码自愈」的按类反证，加上「§4b 在旧码上全过恰与走查互证」的交叉验证——**没有活 bug 也能把断言磨到咬得住**，这是 289 项总门里最值钱的一段。

**何时用**：所修的是**一类结构隐患**而非一条已复现的路径时——走查跑遍现行全部入口、销毁记录 0、页面错误 0，隐患成立但**无可达路径**。此时「复现 bug → 验其修好」的常规办法无从下手；若就此只写「看着没问题」的断言，这道门天生常绿，与摆设无法区分。

**如何构造（四步，缺一即失咬合力）**：

1. **先走查、后动手，并把走查结论摆在最前面**。用探针逐帧（50ms）监视目标节点的 `document.contains`，把现行**全部**入口跑一遍（往返、Esc、后退/前进、播放中切视图、抽屉内跳转……）。**交付说明不得把隐患写成事故**——是「断隐患」还是「修 bug」，一开头就要交代清楚。
2. **注入同型动作，不注入自造故障**。反证所注入的必须与真实入口**一模一样**：此处是 `#map-overlay-body.textContent = ""`，与 `openRelOverlay`／`closeRelOverlay`／`openCmpOverlay` 三处**同一行代码**。注入自造的破坏只能证明「我能修我自己造的伤」。
3. **断言只调两版皆有的 API**。反证里的播报入口取 `showCaption`／`cmpCaption`（新旧码都有），**不得引入只存在于新码的函数**——否则旧码 FAIL 只因「函数不存在」，与被测行为无关，是伪咬合。
4. **断到可观察的终态，不查属性了事**。自愈（节点在浮层内、`hidden=false`、文本含指定串、实播采样 `opacity > 0.5`）、归位（关全屏回内嵌框、**份数恰为 1**）、往返（再开仍在、无重份）、**全程页面错误为 0**。

**如何交叉验证（两条腿，缺一不可）**：

- **纵向 · stash 自证**：把 `site/` 改动 `git stash` 回修前，用**同一份新 harness** 重跑。要求 FAIL **精确落在反证节**、条数可解释、错误信息可对号——r24a-fix2 实测 **16 项 FAIL** ＝ 2 档宽度 × 2 模式 × 4 项，并确实抛出 `Failed to execute 'appendChild' on 'Node': parameter 1 is not of type 'Node'`；`git stash pop` 复原后 **289 项全过**。**只报「新码全过」不算自证。**
- **横向 · 与走查互证**：同批新增的**常规**断言（§4b 往返门里的字幕条四项）在**旧码上应当全过**——这恰好复核了走查结论「现行入口下无可达销毁路径」。若它们在旧码上也 FAIL，则**要么走查漏了一条路径，要么断言写过了头**（把新实现的细节当成了规格），两种都必须查清再交。
- **两节一红一绿，各自证一事**：反证节红，证「隐患真实、新码确实堵住了它」；常规节绿，证「断言写的是规格、不是新实现的形状」。合起来才同时立住**隐患真实**与**断言不虚**。

**留档口径**：反证节**永久保留**在 harness 内，且其上方须写明「**为何是构造式而非复现式**」——否则后人见其无对应 bug，会当冗余删掉。现行实例：`tools/qa/vision_r24a.js` §4b（往返回归门）、§4c（断根反证），两节注释已按此写死。

**适用面不限于 DOM**：凡「修的是一类隐患而非一条故障」皆可套用（事件绑定、缓存失效、竞态守卫、数据迁移的幂等性……）。判据只有一条——**这道门能不能在旧码上红**。

## 8. 明确不做

- 不引外部字体/库/CDN；不做构建步骤。
- 不做暗色主题（绢帛底即品牌，本轮不出夜间版）。
- 不在代码中手写任何史实文案，一切叙事文本来自 site/data/*.json。
