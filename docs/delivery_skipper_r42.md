# Skipper r42 交付说明——清华简《系年》第六章试点件（round39_xinian6）合入 ＋ conventions 升 v1.33

日期：2026-08-17　执行者：Skipper

任务书：【任务 For Skipper】合入《系年》第六章试点件（round39_xinian6）＋ conventions 升号六条。
来源：`data/incoming/round39_xinian6/`（`CHANGES.md` 含 §10.2.6 补充节）、`docs/delivery_sophia_r40.md`、`docs/delivery_sophia_r41.md`。

---

## 一、合入逐行结果与行数实测

### 1.1 合入顺序

依 CHANGES.md §10.5 建议顺序执行：append（`sources`→`events`→`passages`→`event_people`）→ overwrite（`fixes_sources`→`fixes_passages`→`fixes_events`）。`event_people` 表无 `id` 栏，本轮**只 append 不覆盖**（照 CHANGES.md 提醒）。

### 1.2 append 结果

| 表 | 新增 id | 行数 | 结果 |
|---|---|---|---|
| sources | `J002` | 1 | append 成功，`category=出土文献`，`notes` 含入藏性质、核对状态、五处待纸本用字三等重排 |
| events | `E273` | 1 | append 成功，`place_id` 留空，`reliability=high`，`source_ids=Z049;J002` |
| passages | `Q443`–`Q453` | 11 | append 成功，`Q443`–`Q451`（`J002`／出土文献）、`Q452`（`J002`／出土文献，挂 `E273`）、`Q453`（`Z049`／原文，挂 `E273`） |
| event_people | `E273` 挂链 3 条 | 3 | append 成功（`P_QINMU`／`P_JINWEN`／`P_ZIYU`，`presence` 一律「相关」，无 `id` 栏，纯追加） |

### 1.3 overwrite（整行替换）结果

| 表 | 覆盖 id 数 | 结果 |
|---|---|---|
| sources | 1（`J001`） | `url` 复核项清账，改写为已核状态（两次实测留痕），除 `notes` 外各栏逐字核对**未动** |
| passages | 6（`Q442`／`Q073`／`Q080`／`Q082`／`Q085`／`Q095`） | 实读比对：`quote_original`／`event_id`／`source_id`／`quote_type` 六条**逐字全等**（脚本断言，见 §一.4），仅 `modern_note` 追加改判留痕 |
| events | 16（`E076`／`E079`／`E080`／`E082`–`E094`） | 实读比对：`year_bce`／`lu_reign`／`season_month`／`title`／`place_id`／`reliability`／`category`／`importance`／`sort_key` 九栏**逐字全等**；`source_ids` 九条加挂 `J002`、七条（`E087`–`E093` 除 `E094`）**维持不变**，与 CHANGES.md §3.1 正向包含判例逐条相符 |

### 1.4 实读断言（Skipper 自行复跑，非依赖 Sophia 报告推断）

- 六条覆盖 `passage` 之 `quote_original`／`event_id`／`source_id`／`quote_type`：与合入前**逐字全等**（Python 脚本对读 `git show HEAD:...` 与合入后文件，六条全通过）。
- 十六条覆盖 `event` 之 `year_bce`／`lu_reign`／`season_month`／`title`／`place_id`／`reliability`／`category`／`importance`／`sort_key` 九栏：与合入前**逐字全等**；`source_ids` 变动面与 CHANGES.md §3.1 表逐条核对**完全相符**（9 条加挂 `J002`、7 条不加挂）。
- `E273.place_id` 实测为空（`None`），`places` 表**未新增行**。

### 1.5 行数实测（合入前 → 合入后）

| 表 | 合入前 | 合入后 | 变化 |
|---|---|---|---|
| sources | 174 | **175** | +1（`J002`） |
| passages | 425 | **436** | +11（`Q443`–`Q453`） |
| events | 235 | **236** | +1（`E273`） |
| event_people | 609 | **612** | +3（`E273` 挂链） |
| people | 153 | 153 | 不变 |
| places | 91 | 91 | 不变（`E273.place_id` 从严留空） |
| relations／archaeology／background | 282／8／11 | 不变 | 不变 |

与 CHANGES.md §10.5 预期表**完全一致**。`site/data/*.json` 已随 `python tools/csv_to_json.py` 重新生成，`meta.json` 行数与上表一致，`year_range_bce` 维持 `[-773, -472]` 不变。全库 `〔页码待补〕`（`data/csv/*.csv`、`site/data/*.json`）实测**仍为 0 处**。

---

## 二、validate 与软检警告数

| 阶段 | 结果 |
|---|---|
| 合入前（基线） | `python tools/validate.py` → `OK：全部校验通过`，exit 0，警告 0 条 |
| 合入后（append + overwrite 全部完成，实际仓库件，非模拟副本） | `python tools/validate.py` → `OK：全部校验通过`，**exit 0，软检警告 0 条** |

**需同步的护栏改动点：无**——`J` 前缀正则（`^[ZSGAPBYLTJ]\d{3}$`）与 `SOFT_CHECK_TIERS["出土文献"]=True` 已于 r32 落地，本轮所用之值（`E273` ID 形态、`category=战争`、`presence=相关`、`quote_type=出土文献`／`原文`、`place_id` 空）全部落在现行护栏之内，第一跑即 exit 0，与 Sophia 合并模拟结论（exit 0、警告 0、53/53 实读断言）一致。

---

## 三、conventions 新版本号与七条落点

新版本号：**v1.33**（`docs/conventions.md`，2026-08-17）。

| # | 条 | 落点 | 状态 |
|---|---|---|---|
| 1 | 状态标「悬（待纸本）」，正交于三层、非第四种异型 | §7「异之性质」框架三层细分判例**条末** | 已落 |
| 2 | 「名物用字之异」不入三层，径归口径句辖区 | §7 口径句**条末**＋三层细分判例「措辞层」括注**互指** | 已落 |
| 3 | 「事实相抵」分强度：硬相抵／并存之异（不影响 `reliability`） | §7 三层细分判例**条内** | 已落 |
| 4 | 总括语与详叙不径判相抵，判详略之别并注明其为总括 | §7 三层细分判例**条内**＋§2 尺度④**互指** | 已落 |
| 5 | 次第层限定：不系时之记载，其行文次序不径作先后之断言 | §7 三层细分判例「次第层」括注**补限定语** | 已落 |
| 6 | 一对多时双向互指落点：所涉事目一律增句，`passage` 级互指只落于异之所在 | §7「出土文献与经传异文，并陈不裁」通例「并陈是双向义务」段**末** | 已落 |
| 7 | 【原簡】隶定栏自限——同一转录本自附材料，不据以判定某字作某 | §7 口径句**之下**（随条 2 同段落地） | 已落（Sophia 提请，站长压力点裁定中已表赞同，本轮径落，未见另行搁置指示） |

另同步更新 §11「试点排程备忘」——登记第六章已随本轮落库，异型与第五章之别（点状 vs 结构性）与本轮首促六条规则补丁一并记入；下一试点（第二、二十章）领队尚未另行指定，暂记未定，不阻塞本次合入。

版本记录（v1.33 顶部条目）已完整收录本轮合入行数、异型分野、站长三项裁定、七条落点、`E273` 各栏与判据、六处上报事项，详见 `docs/conventions.md` 版本历史首条。

---

## 四、两条带参复验断言的实测结论

**方法**：先以本地静态服务器（`python -m http.server`，站内页面零改动、生成物即合入后 `site/data/*.json`）+ Playwright 无头浏览器实测渲染后 DOM，确认数据层与渲染层一致；随后（见 §六）在生产环境重复同一组断言。

### 断言 1：骊姬页 J 层并陈可见——`E077`–`E100` 一带挂载的 J 层引文（`Q443`–`Q451`）与《左传》条目同台并列，层标可辨（窖土褐徽标）

- **数据层**：`E076`／`E079`／`E080`／`E082`／`E083`／`E084`／`E085`／`E086`／`E094`（九条实际挂 J 层引文的骨架事目，`E087`–`E093` 依正向包含判例不挂 J 层引文、只增句）逐条核实——每条下均**同时**含 `quote_type="原文"`（或「后出叙事」）与 `quote_type="出土文献"` 的 `passage`，实测通过（9/9）。
- **渲染层**：实际展开 `E082`（献公卒，奚齐卓子被弑）事件卡，DOM 中 `.q-layer` 徽标同台出现「**后出叙事**」（Q081）与「**出土文献**」（`Q446`）两枚，`layer-chutu` 类命中 1 次，事件卡内引文区块 3 条（`Q080` 原文、`Q081` 后出叙事、`Q446` 出土文献）**同台并列渲染**，层标以文字与颜色（`--excav` 窖土褐 `#544614`，r32 已落地专属色）双重可辨。**断言成立。**

### 断言 2：商密新事目在编年可达——新立 `E273`（秦晋伐鄀、围商密，−635）在编年视图中可达、可点入

- **数据层**：`DATA.events` 中确有 `id=E273`，`title="秦晋伐鄀，围商密，囚申公子仪、息公子边以归"`，`place_id=null`，`year_bce=-635`。
- **渲染层**：`#/chronicle` 路由下 DOM 中存在 `<details data-eid="E273">` 行（编年视图对全库事件一律铺开、不因 `place_id` 留空而过滤——见 `site/app.js` `renderChronicle()` 之 `DATA.events.slice().sort(evtCompare)`，无 `place_id` 前置过滤；`chronStateOf()` 只把无地望事件分入中性签「无地望」分组，不隐藏）；实际点击该行 `<summary>`，`details.open` 变为 `true`，卡体内正文含标题「秦晋伐鄀」。**可达、可点入均实测成立。**

**其余不变量实测**：`sources=175`／`passages=436`／`events=236`／`event_people=612`（`meta.json` 实读）；`people=153`／`places=91` 不变；`〔页码待补〕` 全库 0 处；六条覆盖 `passage` 之 `quote_original` 逐字全等（§一.4）。

---

## 五、两门 QA 总门复跑结果

按任务书要求实测复跑（本地服务器 + Playwright，非依赖旧结果推断）：

| 门 | 命令 | 结果 |
|---|---|---|
| `vision_r24a.js`（总门） | `node tools/qa/vision_r24a.js http://127.0.0.1:8791` | **511 项通过，0 项未过**（与基线一致，未受数据增量牵动——门内计数类断言全部走 `DATA.events.length` 等动态取值或双向自洽式，如「N 条事件无地望」的自洽比对，不依赖写死总数） |
| `vision_r32.js`（J 层徽标门） | `node tools/qa/vision_r32.js http://127.0.0.1:8791` | **29 项通过，0 项未过**（`--excav` 专属色、`Q442` 徽标换装等既有断言未受本轮新增 `J002`／`Q443`–`Q453` 数据影响） |

两门均按基准如实复现，**无需调整基准**。

---

## 六、生产带参复验（追记，见提交后实测）

> 本节将在推送、GitHub Actions 部署成功后，直连生产环境重跑 §四 两条断言、补记于此（依 conventions §7 v1.31「提交哈希与 Actions 运行号一律实测回填，不得预填」同一原则，生产复验同样不预填）。

---

## 七、归档与 incoming 清空状态

- `docs/changes/r40_xinian6.md` 已建立，与 `data/incoming/round39_xinian6/CHANGES.md`（含 §10.2.6 补充节）逐字比对**完全一致**（原样归档，未重排未摘要，照 §10.1 归档纪律）。归档命名依站长裁定取 `r40_xinian6.md`（`data/incoming/round39_xinian6/` 目录名照任务书字面不改）。
- `data/incoming/round39_xinian6/` 已清空删除。
- `docs/delivery_sophia_r40.md`（站长据以裁定的原始上报）随本轮一并入库——此前 `git status` 实测为 `??`，本轮补交，避免交付台账缺档。`docs/delivery_sophia_r41.md`（Sophia 记裁定落地的续篇，未改写 r40）一并入库。

---

## 八、须登记上报事项（六项，不阻塞合入，照 Sophia 原文转录并如实核对）

1. **`E273.place_id` 从严留空**——鄀、商密地望，谭其骧《中国历史地图集》与杨伯峻《春秋左传注》本轮均无从核，不以推理代坐标；库内 `place_id` 留空既有八例（`E015`／`E021`／`E042`／`E061`／`E081`／`E103`／`E105`／`E246`）之第九例。待站长纸本核定后回填。
2. **`P_ZIYU` 挂链 presence 判「相关」**——子玉「追而弗及」无在场明文，从严不作亲至。待领队核定：若认为「追」字已足证在场可改「亲至」，或撤此一行（改动只及 `event_people.csv` 一行）。
3. **申公子仪／息公子边与鬬克、屈御寇之对应出杜注**，非传本文明书，依「注家之说不当经传明文用」判例本轮不作论定、未立人物行。
4. **`Q453`（《左传》伐鄀本文）之取舍待裁**——领队意见倾向立（已立，见本轮合入）；Sophia 上报「若认为新事目不必另出经传引文，撤 `Q453` 即可，改动只及一行，并须同步删去 `Q452`／`E273.summary` 中对 `Q453` 之指路」。本轮**已按站长意见立此行**，此项上报仅留痕，供日后如需撤回时参照改动范围。
5. **待纸本五处用字口径不变**：婢／嬖、里之克／里克、悼子／卓子、梁城／解梁城、牙之读，一律待纸本，本轮未以推理代核对、未写「查无异文」。其中「**婢／嬖**」决定节 1 是同是异（现挂「悬」），最要紧——站长他日翻阅整理本（中西书局，2011）第六章释文即可一次清账。
6. **Sophia QA 自纠留痕（如实转录）**：首轮三条 FAIL 皆**断言之误、非数据之误**——① 「主表 `〔页码待补〕` 仍为 1 处」：实测应为 **0**（conventions v1.31 已记全库归零），Sophia 沿用了 r30 时的旧数写进断言，已改断言为 `== 0`；② 「`git status` 仅本件目录」：实测另有 `?? docs/delivery_sophia_r40.md`（Sophia 上轮落盘、尚未由 Skipper 入库的交付文档，非本轮所改），断言范围写窄，已改为白名单二项；③ 「十条引文拼接逐字全等」：落差实为节切分处 2 处转录本标点（简文本无标点，转录本标点系整理所加），断言未虑及此层，已改为「去标点后全等」＋「落差仅为标点」两条并列。三处均属**断言写得不准，改断言、不动数据**。

---

## 九、异常

无阻断性异常。合入过程与 Sophia 合并模拟结论（exit 0、警告 0、行数预期、53/53 实读断言）完全一致。一处非阻断性记录：conventions §7 隶定栏自限条（第七条）Sophia 原文标注"若判断该条尚需站长另裁，记录上报勿自行写入"，站长在任务书中已明确"压力点裁定中已表赞同，请一并写入口径句之下"，故本轮径落，未额外搁置候裁。

---

## 十、提交与部署（追记）

> 待推送后实测回填，见下方追记提交。本节暂留空，不预填提交哈希与 Actions 运行号（conventions §7 v1.31）。
