# 交付说明 · Vision · r13c（楚秦双主角前端集成 · 含 P0 缺口闭合）

前置：round13+fix13 数据已合入上线（fde50b0，14 主角进 JSON）。领队将 P0 抢修（前端漏配两位新主角）
升格为完整 r13 任务的第①项，要求并入一次做完、一个提交交付。本文即完整交付（P0 缺口彻底闭合 + 楚秦分区
+ 引文分层徽标 + FAQ）。纯前端，data/csv 与 JSON 零改动。

## 改动点（逐条）

**1. 两位新主角前端配置（P0 缺口闭合）＋ 主题色/徽记入 design_notes**
- `app.js` `PROTAGONISTS` 补 `P_QINMU`（秦穆公 `#955536` 西土赭）、`P_CHUCHENG`（楚成王 `#4C6A57` 江汉苍绿），
  字段结构同既有；分区自然排晋后（齐/鲁/郑/晋/秦/楚）。
- `STATE_EPITHET` 补 秦「据崤函之固，西霸戎狄」、楚「江汉之滨，南土大国」。
- `HOME_BADGE_POS` 补 秦 `[152,300]`、楚 `[510,610]`（**新投影** x=(lng-105)/17*1200、y=700-(lat-29.5)/9*700，
  落各自色块、不与既有徽记重叠）；`HOME_PENDING_HINT` 同步补「秦、楚」。
- 新徽记 `badge_qinmu.svg`（崤函之险：双峰夹峙、中通一线下有隘门——「据崤函之固」，兼记崤之战之创）、
  `badge_chucheng.svg`（凤纹：昂首三翎垂尾——楚人崇凤、筚路蓝缕），48×48 currentColor 同既有语言。
- `design_notes.md → v1.5`：色表升「十四主角」补两行（含配色理由与对比复核）、徽记段补两枚意象、
  新增 **§3.5 引文分层徽标**。

**2. 引文分层徽标体系（§3.5，设计增强）**
- `quote_type` → CSS 档（`QLAYER_CLASS`）：原文＝经传骨架（朱线、无徽标）；言论/评论＝淡墨徽标（左线仍朱，属经传层）；
  后出叙事＝灰虚线左线＋淡墨徽标（示「非当代记事」）；诗歌＝青绿；**经义异闻＝专属乌梅紫 `#6E5A86`**（公羊/穀梁传注异说）。
- 每条非原文引文右上角一枚同色胶囊徽标（`.q-layer`）＋同色左线＋极淡同色底，几何统一仅换色相；
  脚注不再重复类型（已进徽标）。未知 quote_type 走通用纯文本回退（不加徽标、不报错）。

**3. 关于页 FAQ 一行（孔子）**：导览「初识春秋」末补「为何还没有孔子？——本站按时代推进，现已覆盖前773–前621；
孔子生活的春秋晚期，尚在路线图上。」（青绿左线轻样式 `.faq-q`）。`#support-link` 爱发电占位**未动**（本轮不替换）。

## CDP 实测（headless Chrome，全部 PASS）

- **P0 闭合**：`#/p/P_QINMU/timeline`（秦穆公·14 事件）、`#/p/P_CHUCHENG/timeline`（楚成王·9 事件）直达不被重定向；
  二人 map/relations 三视图均可达；全站搜索均命中。
- **崤之战 E118 四源分层**：5 条引文 layers=[原文,言论,后出叙事,后出叙事,经义异闻]（左传经传＋左传言论＋史记晋后出＋
  史记秦后出＋**公羊经义异闻**）；经义异闻徽标 class=layer-jingyi、色 rgb(110,90,134) 紫、左线同色；脚注无类型重复（截图确认）。
- **黄鸟诗经层**：秦穆公线三良殉葬条「诗歌」青绿徽标 rgb(68,118,107) 正常。
- **地图轨迹**：秦穆公亲至 8 站（含 雍 / 韩原 / 殽(崤)）；楚成王轨迹（泓 / 城濮 挂链地名在图上）。
- **ego 关系**：秦穆公、楚成王各成边（秦晋之好 / 楚令尹线，nodes>1、edges>0）。
- **FAQ**：孔子一行到位；`#support-link` 仍「支持链接（筹备中）」未变。
- **分享卡**：对话框正常打开、canvas 就绪。

## 回归 / 无退化确认

- **四档 360/390/414/680 无横向滚动**：measure ALL_OK。
- **r13 回归**（关系全屏缩放、内嵌图不拦滚动、搜索、分享卡、旧链接改写、桌面双栏零变化）：ALL_OK。
- **r14 回归**（人物卡两行/hover 全文、滚动复位、首访引导）：ALL_OK。
- **原十二人**：文姜等时间线正常；首页两模式含秦/楚分区、state-tabs 出「秦」「楚」；页脚「14 条人物线」动态更新。
- **东部底图**：本轮未碰 base_map.svg，东部无改动、无退化。

## design_notes 更新

`docs/design/design_notes.md` v1.4 → **v1.5**：十四主角色表（+秦穆公/楚成王及对比复核）、徽记段补两枚意象、
新增 §3.5 引文分层徽标体系。

## 改动文件清单

- `site/app.js`：PROTAGONISTS +2、STATE_EPITHET +2、HOME_BADGE_POS +2、HOME_PENDING_HINT；
  `QLAYER_CLASS` 映射＋时间线引文渲染加分层徽标（类型移出脚注）。
- `site/index.html`：关于页导览补孔子 FAQ 一行（`.faq-q`）。
- `site/styles.css`：引文分层徽标样式（`.q-layer` 及 layer-yanlun/pinglun/houchu/shige/jingyi）；`.faq-q`。
- `site/assets/icons/badge_qinmu.svg`、`badge_chucheng.svg`（新）。
- `docs/design/design_notes.md`（v1.5）。
- `docs/delivery_vision_r13c.md`（本文）。

## 提交与部署

数据零改动（纯前端，未碰 data/csv 与 JSON）。已一次性提交 main（提交号见 git log），**push/Pages 留给 Skipper**。
（配色属新拟并已写入 design_notes v1.5；穀梁「异闻」层规则 L### 数据侧尚未启用，徽标档已就位，届时自动着色。）
