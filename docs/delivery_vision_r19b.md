# 交付说明 · Vision · r19b

任务：卫国后妃五线（庄姜/宣姜/息妫/骊姬/秦穆姬）前端配套＋轨迹降级通用机制＋楚成王回归走查＋20 人全站回归。数据已由 Skipper 合入线上（54ac9b6：fix19＋round20），本轮只动 `site/` 前端与 `design_notes`，未碰 `site/data/` 生成物、未碰 `data/csv/`。

改动文件：`site/app.js`、`site/index.html`、`site/styles.css`、`docs/design/design_notes.md`（→v1.12）、新增 5 徽记 `site/assets/icons/badge_{zhuangjiang,xuanjiang,xigui,liji,muji}.svg`、QA 脚本 `tools/qa/{screenshot_r19b.js,regress20.js}`。

## 一、轨迹降级机制（通用）

- 新增 `PLAY_DEGRADE_NOTE` 与 `setPlayDegrade(mode, degraded)`：亲至可落图地点 <2（连不成任一段轨迹）时，播放按钮 `hidden`+`disabled`、改显静态胶囊「亲至可考不足两地，暂无轨迹可播——已知地点如图。」（左朱线，`.play-degrade`）。按钮 `disabled` 故全屏浮层播放控件（`setupOverlayControls` 据 `mp.disabled`）一并隐藏，全屏内外一致。
- 单点人物：定点锚点＋事件正常显示；状态行改「亲至可考一地：X」。
- 并观：双方皆 <2 时同句降级（`#cmp-play-degrade`）；只要一轨可动即保留播放（单点者定点、可动者行进）。
- **降级清单（现库）**：穆姬、庄姜、宣姜（各仅 1 亲至落点）。武姜 2、骊姬 2、息妫 2 均可播，不降级。

## 二、楚成王回归走查

- 亲至轨迹 6 站可播：郢(E108/E137)→盂(E134)→郢(E091 重耳奔楚·成王享之)→宋商丘(E135)→申(E136)→郢(E113)。
- E137「郑伯始朝于楚，楚成王赐金」入时间线（亲至，前642）。
- 泓(E111)/城濮(E098)/召陵(E068) 三处 presence=相关 → 空心、不入轨迹，如旧。
- **走查观察（非缺陷）**：任务书简述弧线「郢→盂→宋→申→郢」，实际数据因 E091（前637 重耳退避三舍于楚）多一段回郢，故播放呈「郢→盂→郢→宋→申→郢」。此为数据真实反映（成王确于两役间在郢享重耳），可播无误，仅比简述多一回郢段，据实记录不自裁。

## 三、卫国色相家族与五主角色（design_notes §2）

新设 `--state-wei #2F5480`（缟素靛青系，靛青偏蓝一相，别于郑之青绿）。五主角色（灰阶＝sRGB→线性→WCAG Y→回伽马×255 感知亮度，与旧表同口径；对绢帛＝WCAG）：

| 人物 | 变量 | 色值 | 灰阶 | 对绢帛 | 族内 Δ灰阶 |
|---|---|---|---|---|---|
| 息妫（楚新阶） | `--p-xigui` | `#9E5B7C` | 112 | 4.3 | vs 成王60=52 / 庄王89=23 |
| 骊姬（晋新阶） | `--p-liji` | `#9B4E38` | 100 | 5.1 | vs 文公78=22 |
| 穆姬（秦新阶） | `--p-muji` | `#6E6560` | 103 | 4.9 | vs 穆公61=42 |
| 庄姜（卫） | `--p-zhuangjiang` | `#3A6693` | 99 | 5.1 | vs 宣姜72=27 |
| 宣姜（卫） | `--p-xuanjiang` | `#2A4A70` | 72 | 7.8 | vs 庄姜99=27 |

族内深浅 Δ灰阶均 ≥13（色盲/灰度冗余照旧）；各族最亮者（息妫 4.3）沿用旧库放宽容差（郑昭 4.3 先例），对白皆 ≥4.1（五女最低息妫 5.0）。

五徽记（48×48 圆框线描 currentColor，形各异、与其人诗篇/本事呼应）：庄姜=双燕（燕燕于飞）、宣姜=新台临河、息妫=桃花（桃花夫人）、骊姬=觚·鸩（鸩酒嫁祸）、穆姬=薪火（登台履薪）。庄姜双燕别于楚成王凤纹（双简燕形／单繁凤形＋异色）；骊姬觚别于问鼎之鼎与会盟之鼎（喇叭口沙漏形）。

## 四、卫分区与首页地图

- `HOME_BADGE_POS.卫=[692,214]`（居底图卫块中，卫块 `data-state="卫"` 已存在，自动成键盘可达热区）；卫色块可点、落庄姜/宣姜双徽簇；右面板列二人卡。
- `STATE_FAMILY_VAR` 增卫；分组按「主要发生国」（conventions §6.6）：庄姜/宣姜 `home:"卫"`、息妫 `home:"楚"`、穆姬 `home:"秦"`、骊姬随首国晋。楚簇现 3 徽、秦 2、晋 2、卫 2。
- 跨国卡片流向改「出身→归宿」首末国：庄姜/宣姜「齐→卫」、息妫「陈→楚」（中间「息」不列）、穆姬「晋→秦」。
- **关系全景阵营着色对齐**：新增 `panoStateKey()`＝主要发生国（主角取 meta.home，否则 state 首国），排位/底晕改用之，使节点色与阵营弧一致——庄姜/宣姜归卫弧、息妫归楚弧、穆姬归秦弧、武姜归郑弧（**顺带修正旧武姜「郑色节点却落申槽」的色位错置**）。非主角仍按 state 首国。

## 五、三条必查关系边走查（ego 图）

均在库、ego 图正常渲染：
1. 宣姜—许穆夫人：`宣姜 —亲属-直系/母→ 许穆夫人`（high）✓
2. 庄姜—卫桓公：`庄姜 —亲属-直系/养母（以桓公为己子）→ 卫桓公`（high）✓
3. 息妫—楚成王：`息妫 —亲属-直系/母→ 楚成王`（high）✓，ego 图中「母」边居中直连。

诗经 P 层徽标：Q153 硕人 / Q155 燕燕 / Q156 新台 / Q158 二子乘舟 均 `quote_type=诗歌`，按 §3.5 显青绿徽标，正确。

## 六、20 人全站回归（QA 截图＋状态断言，Playwright chromium）

- 播放按钮：17 人正常可播（2–14 站），3 人降级（穆姬/庄姜/宣姜），无页面报错。清单见一。
- 并观：庄姜×宣姜降级；穆姬×秦穆公、息妫×楚成王、庄姜×文姜均可播（单点定点＋对方行进）。
- 搜索：庄姜/宣姜/息妫/骊姬/穆姬全部命中，均标「人物·主角」，state 标注正确（齐/卫、陈/息/楚、晋、晋/秦）。
- 分享卡：`brand-caption` 自动「20 条人物线」；色签行随 `PROTAGONISTS.length=20` 自适应（20 签宽 786px<1080，居中留白足）。
- 四档宽度沿用既有 `--content-max`/响应式，未改布局骨架；680px 下降级胶囊随工具条 `flex-wrap`，正常换行。
- QA 截图（`tools/qa/screenshots/`，.gitignore 不入库，可重跑）：r19b-badges、home-clean、home-wei-panel、muji-map、xigui-ego、pano 等。

## 七、质量门与提交

- `python tools/validate.py` → OK（本轮未改数据，仍过一遍确认）；`node --check site/app.js` 通过；`node tools/test_binguan_play.js` → TEST PASS（并观引擎未回归）。
- 提交：本前端批次 commit（r19b），未推送/未部署。

## 八、线上复验（已部署·已回填）

**部署闭环（领队裁定/Skipper 执行）**：领队批准推送部署；武姜色位修正裁定「保留」，不回退。Skipper 推送 commit `eee27ab` 到 `origin/main`（`3039fd5..eee27ab`），GitHub Actions「Deploy site to GitHub Pages」**Run 30305635423 success**（绿），线上已部署。

**真机复验**：QA 脚本已补 `QA_BASE_URL` 支持（首次误以本地服务器跑，修正后重跑），核验时间 **2026-07-27T21:14Z**，`QA_BASE_URL=https://chunqiu.timechorus.com`。

- **资产上线（curl 直核生产）**：`/assets/icons/badge_zhuangjiang.svg` 200；`styles.css` 含 `--p-xigui:#9E5B7C`（新色变量已上线）。
- **20 人回归（regress20，真机）**：17 人可播（2–14 站）、降级三人 **穆姬/庄姜/宣姜**（各 1 落点，状态「亲至可考一地：X」），**页面错误＝无**，与本地断言逐字一致 → **零退化**。
- **播放降级断言（真机）**：庄姜地图 `playHidden=true, degradeHidden=false`，降级句「亲至可考不足两地，暂无轨迹可播——已知地点如图。」，状态「亲至可考一地：朝歌。」；楚成王地图 `playHidden=false`，状态「亲至轨迹共 6 站，前671 起。」→ 弧线可播。
- **并观降级（真机）**：庄姜×宣姜 播放隐藏＝是、降级＝是（双方皆 1 落点）；穆姬×秦穆公（交会 2 处）、息妫×楚成王 均可播不降级 → 单点者定点、可动者行进，符合机制。
- **搜索命中五主角（真机）**：庄姜「齐/卫」、宣姜「齐/卫」、息妫「陈/息/楚」、骊姬「晋」、穆姬「晋/秦」全部命中、均标「人物·主角」，state 标注正确。
- **三条必查关系边（真机 ego 右栏）**：宣姜 ego 含「许穆夫人」母边＝是；庄姜 ego 含「卫桓公」养母边＝是；息妫 ego 含「楚成王」母边＝是。
- **诗经 P 层徽标（真机·庄姜时间线展开态）**：两枚 `.q-layer`＝「诗歌」，计算色 `rgb(68,118,107)`＝青绿 `#44766B`，按 §3.5 正确（硕人 E138＋燕燕 E141）。
- **卫分区/首页地图（真机截图 r19b-home-map）**：卫色块落庄姜/宣姜双徽簇、可点、选中环显示；楚簇 3 徽、秦 2、晋 2、卫 2。
- **关系全景阵营对齐（真机截图 r19b-pano）**：息妫入楚弧、庄姜/宣姜成卫弧（靛青节点）、穆姬近秦、骊姬近晋、武姜归郑弧，与本地一致。
- **分享卡**：`brand-caption` 生产端自动「20 条人物线」。

**线上复验结论：各项核验通过，降级清单线上确认为穆姬/庄姜/宣姜，零退化项、零页面错误。**

（QA 截图存 `tools/qa/screenshots/`，.gitignore 不入库、可按上法重跑；本次 `r19b-zhuangjiang-map` 一项因脚本等 `#btn-play` 可见而超时——该按钮降级态本应 `hidden`，属脚本选择器预期外，非站点异常，降级态由 `r19b-muji-map` 覆盖。）

## 九、验收偏差上报 / 记录上报

- 无史学疑点、无排程改动建议自裁。
- 楚成王弧线「多一回郢段」如二·走查观察所记，据实呈现，非缺陷，供领队知悉。
- 「主要发生国」阵营对齐顺带修正武姜旧色位错置（郑色落申槽），属既有小瑕疵的清偿，若领队认为武姜应保留原申槽位可回退 `panoStateKey`，请示下。
