# 交付说明 · Sophia r50（地望回填备料件）

**执行者**：Sophia（史料研究员）
**日期**：2026-09-19
**任务书**：`team/round50_prompts.md` §二【任务 For Sophia】r50 地望回填备料（现行文本，2026-09-19 实读）
**本轮之源**：站长 2026-09-19 纸本回填九点地望并出三项裁定，原表 `team/yema_backfill_r46.md`（§D／§A／§B／§C 站长填写原文，§E 主会话核读与四条上报疑点，§E6 两费分立之依据链全过程）
**权威规范**：`docs/conventions.md` **v1.42**（实读）
**基线**：`main` **`a9c6ea0`**，与 `origin/main` 同哈希，工作区干净（实读）
**产出目录**：`data/incoming/round50_diwang/`
**交付件**：`CHANGES.md`、`places_new.csv`、`fixes_places.csv`、`sim_diwang.py`、本文件

> **★本文件 §一–§五系交件当日（2026-09-19 上午）之状态；同日领队／站长出续裁六条（裁六–裁十一，任务书 §一之二），本件依裁修订，全程见 §六。**
> 依 conventions §7 v1.29「判定反转须如实记录，不得抹平」，**上文原文照留不改**，凡状态有变者就地加【续裁】注记。
> **续裁所致之数据改动只三处**：`L_DAYE.certainty`／`L_CHENGFU.certainty` 各 `low` → `medium`（裁十），`L_CHENGYI.description` 追加裁六之结与互见之注（纯追加）。**行数不变。**

---

## 一、做了什么

**只动 `places` 一表**：新立 **7** 行、整行替换 **4** 行，合 **11** 行。`data/csv/`、`site/data/`、`docs/conventions.md`、`tools/`、`events.csv` **一字未动**；未 `git add`／`commit`。

| 件 | 内容 |
|---|---|
| `places_new.csv` | 新立 7 点：`L_FEIJISHI`（费·季氏之费）／`L_FEIBO`（费·费伯之费）／`L_HOU`（郈）／`L_CHENGYI`（成）／`L_YUN`（郓·西郓）／`L_GUIYIN`（龟阴）／`L_YANGGUAN`（阳关） |
| `fixes_places.csv` | 整行替换 4 点：`L_JIAGU`／`L_DAYE`／`L_CHENGFU`（各补 `lat`／`lng`／`coord_certainty` 三栏＋`modern_location`，`coord_basis` 纯追加）、`L_HUAN`（**只动 `coord_basis` 一栏，纯追加**） |
| `CHANGES.md` | 逐点之判、两费分立之依据链、留痕解除三处、讙之精度判、上报六项、修正建议十二条、conventions 与 `B003` 拟文两件、合并模拟实测 |
| `sim_diwang.py` | 合并模拟＋**396 条**实读断言（供 Skipper 复跑）。**不属数据，不入 `data/csv/`**——**续裁后增补至 415 条，见 §六** |

### 逐点之 id 与坐标（十二点，全数）

| # | 点 | 办法 | id | lat／lng | `certainty` | `coord_certainty` |
|---|---|---|---|---|---|---|
| 1 | 费·季氏之费 | 新立 | `L_FEIJISHI` | **35.33／117.90** | medium | medium |
| 2 | 费·费伯之费 | 新立（孤点） | `L_FEIBO` | **34.98／116.45** | low | low |
| 3 | 郈 | 新立 | `L_HOU` | **35.81／116.63** | medium | medium |
| 4 | 成 | 新立（★上报） | `L_CHENGYI` | **35.79／116.85** | medium | medium |
| 5 | 郓（西郓） | 新立 | `L_YUN` | **35.60／116.03** | medium | medium |
| 6 | 龟阴 | 新立 | `L_GUIYIN` | **35.80／117.50** | **medium** | **low** |
| 7 | 阳关 | 新立 | `L_YANGGUAN` | **35.97／117.13** | medium | medium |
| 8 | 夹谷 | 补三栏 | `L_JIAGU` | **36.10／117.80** | low（维持，裁十准） | **low** |
| 9 | 大野 | 补三栏 | `L_DAYE` | **35.60／116.15** | **low → medium**（裁十）※ | **low** |
| 10 | 城父 | 补三栏 | `L_CHENGFU` | **33.87／113.28** | **low → medium**（裁十）※ | **medium** |
| 11 | 讙 | 只补页码、议精度 | `L_HUAN` | **不动**（35.85／116.70） | low（不动） | **low·维持不升** |
| 12 | 舒州 | **一字不动** | `L_SHUZHOU` | — | — | — |

※ 二行之 `certainty` 交件当日俱作「low（不动，列修正建议）」，**2026-09-19 裁十准升 `medium` 并令本件动手改之**，已落实；原状态之文照留，全程见 §六。

十点坐标一律取两位小数概位，逐点按 conventions §4 公式回校 x／y 并写入 `coord_basis`（脚本以实算复核所书之值，十点逐一相符）；**无一点出自谭图图面之量度**。

---

## 二、依据

1. **史料依据**：杨伯峻《春秋左传注》十处页码（僖元 279／隐元 10／昭二十五 1468／定十二 1587／定十 1576／成四 817／定七 1560／哀十四 1682／昭十九 1402／哀六 1633／桓三 97／定八 1570／隐五 39）与谭其骧《中国历史地图集》齐鲁幅（页 26–27）、楚吴越幅（页 29–30），**俱系站长 2026-09-19 纸本亲核回填**；本库逐处标核对状态「**纸本，站长核对**，2026-09-19」，页码末缀一律取「**页码同次核**」（§7 v1.29 二式之一）。
2. **引注体例**：谭图一律取统一式「《中国历史地图集》第一册·春秋·齐鲁幅（页 26–27）」（裁四），并**逐处附幅名未照第一册目录核之留痕**；站长原填之形（「《春秋历史地图集·春秋·齐鲁》26–27 页」）只作留痕，不作正式引注。
3. **判档尺度**：照 `L_BOJU`（柏举）、`L_JIELIANGCHENG`（解梁城）、`L_YINGGU`（颍谷）三先例——**优先序前二位一致则由 `low` 升 `medium`，落于同一坐标物者亦止于 `medium`**；十点无一取 `high`（判据链末端未闭，见 `CHANGES.md` §11.2）。宁降不虚。
4. **口径一（两栏不连坐）之实做**：**龟阴** `certainty` medium／`coord_certainty` low（地名所指可辨，而点戳不准），**讙** 证增而点不升（详下），**费伯之费** 两栏同 low 而各有其判据、非连坐。
5. **口径四（留痕不抹平）之实做**：`L_JIAGU`／`L_DAYE`／`L_CHENGFU` 三行原自限句（「本轮无从核」「三栏留空是判断，不是遗漏」「幅界存疑·一并留痕待核」）**逐句照留**，就地续记解除之由与日期，并明书**不得改写成「一直如此」**；脚本以 `startswith` 逐行自证 `coord_basis` 系纯追加。

---

## 三、几处拿不准、须领队或站长裁而我未自行取舍者

> 以下六项详见 `data/incoming/round50_diwang/CHANGES.md` §7；其中前两项**宜合入前定**（涉 ID 不可复用或涉推翻在案之判）。

1. **★成（`L_CHENGYI`）与库中既有 `L_CHENG`（郕）今地相涉——任务书未及此行**。`L_CHENG`（郕国，汶上/宁阳一带，35.75／116.80，两栏俱 low）与本件所立之成相去**不足十公里**。郕是国、成是鲁孟氏之邑，二者是否一地（国灭而地为邑？抑或二地同名？）**经传无明文**，杨注于隐五、定十二分处出注而未明言其为一地。**此与 r46 回填表 D2「讙」之型完全相同**（彼亦「任务书称未立点、而库中已有一行」，经站长核而定为一地，遂不立新点）——故**从严立为两点、互见不合并、不改 `L_CHENG` 一字，不自决**。裁为一地则本行撤回、改补 `L_CHENG` 三栏（拟文已备）。
2. **★`E281.place_id` 本件不挂——任务书与归档件正面相抵**。口径六称其为「**空位补入**」，而 `docs/changes/r46_gugan.md` §2 明记「**`place_id` 留空二条，俱是判断不是遗漏**：`E281`（阳虎行迹历鲁之阳关、齐之西鄙、宋、晋四方，**无单一事发地**）」。故该空非「空位」而是「判断」，回挂须先推翻 r46 原判，**属事件层之判**——与口径六自己为 `E282`／`E274` 所立之理由同型。本件**不挂、不改 `events` 一字**，请领队裁；若裁回挂，另须依 §7 v1.29 于 `E281.summary` 记明原判之被推翻（本件不代拟该句）。
3. **四个新 ID 之形，请合入前核可或改判**（ID 不可复用）：`L_FEIJISHI`／`L_FEIBO`（**两费俱加后缀，不予其一以光板 `L_FEI`**——Y1 之病根正是「哪一个是『那个』费」，光板 ID 等于把已被推翻的默认又装回去一次）；`L_CHENGYI`（备选 `L_CHENGMENG`）；`L_YUN`（与既有 `L_YUAN` 形近而照立不加后缀，照 `L_CHENGFU` r47「无经传第二称则取后缀即杜撰」之例）。
4. **`L_JIAGU.state` 合入后仍为空**（实测全库仅此一行）。口径七云「`state` 栏必填」，而该行留空系 r45 明记之判（会地属齐属鲁经传无文，不以今地异说反推其国属）；本件判口径七施于**新立七点**，不及既有之行，**故不填**。若领队以为宜一并填，请示下——填之须先推翻 r45 之判。
5. **三点 `certainty` 之议，本件不代改**（逾「补三栏」之命）：`L_DAYE` 建议升 `medium`、`L_CHENGFU` 建议升 `medium`、`L_JIAGU` 建议**维持 `low`**（杨注自言「夹谷有三」，且引杜注「夹谷即祝其也」，东海祝其说去莱芜三百余里，系**重大异说**）。`L_HUAN.certainty` 亦同（见下）。
6. **一处对任务书产出清单的补件**：任务书产出四项未列 `fixes_places.csv`，而「补三栏／补页码」四点之行俱在主表之内、非新增而是整行替换，不能混入 `places_new.csv`（混入则 Skipper 分辨不出哪几行 append、哪几行按 id 覆盖）。照 fix44c／r49 先例另立一件，**仍在 `data/incoming/` 之内、`data/csv/` 一字未动**。**如实登记请核可**。

### 讙（`L_HUAN`）之判，单列一节

口径一问「升与不升由你依两栏分工判」。**判：`coord_certainty` 维持 `low`，不升。**

若只论证之多寡（杨注三处互见＋谭图同标），依 `L_BOJU` 之型似可升；**而 `coord_certainty` 所答者是「点有没有戳准」，不是「证有几家」**。本行现点 35.85／116.70 较宁阳县治（约 35.76／116.81）**正北约十公里、正西约十公里**，方位是**正西北**；而二书主流之指是「**北而稍西**」（杨注三处中二处、谭图一处）。**证既增而点未随之校，则不得以证之增换取点之升**。宁降不虚。

`certainty`（议升 medium）、坐标（议收紧至约 35.88／116.78）、`modern_location`（「肥城」宜收窄）三项**逾本件之问，列修正建议不代改**。本行本轮实际所动者**惟 `coord_basis` 一栏，且系纯追加**。

### 另附：三处任务书与实况不符，请领队回写（活文档原则）

| 处 | 任务书作 | 实况 |
|---|---|---|
| §二 #11 | 三处互见「**皆作**宁阳县北而稍西三十余里」 | 站长原填之定公十年注实作「**西北**三十余里」——二作「北而稍西」、一作「西北」，非三处全同 |
| 口径四 | 「`L_CHENGFU` 之楚吴越幅**幅界存疑**」 | `L_CHENGFU` 数据行内并无「幅界存疑」字样；该存疑在 **r46 回填表 B9 之问**中 |
| 口径六／十二点表 #4 | `E281` 之空为「空位补入」／成为「新立」 | 见上第 2、第 1 项 |

---

## 四、验收自查结果

| 项 | 实测 |
|---|---|
| **合并模拟** `python data/incoming/round50_diwang/sim_diwang.py` | 交件当日 **396 PASS, 0 FAIL**；**续裁修订后 415 PASS, 0 FAIL**（断言增补 19 条，见 §六），俱 exit 0 |
| **临时根质量门** `validate.py`（合入后态） | 「OK：全部校验通过」，**exit 0** |
| **仓库根质量门** `python tools/validate.py`（基线复核） | 「OK：全部校验通过」，**exit 0** |
| **逐表行数**（`csv.DictReader` 实读，不含表头） | `places` **97 → 104**；`sources` 195／`passages` 506／`events` 265／`people` 166／`event_people` 673／`relations` 289／`archaeology` 8／`background` 11 **九表余八表全等** |
| **孤点**（未被 `events.place_id` 引用） | **10 → 17**（新立七点俱为孤点；`E281` 不挂，`E282`／`E274` 依口径六不改）。**孤点照样上图** |
| **上图之点**（`lat`／`lng` 俱非空） | **90 → 100**（净增 10：新立 7＋补栏 3） |
| **`data/csv/` 改动** | **零**。`git status --porcelain` 实测两条 `??`：`data/incoming/round50_diwang/`、`docs/delivery_sophia_r50.md` |
| **护栏** | 两 CSV 表头与主表逐字全同、无 `novel*` 列、逐 cell 无嵌入换行（脚本断言） |
| **留痕自证** | 四行 `coord_basis` 逐行 `startswith` 原文（纯追加），其余七栏一字未动（脚本断言） |
| **`L_SHUZHOU`／`L_CHENG`／`events`** | 一字未动（脚本断言：`E281` 仍空、`E282` 仍 `L_QUFU`、`E274` 仍 `L_JIAGU`、本件无 `events` 增量文件） |

### 一处自查中的自纠，如实报（QA 诚实优先）

首版 `sim_diwang.py` 跑出 **2 条 FAIL**，逐条查因，**两条俱是断言之误、非数据之误**：① 「`L_HUAN` 明书『一直如此』之自诫」——`L_HUAN` 本无自限可解（其为补页码之行，非解除之行），首版把四行一律当作解除之行；② 「`L_CHENGFU` 谭图引注取统一式」——首版以「齐鲁幅」三字为钥，而 `L_CHENGFU` 注内有一句**自辨语**「与本轮新用之齐鲁幅不同，勿相混」（其所引实为楚吴越幅），被误当作引注。**二者俱改断言、不改数据**，原委留在 `CHANGES.md` §9.1 与脚本注中。

---

## 五、拟文两件（只拟，未落笔；合入是 Skipper 之事）

全文见 `CHANGES.md` §8。**涉版本号处一律写「升下一号」，未写死号。**

1. **`sources.B003.notes` 追加＋conventions §7 v1.35 段①就地续记**——已知之幅增第三：**齐鲁幅＝页 26–27**，**附幅名未照目录核之留痕**（页序旁证：页 26–27 恰落于已知两幅之间）；并明立一句「**幅名之核与页码之见是两件事**」，与 v1.35 ⑤「版次可核与该书已核是两件事」同构。
2. **conventions §7 新增通例「同名异地，两点分立，不择一而废其余」**——三条要件（ID 与 `ancient_name` 须可分且**二者俱加后缀、不以光板 ID 许其一**／互见须双向可寻／无事目系之之点其立点之由须入 `description`），首例即两费，并连同其**错误史**（误取→核出→复翻→两存）一并写入；末附与 §2 同音撞名判例、与 `L_CHENGFU`（v1.41）两条之分账。

---

## 六、2026-09-19 续裁六条之落实（交件后；§一–§五照留不改）

**依据**：`team/round50_prompts.md` §一之二「Sophia 交件后之续裁（2026-09-19，六条）」，现行文本实读。
**全程详记**：`data/incoming/round50_diwang/CHANGES.md` **§13**。

| 裁 | 内容 | 本件之动 |
|---|---|---|
| **裁六** | 郕与成**从严两点分立，登记待核**；`L_CHENG` 一字不动，两行互见并注明「是否一地，经传无明文，本轮未核」 | `L_CHENGYI.description` **纯追加一段** |
| **裁七** | `E281` 不挂，`events` 一字不动 | **无动**（本已未动） |
| **裁八** | 四新 ID 全数核可（两费俱加后缀之理由并经采纳写入任务书；备选 `L_CHENGMENG` 不取） | **无动**，四 ID 定案 |
| **裁九** | `L_JIAGU.state` 维持留空，本件之判得准 | **无动** |
| **裁十** | `L_DAYE`／`L_CHENGFU` 之 `certainty` **low → medium**；`L_JIAGU.certainty`、`L_HUAN.coord_certainty` **维持 low** | **二行二栏改值＋四行 `coord_basis` 纯追加** |
| **裁十一** | `fixes_places.csv` 另立核可 | **无动** |

### 改动逐行（续裁所致，合三处）

| 行 | 所动之栏 | 交件当日 → 续裁后 | 性质 |
|---|---|---|---|
| `L_DAYE` | `certainty` | `low` → **`medium`** | 改值（裁十） |
| | `coord_basis` | 追加【★裁十】一段（升档判据＋不升 `high` 之由＋升档之日） | **纯追加** |
| `L_CHENGFU` | `certainty` | `low` → **`medium`** | 改值（裁十） |
| | `coord_basis` | 追加【★裁十】一段（同上，另含 §13.2.1 之措辞如实） | **纯追加** |
| `L_CHENGYI` | `description` | 追加【★裁六】一段（裁之结、裁之理由、与裁二之别、互见只落一侧、不入拟文二之用例） | **纯追加** |
| `L_JIAGU` | `coord_basis` | 追加【★裁十】一句（维持 `low` 得准） | **纯追加** |
| `L_HUAN` | `coord_basis` | 追加【★裁十】一句（`coord_certainty` 维持 `low` 得准；修-D／E／F 仍开放） | **纯追加** |

**`certainty` 之改只此二栏**；`L_CHENG`、`L_SHUZHOU`、`events` 全表、`L_JIAGU.state`、`docs/conventions.md` 一字未动（脚本断言）。

### 裁六之落实中，一处如实记：互见只落一侧

裁六令「两行互见」而又令「`L_CHENG` 一字不动」——二者相合之惟一解是**互见只落于新点一侧**（`L_CHENGYI` 指向 `L_CHENG`，反之不然）。本件照此办，并于该行明书「**此系裁定所限，非遗漏**；反向之互见俟本待核项结案时一并补入」。**本件不为此另出 `L_CHENG` 之修正建议**——提之即与「一字不动」相牴；其回补之责已由裁六自身入承接登记。

**另记一分际**：郕与成是「**疑同而未核**」，与两费之「**已定之异地**」不同型，故**不收为本批所拟通例「同名异地，两点分立」之用例**（首例仍只两费）。二者若混，该通例之适用条件会被悄悄由「实为两地」放宽成「疑为两地」——那正是本库不愿开的口子。

### 裁十之落实中，一处措辞之如实（只报不改判）

裁十之文述二行「**注文自书**其取 `low` 之由系『优先序之首二位本轮均系纸本不可得而未核』」。**实读核之**：此语**于 `L_DAYE` 确切**（该行注文明书「其与今巨野泽之对应本库本轮未取得优先序内之依据，故取 `low` 而不取 `medium`」）；**于 `L_CHENGFU` 则不尽然**——该行「本轮无从核」一段所说者是**三栏留空**之由，并未逐字自陈 `certainty` 何以取 `low`，其取 `low` 之由在 `docs/changes/r46_gugan.md` §7.1，**不在本行注文之内**。

**措辞之差不影响本裁之成立**（所据之实同为「今地无从核」，今已核则前提同样已消），故**照裁执行**；惟**不得以裁定之转述冒充本行之原文**，已就地记入 `L_CHENGFU.coord_basis` 与 `CHANGES.md` §13.2.1。

### 脚本断言之增补与一处锁之收放

`sim_diwang.py` 增 **19 条**（396 → **415 PASS, 0 FAIL**）：裁十一组（二行升档之实转、二处维持、四钥入注、`L_DAYE` 两栏不连坐）、裁六一组（互见语、指向、只落一侧及其由、与裁二之别、不入通例用例、当日原句仍在、`L_CHENG` 原值实读复核）。

**并改一处断言之锁**：原「四行七栏一字未动」中之 `certainty` 一栏，于 `L_DAYE`／`L_CHENGFU` 二行依裁十升档，故二行改锁六栏，升档之实转另设专条逐项验——**改的是适用范围，不是放松**，专条比原锁更严。

### 仍开放者（不阻合入）

① `L_HUAN` 之修-D（`certainty` 议升 `medium`）／修-E（坐标议收紧至约 35.88／116.78，回校 x=832、y=204）／修-F（`modern_location`「肥城」议收窄）——续裁只及 `coord_certainty` 一栏；② 修-G／修-H（`sources.Z018`／`Z125` 两源 `notes` 之追记）——本件不动 `sources` 一字。

---

## 七、承接与不属本件者

- **舒州 `L_SHUZHOU`**（Y2 两书南北相反约五百公里）：依裁五**一字不动**，候站长复核谭图标注原样。
- **C 组三问**（C11 巳月二字之有无、C12 定元经「九月，大雩。立煬宮」、C13 巳／已）：依裁五列 r50-B 件，本件**不涉 `passages`／`sources` 一字**。
- **谭图齐鲁幅之幅名**：本轮**未照第一册目录核**，十处引注逐处留痕；结案后照楚吴越幅 v1.37 之例回写 `B003` 与 conventions。
