# delivery_skipper_r19b.md —— fix19 + round20 并行备料撞号重编合入

任务书：【任务 For Skipper】fix19（楚成王亲至补录）与 round20（卫国后妃五线：庄姜/宣姜/息妫/骊姬/秦穆姬）并行合入，四项裁定落地，conventions 新增两条规则。

## 一、撞号诊断

两批备料各自独立"接续当时主表最大号"起编，未预先划分号段，实际撞号如下：

| 表 | fix19 占用 | round20 原编号（撞号/重复段） |
|---|---|---|
| events | E134–E137（4条） | E134–E149（16条，与 fix19 全段重叠） |
| sources（Z前缀） | Z068–Z070（3条） | Z068、Z069（与 fix19 同号，内容完全不同） |
| sources（S前缀） | 无 | S009（与既有 **S005**《史记·秦本纪》同书同篇同 URL，属重复而非撞号） |
| passages | Q148–Q152（5条） | Q148–Q164（17条，与 fix19 全段重叠） |
| relations | 无 | R165–R201（37条，续接既有主表最大号 R164，本身不撞号） |
| places | L_YU、L_SHEN（拼音号） | L_XI、L_CAI（拼音号，不冲突） |
| people | 无 | 14 个新 `P_XXX`（不冲突） |

责任判定：两批各自编号逻辑均无误（都正确续接了各自视角下的主表最大号），撞号根源是任务书并行下达时未预分配号段，已按裁定 1 写入 conventions v1.15 §2「并行备料 ID 网段预分配」新规则备案（见下）。

## 二、合入顺序与重编（裁定1落地）

**先合 fix19（E134–E137 保号原样，无任何改动）；后合 round20，events 从 E138 起顺延，sources/passages 续接 fix19 尾号之后，全部外键联动重编。**

### 重编映射表（旧号→新号，round20 视角；fix19 全表原样未改）

**events（offset +4，E134→E138 … E149→E153）**

| 旧号 | 新号 | 事件 |
|---|---|---|
| E134 | E138 | 卫庄公娶齐女庄姜 |
| E135 | E139 | 陈女戴妫生完，庄姜以为己子 |
| E136 | E140 | 州吁嬖而好兵，庄姜恶之 |
| E137 | E141 | 州吁弑桓公自立 |
| E138 | E142 | 卫宣公为急子娶齐女而自纳之（新台） |
| E139 | E143 | 急子寿子争死（寿朔之难） |
| E140 | E144 | 二公子立黔牟，卫惠公奔齐 |
| E141 | E145 | 齐人使昭伯烝于宣姜 |
| E142 | E146 | 楚文王败蔡师于莘，获蔡哀侯 |
| E143 | E147 | 楚文王灭息，以息妫归 |
| E144 | E148 | 楚文王以妫故伐蔡 |
| E145 | E149 | 息妫生堵敖及成王 |
| E146 | E150 | 令尹子元蛊文夫人 |
| E147 | E151 | 晋献公立骊姬为夫人 |
| E148 | E152 | 秦穆公娶晋献公女穆姬 |
| E149 | E153 | 韩原之后，穆姬登台履薪 |

`event_people_new.csv`、`passages_new.csv` 中所有 `event_id` 外键同步按此表替换；末行 `E092,P_MUJI,...`（挂靠既有事件）不在映射表内，原样保留。

**sources（Z 前缀续接 fix19 尾号 Z070；S 前缀去重复用既有号）**

| 旧号 | 新号 | 内容 | 处置说明 |
|---|---|---|---|
| Z068 | Z071 | 《左传·隐公四年》 | 续接 fix19 Z070 之后 |
| Z069 | Z072 | 《左传·桓公十六年》 | 续接 |
| S009 | **S005**（复用，未新建行） | 《史记·秦本纪》 | 与既有 `S005` 同书同篇同 ctext URL，round20 的 `events_new`/`passages_new` 中 `S009` 引用全部改指 `S005`，未新增 sources 行——按既有"同源复用不新开号"惯例（如 G004、Z050 本轮均被复用）处理，非史料判断，纯 ID 去重 |

`events_new.csv`（E141/E142/E143/E144/旧号、对应新号 E145/E146/E147/E148 中的 E142/E152）及 `passages_new.csv` 中所有引用 `Z068`/`Z069`/`S009` 的 `source_id`/`source_ids` 字段同步替换。

**passages（offset +5，Q148→Q153 … Q164→Q169）**

| 旧号 | 新号 |&nbsp;|旧号|新号|
|---|---|---|---|---|
| Q148 | Q153 || Q157 | Q162 |
| Q149 | Q154 || Q158 | Q163 |
| Q150 | Q155 || Q159 | Q164 |
| Q151 | Q156 || Q160 | Q165 |
| Q152 | Q157 || Q161 | Q166 |
| Q153 | Q158 || Q162 | Q167 |
| Q154 | Q159 || Q163 | Q168 |
| Q155 | Q160 || Q164 | Q169 |
| Q156 | Q161 |||

（Q162 旧号 `event_id=E082`——既有事件，不在 events 映射表内，原样保留，仅 passage 自身 ID 重编。）

**relations**：R165–R201（37条）与既有主表最大号 R164 自然续接，**无撞号，未重编，原样合入**。

**places / people**：拼音型 ID（`L_XI`/`L_CAI`/`L_YU`/`L_SHEN`、14 个新 `P_XXX`）互不冲突，**未重编，原样合入**。

合入执行用一次性脚本（`tools/_merge_r19b.py`，跑毕已删除，不进入常驻管线）机械套用上述映射表，逐表读写，避免手工改数出错；输出的映射 JSON 已与本文件表格核对一致。

## 三、四项裁定落地确认

1. **裁定1（合入顺序与重编）**：见上，已执行。
2. **裁定2（E115 秦穆公 presence 改"相关"）**：`data/csv/event_people.csv` 第281行 `E115,P_QINMU,...,亲至` → `...,相关`。已核实秦穆公仍余韩原(E084)/雍(E092等)/氾南(E116)/殽(E120) 4 地亲至，不影响其亲至落图 ≥2 的通例。
3. **裁定3（E137 保留）**：fix19 的 E137（郑伯始朝于楚，楚成王赐金）按裁定保留，随 fix19 全段一并合入，未删减。
4. **裁定4（五位女性主角 is_protagonist=1）**：
   - 新入库三人（人物本身 `is_protagonist` 在 `people_new.csv` 中改为 1 后合入）：`P_ZHUANGJIANG` 庄姜、`P_XUANJIANG` 宣姜、`P_XIGUI` 息妫；
   - 既有两人（直接改主表行）：`P_LIJI` 骊姬、`P_MUJI` 秦穆姬。
   - 合入后主角计 **20**（原15 + 本轮5），grep 核验见下方验收结果。
5. **裁定5（people.state 校核）**：逐一核对 `庄姜/宣姜 state=齐/卫`、`息妫 state=陈/息/楚`、`秦穆姬(P_MUJI) state=晋/秦`——round20 备料时已按此标法填写、`P_MUJI` 既有行也早已是`晋/秦`，**核校后确认全部合规，无需改动**。（骊姬 `P_LIJI` 未列入裁定5的 state 校核范围，`state=晋` 不变。）

## 四、conventions v1.15（任务书原文写"v1.12"，见下方版本号更正说明）

**版本号更正**：任务书原文要求写入"conventions v1.12"，但 `docs/conventions.md` 在本轮开工前已是 **v1.14**（v1.12 是 round15 的历史版本号，已被使用），若强行写"v1.12"会破坏版本号单调递增、与既有历史记录冲突。按仓库结构治理惯例（版本号只能顺位递增，不得回退复用），本轮实际落地为 **v1.15**，并在 conventions.md 版本头逐字记录此更正说明。**上报领队确认**：若领队本意确指别的版本基准，请指正，我可据实调整版本号（但不会回退到 v1.12 复用已用过的号）。

两条新规则均已写入 `docs/conventions.md`：

- **§2「并行备料 ID 网段预分配」**（新增段落，紧接 ID 退役规则之后）：记录本次 fix19/round20 撞号案例、责任判定（根源在任务书并行下达未预分配号段，非备料执行者之误），并定新规则——此后并行备料任务书须在正文预分配互不重叠 ID 网段；未预分配时合入者按"先合批次保留原号、后合批次顺延重编"处理，重编映射表须完整入交付说明。
- **§6.6「people.state 分组标注规则」**（新增小节，紧接 §6.5 relations 表之后，标注为"r19b 任务书裁定5"以避免与 §13 全局裁定5〈站名裁定〉编号偶合混淆）：由旧惯例"首国"修订为"人物线主要发生国＋出身→归宿标注"，`/` 分隔按出身→归宿顺序列出人物线实际展开的全部主要国，附本轮五位女性主角为范例；明确为溯及既往的标注纪律、不要求普查改写既有全部人物行。

## 五、validate / 生成 / 提交 / 部署核验

- `python tools/validate.py`：**OK：全部校验通过**（合入前、合入后、conventions 改动后各跑一次，均 exit 0）。
- `python tools/csv_to_json.py`：重新生成，输出行数与预期完全吻合：
  ```
  events.csv -> 151 行（预期≈151 ✓）
  people.csv -> 97 行（预期≈97 ✓）
  event_people.csv -> 383 行
  passages.csv -> 170 行
  sources.csv -> 104 行
  places.csv -> 72 行
  relations.csv -> 201 行
  ```
- **E134 唯一性复核**（grep 全仓 `data/csv/` + `site/data/`）：仅命中 fix19 的"盂之会"一处（events/event_people/passages 各自出现次数与该事件应有的行数吻合），**无 round20 旧编号残留**。
- **ID 唯一性复核**：events/sources/passages/relations/people 五张表按 id 排序去重后均无重复行。
- **主角计复核**：`site/data/people.json` 中 `is_protagonist=1` 计 **20** 人，含骊姬、穆姬、庄姜、宣姜、息妫。

git 提交与部署、线上带参复验将在下一步执行（本文件先完成数据侧核验记录，提交后补记 commit hash 与 Actions/线上结果）。

## 六、已知问题 / 交接备注

1. **S009→S005 复用为 Skipper 执行期判断**：round20 CHANGES.md 未提及这层重复（Sophia 备料时应是各自独立核对当时的主表，未必留意到"这段引文其实是既有 S005 同一篇"），本轮判定纯属 ID 去重（书目相同、URL 相同），不涉及史料可靠度或事件真伪的裁量，未新建 sources 行。如领队/Sophia 认为两条引文各有側重值得分列两行，请指示，可回补为独立号（不影响已合入的 events/passages 内容本身，只是引用哪个 source id 的问题）。
2. **conventions 版本号 v1.12→v1.15 更正**：见上，已在 conventions.md 内联记录，等待领队确认是否即为其原意。
3. 一次性合入脚本 `tools/_merge_r19b.py` 已在核验通过后删除，不遗留在仓库；如后续需要复核合入逻辑，可参考本文件的映射表重建。
