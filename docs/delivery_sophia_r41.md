# 交付说明 · Sophia r41 —— 第六章件收尾补充（并入 `round39_xinian6`）

任务书：【微任务 For Sophia · 第六章件收尾补充】。
产出：**并入既有** `data/incoming/round39_xinian6/`（未另起目录，照任务书）。
权威依据：`docs/conventions.md`（v1.32）；上轮：`docs/delivery_sophia_r40.md`、本件 `CHANGES.md` §0–§9。

**取号与文档去留之判断（任务书令我自定并说明）**：**另起下一号 `r41`**，不改写 `docs/delivery_sophia_r40.md`。理由二：① r40 那份文档是**站长据以作出本轮六条裁定的原始上报**，改写它等于抹掉裁定所据之原文，与「判定反转须如实记录、不得抹平」（§7 v1.29）同一道防线；② 本轮是**裁定后的落地**，与 r40 的「备料与压力点上报」是两个动作、两套验收，各自成篇更便于回溯。`docs/` 现有最大号为 `delivery_sophia_r40.md`，故取 **r41**。

---

## 一、做了什么（三项）

| 项 | 内容 | 落点 |
|---|---|---|
| ① 按裁定改写标注 | 新设「悬（待纸本）」正交状态标／相抵细分硬相抵与并存之异／名物用字之异析出／总括语改归详略之别 | 改写 `passages_new.csv` 7 条、`fixes_passages.csv` 4 条、`fixes_events.csv` 11 条、`sources_new.csv`（`J002.notes`） |
| ② 补立骨架事目＋「徙之中城」落地 | 新立 `E273`（《左传·僖公二十五年》秦晋伐鄀、围商密）；`Q452`／`Q453` 两条引文；3 条挂链；「徙之中城」落 `medium` | 新建 `events_new.csv`／`event_people_new.csv`，`passages_new.csv` ＋2 |
| ③ `J001.notes` url 清账 | 清「未能实测复验」一句，改写为已核状态（两次实测、核对人与日期俱全、留痕不抹平） | 新建 `fixes_sources.csv` |

**件之性质随之变为：新增 16 行 ＋ 整行替换 23 行 ＋ 新立骨架事目 1 条。** 合入后 `sources` 174→**175**、`passages` 425→**436**、`events` 235→**236**、`event_people` 609→**612**；`people` **153**、`places` **91** 及其余各表**行数一律不变**。

`data/csv/`、`tools/`、`site/` **一字未动**（红线 1、3），未 `git add`／`commit`；本件为备料件，**无提交、无部署动作，故本文档无提交哈希与 Actions 运行号可回填**（依 §7 v1.31「实测回填、不得预填」，无动作即不填）。

---

## 二、重算后的分布（改前／改后）

| 指标 | 改前（r40） | **改后** |
|---|---|---|
| 顶层：同／异／悬 | 5／7／— | **7／4／1** |
| 措辞层 | 3 处（全悬） | **0 处** |
| 行动层 | 4 处 | **3 处**（硬相抵 1、并存之异 2） |
| 次第层 | 3 处（计入相抵 2） | **3 处**（计入相抵 2，皆硬相抵） |
| **计入相抵合计** | **9 处** | **5 处**（硬相抵 **3**／并存之异 **2**） |
| 名物用字之异（不入三层） | —（混于措辞层） | **3 处** |
| 「悬（待纸本）」状态标 | —（无此标） | **4 处** |
| 详略之别 | 6 处 | **7 处** |
| 待纸本用字 | 5 处 | **5 处**（口径不变；4 处挂悬、1 处不挂） |

**四项裁定的净效果**：r40 的「十处相抵」收窄为**五处真相抵**（其中只三处不可并存），另三处推给「不可证某字作某」、一处推给详略、一处推给「悬」。**从严的方向一致：能不判相抵的，都不判。**

逐处对照、十二节重判表、逐行改写落点清单，见件内 `CHANGES.md` §10.2。

---

## 三、要点速览

- **「悬」标所挂 4 处**：节 1 婢妾／嬖（悬**同异之格局本身**）＋名物用字三项（悬「是异写抑或实异」，不改同异格局）。节 6「牙」**不挂悬**——《左传》无对应之字，无「异」可悬。
- **新事目 `E273`**：接台账尾号（主表实测最大 `E272`）；`-635`／鲁僖公二十五年／秋／战争／`importance` 2／`sort_key` 15／`reliability` **high**／`source_ids` `Z049;J002`／**`place_id` 从严留空**。
- **「徙之中城」落 `medium`**：尺度②（J 独有而属编年骨架——迁国存灭）；非 `high`（J 层单独支撑，无独立佐证）、非 `low`（尺度③需与经传相抵，本节不相抵，只是《左传》无此文）；著录于 `Q452.modern_note`，**不改 `E273` 之 `high`**。
- **`J001` 清账**：两次实测（2026-08-15 Skipper `curl` 直连 200、页名核为简体「系年」；2026-08-17 Sophia `action=raw` 取源 200、22810 字节），核对人与日期照 §7 v1.26 通例标注，原句留痕不抹平。
- **《系年》第六章十二节全数落库**：`Q443`–`Q452` 拼接即全章。

---

## 四、验收自查

| 验收项 | 结果 |
|---|---|
| 成果并入 `round39_xinian6/`（未另起目录） | **通过**——`CHANGES.md` 新增 §10（含改前改后对照），§0–§9 原文一字未删、就地加指路句 |
| 合并模拟 `python tools/validate.py` 通过（临时副本、主表不动、不改 `tools/`） | **通过**——**exit 0，`OK：全部校验通过`，警告 0 条**；`validate.py` 副本与仓库件 **SHA-256 全同**（`d0707e22c1d8…`）；`git status` 实测 `data/csv`／`tools`／`site` 零改动 |
| 新增事目未触发护栏问题 | **通过**——第一跑即 exit 0，**需 Skipper 同步的护栏改动点：无** |
| 断言类自查以实读为准 | **通过**——**53／53**；一处 QA 自纠留痕（三条 FAIL 皆断言之误、非数据之误，详下） |
| 一律简体中文·原文摘录保持原字 | **通过**——件内全文机械扫描无西里尔／日文／韩文串入（首稿曾串入一处，已就地纠正并记于此）；引文繁体照录 |

**QA 自纠留痕（不抹平）**：首轮三条断言报 FAIL，逐条查后**三条皆断言写得不准、数据无误**——① 「主表 `〔页码待补〕` 仍为 1 处」：实测 **0**（conventions v1.31 已记 `L_YU` 回填后归零），我沿用了 r30 的旧数；② 「`git status` 仅本件目录」：另有 `?? docs/delivery_sophia_r40.md`（我上轮落盘、尚未入库，非本轮所改），断言范围写窄；③ 「十条引文拼接逐字全等」：落差实为**节切分处 2 处转录本标点**——简文本无标点，转录本之标点系整理所加，非字之遗漏。三处均改断言、不动数据（同 r40 那处「断言误伤 `notes` 自述句」之办法）。

---

## 五、拟入 conventions 的六条（**升下一号**，由 Skipper 随合入写入）

条文拟稿与落点逐条列于件内 `CHANGES.md` §10.6，**文字请领队核定后定稿**；本件不写死版本号。

| # | 条 | 落点 |
|---|---|---|
| 1 | 状态标「悬（待纸本）」正交于三层，非第四种异型 | §7 三层细分判例条末新增一段 |
| 2 | 「名物用字之异」不入三层，径归「不可证某字作某」口径句辖区 | §7 口径句条末新增一段＋三层细分「措辞层」括注互指 |
| 3 | 事实相抵分强度：硬相抵／并存之异（并存之异仍在相抵之内，不影响 `reliability`） | §7 三层细分判例条内新增一段 |
| 4 | 总括语与详叙不径判相抵，判详略之别并注明其为总括（与尺度④分工：尺度④管篇末断语，本条管行文中之总括） | §7 三层细分判例条内新增一段＋§2 尺度④处互指 |
| 5 | 次第层限定：不系时之记载，其行文次序不径作先后之断言 | §7 三层细分判例「次第层」括注补限定语 |
| 6 | 一对多时双向互指落点：所涉事目一律增句，`passage` 级互指只落于异之所在 | §7「并陈不裁」通例之「并陈是双向义务」段末新增一句 |

**另附一条建议**（r40 §9.3 所报，非六条之一）：【原簡】隶定栏之自限——仍属同一转录本之自附材料，不据以判定某字作某，建议写入口径句之下。

---

## 六、拿不准处 · 需站长／领队裁定（九条，全文见 `CHANGES.md` §10.7）

1. **悬标计数与任务书字面之出入**：任务书写「挂措辞层三处及名物用字项」；经裁定三（名物用字析出）连带作用，重判后**措辞层为 0 处**，悬标实挂 **4 处**（节 1 ＋名物用字三项）。**位置与任务书所指全同，只是归属重排**，非另作裁量。请核可此计法。
2. **`E273` 落点留空待纸本**：鄀／商密之地望，谭图与杨注本轮**均无从核**，依「宁降不虚」`place_id` 从严留空、**不立坐标未定之新点**（`places` 维持 91）。请站长得便回告谭图第一册相应幅（丹淅之间）有无「商密」「鄀」落点及方位，即可补立地点行并回填。
3. **`P_ZIYU` 一条挂链请核**：子玉「追秦師，弗及」——从严判「相关」（无其在商密之明文），亦不用「不在」。若认为「追」字已足证在场可改「亲至」，或撤此一行。
4. **申公子仪／息公子边不立人物行**：其与鬬克、屈御寇之对应出**杜注**而非传文明书，依「注家之说不当经传明文用」不作论定。
5. **`Q453`（《左传》伐鄀本文）之取舍**：为使第 12 节并陈与前十一节同为双向对称而立；若认为新事目不必另出经传引文可撤（改动只及一行）。
6. **待纸本五处用字口径不变**：婢／嬖、里之克／里克、悼子／卓子、梁城／解梁城、牙之读，**一律待纸本，未以推理代核对**、未写「查无异文」。「婢／嬖」决定节 1 是同是异（现挂「悬」），最要紧。
7. **归档命名仍待一句话定**：目录 `round39_xinian6/`（照任务书字面），交付文档 r40／r41；`docs/changes/` 之归档名（`r39_xinian6.md` 抑或 `r40_xinian6.md`／`r41_…`）请定。
8. **台账尾号**：本件后 `passages` 至 `Q453`、`events` 至 `E273`；`Q419`／`Q420` 空号仍留空（r28 裁定 a），本件不填补。
9. **`docs/delivery_sophia_r40.md` 尚未入库**（`git status` 实测 `??`）：系我上轮落盘之交付文档，本轮未触及，提请 Skipper 合入时一并提交，以免交付台账缺档。

---

## 七、交给 Skipper 的合入提示

- **护栏改动：无**（`J` 前缀正则与出土文献软检档 r32 已落；本件所用之值全在现行护栏内）。
- **本件新增三个文件**（r40 原件所无）：`events_new.csv`（append 1）、`event_people_new.csv`（append 3）、`fixes_sources.csv`（按 `id` 覆盖 1）。
- **建议合入顺序**：append（`sources`→`events`→`passages`→`event_people`）→ overwrite（`fixes_sources`→`fixes_passages`→`fixes_events`）。
- **`event_people` 表无 `id` 栏**，本件对该表**只 append 不覆盖**。
- 合入后重跑 `python tools/validate.py` 与 `python tools/csv_to_json.py`（生成物本件未动）。
- conventions 六条**升下一号**写入，文字待领队核定。
