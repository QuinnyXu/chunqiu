# 交付 · Vision r49 —— 孔子·胡簋定稿入库与 34 席升位

任务：`team/round49_prompts.md` 任务 5〔任务 For Vision · r49 徽记定稿〕与任务 7〔任务 For Vision · r49 升位〕，前置〔领队裁定二·五（补档）〕与〔领队裁定三 · 任务 5 定稿过裁（2026-09-09）〕。

本文档补上 r49 本该有而先前缺失的那一份交付文件（裁定二·五所指「`docs/` 无 `delivery_vision_r49`」一条）。**三档复测报告全文在任务书附录**（`team/round49_prompts.md`〔2026-09-08 交付／2026-09-09 补档·待裁〕节），本文档不重抄其表，只记入库、接线与验证。

## 〇、开工前实读与边界

实读三处现行文本后落笔，不凭派件转述：`team/round49_prompts.md` 裁定三（:122）与任务 7（:129–137）、`docs/design/design_notes.md` v2.10 徽记条与 §2.5.1、`site/app.js` `PROTAGONISTS`（开工时 33 席，尾五席 `P_XIAJI`／`P_HELU`／`P_WUYUAN`／`P_FUCHA`／`P_GOUJIAN`）。

边界照裁定与派件：**未切分支**（全程 `main`，开工 HEAD `c2020ad`）；**未动 `engine-split`**（其 `site/engine/views/person_nav.js` 内另有一份 33 席名册，两枝名册分岔一事由领队排期，本件不代办）；**未推送**（提交在本地，候站长口令）；**未手改 `site/data/`**（红线 2）；**未引任何外部资源**（红线 6）。

## 一、入库（任务 7 之 1）

| 物 | 自 | 至 | 处置 |
|---|---|---|---|
| 定稿徽记 | `docs/design/badge_kongzi_r49/badge_kongzi.svg`（草案位，未跟踪） | **`site/assets/icons/badge_kongzi.svg`** | 移入，**注释头照留并更新状态句** |
| 复测脚本 | `tools/qa/badge_silhouette_r49.js`（未跟踪） | 原地**入正编**（纳入版本管理） | `KONGZI_SRC` 由草案位改取 `ICONS` 目录，与其余 33 枚同源；头注同步 |

草案目录 `docs/design/badge_kongzi_r49/` 随之移空删除。

**注释头是更新不是删除**（裁定所命）：原「状态：暂存草案位……候裁接线时移入」一句，改为记明「2026-09-08 绘时为『候裁、未接线』；2026-09-09 经领队裁定三**过裁**，随 r49 任务 7 升位件入库并接线（`site/app.js` `PROTAGONISTS` 第 34 席 `P_KONGZI`）」，并附裁定三.1 之前置条件一句。会话、日期、目的、依据（design_notes 二裁三条）、复跑法五项一字未删。

**移位后复跑自证**：`cd tools/qa && node badge_silhouette_r49.js` 三档实数与 09-08、09-09 前两次逐位相同（24px 0.350／34 对更紧、22px 0.483／18 对、18px 0.408／81 对；鲁弧三档 0.235／0.378／0.358 皆不抬高）。**换路径不换数**，已实跑核过。

## 二、接线（任务 7 之 2）

`site/app.js` 仅一处新增：`PROTAGONISTS` 于 `P_CAOGUI` 之后插入

```js
{ id: "P_KONGZI",      color: "", badge: "badge_kongzi",      fallback: "孔子" },
```

并附四行注释（第 34 主角、`state` 只「鲁」故不需 `home` 覆盖、环序落位之由、徽记出处）。**其余联动位一处未改，因为一处也不需要改**——这是实读之后的结论，逐条记明：

1. **环序 i=8 不是手排出来的，是排出来的**。`panoPeople()` 的排序键为 `(STATE_ORDER 首国, is_protagonist, id.localeCompare)`：齐弧 7 人占 i=0–6，鲁弧内 `P_CAOGUI` ＜ `P_KONGZI` ＜ `P_LUHUAN`，故孔子自然落 **i=8**、左曹刿右鲁桓公——与 r49 任务 1 实测所记之位一致（任务书 :171）。**未在任何地方写死序号。**
2. **徽记表即 `badge:` 字段**。站内无第二份徽记名录，`fetchSVG(name)` 直取 `assets/icons/<name>.svg`，故「注册簋形」＝填这一个字段。
3. **首页簇 4→5 是数据驱动的**：`homeGroups` 按分区自动成组，`clusterSlots()` 现算排位，`BADGE_FOLD=6` 未触发折行。**`HOME_BADGE_POS` 无须补项**——鲁分区之簇心 `[847, 256]` 早已在表，本轮不新增国色家族。
4. `PROTAGONISTS` 之数组次序＝选人页分区内顺序，孔子置于曹刿之后，与齐组「晏婴殿后」同例（后上线者次于本组之末）。
5. 未动 `STATE_FAMILY_VAR`、`STATE_ORDER`、`panoStateKey()`、`resolveProtoColors()`——孔子属鲁，国色 `--state-lu` 现成。

**未改而记一笔**：`app.js:50` 注「勾践……全库第 33 位主角（r28 裁定 1 升格）」，记的是**升格次序**（第 33 个被升为主角者）而非数组位次，故本轮不动；日后若有人误读为位次，此处已有说明可查。

## 三、`protoRoster()` 告警消失——实测核验（任务 7 之 2 末句）

**怎么见的**：写临时脚本（只在 scratchpad，不入库）起本地静态服务器、以 Playwright 挂 `page.on("console")` 收 `warning` 级消息，加载 `#/` 后逐条打印；**同一脚本、同一口径，对「HEAD 干净副本」与「工作区」各跑一次**（HEAD 副本由 `git archive HEAD site` 导出，不动工作区）。控制台文本原样如下：

| 态 | 名册 | 差集 | console warning |
|---|---|---|---|
| **接线前**（HEAD `c2020ad` 副本） | 前端 33 · 数据 34 · 可进 33 | 数据独有 `[P_KONGZI]`、前端独有 `[]` | **2 条**：`主角名册不一致——数据有而前端无：P_KONGZI；前端有而数据无：无` |
| **接线后**（工作区） | 前端 34 · 数据 34 · 可进 34 | 数据独有 `[]`、前端独有 `[]` | **0 条** |

页脚品牌语同步：`分享给同好——33 条人物线，择一而入。` → **34 条**；选人页卡片 33 → **34**。**告警确已消失，是见其无、不是推其无。**

## 四、`design_notes` 落笔（任务 7 之 3）——v2.10 → **v2.11**

1. **§2.5 徽记条新增「孔子＝胡簋」一条**，体例从现库诸枚：形之描述与文件名、取《左传·哀公十一年》之句、为何取这一句、**只取簋之通形不据杜注断瑚琏**（conventions §7 在徽记取材上的首次适用）、二裁两条限制之守住实况（无腹带；纵展 y15–36.4／横展 x8.9–39.1 比探针 G2 更外撑）、**不取者与其故**（木铎／杏坛／素王／绝笔四项「著名而不在经传」一概不取，同介之推、勾践之例；乙案麟不必另测亦不另画）、避形交代（对问鼎与鲁弧四枚之三档实数）。
2. **附裁定三.1 之前置条件一句**（写在徽记条内，⚠ 标出）：本枚 22px 最紧对 0.483 落分布 3.4%，属「过而偏紧」；**卫组或鲁组日后再增以「纵向闭合廓形」为主形之枚，须先重跑 r49 定稿复测表**。
3. **裁定三.2 之留痕**同条记明：本枚 × `huimeng` 0.302 而问鼎自身 × `huimeng` 0.287，按 §4 第二位判据不构成不合格，**不改形**，呈站长知悉。
4. **§2.5.1 补「r49 实测」表**（三档 × 现库 33 枚 528 对基线／孔子最紧对与位次），并附点名逐对、鲁弧三档「皆为原对原值」之自证、**六稿逐档留数**（KA–KF，同 v2.5 勾践之例）、以及「KA 与 KB 只有 18px 分得开」这一条反证第三档之立。

**顺带勘正两处**（皆有实测据、只改数不改判据，一并声明于版本行）：

- **§2.1 国色家族表「人数」列一次校齐**：齐 6→**7**（漏 r22 晏婴）、晋 3→**4**（漏 r26b 叔向）、吴 2→**3**（漏 r28 夫差）、鲁 4→**5**（本轮孔子）。取值来自 `vision_r24a.js` 走查现算之实测名册（齐7·鲁5·郑5·晋4·秦2·楚3·卫2·宋1·陈1·吴3·越1，合 34）。**只动「人数」一列**，色值、灰阶、色相、饱和、双底线诸列一字未动；漏记之由写在明处，不抹平。此列若不校齐，本轮把鲁改成 5 反会让读者以为其余各行是当期值。
- **§2.2 待议表第 3 行**「全景槽距随人数外推」补 34 人实测值：`33 人 47.91px`（原文照留）后加注「r49 升位后 34 人 **46.50px**，DOM 实测与公式四位小数内一致」。

## 五、QA 计数同步（照 `docs/changes/r49_kongzi.md` §12 之 2 体例，逐处列明）

**`tools/qa/vision_r28.js`——8 处由 33 改为 34**：

| # | 位置 | 由 | 改为 |
|---|---|---|---|
| 1 | `roster.dataPro.length === 33` 断言与文案 | 数据侧主角 33 人 | **34** |
| 2 | `pick.n === 33` 断言与文案 | 选人页 33 张人物卡 | **34** |
| 3 | `pick.enabled === 33` 断言与文案 | 33 张卡皆可点入 | **34** |
| 4 | 页脚品牌语正则 `/33\s*条人物线/` | 33 条 | **34 条** |
| 5 | `pano.n === 33` 断言与文案 | 全景默认环 33 槽 | **34 槽** |
| 6 | 出图名 | `r28_03_pick_33.png` | **`r28_03_pick_34.png`** |
| 7 | 出图名 | `r28_05_pano_33.png` | **`r28_05_pano_34.png`** |
| 8 | 头注与三处节题（§2 标题、§3 标题、文件抬头） | 33 人／33 槽距／`2R·sin(π/33)` | **34 人／34 槽距／`2R·sin(π/34)`** |

**改的是被测规模，判据、阈值、比对口径一字未动**：槽距仍照 `2R·sin(π/n)` **现算再对 DOM**（不写死 46.50），门槛仍是「＞节点直径 30」「＞徽记边长 22」。改动之由与本表同写入该脚本头注，日后可就地查。

**未改者与其故**：

- **`tools/qa/regress20.js` 的 `IDS` 20 人名单不动**——它是 r20 立的**固定 20 人回归基准**（脚本名即此意），不是全名册清点；把它扩到 34 会把「回归基准」变成「当期快照」，反而失去比对功能。照原样重跑即可（结果见下节）。
- **`tools/qa/vision_r24a.js` 一处未改**——该门早已是**对账式**（`protoRoster().enterable` 与 `PROTAGONISTS`、数据侧三方互证），本就不含写死人数，33→34 自动跟上。这正是 r24a「写死一个 29 只会在下一次加人时重演」那条教训的兑现。
- **`tools/qa/vision_r22.js`／`regress20.js` 无断言**（报告式脚本），无计数可改。
- **生产复验脚本**（`r43/r44/r45_prod_check.js`、`vision_r46.js`）经查**不含主角计数**，本轮无须动。

## 六、QA 全批重跑（前／后对比，皆本机实跑）

「前」＝ `git archive HEAD site` 导出之干净副本（33 席），「后」＝工作区（34 席）；同一脚本、同一口径。

| 脚本 | 前 | 后 | 判 |
|---|---|---|---|
| `vision_r24a.js`（回归总门） | **506 通过 / 5 未过** | **512 通过 / 1 未过** | **闭合 4 项**（皆名册缺口：名册两侧无差集、`PROTAGONISTS 33 ＝ 数据 34` 、一字不差对账、页脚口径），**未新增任何未过项** |
| `vision_r28.js`（专项走查） | —（其 33 计数系本轮改） | **0 FAIL**；34 人全流程、34 槽、槽距现算 **46.50 ＞ 30 ＞ 22**、首页徽记圆 34 枚、全程无 pageerror／console error | 过 |
| `regress20.js`（20 人回归基准） | — | — | **前后输出逐行相同**（`diff` 空），20 人轨迹与降级清单一字未变 |
| `vision_r22.js` | 主角数 33、可播 29 人 | 主角数 34、可播 30 人 | `diff` **仅三处**，皆预期内：①主角数 33→34；②新增一行 `P_KONGZI 可播`（亲至轨迹共 3 站、前 525 起，另有 4 处相关地点空心示之）；③可播 29→30（降级 4 人名单不变） |
| `badge_silhouette_r49.js` | — | 三档实数与 09-08／09-09 两次逐位相同 | 过 |

**唯一未过项照实报，不粉饰**：`vision_r24a.js` 之 `[FAIL] 无地望事件 11 条一律中性签（实测中性 12）`。此项**基线即然**（HEAD 副本同报），与本件无涉，且已由领队裁定二·三立为**独立待议项**（「断言与现库数据之既存出入，改断言须另裁，不得顺手」）——故本轮**不动它**。

## 七、截图（供站长抽看）

| 图 | 路径 | 内容 |
|---|---|---|
| 34 席全景 | `tools/qa/screenshots/r49_03_pano_34.png` | 主角环 34 节点、34 枚徽记；孔子在鲁弧、左曹刿右鲁桓公（i=8 之目验） |
| 首页五枚鲁簇 | `tools/qa/screenshots/r49_02_home_lu5.png` | 鲁块上 鱼纹／圭璧／鼓纹／辙／**胡簋** 五枚并列，未折行、未相犯 |
| 选人页 | `tools/qa/screenshots/r49_01_pick_34.png` | 34 张人物卡全页，鲁分区 5 张 |
| 走查同批 | `tools/qa/screenshots/r28_03_pick_34.png`、`r28_05_pano_34.png` | r28 专项走查所出之同类两图（34 席） |
| 徽记对照网格 | `tools/qa/screenshots/r49_badges_24px.png`、`_22px.png`、`_18px.png` | 34 枚剥外圈 silhouette 三档对照 |

（`tools/qa/screenshots/*.png` 在 `.gitignore` 内，图只在本机，不入公开仓库。）

## 八、验证门与仓库状态

1. `python tools/validate.py`（改动后）：**`OK：全部校验通过`**。
2. 本轮**未动 `data/csv/` 与 `site/data/`**，故未跑 `csv_to_json.py`（无生成物需重生）。
3. 改动面（`git status`）：**改 3 件**——`site/app.js`（+6 行：1 行名册＋5 行注释）、`docs/design/design_notes.md`、`tools/qa/vision_r28.js`；**新增 2 件**——`site/assets/icons/badge_kongzi.svg`、`tools/qa/badge_silhouette_r49.js`。**删 1 件**——草案位 `docs/design/badge_kongzi_r49/`（移空）。
4. **已提交，未推送**——本件提交 `efc5cb4`（本文档自身之哈希由随后一条追记回填）；`main` 本地 ahead，候站长口令再 `push`。
5. `team/` 与 `tools/qa/screenshots/*.png` 皆 `.gitignore` 项，未入公开仓库。

## 九、上报事项

1. **`vision_r24a.js` 之 1 项未过系既有待议项**（中性签 11 vs 12），基线即然，未动——见 §六。
2. **裁定三.2 之跨尺度留痕**（本枚 × 会盟图标 0.302 ＞ 问鼎 × 会盟 0.287）已写入 design_notes 徽记条，**未改形**，呈站长知悉。
3. **前置条件已入档**：卫组或鲁组日后再增「纵向闭合廓形」为主形之枚，先重跑 `tools/qa/badge_silhouette_r49.js`。
4. **两枝名册分岔**：`engine-split`（`f84ee56`，`timechorus-engine v0.1.0` 之源）内 `site/engine/views/person_nav.js` 仍为 33 席，本件按令**一字未动**，何时并、怎么并候领队排。
5. **§2.1「人数」列此前逐轮漏同步**已校齐并记明漏记之由（§四），若领队认为此项应另件处理，可就地回退该列四格——色值诸列本就未动，回退不牵连。
