# 交付说明 · Vision r38（微件）—— §7.5 补记两笔 ＋ `app.js:3972` 一字统一核对

**日期**：2026-08-16　**执行**：Vision　**承**：r37（`da8978c` 等四次部署全绿，总门 511/0）四条上报，站长裁定其中两条归我

**一句话**：**`docs/design/design_notes.md` §7.5 补记两笔并升 v2.9（旧 v2.8 段一字不改下沉为「历史：」首条）；`app.js:3961`／`3972` 实读核对——两处措辞已然同口径（均作「有主角之国」），确认无须改动，`site/` 一字未动，以 `git status`／`git show --name-only` 佐证；三门 511/0、29/0、validate exit 0；§7.6 计数仍 3/5，本件未新增此族断言。**

---

## 一 · 本件改动面（一个文件，一行不出 `docs/`）

| 文件 | 改动 | 层 |
| --- | --- | --- |
| `docs/design/design_notes.md` | 标题行 v2.8 → v2.9；新 v2.9 版本行；旧 v2.8 行**逐字不改**下沉为「历史：」首条；§7.5 末追加两段补记 | 文档层 |

`git diff --stat`：**1 file changed, 8 insertions(+), 2 deletions(-)**。`site/`、`data/csv/`、`site/data/`、`tools/`、`docs/conventions.md` **均未触及**（末者系与 Skipper 并行件的分工边界，本件严守）。

**回写办法**：二进制读写（Python `open(...,'rb')`／`'wb'`），换行符实测守恒——改前 CRLF 916 ／ 裸 LF 0，改后 CRLF 922 ／ 裸 LF 0（+6 行即新增的 6 个物理行），无 BOM，改前后皆无。文件 sha256 `51b7f0ba…52a094e` → `5ba4e221…cb79d999`。

**一处须记的仓库事实**：`git check-attr` 实测本文件 `text: set`／`eol: lf`，且 `core.autocrlf=true`——即**仓库 blob 一贯存 LF、工作区存 CRLF**（`git show HEAD:` 实测 HEAD blob 为 LF 916 ／ CRLF 0）。我保持工作区 CRLF 即与既有一贯，提交时由 git 按既有属性归一，`git diff` 的 CRLF 提示属该配置的常态提示，非本件引入。

---

## 二 · §7.5 补记两笔（正题）

补记**只追加、不改原文一字**——§7.5 原有正文（含 r27 所写「同类已清者……」一句、r35 补记、两路规约）逐字保留，两笔以〔r38 补记一〕〔r38 补记二〕另起段落缀于路 2 之后、§7.6 之前。

### 笔一 · `app.js:88–89` 明裁归入路 1 除外项

> **〔r38 补记一〕路 1 除外项的代码实体，经站长明裁钉住：`site/app.js:88–89`。** 路 1 所举的除外样板句「r26 出过一次实账」，其在代码中的实体即主角名册对账函数头注的那两行——「r26 出过一次实账：数据侧 29、前端 27（晏婴、叔向缺席），而页脚人数取的是数据侧，于是**当时**站内对读者报「29 条人物线」，其中两条点不进去」。r36 执行「注释从此不记库存」时曾把这两个数字一并清掉，r37 按乙案逐字恢复、并补「当时」二字，使轮次锚落在句内而非仅靠上下文。**故此处两数永久保留、日后清账勿动。** 判据不是「注释里不许有数字」，是「**不许声称今天的库存**」——病历带日期，写下即永远为真。它与同文件 `5310` 那句「现 29＝29」方向相反的处置（一恢复、一清掉）出自同一条判据，是本除外项最清楚的一对示例。

**为何这样写**：任务书要求「引 r37 恢复的带『当时』二字之本为除外项的范例」。§7.5 路 1 原文的除外样板句本就是「r26 出过一次实账」，但此前**只在规范里举了句子、未指认其在代码中的实体**——r36 因此把这句的实体清掉而规范未红。本笔把样板与实体正式对上，日后清账者读到规范即知该行不可动。

**实读复核（本轮，非引 r37）**：`site/app.js` 第 88–89 行现行原文——

```
 88:  * r26 出过一次实账：数据侧 29、前端 27（晏婴、叔向缺席），而页脚人数取的是数据侧，
 89:  * 于是当时站内对读者报「29 条人物线」，其中两条点不进去——**多许的两条，读者是找不到的**。
```

「当时」二字在句内，轮次锚 `r26` 亦在句内，与补记所述相符。**本件未改该两行。**

### 笔二 ·「已清」之说自 r37 兑现，三次收敛履历照留

> **〔r38 补记二〕上文「同类已清者……`index.html` 里一条注释」之说，自 r37 起方为真。** r27 写下「已清」时，「全景默认只画主角（现 N 人）」这句同文在站内实有**三处**，当时只清了 `index.html:217` 一处，未察 `app.js` 另有两处同文；`app.js:4003` 由 r35 走查查出、r36 清，`app.js:3512` 由 r36 复扫查出、r37 清，三处齐后全站复扫无第四处。**「已清」之说自 r37 起为真，此前不足。** 这段履历**照留不抹平**——同 r33 所立「判定反转须如实记录」一路：本处非判定反转，而是**「已清」的范围三次收敛**，同样是依据链的一部分。后人须能看见它曾漏在哪里，才知道「清完再全站复扫一遍」这道手续为何不可省。

**「照留不抹平」如何落到纸面**：§7.5 原句「同类已清者另有……`index.html` 里一条注释」**一字未改**——它记的是 r27 当日的认知（当时确以为只有一处），改掉它就是抹平错误史。补记另起一段说明该说其后经三次收敛方为真，两段并读即见完整履历。

**本轮独立复扫复证（不引 r37 结论）**：`grep -rn "默认只画主角\|只画主角" site/ --include=*.js --include=*.html --include=*.css --include=*.svg` 命中三处，全部已洁——

| 落点 | 现行原文 | 是否含库存数 |
| --- | --- | --- |
| `site/app.js:3512` | `// 全景「显示全部」：false=只画主角（默认），true=全库（r24a-2 裁定②b）` | 否 ✓ |
| `site/app.js:4003` | ` * **默认只画主角**，勾「显示全部」才回到全库。` | 否 ✓ |
| `site/index.html:217` | `<!-- 全景默认只画主角（r24a-2 裁定②b，人数随入库变动，勿在此写死）；…… -->` | 否 ✓ |

**无第四处。** 另按「现 N ／ 现库 N」式库存声明对 `app.js`／`styles.css`／`index.html` 复扫一遍：命中 1 行 `app.js:3999`「源文件 2 → **呈现** 2.6」，系「呈现」被切词所致的假阳性（与 r37 所报同一处假阳性），**真阳性 0**。

### 版本号处置

站长本件**明确指定升 v2.9**（非「升下一号」的通例写法，故不推算）。标题行同步为 `design_notes v2.9`；旧 v2.8 段照体例下沉为「历史：」首条，**逐字未改**（diff 中该行为一删一增，内容仅差句首「版本：」→「历史：」六字符，其余完全相同，可逐字核）。

---

## 三 · `app.js:3961`／`3972` 一字统一——**核对后无须改动**

站长裁「一字统一」，并要求**先实读两处现行原文核对用词是否已然一致**。实读结果——

| 行 | 现行原文（本轮 `awk NR==` 实读） |
| --- | --- |
| `3961` | `/* 国别底晕（r14，裁定1·4）：为有主角之国各铺一段极淡国色环弧，强化阵营分区。` |
| `3972` | `    if (!STATE_FAMILY_VAR[st]) return; // 仅有主角之国铺底晕` |

**核对结论：两处用词已然一致，均作「有主角之国」，无须改动。** 缘由是 r37 实写时已按 3961 口径落笔（这正是 r37 §三第 2 条所自陈、所报「一字可回退」的那一字：初判字面「仅有主角国」，实写「仅有主角**之**国」）。站长本件裁「一字统一」，与 r37 已落之字**同向**，故裁定所指的状态**已经在库**。

**改前＝改后**：`3961` 与 `3972` 本件**均未改动**，改前改后原文即上表两行，逐字相同。

**不为凑一个动作而改**（照任务书明训）。顺带一并核过同族第三、四处，口径亦同、亦未动：`app.js:3448`「同国节点连续成弧，**有主角之国**另铺国色底晕」、`app.js:4056–4057`「**有主角之国**各占一段环弧……仅为**有主角之国**着色」——本轮 `grep -n "主角之国\|主角国" site/` 实测：命中 **8 行**（`app.js:117/177/594/3448/3961/3972/4056/4057`，含「**无**主角之国」一类说法），**无一处作「主角国」**，用词已全站统一。

---

## 四 · 行为零变化 —— 本件未触及 `site/`，故以「未触及」佐证代替词法显微镜

任务书两分支：「若 ② 实际触及 `app.js` 则照 r37 词法显微镜自证；若核对后无须改动，则本件只动 `docs/`，明说 `site/` 一字未动并以 `git status`／`git show --name-only` 佐证」。**本件落在后一分支。**

**佐证一 · `git status --porcelain`（提交前实测）**：

```
 M docs/design/design_notes.md
```

——工作区改动**仅此一行**，`site/` 下无任何条目。

**佐证二 · `git show --name-only`（提交后实测，同 r34 用法）**：见 §六回填。`git show --name-only HEAD | grep -c "^site/"` 应为 **0**。

**故本件对生产的行为影响为零，不是"经证明为零"，是"根本没有改动进入 `site/`"**——比词法显微镜更强的一种佐证：显微镜证「改了但等价」，此处证「未改」。r37 的显微镜结论**未被引用**，本件不需要它。

---

## 五 · 三门实测（本轮实跑，数照报）

| 门 | 结果 | 备注 |
| --- | --- | --- |
| `node tools/qa/vision_r24a.js`（总门） | **===== 汇总：511 项通过，0 项未过 =====** ✓ | **511/0**，与 r37 同数、未回退 |
| `node tools/qa/vision_r32.js` | **合计 29 项，失败 0 项** ✓ | 29/0 |
| `python tools/validate.py` | **OK：全部校验通过**，`exit=0` ✓ | |

三门跑的都是 `site/`／`data/` 的现状，而本件未动其一字，故三门本应与 r37 完全同数——**实测确认如此，即「文档件不动代码」这一断言的机器旁证**。

### §7.6 计数核对

**现计数仍为 3 / 5**（§4b、§4c、§21），**本件未新增此族断言**——本件未写任何 QA 断言，`tools/qa/` 一字未动。照任务书要求明说，不静默。

---

## 六 · 提交与部署（**动作完成后实测回填，未预填**）

本节各项在动作实际完成后据实回填，不预填——此即 r37 §十.0 自曝并建议立条者。该条已由 Skipper 于本轮并行件落地（`f822e63`，**conventions v1.31**「提交哈希与运行号一律实测回填，不得预填」），**本件是该通例写入后的首件实施**：表中三项均待推送与 Actions 跑完后才落笔。

**一笔须记的并行事实**：推送时发现远端 tip 已前进至 `7faaaef`——即 Skipper 同轮并行件（`docs/conventions.md` ＋ `docs/delivery_skipper_r38.md`）先落。两件**文件集不相交**（我只动 `design_notes.md`，它只动 `conventions.md`），故本提交干净地叠在其上，**无冲突、无合并提交**，任务书所定的防撞车分工实测有效。两件共用 r38 轮次号、各写各的交付文档（`delivery_skipper_r38.md` ／ `delivery_vision_r38.md`）。

| 项 | 值 |
| --- | --- |
| 提交哈希 | **`5071543`**（`docs(vision r38): design_notes 升 v2.9……`，2 files changed, 138 insertions(+), 2 deletions(-)） |
| Actions 运行号 | **`31977957344`**，`headSha=50715438…`，**conclusion=success** ✓ |
| `git show --name-only --format="" 5071543 \| grep -c "^site/"` | **0** ✓（该提交只含 `docs/delivery_vision_r38.md` 与 `docs/design/design_notes.md` 两个文件） |
| 生产复验 | **不适用**——未触及 `site/`，以上一行计数为 0 佐证生产未变（同 r34 办法） |

---

## 七 · 须裁定的疑点

**无新增疑点。** 承 r37 上报四条，本件办结其中两条（§7.5 补记、`3972` 一字），另两条按站长裁定不归我办——交付体例新条款归 Skipper，`styles.css:53`（块级轮次锚是否须句内显式化）未点名故本件**不动、不再提请**，其原始上报仍留在 delivery_vision_r37 §十 待日后派件。
